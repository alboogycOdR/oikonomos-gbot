# REVIEW.md — Review Log (ORCH-only writes)

## Per-unit performance tallies

| Unit | Reviews | First-pass approvals | Rework | Common rework causes |
|---|---|---|---|---|
| GB | 26 | 16 | 5 | TASK-015: flaky sweep test (unscoped global sweep collides with a parallel test file's fixture, 1-in-4 suite failure) + an explicit AC miss (integration tests not switched off ../../db/src/approvals.js, which is what makes src/dist drift invisible to the SQL pin test). Substantive work passed all three mutation probes. TASK-080: missing continuity guard between an invalidated approval and its replacement (run_id/capability_id/tenant_id not compared) — an AC gap, not a stated-AC miss; core transaction/atomicity work was exemplary under independent mutation. |
| CX | 38 | 14 | 13 | TASK-070: publication-tree control degrades to opaque exit-2 (no diagnostics) on an executable file swallowed by .gitignore, instead of the mandated omitted-file report; negative-controls' RFC 6761 allowlist is exact-match only so it false-positives on api.example.com, and the shipped test pins that as correct. §4 truncation-ladder test coverage (rungs 2–3 + episodic content untested) — cleared on re-review; TASK-007 coverage GATE dormant (100%-branch threshold never invoked by pnpm test/CI) + Test_Evidence coverage command non-reproducible (`test -- --coverage` forwards flag as positional) — both cleared on re-review (coverage.enabled:true; recorded command independently reproduced); TASK-021 liveness gate: coverage check keyed on ANSI-coloured output so its verdict depends on terminal TTY detection; hook check keyed on file presence (ADR-005 s2 forbids); control-queue check fails open on an absent directory. TASK-018 protected-path gate DORMANT (run-local.mjs runs only the self-test, real gate is PR-only and the repo has no remote) — same defect shape as TASK-007, i.e. a repeat of 'configured but nothing invokes it'. TASK-016 broker: all six stated ACs met and the ADR-003 handling exemplary (distinct RoleGrantCeiling type; negative test killed both mutations) — but TWO FAIL-OPEN paths outside the stated ACs: a NULL role grant skips the role check entirely (no grant passes where an insufficient grant is denied), and T4_irreversible falls into the approval branch and is allowed on nonce consume, against Synthesis:146 'Tier 4 is denied'. Plus DI ports non-assignable to the real approvals signatures, inviting a wiring that silently skips digest verification (N10) |
| S5 | 16 | 8 | 6 | TASK-063: unvalidated caller-supplied expiresAt on the approval edit route could silently brick a live approval (accepted past timestamp → invalidated original + already-expired, ungrantable replacement) — an unstated-AC gap surfaced by adversarial review, not a stated-AC miss; every literal AC was independently mutation-proven true first. Corrupt-db escalation unreachable (marker-match on head-truncated atlas.py traceback) — spec §5 safety carve-out unmet; also a prior worktree/main-checkout protocol violation on the same task. Cleared on re-review (direct sqlite probe). Non-gating on TASK-003: malformed devteam-control block (bare unquoted JSON keys, fail-closed rejected) + dossier heartbeat never committed to branch — process hygiene, first occurrence on OIK lineage, harness was concurrently broken. TASK-012: dossier WRITTEN but never COMMITTED (evidence existed and was verified, so not the TASK-003 repeat-absence trigger) - next occurrence in any form is rework, for every unit. |

Evidence here refines assignment heuristics (protocol §8) after ~10 reviews.

## Verdicts

| Task | Unit | Verdict | Findings | First-pass | Timestamp |
|---|---|---|---|---|---|
| TASK-002 | CX | approved | Territory clean (5/5 in Owned_Paths, no PLAN.md edits). 618 Python + 30 core + 36 Node green, re-run by ORCH — matches Test_Evidence. §7 A1 exit criteria all pass on repo: full scan 1.41s (<10s), `where decide`→supervisor.py:224+callers, `impact builder_registry.py` lists budget.py+validate_plan.py, one-touch re-parses exactly 1, exit codes 0/1 never 2. Non-blocking: (1) query uses LIKE not FTS5 MATCH/bm25 (fast now, revisit at scale); (2) no ls/find check evidence in history (first-occurrence, all files net-new). | yes | 2026-08-13T16:07:16Z |
| TASK-004 | S5 | approved | Territory clean: net diff master...task/TASK-004-s5 = exactly the 3 Owned_Paths (docs/ATLAS.md, scripts/atlas_cards.py, tests/test_atlas_cards.py, all net-new); own commit a27da61 touched exactly those 3; legit merge 862c0e3 carried only master's already-landed TASK-003 files + PLAN.md forward, zero out-of-territory S5 edits; c8b9872 filesystem-check evidence present (16:30:30Z preflight); no S5 PLAN.md/frontmatter edits (PLAN.md byte-identical to master). Spec verified §1 Cards/§3/§6 A3/§7/§8 Q1/R1/R4 — all 7 ACs map to spec text; five card fields + one headless sonnet-4-6 call/changed-file + hash-pin + never-in-scan confirmed; fake-model-transcript test discipline confirmed (zero live calls). ORCH re-ran in worktree: 21 cards + 657 full Python + 36 Node green (matches Test_Evidence). §7 A3 exit criteria verified END-TO-END through atlas.py CLI (isolated temp repo, fake cards_cmd): regenerate-only-on-hash-change + doctored-hash FRESH→STALE flip via core.query, exit 0/1/never-2, R4 clean degradation (model unreachable → cards exit 1, scan/query/status exit 0). Non-blocking: S5 evidence stamps 16:45/16:46Z run ahead of the ~16:33Z actual commit clock (UTC-hygiene reminder, no correctness impact). Unlocks TASK-005 (CX) — Wave 2 complete. | yes | 2026-08-13T16:41:36Z |
| TASK-005 | CX | rework | Territory clean (net diff master...task/TASK-005-cx = exactly scripts/atlas_pack.py + tests/test_atlas_pack.py, both net-new; single commit 231031f, no merge, no PLAN.md/frontmatter edits; c8b9872 preflight present 16:49Z). Composer functionally CORRECT — ORCH verified §4 end-to-end incl. all 3 truncation rungs manually (fresh card: budget 520 drops episodic+neighborhood/keeps card body ≤520; 500 drops all 3 incl. card bodies ≤500; below floor → clean exit-1, never 2), real-repo pack = territory outline + head-fallback + pointer-only neighborhood (no bodies) + matched episodic hits + R1 footer verbatim. Suites green, ORCH re-run in worktree: 6 pack + 663 full Python + 36 Node (matches Test_Evidence). REWORK is a §4 hard-cap TEST-COVERAGE gap (brief flagged as rework-not-note): (1) truncation ladder tests only rung 1 (episodic drop) — rungs 2 (neighborhood) + 3 (card bodies) uncovered, need a fresh-card fixture to exercise; (2) episodic-hit CONTENT unverified (only the "## EPISODIC HITS" header asserted, which renders even when empty); (3) preflight_paths reuse cosmetic — describe() called+discarded, resolution reimplemented via glob (honesty-of-coupling fix, semantics already match). Non-blocking: outline lists locals as `const` (atlas_core, out of territory); em-dash mojibake is console-cp only. | yes | 2026-08-13T16:59:34Z |
| TASK-005 | CX | approved | RE-REVIEW after rework. All 3 prior findings fixed with real discriminating assertions: (1) truncation ladder now full — test_budget_drops_lowest_priority_sections_first (budget 160, fresh-card fixture) = rung 2 (episodic + one-hop neighborhood dropped, territory card BODY kept, tokens ≤160); test_budget_drops_territory_card_bodies_last (budget 150) = rung 3 (all three dropped, card body collapsed to short card, tokens ≤150); both assert the exact cumulative "Truncation: …" line. (2) Episodic CONTENT asserted — actual "TASK-010 (dossier): Widget pack previous rework evidence." line, not just the header. (3) preflight_paths dead call removed (import + discarded describe() gone), glob/file/dir rules kept with an honest comment; verified describe() uses the same "*?[" + recursive-glob classification. Territory clean: net diff master...task/TASK-005-cx = exactly scripts/atlas_pack.py + tests/test_atlas_pack.py (both net-new, CX commits 231031f + a521811, no merge, no PLAN.md/frontmatter edits). ORCH re-ran in worktree: 7 pack + 664 full Python + 36 Node green (matches Test_Evidence); all four target tests confirmed run+passed. Real-repo §4 spot-check intact: 4 sections + freshness footer + R1 verbatim + pointer-only neighborhood (no bodies) + tiny budget → clean exit 1 never 2 + JSON machine-readable. Merged --no-ff, branch deleted. Unlocks TASK-006 (S5, A5) — dispatch gated on PROTECTED_EXCEPTIONS grants for its all-protected Owned_Paths. | no | 2026-08-13T17:22:00Z |
| TASK-006 | S5 | rework | Territory CLEAN (net diff master...task/TASK-006-s5 = exactly the 10 Owned_Paths / 12 files, zero outside; two S5 commits 0878709+408d4d4; no PLAN.md commit on branch). Pre-review protocol violation remediated & CONFIRMED: final diff exists ONLY as branch commits, main checkout has no tracked modifications. autopilot.json read line-by-line = ONLY the atlas block appended (builders registry / review_cmd / judgment_model untouched). tests/test_maintenance.py = exactly the two `len(steps)==6→7` assertions. §7 byte-identical-when-disabled EMPIRICALLY verified (not "structurally"): same-cwd dispatch.ps1 -DryRun with db present + enabled=false `-ceq` the master-overlay run, both 2705 chars. §7 dry-run section render verified (enabled + eligible TASK-003 @3000 → `## PROJECT MAP (ATLAS)` section + R1 footer verbatim; disabled → absent; fail-open → one warning line, PROMPT untouched). Briefings ×3 carry R1 verbatim + query/where/impact CLIs (R3); decompose impact-prose, onboard ask-step + R2 gitignore, board_publisher optional atlas key (existence-checked). R2 confirmed (scan → gitignored db, no git status change). Suites re-run by ORCH in worktree: 664 pytest + 36 node + 49 maintenance green (matches Test_Evidence). **BLOCKING:** maintenance.py corrupt-db escalation is UNREACHABLE — `_step_atlas`/`_atlas_run` matches `_ATLAS_CORRUPTION_MARKERS` against `_oneline(...,400)` head-truncated output, but atlas.py crashes with an uncaught traceback on `sqlite3.DatabaseError` (except tuple omits sqlite3.Error) so the marker line is always past char 400; both corruption classes tested → passed=True / no escalation / no delete+rescan remedy → spec §5 + AC unmet (silent nightly rot). Fix in-territory (probe db via sqlite3 or scan untruncated output + regression test). Non-blocking: dispatch.ps1 header hyphen vs spec/dispatch.sh em-dash. PROTECTED_EXCEPTIONS grants remain LIVE for the rework; not merged, branch retained. | no | 2026-08-13T18:27:31Z |
| TASK-006 | S5 | approved | RE-REVIEW after rework. Blocking corrupt-db finding FIXED for real: `_atlas_db_corrupt(db)` (scripts/maintenance.py) probes `.devteam/atlas.db` directly via sqlite3 `PRAGMA integrity_check` (+ `sqlite3.DatabaseError` on a non-db garbage file), independent of atlas.py's head-truncated traceback; `_step_atlas` escalates on `marker_hit OR _atlas_db_corrupt(db)` — corrupt path now REACHABLE. Verified: corrupt-db → passed=False, remedy detail contains "delete"+"re-run"+"scan --full" (spec §5 delete+full-rescan); ordinary scan failure → passed=True log-only (fail-open kept); missing/valid db → not corrupt (no false escalation, probe only runs post scan-failure). Regression test DISCRIMINATING: test_corrupt_db_escalates_with_delete_and_rescan_remedy strips the marker from the tail (`assert "DatabaseError" not in truncated_tail`) so escalation can only come from the direct probe — exactly the finding's failure mode; + TestAtlasDbCorrupt ×3 + fail-open/ok-path. 4 target tests confirmed run+passed individually (worktree). ORCH re-ran in worktree: pytest 671/0 (664 + 7 new), node 36/0. Territory CLEAN: net diff = exactly 12 files / 10 Owned_Paths, zero outside; four S5 commits (0878709, 408d4d4, 46492e8, 10b50fd); no PLAN.md commit on branch; main checkout no tracked mods. §7 byte-identical-when-disabled holds: this pass's only dispatch.ps1 change (10b50fd) is a comment inside the atlas-enabled branch — disabled gate + PROMPT assembly untouched, prior empirical 2705==2705 stands. Cosmetic hyphen ADDRESSED non-gating (ASCII hyphen kept for PS 5.1 codepage safety + explanatory comment). Merged --no-ff, branch deleted. **ATLAS PLAN COMPLETE (5/5 tasks done+merged).** Follow-up: remove now-unneeded PROTECTED_EXCEPTIONS grants (hooks/lib.js fc27360) in a dedicated [ORCH] commit. | no | 2026-08-13T19:42:00Z |
| TASK-003 | GB | approved | Territory clean: own commit 289499a = exactly the 2 Owned_Paths; merge 6f61a81 carried only ORCH's master fix f938fa0 + PLAN.md forward, no GB out-of-territory edits; c8b9872 filesystem-check evidence present; no GB PLAN.md/frontmatter edits. Spec verified §1/§3/§6 A2/R4 — all 6 ACs map to spec text; parser reuse confirmed (parse_tasks/ROW_RE/parse_instincts, no duplicate). ORCH re-ran in worktree: 18 episode + 636 full Python + 36 Node green (matches Test_Evidence); real-repo smoke episode hits TASK-002:1 + TASK-003:1, forward-slash, exit codes correct. Non-blocking: incremental never converges to changed:0 — a present source that parses to zero episodes (INSTINCTS.md today) is never recorded in the episodes table, so it re-counts as changed and rebuilds episodes_fts every run (cosmetic/perf, no data impact, no AC violated); idempotency of a zero-yield source is also untested. Fast-follow. | yes | 2026-08-13T16:32:48Z |

### OIKONOMOS project (PLAN v1.x — new task lineage; distinct from the ATLAS-pack rows above)

| Task | Unit | Verdict | Findings | First-pass | Timestamp |
|---|---|---|---|---|---|
| TASK-001 | GB | approved | OIK-001 monorepo scaffold. Territory clean: net diff master...task/TASK-001-gb = 61 files all in Owned_Paths + own dossiers/TASK-001.md; single commit d9c0b8e tagged [TASK-001]; no PLAN.md edits on branch (strict mode). c8b9872 preflight filesystem-check pasted verbatim in dossier (all 15 Owned_Paths entries NEW). Spec verified against Build Handover §3 (via subagent extraction): 10 packages (broker, policy, approvals, audit, agent-providers, harness-factory, db, connectors, memory, shared) + 4 services (control-api, worker, gateway-telegram, workspace) — names verbatim. tsconfig.base.json strict:true, no workspace overrides, engines.node>=22 on all 15, in-source Vitest test on all 14 src/index.ts, .nvmrc=22. Independent test run (subagent, wt-grok worktree): pnpm install/typecheck/build/test all exit 0; 15 projects resolved (root+14); 14/14 tests pass — matches Test_Evidence exactly. Non-blocking: scaffold stubs carry one placeholder test each (expected at this stage). Merged --no-ff ac55f31, branch deleted. Unlocks Wave B (TASK-003/004/005, all Depends_On TASK-001). | yes | 2026-08-14T16:58:00Z |
| TASK-002 | CX | approved | OIK-011/012/013 Postgres data-layer bootstrap. Territory clean: net diff master...task/TASK-002-cx = 11 files in infra/compose/** + infra/postgres/** + own dossiers/TASK-002.md; commits a2a852c + 50d43a9 tagged [TASK-002]; no PLAN.md edits on branch. c8b9872 preflight evidence in dossier. Schema verified VERBATIM against Synthesis Spec §5.1 (via subagent extraction): 4 enums (values identical), 8 tables (all columns/types/FKs/UNIQUE constraints identical), the single mandated HNSW index on knowledge_chunks.embedding vector_cosine_ops (§5.1 declares exactly one CREATE INDEX — builder matched, did not fall short), 2 append-only rules (DO INSTEAD NOTHING). No improvised columns/indexes. Independent LIVE validation (subagent, Docker Postgres 16, isolated container/host-port): extensions vector+pgcrypto live, 8/8 tables, idempotent second apply, append-only UPDATE 0/DELETE 0 assertion passed, down-migration→0 tables, container cleaned up — matches Test_Evidence. N4 clean (.env.example CHANGE_ME_LOCAL_ONLY placeholders only). Prod compose binds ${TAILSCALE_POSTGRES_HOST} with clawsrv live-verify documented as deferred ops. Zero Node/npm deps. Merged --no-ff c8ca135, branch deleted. | yes | 2026-08-14T16:58:00Z |
| TASK-003 | S5 | approved | OIK-017/018 canonical JSON + sha256 action digest (packages/shared, N10 single impl). Territory CLEAN: net diff master...task/TASK-003-s5 = exactly the 8 Owned_Paths files (packages/shared/src|test|README), single commit 3560027 tagged [TASK-003]; no PLAN.md/frontmatter edits on branch. Spec verified against Handover §4.3 (subagent extraction): action_digest = sha256(canonicalJson({toolName,input,destination})) — README + actionDigest.ts match the formula and 3-field structure exactly. §4.3 says only "sorted keys" (silent on UTF-8-byte vs UTF-16-code-unit tiebreak) — S5's RFC 8785 JCS UTF-16 code-unit ordering is a defensible, explicitly-documented interpretation, NOT a violation. Every AC has a discriminating test: 200-trial key-order property test; cross-process child_process spawn reproducibility; lone-surrogate/NFC-NFD/-0/exponent/bigint/symbol/function/class-instance rejection; undefined rejected in all 3 positions; null-vs-absent; N4-safe no-collision fixture set. Independent re-run by ORCH (subagent, wt-s5 worktree): tsc --noEmit exit 0; Vitest 5 files / 36 tests, 36 passed — the two critical tests confirmed run+passed by title — matches Test_Evidence. Zero runtime deps (node:crypto only). PROTOCOL FINDINGS (non-gating, flagged for S5 discipline): (1) devteam-control block was invalid JSON (bare unquoted keys) → control.py rejected fail-closed → state set by ORCH not builder; (2) dossiers/TASK-003.md heartbeat/preflight evidence never committed to the branch (no c8b9872 evidence in history — first absence on OIK lineage, not auto-rework per review step 2; repeat is a rework trigger). Both mitigated by the concurrent hooks-crash (root type:module) outage. Merged --no-ff 0ea60ad, branch deleted. Unblocks TASK-007 (with TASK-005). | yes | 2026-08-14T21:01:00Z |
| TASK-005 | CX | approved | OIK-005/006 lint enforcement (N9 query()-only-in-harness-factory + packages/policy zero-I/O). Territory CLEAN: net diff master...task/TASK-005-cx = eslint.config.mjs + infra/lint/** + granted package.json/pnpm-lock.yaml + own dossiers/TASK-005.md; commits 6e05ea3 + 2b541ee tagged [TASK-005]; no PLAN.md/frontmatter edits. Narrow-grant COMPLIANT: package.json adds only the `lint` script + eslint@10.8.1 + @typescript-eslint/parser devDeps; existing scripts/engines/packageManager untouched; onlyBuiltDependencies reformat (1-line→3-line, still exactly [esbuild]) is cosmetic pnpm rewrite — flag not rework. RuleTester unit tests confirmed (both files import { RuleTester } from "eslint"): 2/2 suites pass. ORCH independent verify (subagent, wt-codex): pnpm lint exit 0 on current tree; live probes — packages/policy/src import node:fs → oikonomos/policy-no-io fires; packages/broker/src import @anthropic-ai/claude-agent-sdk → oikonomos/no-direct-agent-sdk-query fires; both probe files deleted, git clean. All 5 ACs met. Merged --no-ff 049224a, branch deleted. Unblocks TASK-007 (with TASK-003) and satisfies TASK-004's pnpm lint CI dependency. | yes | 2026-08-14T21:01:00Z |
| TASK-008 | GB | approved | OIK-004/007 fast-follow — CI scanners honor .gitignore (walk.mjs) ⚑ protected (author GB / reviewer ORCH opus-4-8 — different-model rule holds). Territory CLEAN: net diff master...task/TASK-008-gb = infra/ci/lib/walk.mjs + infra/ci/test-banned-modes.mjs + infra/ci/test-secret-scan.mjs + own dossiers/TASK-008.md; single commit 7b93dfa tagged [TASK-008]; no PLAN.md/frontmatter edits on branch. c8b9872 preflight in dossier. Implementation: git mode filters walk candidates via `git check-ignore --stdin -z` (index-consulting → TRACKED enforcement files matching a stray ignore rule stay scanned, the AC2 correctness key); fallback mode applies FALLBACK_SKIP_DIRS incl. .devteam only when scan root is not its own git work tree; gitEnv() strips GIT_* to block %TEMP% parent-repo pollution; git status≥128 fails closed. ORCH independent verify (subagent, wt-grok worktree): test-banned-modes 8/8 + test-secret-scan 11/11 exit 0; REAL-repo banned-modes.mjs + secret-scan.mjs BOTH exit 0 with the gitignored .devteam/ (live bypassPermissions token + sk-shaped fixture) present — the local false-positive this task existed to kill is confirmed dead — matches Test_Evidence. All 5 ACs met: AC1 live exit-0 (verified); AC2 tracked catch (test); AC3 ignored-path skip PROVEN gitignore-membership not allowlist (isEnforcementSurface===true ∧ isAllowlisted===false ∧ gitIgnoredPaths reports it); AC4 fallback documented in walk.mjs JSDoc (README.md left untouched — outside narrow Owned_Paths) + clean-checkout unchanged; AC5 self-tests retained 6→8/9→11. Amendment A enforcement surfaces NOT weakened (skip by repo-membership, never carve-out). Merged --no-ff 39a4299, branch deleted, worktree removed. Leaf fast-follow — no dependents unlocked. | yes | 2026-08-15T00:00:00Z |
| TASK-004 | GB | approved | OIK-002/004/007 CI skeleton + banned-mode grep + secret scan ⚑ protected (author GB / reviewer ORCH opus-4-8 — different-model rule holds). Territory CLEAN: net diff master...task/TASK-004-gb = .github/** + infra/ci/** + own dossiers/TASK-004.md; 4 commits tagged [TASK-004]; no PLAN.md/frontmatter edits. c8b9872 preflight evidence in dossier (before + after). Allowlist verified char-for-char against ADR-002 Amendment A (subagent extraction): enforcement surfaces {packages,apps,services,infra,evals}/** + .github/** + .claude/settings*.json + .claude/agents/** hard-coded and un-overridable; prose carve-out docs/**|specs/**|dossiers/**|briefings/**|.claude/commands/**|PLAN.md|REVIEW.md|AUTOPILOT_LOG.md|INSTINCTS.md|*.md; dev-tooling autopilot.json|scripts/**; secret-scan exempts exactly the one named file hooks/run-tests.js (not a glob). Three banned tokens (bypassPermissions, acceptEdits, --dangerously-skip-permissions) assembled at runtime so this infra/** source carries no live literal. ORCH independent verify (subagent, wt-grok): test-banned-modes 6/6, test-secret-scan 9/9; REAL-repo banned-modes.mjs exit 0 clean + secret-scan.mjs exit 0 clean (the previously-blocking "current-repo-pass" AC now MET under Amendment A); run-local.mjs green with lint correctly SKIPPED (not failed) pending TASK-005. All 6 ACs met. Merged --no-ff 17279bb, branch deleted. | yes | 2026-08-14T21:01:00Z |
| TASK-007 | CX | rework | OIK-019 packages/policy risk-tier resolution ⚑ protected (author CX / reviewer ORCH opus-4-8 — different-model rule holds). PASSED: territory CLEAN (net diff master...task/TASK-007-cx = packages/policy/src+test + package.json + vitest.config.ts + pnpm-lock.yaml + own dossiers/TASK-007.md, 6 files; single commit b2f412c [TASK-007]; ZERO PLAN.md/frontmatter edits on branch — strict mode). c8b9872 preflight in dossier. Narrow grant EXACT: package.json adds only @vitest/coverage-v8@^3.2.4; vitest.config adds only the coverage/threshold block; pnpm-lock.yaml's sole importer change is under packages/policy (no other workspace touched) — the 421 added lines are coverage-v8's transitive tree. Spec-faithful to GOVERNING doc: Handover §4.2 `max(default_tier, roleGrantOverride)` more-restrictive-wins implemented via tierRank(indexOf on least→most-restrictive enum); undefined override → default; unregistered toolName → {decision:"deny", reason:"capability.unregistered"} verbatim per §4.2/N3; RiskTier enum matches DB migration 001 risk_tier exactly (T0_observe…T4_irreversible). Pure/zero-I/O, typed union return, no dead code; lint exit 0, typecheck exit 0. Resolver is genuinely 100%-branch-covered (ORCH subagent, worktree: 9/9 pass, src/index.ts 100% stmt/branch/func/line) and the branches:100 threshold DOES fail closed WHEN coverage is enabled (proven: injected uncovered branch → 90% → exit 1). **BLOCKING (2, one root):** (1) the 100%-branch GATE is DORMANT — packages/policy `"test":"vitest run"` (no --coverage), root `pnpm test`==`pnpm -r test` runs that, CI `test` job runs `pnpm test`, vitest.config coverage block omits `enabled:true`; vitest enforces thresholds only when coverage is enabled, so no automated path ever evaluates the bar → a future uncovered branch in this governance-core protected module passes CI green (the WBS acceptance the criterion exists to enforce). Fix in-territory: `vitest run --coverage` in the test script OR `coverage.enabled:true`. (2) Test_Evidence command `pnpm --filter @oikonomos/policy test -- --coverage` forwards `--coverage` past `--` as a positional test filter → collects NO coverage (exit 0/9 passed/no table) yet claims 100% — not reproducible; re-run with coverage actually enabled and paste the real table. NON-BLOCKING (ORCH-owned, no builder rework): Handover §4.2 (max/more-restrictive-wins, "no lower than default") contradicts Synthesis §5.1 `role_grants.max_tier` comment "role ceiling; broker takes min(default, ceiling)" — opposite direction. Precedence (Handover>Synthesis) makes the implemented max() correct; ORCH to author an ADR recording the supersession and warning that the future broker wiring must not feed the DB max_tier ceiling into this max() param (fails OPEN). Not merged; branch retained for rework. | no | 2026-08-15T06:24:00Z |
| TASK-007 | CX | approved | RE-REVIEW after rework. OIK-019 packages/policy risk-tier resolution ⚑ protected (author CX / reviewer ORCH opus-4-8 — different-model rule holds). Both prior BLOCKING findings RESOLVED and INDEPENDENTLY REPRODUCED (old paste NOT accepted — ORCH ran the recorded command itself): (1) coverage GATE now active — `coverage.enabled:true` in packages/policy/vitest.config.ts makes `vitest run` (the `test` script), root `pnpm test`, and CI collect coverage + enforce `branches:100`. ORCH ran `pnpm --filter @oikonomos/policy test` in worktree: exit 0, 9/9, coverage table present, index.ts 100% stmt/branch/func/line; PROVEN fail-closed — reachable uncovered branch in executed `tierRank` → branches 90% → exit 1 "Coverage for branches (90%) does not meet global threshold (100%)". (2) Evidence REPRODUCIBLE — recorded command genuinely yields the 100% table. Post-merge integration check on master (ORCH subagent) fully green: install --frozen-lockfile 0; policy test 0 gate active; pnpm lint 0 (AC4 zero-I/O); root `pnpm -r test` 0 with policy running coverage-enabled in the aggregate (Coverage enabled with v8, 100/100/100/100), DB integration skipped (no DATABASE_URL). Territory CLEAN: net diff = 6 files (packages/policy/src+test + package.json + vitest.config.ts + pnpm-lock.yaml + own dossier); commits b2f412c + 545e8ac both [TASK-007]; ZERO PLAN.md/frontmatter edits on branch. c8b9872 preflight in dossier. Spec-faithful to Handover §4.2 (`max(default,override)` more-restrictive-wins; unregistered→deny `capability.unregistered` verbatim; RiskTier enum = DB risk_tier); ADR-003 (ACCEPTED) settles max() in CX's favour — no code change needed. Pure/zero-I/O/typed/no dead code. Merged --no-ff d50f230, branch deleted, worktree removed. NON-BLOCKING FAST-FOLLOW (protected → GB/CX + different-model review): threshold gates on `branches:100` ONLY (no funcs/stmts/lines), so a future fully-uncovered NEW function drops Funcs% without failing the build (v8 doesn't count branches in a never-called fn); current coverage 100% on all four + written AC is explicitly BRANCH coverage (met+enforced) → defense-in-depth, not an AC violation; may tighten vitest.config.ts to funcs/stmts/lines=100. PLAN NOW EXHAUSTED (8/8 done+merged) → /devteam-decompose next. | no | 2026-08-15T08:17:51Z |
| TASK-006 | CX | approved | OIK-014/016 packages/db typed query layer + pooling + inbox-triage seed. Territory CLEAN: net diff master...task/TASK-006-cx = packages/db/src|test/** + packages/db/package.json + pnpm-lock.yaml + own dossiers/TASK-006.md (8 files); single commit da6ca06 tagged [TASK-006]; no PLAN.md/frontmatter edits on branch (strict mode). c8b9872 preflight in dossier (src/index.ts pre-existing, test/ + seeds/ new territory). Narrow-grant COMPLIANT: package.json adds ONLY `pg`@^8.23.0 (dep) + `@types/pg`@^8.21.0 (devDep) — no other manifest key touched (only cosmetic trailing-newline added). Spec verified: SQL column set (capability_id/description/default_tier/adapter/enabled; role_id/capability_id/max_tier/constraints) matches TASK-002 migration 001_schema_v1 VERBATIM; risk_tier enum type identical (T0_observe…T4_irreversible); seed tiers/constraints match Gmail connector manifest Handover §4.4 EXACTLY (email.list T0_observe, email.create_draft T1_draft, email.send T3_external enabled:false; inbox-triage max_tier T1_draft, constraints {rate_per_hour:40, domains:["*"]}). AC1 no raw SQL outside packages/db — git grep of packages/*/src + services/*/src confirms none (all SQL in database.ts, parameterized, no injection). AC2 pool limits configured+documented (defaultPoolConfig max:10/idle:10s/connTimeout:5s/maxUses:7500, JSDoc). AC3 seed idempotent via ON CONFLICT DO UPDATE upserts — double-run identical rows (test). AC4 integration green on live compose Postgres. ORCH independent verify (subagent, compose pg16 + migration applied): both tests RAN not skipped (control: with DATABASE_URL → 2 passed / without → 2 skipped), 2/2 pass incl. the idempotency+manifest-tier test hitting the live DB; typecheck exit 0, build exit 0; pnpm lint exit 0 on the branch (no-direct-agent-sdk-query does NOT false-positive on pg pool.query()); worktree clean. Code quality clean: empty-connectionString + missing-DATABASE_URL guards, private #pool, typed row mappers, no credentials, no dead code. All 4 ACs met. Merged --no-ff cfb2840, branch deleted, worktree removed. Unlocks TASK-007 (packages/policy risk-tier resolution) — its Depends_On {TASK-005, TASK-006} now both done; TASK-007 is PROTECTED, GB/CX only, NEVER S5. | yes | 2026-08-15T00:35:00Z |
| TASK-009 | CX | approved | Workspace dependency integration — E3 manifests + lockfile (single-owner, cross-cutting). Territory CLEAN: net diff master...task/TASK-009-cx = packages/approvals/package.json + packages/audit/package.json + packages/broker/package.json + pnpm-lock.yaml + dossiers/TASK-009.md (5 files); single commit 5a7ec02 tagged [TASK-009]; no PLAN.md/frontmatter edits on branch (strict mode). c8b9872 preflight in dossier (all 4 Owned_Paths inspected pre-write). Spec verified: workspace:* dependencies match Handover §3 package boundaries EXACTLY — approvals→@oikonomos/shared+@oikonomos/db; audit→@oikonomos/shared+@oikonomos/db; broker→@oikonomos/shared+@oikonomos/policy+@oikonomos/approvals+@oikonomos/audit. Zero external runtime dependencies added (workspace: only). AC1 workspace:* protocol ✓; AC2 pnpm install --frozen-lockfile ✓; AC3 pnpm -r typecheck/build/test/lint all exit 0 ✓; AC4 git diff --stat shows only manifests+lockfile+dossier (zero src/**|test/**) ✓. ORCH independent verify (subagent, wt-codex-oikonomos): install --frozen-lockfile exit 0 (15 projects, lockfile up-to-date); typecheck 14/14 exit 0; build 14/14 exit 0; test 55/55 passed, 2 skipped (packages/db integration clean-skip, no DATABASE_URL); lint exit 0 — matches Test_Evidence exactly. All 4 ACs met. Merged --no-ff bd97f62, branch deleted. **TASK-009 MERGE UNLOCKS TASK-011/TASK-013** (both have Depends_On: TASK-009). Wave 2 becomes eligible. | yes | 2026-08-15T13:15:00Z |
| TASK-010 | GB | approved | OIK-020 packages/policy role constraints (rate limits, domains) ⚑ protected (author GB / reviewer ORCH opus-4-8 — different-model rule holds). Territory CLEAN: net diff master...task/TASK-010-gb = packages/policy/src/index.ts (76 lines constraint evaluators) + packages/policy/test/role-constraints.test.ts (161 lines, 12 tests) + dossiers/TASK-010.md (3 files); single commit 2496555 tagged [TASK-010]; no PLAN.md/frontmatter edits on branch (strict mode). c8b9872 preflight in dossier (2 Owned_Paths globs inspected). Code quality: evaluateRateLimitConstraint + evaluateDomainConstraint are pure, independent predicates (no coupling); denial reasons distinguishable ("constraint.rate_per_hour" vs "constraint.domains") for audit; zero I/O (no fs/net/http/child_process/db/process.env imports, all inputs caller-supplied plain data). Spec verified: Handover §4.4 RoleConstraints shape implemented ({rate_per_hour?: number, domains?: readonly string[]}); ["*"] wildcard support; fail-closed on empty domains allowlist. AC1 rate constraint independent + tested ✓ (5 dedicated tests: absent-allows, below-allows, equals-denies, exceeds-denies, independence-from-domains); AC2 domain constraint independent + tested ✓ (6 dedicated: absent-allows, ["*"]-allows, exact-match-allows, non-match-denies, empty-fails-closed, independence-from-rate); AC3 denial reasons distinguishable ✓ (1 dedicated test: reason strings differ); AC4 zero I/O / pnpm lint green ✓; AC5 100% coverage maintained ✓ (gate from TASK-007 live and enforced). ORCH independent verify (subagent, wt-grok-oikonomos direct policy test): 21/21 passed (9 risk-tier TASK-007 + 12 constraints TASK-010); coverage 100% stmt/branch/func/line on index.ts — matches Test_Evidence exactly. All 5 ACs met. Merged --no-ff 551eb6a, branch deleted. Critical path TASK-009→TASK-010 (009 merge unlocks 013→014 atomic consume toward OIK-039 G-GOV gate). | yes | 2026-08-15T13:16:00Z |
| TASK-025 | CX | approved | RE-REVIEW after rework. **MASTER'S run-local IS GREEN** - verified on master post-merge (`local CI: all runnable jobs green`, exit 0), closing the defect ORCH introduced at the TASK-021 merge. The blocking hazard (self-test overwriting the SHARED installed hook, ~350ms repo-wide firewall outage, permanent inert hook on SIGKILL) is fixed WITHOUT losing the property - the real risk, since deleting the inert-hook case would have removed the hazard and the detection together. Probe redirected to a `core.hooksPath` fixture: real hook READ once and copied to mkdtemp; every write/chmod/rm targets the temp copy; per-invocation `git -c core.hooksPath=<temp>` so nothing persists; staging runs against a throwaway GIT_INDEX_FILE so the real index is untouched; both temp dirs removed in finally. REAL HOOK BYTE-IDENTICAL AT THREE CHECKPOINTS (before self-test, after self-test, after full run-local) - 1215 / beccfa89245dc3fc1cbfdaf18b2c13ea, matching the baseline ORCH recorded before dispatch specifically to enable this check. PROPERTY SURVIVED AND STRENGTHENED: inert-hook assertion intact, now bracketed by a positive control running the REAL hook's bytes and requiring COMMIT REJECTED first - the old version proved only the negative pole. Two injections prove the probe is not vacuous: empty hooks dir FAILS (so the fixture is what git executes, not the ambient hook), and disabling the staging line collapses BOTH hook tests (so the rejection comes from a real staged out-of-territory change). Non-blocking regex also fixed ( for the colon) so UNOBSERVABLE reports its real cause instead of blaming the hook, proven by bogus worktree_suffix injection; and CX added the UNOBSERVABLE self-test I had raised as optional, 7 -> 8 tests. No fail-open, no swallowed errors, no dead code, other four checks all still bite. Territory clean both commits, tagged, no authored PLAN.md edits. Merged --no-ff 5cbcda1. NON-BLOCKING NITS: 'Unable to locate installed pre-commit hook.' lacks the UNOBSERVABLE prefix so is misattributed to the hook (same class as the regex finding, fails closed); the mainRoot===null branch has no self-test; dossier entries listed out of chronological order. UNLOCKS TASK-016 (CX, broker) and TASK-023 (GB). | no | 2026-08-16T09:30:00Z |
| TASK-025 | CX | rework | Fix for the ORCH-introduced defect that left master's run-local red (hook-liveness probe could not observe from the main checkout, where the unit resolves to ORCH and the hook legitimately ALLOWS). **THE FIX IS GENUINE, NOT WEAKENED** - the central risk, since a weakened check and a fixed one are both green everywhere. Independent injection from BOTH locations: present-but-inert hook (#!/bin/sh + exit 0) FAILS; non-zero hook with an UNRELATED message also FAILS, because it requires the literal [territory-precommit] COMMIT REJECTED evidence not merely non-zero. Mechanism is real discovery - git worktree list --porcelain intersected with autopilot.json worktree_suffix values - probing inside a registered builder worktree where the hook is actually restricted. No-worktree falls back to UNOBSERVABLE-and-FAIL, never a pass; registry and worktree-list failures funnel to the same failing path. Other four checks UNTOUCHED (only non-hook hunk is adding basename to an import) and all four still detect their inert states under injection. Scope exactly 3 files. Self-test 6 to 7, and the new case performs the inert-hook attack unprompted. BLOCKING: the self-test resolves the REAL installed hook via git rev-parse --git-path and writes an inert exit-0 over it. VERIFIED NEW - master's self-test does not touch the real hook. .git/hooks/pre-commit is SHARED by all four worktrees, so for ~350ms the territory firewall is inert REPO-WIDE while this project runs builders concurrently by design; run-local invokes the self-test EVERY time so the window opens routinely; and try/finally does not survive SIGKILL, so an interrupted run leaves a PERMANENTLY INSTALLED INERT HOOK - the exact present-but-inert state the check exists to detect, created by the test that detects it. Fix: probe a throwaway fixture repo or a scoped core.hooksPath, never the installed hook - the pattern is already CX's own from TASK-022 and TASK-018. NON-BLOCKING: dead fallback guard - regex expects 'UNOBSERVABLE territory pre-commit hook:' but the message is 'hook FROM <path>:', so the precise cannot-observe diagnostic is discarded and replaced by one blaming the hook (fail-closed preserved, diagnosability broken); the UNOBSERVABLE branch has no self-test; the check is unrunnable in a fresh CI clone (no worktrees) so a hosted runner must provision one, adjacent to TASK-023; and worktree registration is inferred by string-building rather than read from git. Considered merging with the hook-mutation as a follow-up to clear red master faster and REJECTED it - introducing a new hole in the territory firewall to fix a CI mirror is the wrong trade. Not merged; branch retained. | no | 2026-08-16T06:45:00Z |
| TASK-021 | CX | approved | RE-REVIEW after rework. ADR-005 control liveness gate ⛑ protected. ALL FIVE FINDINGS FIXED, verified by real failure INJECTION not by reading. (A) Coverage/ANSI: stripping proven load-bearing against real captured bytes (raw match false, stripped true); identical verdict under FORCE_COLOR=1/3 and NO_COLOR=1; fails for real when coverage is disabled. Self-test asserts the captured output CONTAINS escapes, so it cannot degrade into testing a clean string. (B) Hook check - DECISIVE: a present-but-inert hook (#!/bin/sh + exit 0), exactly what the old Test-Path version passed, FAILS; a hook rejecting for an unrelated reason also fails, because it requires the specific territory-rejection evidence, not merely non-zero. Real `git hook run` against an isolated alternate index. Genuine ADR-005 s2 evidence. (C) Control queue: resolves MAIN checkout via --git-common-dir, fails on undrained blocks AND fails as unobservable when that directory is absent - no fail-open. (D) Dist staleness: real oldestDist<latestSource arm tested with back-dated mtimes plus live injection; apps/ added. (E) Dossier claim now REPRODUCIBLE in all four clauses, correcting the earlier false 'fully green'. No fail-open, no swallowed errors, no dead code, ATLAS descope intact, controls-live proven BLOCKING in run-local by injection. 8 commits, all tagged, territory clean, no authored PLAN.md edits. Merged --no-ff 54db249. **ORCH DEFECT INTRODUCED BY THIS MERGE: master's run-local is now RED.** The hook probe requires a REJECTION, but in the MAIN CHECKOUT territory-precommit.js:84 resolves the unit to ORCH (deliberately unrestricted), so the hook correctly ALLOWS and the check cannot distinguish live-and-allowed-because-ORCH from inert. Every verification I commissioned and ran myself executed inside the WORKTREE; I approved a gate without once running it where ORCH and CI run it. pnpm -r test on master stays green; run-local does not. Tracked as TASK-025 (critical) rather than reverting 8 commits of verified work over one narrow understood defect. Also: validate_plan.py caught me drawing overlapping infra/ci territory for the THIRD time today when creating TASK-025 - chain now TASK-025 -> TASK-023 -> TASK-024. UNLOCKS TASK-016 and TASK-023 (behind 025). | no | 2026-08-15T22:35:00Z |
| TASK-021 | CX | rework | ADR-005 control liveness gate ⛑ protected. WHAT PASSED, and it is most of it: all FIVE assertions genuinely induce failure in the self-test, which asserts BOTH individual status AND aggregate exit code - more than the AC required. ORCH independently broke FOUR of five REAL conditions (moved .git/hooks/pre-commit, dropped a stray json into .devteam/control, nulled GB's model pin, aged packages/shared dist) and the correct NAMED check flipped every time. Zero try/catch anywhere; fails closed on spawn failure (status ?? 1) and unreadable autopilot.json. run-local wiring is BLOCKING not advisory - status counted into `failed`, unconditional exit 1. ATLAS descope HONEST: 58 lines genuinely deleted, sole remaining reference is the README documenting the deferral to TASK-024 with the correct reason. Territory clean, commits tagged, no authored PLAN.md edits (verified with --no-merges after my own audit command produced a false positive by not excluding the master merge). BLOCKING 1: the coverage check matches /% Coverage report from v8/ against captured vitest stdout, but real output interleaves ANSI escapes THROUGH that phrase - the reviewer saw it FAIL and exit 1, I saw it PASS and exit 0, and the determinant is the child process's TTY detection. A gate whose verdict is decided by terminal colour rather than by whether coverage was collected is the ADR-005 failure class inside the gate written to enforce ADR-005. BLOCKING 2: the hook check shells to install_git_hooks.ps1 -Verify, which only does Test-Path plus a marker-string match - keying on CONFIG PRESENCE, which s2 explicitly forbids and for which s2 names the right evidence outright ('a rejected commit'). A present hook with core.hooksPath redirected, or --no-verify in a dispatch path, passes while the control is inert. BLOCKING 3: control-queue check does `existsSync ? readdir : []`, and .devteam/control DOES NOT EXIST in that worktree - verified - so it currently passes by observing nothing; drained is indistinguishable from never-existent. BLOCKING 4: the dossier claims run-local is 'fully green'; it was not in the reviewer's environment. Environment-dependent rather than deliberately wrong, but this is the SECOND unreproducible green on this project (TASK-007 was the first). NON-BLOCKING: the self-test never exercises a single real collector, which is exactly how BLOCKING 1 shipped; the dist staleness arm is untested (early-returns on distFiles.length===0); apps/ is never scanned though CLAUDE.md names it a source root; sourceFiles.length===0 skips a package (fail-open); hard Windows/PowerShell-5 dependency fails closed with a false accusation on Linux; dead `here` const. Three of four blocking items are the same underlying mistake - observing something other than the control - which is the hard part and why ADR-005 s2 spends a paragraph on it. Not merged; branch retained. | no | 2026-08-15T21:05:00Z |
| TASK-015 | GB | approved | RE-REVIEW after rework. OIK-023 invalidation + OIK-024 sweeper + ADR-004 render remediation + barrel fix ⛑ protected. BOTH BLOCKING FINDINGS GENUINELY FIXED, each better than the minimum. FLAKINESS cured by REAL ISOLATION not loosening: store.ts:163 scopes the sweep to the fixture's run via an OPTIONAL parameter (production still uses the unscoped path), and it did NOT go sequential to dodge the race - RACERS=8 still run concurrently and vitest.config.ts is untouched (grep for sequential/fileParallelism/singleThread/isolate returns nothing). The assertion is SHARPER than before: dropping status='pending' still fails, now deterministically (expected 96 to be 12) where the unscoped version gave a noisy 416. The vacuous `expired >= 0` assertion was DELETED and replaced with a real cross-run isolation test. VERIFIED OVER 10 CONSECUTIVE FULL-SUITE RUNS, ALL GREEN - four beyond the six requested, on the correct reasoning that 6 runs at a 1-in-4 rate still leave ~18% chance of missing it. AC MISS fully corrected: zero db/src imports remain, both offenders now import from @oikonomos/db, PLUS a mechanical guard that scans every test file and fails if db/src/approvals reappears. SRC/DIST BLINDNESS CLOSED, characterised by mutation: changing packages/db/src without rebuilding leaves the suite passing 57/57 (unbuilt src genuinely is not what executes), while the same mutation AFTER build fails 7 tests including the pin - the pin now tracks the executed artifact. NO REGRESSION: constant actionRender still fails 2; relaxing status='granted' still fails 4 incl. the N8 pin and CAN-07; consume SQL diff EMPTY, byte-identical to Handover 4.3. Dead ConsumeApprovalFn removed. Merged --no-ff; master verified green with install-then-build-then-test per the TASK-022 lesson (exit 0, approvals 43 passed/14 skipped). NON-BLOCKING RESIDUAL: createRequire(new URL('../../db/package.json')) pg lookup remains in 5 places - borrows a sibling's pg across a package boundary; clean fix is a direct devDependency, never in these Owned_Paths. ORCH-OWNED FINDING RAISED HERE, pre-existing: CI's test job never builds while dist/ is gitignored, so on a clean checkout @oikonomos/* cannot resolve at all (11 of 13 approvals files collapse) - the job would fail on the first CI run and has never been observed only because there is no remote. Textbook ADR-005: a CI job that appears to verify the workspace while being incapable of running. Tracked as TASK-023. UNLOCKS TASK-016. | no | 2026-08-15T17:25:00Z |
| TASK-022 | CX | approved | Repair of the obsolete packages/db persistence-surface guard that had held MASTER RED since 2e9cb67 (the TASK-014 merge, 15:04:43Z, ~2h). MASTER VERIFIED GREEN by ORCH after merge: pnpm -r test exit 0 across every workspace. Territory CLEAN - single commit 810aa93, exactly 2 files, dossier committed with preflight, zero PLAN.md commits; ~12 minute turnaround. THE GUARD WAS RE-POINTED, NOT DELETED, which is the whole point: the exact-sorted-list pin survives and now includes consumeApproval + CONSUME_APPROVAL_SQL, so an unexpected new export still trips it. AC2 asked only for a temporary stub-and-revert demonstration; CX delivered a PERMANENT second test (rejects a newly exported status-mutating approval helper) injecting updateApprovalStatus into a synthetic surface and asserting the pin throws - the guard's own liveness is now asserted in the suite, an ADR-005-shaped instinct applied unprompted. The two tests compose soundly: test 1 pins the REAL module surface, test 2 proves the pin rejects additions. AC4 handled by targeting approvals.ts not the barrel, with a comment explaining why it holds either side of TASK-015. All 5 ACs met. Merged --no-ff e6463ef. ROOT CAUSE WAS ORCH's, recorded in full on the task: TASK-020's AC5 made that guard TEMPORAL by construction, TASK-014 landed consumeApproval exactly as designed and invalidated it, and I merged without running the full recursive suite - while the TASK-014 and TASK-012 review agents were each scoped to their own package, so neither could have seen it. CLAUDE.md review standard step 3 now mandates pnpm -r test with the incident named. SEPARATE TRAP EXPOSED: the branch tested green while master tested red on identical code, because wt-codex had pg installed from an earlier agent's install and the main checkout did not - a post-merge check must run pnpm install --frozen-lockfile BEFORE pnpm -r test whenever the lockfile moved. UNBLOCKS TASK-021. | yes | 2026-08-15T17:10:00Z |
| TASK-015 | GB | rework | OIK-023 invalidation + OIK-024 sweeper + ADR-004 render remediation + barrel fix ⛑ protected. WHAT PASSED, MUTATION-PROVEN ON ALL THREE: (a) ADR-004 render is genuinely DERIVED - input type and its requireNonEmpty validation both removed, issue.ts feeds ONE action object to both bindActionDigest and actionRender, render.ts uses the same @oikonomos/shared canonicalJson over the identical key set the digest hashes; render.test.ts varies ALL THREE digest-covered fields, proves key-order insensitivity, and proves a caller-sneaked actionRender is ignored; making actionRender() constant FAILED 2 tests. (b) Invalidation is one atomic pinned statement, transitions to 'invalidated', cannot then be consumed even with the original payload; two mutations each failed the right tests. (c) Sweeper idempotence real - 8 promises before any await, each its own Pool, total-across-runners asserted, second run zero; dropping status='pending' failed sharply. TASK-014's consume SQL byte-identical (empty diff, no drift). TASK-013/014 tests show ZERO relaxations - the single deleted assertion could not survive (it tested a parameter that no longer exists) and issue.test.ts was STRENGTHENED to assert derivation. Territory clean, dossier committed, no PLAN.md commits. BLOCKING 1: sweep.integration.test.ts is FLAKY - failed 1 of 4 full-suite runs (expected 13 to be 12) because EXPIRE_PENDING_SQL is unscoped and vitest parallelises files, so the sweep occasionally collects consume.integration.test.ts's expired fixture. Test-isolation defect, not an idempotence failure; blocking because a suite failing a quarter of the time trains re-running over investigating (same reasoning as TASK-008's crying-wolf scanner). BLOCKING 2: explicit AC MISS - the AC said switch store.ts AND the integration tests off ../../db/src/approvals.js; store.ts was done completely, the tests were not (consume-sql.test.ts:7,13 and consume.integration.test.ts:16). Consequence is not cosmetic: @oikonomos/db resolves to dist/, so the code under test EXECUTES dist while the SQL pin test READS src - a mutation to packages/db/src/approvals.ts went UNDETECTED until rebuild. The test guarding N8's atomic statement is guarding a file that is not the one running - the exact ADR-005 class, inside the control that protects N8. NON-BLOCKING: dead ConsumeApprovalFn (leftover injection seam); residual createRequire(new URL('../../db/package.json')) pg lookup - same class but less fragile, anchors a committed file, and was outside Owned_Paths to fix properly. ORCH-OWNED, NOT REWORK: @oikonomos/db exports gitignored dist/, so every cross-package test may execute stale build output (10/13 files fail to load without a prior build) - predates this task, added as a sixth liveness assertion to TASK-021 instead. Not merged; branch task/TASK-015-gb retained. | no | 2026-08-15T16:20:00Z |
| TASK-014 | GB | approved | OIK-022 atomic consume (N8) - the most security-critical function in the platform. PINNED SQL VERBATIM: ORCH compared CONSUME_APPROVAL_SQL clause-by-clause against Handover 4.3 personally before delegating - character-identical bar the semicolon, all four predicates present. Single pool.query, no BEGIN/COMMIT/SELECT/FOR UPDATE/advisory lock. TWO MUTATION PROOFS make this more than a passing suite: dropping `status='granted' AND expires_at>now()` failed 3 integration tests (every predicate load-bearing, not decorative), and replacing the impl with check-then-update (SELECT + 20ms + unconditional UPDATE) failed the parallel test Expected 1 / Received 9 - the concurrency test genuinely DETECTS the forbidden read-then-write shape. Race is real: 16 promises in flight before any await (not TASK-013's Promise.resolve costume), 16 separate Pools = separate physical connections, plus a second test pre-establishing connections to remove jitter; exactly-one asserted. Row count gates via discriminated union (denial cannot carry the row), rowCount ?? 0 defaults deny, rowCount > 1 throws. Expiry asserted with state; replay asserts consumedAt TIMESTAMP EQUALITY, catching silent re-stamping. 37/37 under isolated pg16, 25/12-skipped without. Zero catch blocks, all parameterised, nonce validated pre-pool. TASK-013 tests ADDITIVE ONLY - 2 lines, zero deletions, zero relaxations, all 9 still pass. Merged --no-ff 2e9cb67. NON-BLOCKING (ORCH's scoping consequence, 3rd territory-forced workaround this session): store.ts imports db by FILESYSTEM PATH (`../../db/dist/approvals.js` dynamic import) because I granted approvals.ts but not the barrel - production code depending on a sibling's BUILD OUTPUT, breaks under bundling. Re-scoped to TASK-015 with a barrel grant. Also: per-call Pool on the hottest security path (same as TASK-020, TASK-016 must not ship on it); and the rowCount===1 && rows[0] guard falls through to plain deny instead of throwing like its sibling. UNLOCKS TASK-015 and TASK-016. | yes | 2026-08-15T15:10:00Z |
| TASK-012 | S5 | approved | OIK-026 redaction middleware + the append-only proof re-scoped off TASK-011. THE RE-SCOPE IS PROPERLY DISCHARGED: append-only.integration.test.ts writes through the package's own writer then executes REAL SQL - UPDATE ... WHERE event_id asserts rowCount 0, re-read asserts actor and payload unchanged, DELETE proven symmetrically. It would fail if TASK-002's rules were dropped, which is exactly what the previous structural argument could not establish; it RAN under the DB (188ms), not skipped. Redaction covers all three required families plus two more, each named; nesting proven for BOTH nested object and array-of-objects-and-arrays at unit and integration level; redactValue recurses over arrays/deep objects/Dates/non-strings without mutating input. N4 exemplary - fixtures FRAGMENT-ASSEMBLED at runtime so no contiguous credential-shaped literal exists in source. TASK-011's tests byte-unchanged and passing. Narrow grant held EXACTLY (pg devDep only; src/** still zero raw SQL, zero pg import). 35/35 under isolated pg16, 24/11-skipped without; secret-scan exit 0. Minor consistency note (judged MET, not a finding): the redaction test asserts on RETURNING data rather than its own SELECT - that IS the persisted row returned by Postgres after a jsonb round-trip, so it satisfies 'not the middleware's return value'. PROTOCOL FINDING - dossier written (34 lines, 3 preflight refs) but NEVER COMMITTED, so the merge did not carry it and the file is LOST from history. Judged NOT to fire TASK-003's repeat trigger on the wording: step 2 makes it a repeat ABSENCE of evidence, and the evidence was not absent - it was written and ORCH read and verified it, unlike TASK-003 where it was never written. Preflight transcribed verbatim into Review_Findings so the c8b9872 record survives. NEXT OCCURRENCE IN ANY FORM IS REWORK, for every unit. ORCH declined to commit it on S5's behalf - that would forge authorship and bury the finding. Merged --no-ff a4f3dda. | yes | 2026-08-15T15:10:00Z |
| TASK-011 | S5 | approved | OIK-025 audit append-only writer. AC1/AC2/AC4/AC5 independently verified (14/14 under isolated pg16; 9 passed/5 skipped without DATABASE_URL, clean skips; lint/typecheck/build/secret-scan exit 0). Denial test asserts a real persisted row via RETURNING'd eventId and proves the deny was not collapsed into the allow. Write failure is a GENUINE Postgres reject surfaced as AuditWriteError preserving the original as `cause`; src/index.ts:53-57 is the only catch and it re-throws. OIK-014 boundary intact: zero raw SQL, zero pg/Pool/Client in packages/audit. N4 clean. Territory clean, c8b9872 verbatim, zero PLAN.md commits. **AC3 not met as written - ORCH SCOPING DEFECT, not an S5 failure, explicitly not rework:** proving UPDATE/DELETE no-ops needs raw SQL, and packages/audit has no driver, @oikonomos/db exposes no generic query() by design (TASK-020), and package.json was not in Owned_Paths - unsatisfiable in the granted territory. S5's structural test (no mutating export, exact export list pinned) is valuable and kept, but would stay green if TASK-002's rules vanished. Property verified manually by ORCH (UPDATE 0/DELETE 0, row intact); re-scoped onto TASK-012 with a narrow pg devDependency grant. Merged --no-ff 981af0e. | yes | 2026-08-15T14:30:00Z |
| TASK-013 | GB | approved | OIK-021 approvals issue + bind ⚑ protected. All 5 ACs hold. AC2 persist-before-wait GENUINELY PROVEN - the test blocks insert on a released gate, spins until the row is visible, and asserts the issue promise has NOT settled at that instant; reversing the order fails it two ways (not the weak 'row exists afterwards' check). AC3 N10 proven behaviourally: bind.ts delegates to @oikonomos/shared, zero createHash/Math.random/JSON.stringify in src, and a test recomputes the digest via shared and asserts byte-equality plus key-order independence. AC4 randomUUID CSPRNG with 16 genuinely concurrent Postgres issuances, backed by uuid NOT NULL UNIQUE. AC5 16/16 under DB, 13/3-skipped without. ZERO catch blocks, all SQL parameterised, fail-closed on surprising DB status, RFC 6761 fixtures. Territory clean, c8b9872 verbatim. Merged --no-ff 6d5e3db. NON-BLOCKING: n10 regex test is evadable (aliased import, crypto.subtle, JSON.stringify unbanned); nonce.test.ts's 200-way check is Promise.all(Promise.resolve(...)) - sequential in a concurrency costume, harmless since the 16-way DB test is the real proof. action_render confirmed caller-supplied - NOT held against this task, remediation on TASK-015 per ADR-004. UNLOCKS TASK-014 (GB, atomic consume, N8). | yes | 2026-08-15T14:30:00Z |
| TASK-018 | CX | approved | RE-REVIEW after rework. OIK-003 CODEOWNERS + protected-path CI ⚑ protected (author CX / reviewer ORCH-opus). BLOCKING FINDING GENUINELY CLOSED: run-local.mjs now invokes the REAL gate, and all four ways it could have been made toothless are shut - base is merge-base (not HEAD, which would always be empty), try/catch pushes status 1 so an unresolvable base FAILS, no env gate and no default marker (exact-string match, anything else rejected at parse), and the job status counts into the exit code. STRONGEST EVIDENCE IS BEHAVIOURAL: run-local exits 1 on CX's own branch, naming the six protected files it touches - CX left his own work failing his own gate rather than wiring a self-exemption. Bidirectionality re-proven by ORCH in a throwaway repo: no marker exit 1, marker exit 0, WRONG marker exit 1, unprotected diff exit 0. Both ADDED criteria met - CODEOWNERS and PROTECTED_PATHS are identical 11-entry lists covering hooks/**, .claude/** as a whole tree, and .codex/**, plus a self-test that mechanically holds the two in sync (beyond the ask). Suites green 8/8, 8/8, 11/11, both scanners 0. Merged --no-ff b360999. ORCH FOLLOW-UP (not a defect here): run-local now exits 1 on every protected branch until a human passes the marker - correct fail-closed, but I must verify autopilot.json's review_cmd supplies the marker ONLY after the different-model review actually ran, never unconditionally. NON-BLOCKING: c8b9872 as prose not pasted output, 2nd occurrence - satisfies the standard (a Progress_Note is accepted), recorded as a consistency preference. | no | 2026-08-15T14:30:00Z |
| TASK-020 | GB | approved | packages/db persistence surface for audit_events + approvals (single-owner integration unblocking TASK-011/013). Territory CLEAN: single commit 0663ef1 [TASK-020], exactly 7 files, all Owned_Paths + own dossier, zero PLAN.md commits. scripts/dispatch.ps1 + PLAN.md in `git diff master..HEAD` are BASE DRIFT only (branch point 40ef616 vs master 059b5d1), touched by no commit — established via `git log --name-only`; a diff-only reading would have misreported both as violations. c8b9872 preflight pasted verbatim, 4/4 entries, pre-code. Spec fidelity checked against migration 001 itself: approvals + audit_events columns verbatim, `approvalStatuses` matches the `approval_status` enum exactly and in order. `requireUuid(nonce)` is correct not over-constrained — column is `nonce uuid NOT NULL DEFAULT gen_random_uuid()` with `UNIQUE (nonce)`, the DB-level uniqueness TASK-013's concurrency AC will rest on. AC1 proven by re-SELECT through an INDEPENDENT pool asserting server-generated event_id/at (not echo). AC2 proven a REAL server-side 22P02 — I read auditEvents.ts to rule out a client-side guard; runId is bound straight to $2 with no local validation. AC5 verified by independent grep (no consumeApproval / `UPDATE approvals` / `SET status`) AND structurally by persistence-surface.test.ts asserting the EXACT sorted export list, so a future status-mutating helper breaks the test rather than slipping past negative checks — the N8 statement stays TASK-014's, which is what this task existed to protect. Independent re-run (fresh isolated pg16:55433, own volume, migration under ON_ERROR_STOP=1): 8/8 with DB, 2 passed/6 skipped without, lint+typecheck+build exit 0 — matches Test_Evidence exactly. Merged --no-ff, master green post-merge. NON-BLOCKING: (1) withPool creates+destroys a Pool per call and ADR-001 L1 puts an audit write on every broker decision, so this sits on the hot path — territorially justified (database.ts out of scope) but TASK-016 must not ship the broker on it; (2) withPool/requireNonEmpty duplicated verbatim across both new modules; (3) `at` is caller-settable on an append-only audit table (backdating) — recommend TASK-011 not surface it; (4) tenant_id 'basileia' hardcoded in SQL, duplicating the schema DEFAULT. UNLOCKS TASK-011 (S5) and TASK-013 (CX). | yes | 2026-08-15T13:20:00Z |
| TASK-018 | CX | rework | OIK-003 CODEOWNERS + protected-path CI enforcement ⚑ protected (author CX / reviewer ORCH opus-4-8 — different-model rule satisfied). WHAT PASSED: territory CLEAN (single commit a739c74 [TASK-018], 7 files all in Owned_Paths + own dossier, zero PLAN.md commits; PLAN.md in diff is base drift only). Gate logic CORRECT and proven BIDIRECTIONALLY by ORCH in a throwaway repo unrelated to the builder's harness — protected diff without marker exit 1 naming the offender, with `--approval-marker fable-reviewed` exit 0, WRONG marker exit 1 (exact-string match, not truthiness), unprotected-only diff exit 0. CODEOWNERS covers all six CLAUDE.md/Handover §3 protected paths with none omitted, and additionally self-protects `.github/CODEOWNERS` + `.github/workflows/**` (unasked-for and correct). Suites: test-protected-path-review 8/8, run-local 9/9 jobs, banned-modes 8/8, secret-scan 11/11, both real scanners exit 0 — all matching Test_Evidence. BLOCKING (1): THE GATE IS DORMANT. run-local.mjs:56 invokes only `test-protected-path-review.mjs` (the self-test), never `protected-path-review.mjs` against a real diff; the real gate runs only at ci.yml:81 under `if: github.event_name == 'pull_request'`, and this repo HAS NO REMOTE. So nothing in any automated path evaluates a protected diff — a protected-path change can be committed and merged today with no gate firing, the exact outcome WBS OIK-003 exists to prevent, and AC4's own claim that the local script is 'the operative control' is untrue when a human must remember to invoke it. This is the SAME defect shape as TASK-007 (threshold configured, correct, fail-closed when invoked, invoked by nothing), which was reworked; approving it would retire that precedent three tasks after setting it. Fix is small and in-territory: run the real gate from run-local.mjs, solving deliberately the wrinkle that run-local is itself often executed on branches legitimately touching protected paths (merge-base + marker pass-through, or a distinct explained non-zero state a marker clears). NON-BLOCKING: marker is SELF-ASSERTED (CI greps the PR body for a comment the author can paste; no reviewer-identity or cryptographic binding, so directive §3's different-model requirement is honor-system at this layer — documented honestly in the script header, CODEOWNERS+branch protection is the real fix, deferred until a remote exists). ORCH DECISION ITEM: `.claude/**` only partially covered (settings*.json, agents/**, .codex/** yes; commands/**, hooks/**, skills/** no) — AC1 strictly MET since CLAUDE.md says 'all settings and subagent configs', but slash commands and hooks are executable control-plane surface and the DEVDEPARTMENT firewall names `.claude/**` wholesale; widen both lists or record why the carve-out is deliberate. Not merged; branch retained. | yes | 2026-08-15T13:20:00Z |
| TASK-023 | GB | approved | Clean-tree fix proven in BOTH directions (14 dist dirs removed; `pnpm -r test` alone exits 1 with 11/13 approvals files collapsing, full sequence green). Ordering check paired with a genuinely behavioural scratch-workspace test, proven able to fail by two injections. Did NOT neuter CX's gate: +29/-0 structurally isolated, all five original checks still fail under injection from the main checkout; hook restored byte-identical (beccfa89/1215B). Master now 13/13 green, clearing the controls-live red outstanding since TASK-021. | first-pass: yes | 2026-08-16T10:40Z |
| TASK-016 | CX | rework | ALL SIX STATED ACs MET; ADR-003 handled by type-level prevention (RoleGrantCeiling, deliberately not roleGrantOverride) and the negative test genuinely bites — both mutations killed tests. §4.1 contract verified character-identical by ORCH directly. THREE BLOCKING: (1) missing role grant fails OPEN — `roleGrant !== null &&` skips the check when no grant exists, so no-grant passes where insufficient-grant is denied (Synthesis:199 'which role may request which capability'); (2) T4_irreversible allowed via the approval path, against Synthesis:146 and D2 deny-by-default; (3) DI ports provably non-assignable to real approvals exports — the mis-wiring invited passes `action` into the deps slot, leaving action undefined so consume.ts:62 SKIPS digest verification. Findings 1–2 sit outside my ACs (my spec gap, stated as such) but are bound by CLAUDE.md non-negotiable #3 fail-closed; sent back rather than ticketed because the next work on this path is the OIK-039 canaries and the G-GOV gate. | first-pass: no | 2026-08-16T10:40Z |
| TASK-016 | CX | approved (2nd pass) | All three blocking fail-open/contract defects genuinely fixed, EACH PROVEN BY MUTATION: absent role grant now denies as role.grant_missing (mutation restoring the fall-through killed a test); T4_irreversible denies BEFORE the nonce block and a test supplies a VALID nonce with a permitting ceiling and still gets deny with verifyAndConsume never called (mutation deleting the branch reproduced the exact original fail-open); DI ports now ALIAS the real types (typeof issueApproval / typeof verifyAndConsume) making structural drift impossible by construction, with assignability proven by compiling the adapter against the port type and a control line confirming the OLD shape still errors TS2322. NO REGRESSION: ADR-003 trap re-tested specifically because the rework touched the same function - mutation still kills 2 of 9. Audit now spans nine return sites, all awaited. Guard order strictly narrowing; no route to allow for T4 or missing grant. Master re-verified green on the FULL recursive suite. Two residuals written into TASK-017 as explicit ACs rather than waived: no behavioural N10 proof against the real consume, and an internal dependency throw produces no audit event. | first-pass: no | 2026-08-16T11:20Z |
| TASK-024 | GB | rework | Mechanism CORRECT and all three of ORCH prior failure modes fixed: main-checkout resolution via git rev-parse --git-common-dir proven empirically (worktree has no .devteam at all), pure coverage set-difference with zero commit-topology or timestamp dependency, judgeable failure output naming missing files, and fail-closed verified across SEVEN degenerate states. Other six checks untouched, all still fire under injection; hook restored byte-identical. BLOCKING: tolerance of 8 is stated and enforced but NOT justified - basis is twice-the-diagnosis headroom, and fixture-database measurement shows the check does not fail until missing >= 9, so an entire package (agent-providers, 4 files) plus four more can drop from the index unnoticed. A liveness check that cannot detect a package-sized dropout is the configured-but-not-asserting failure ADR-005 exists to prevent. Directed to <=4 plus a behavioural test removing one whole package. Non-blocking: bare python hardcoded against the repo three-way probe convention (fails closed, but spuriously red on POSIX); empty-tracked-list fail-open; README silent on local-only and the new Python requirement. | first-pass: no | 2026-08-16T11:45Z |
| TASK-017 | CX | rework | Fail-closed structure SOUND and 11/11 mutations behaved as expected. AC3 kill switch read per request, no-restart proven by flipping between two calls in one process with a distinct toolUseId. AC4 ordering trap SOLVED correctly and explicitly - recordDecision failure becomes a typed AuditUnavailableError yielding a DENY with an unavailable sentinel, never a swallow. AC5 now executes the REAL approvals functions and RAN (19 tests, 0 skipped). AC6 clean - one import line removed across the whole branch, all nine TASK-016 tests byte-identical, ADR-003 and T4 mutations still kill. BLOCKING 1: replay cache keyed on toolUseId ALONE - a second role in a DIFFERENT tenant with NO role grant reuses the toolUseId and receives the first role allow, unaudited. toolUseId is attacker-selectable harness payload; the WeakMap keyed on the deps composition gives no isolation. Same shape as non-negotiable #7 - authorisation defeated by a caching layer in front of it. BLOCKING 2: cache unbounded, no eviction/TTL - verified an entry still replaying after 5000 keys and a policy flip to deny, silently reintroducing the staleness AC3 exists to forbid. Non-blocking: AC5 headline assertion non-discriminating (only row.status proves it); the >10s timeout is enforced NOWHERE in the repo and the test name overstates it - carried to OIK-084. | first-pass: no | 2026-08-16T13:05Z |
| TASK-024 | GB | approved (2nd pass) | Tolerance now 4 with a genuine DERIVATION (three dated observations, window = largest observed lag) replacing headroom, locked by a self-test assertion so drift back to headroom breaks a test. Trip point measured empirically from real fixture databases: moved from missing>=9 to missing>=5, catching both real package dropouts. Behavioural anchor keyed on a REAL package (packages/policy, 6 files verified against git ls-files) and carries assert.ok(files.length > TOLERANCE) so the test stays honest if the constant moves - better than what I specified. Python now the three-way probe matching dispatch.ps1, validated BY EXECUTION so a stub launcher is rejected; fails closed naming the cause. Empty-tracked-list fail-open closed. Regression clean: 3 added lines, no deletions, all six prior checks still fail under injection and the ATLAS line never masks them; hook byte-identical. Master gate now 7/7. Residual surfaced UNPROMPTED by GB: at zero baseline a 4-file package can vanish within tolerance - approved anyway because the conflict is structural not numeric (window must absorb lag of 4 AND catch a 4-file package), carried as TASK-026 for a structural package-dropout assertion. | first-pass: no | 2026-08-16T13:25Z |
| TASK-017 | CX | rework (2nd) | CROSS-TENANT LEAK GENUINELY FIXED - key now tenant+role+toolUseId, test asserts both non-inheritance AND independent audit, and the decisive mutation BITES (reverting to toolUseId alone reproduces the exact leak). TTL bites; fake timers not real sleeps. AC5 discrimination properly fixed with a state-aware fake and an unmutated control case. 21/21, all four regression traps still kill. THREE BLOCKING, all in the cache mechanism this task introduced: (1) LRU cap has NO liveness assertion - deleting the whole eviction block passes 21/21 and lint does not flag the unused constant; CLAUDE.md makes that a hard reject. (2) In-flight entries are evictable - probe with a pending victim and 1100 flood keys produced 2 capability lookups, 2 verifyAndConsume calls on one nonce, 2 audit events and TWO CONTRADICTORY verdicts for one toolUseId (ADR-001 R2/CAN-08); non-negotiable #8 prevents a double spend but the winner is a race. (3) TTL silently narrows R2 (1 audit in-window, 2 after expiry) on a justification that conflates the broker RESPONSE deadline with the L1-to-L3 INTERVAL - an error ORCH made first in review; settled by writing ADR-007 since docs/ is outside builder territory. Minor: tenant isolation not independently tested (dropping tenantId passes the suite). | first-pass: no | 2026-08-16T14:10Z |
| TASK-026 | GB | rework | Core CORRECT and well-proven: assertion is genuinely structural (pure set op, no tolerance reference, runs before and independent of the numeric gate); tolerance untouched at 4 with the prohibition recorded in the comment; zero-baseline case proven independently against the real 260-file list - agent-providers (4 files = tolerance) now FAILS while the numeric check alone still would not catch it; dropout and lag emit different messages discriminated by SHAPE not magnitude, pinned negatively with assert.doesNotMatch; discovery derived from pnpm-workspace.yaml verified end-to-end with a live throwaway package; both mutations bite with DISJOINT failure sets proving independence. BLOCKING: droppedWorkspacePackages returns [] (= PASS) when globs are empty, and the hand-rolled YAML parser returns [] for FLOW STYLE - ORCH verified both directly: block style parses, packages: ["packages/*"] does not. A legal reformat of pnpm-workspace.yaml silently renders the control inert with every test green - the exact ADR-005 s2 failure mode inside a control written to detect that failure mode. Must fail as unobservable on zero globs/zero packages, with a test. Non-blocking: unguarded readFileSync throws a stack trace instead of a fail() diagnosis. | first-pass: no | 2026-08-16T14:35Z |
| TASK-026 | GB | approved (2nd pass) | Blocking fail-open closed AND the tests bite - every guard mutated and caught, each independently probed. Zero-globs now fails as unobservable at BOTH collector and checker, so the hand-built-evidence path that leaked in round 1 is covered; the packages-present-but-nothing-mapped half is there too. Flow-style YAML now PARSES rather than merely failing closed; six YAML forms parse, four fail closed, and no form yields a silent [] reaching a PASS (residual: BOM fails closed - safe direction, noted). Structural and numeric mutations kill DISJOINT sets, proving independence. Zero-baseline case rebuilt by the reviewer against the real index rather than trusting the fixture. All six other checks still bite; hook byte-identical; healthy repo not reddened. Test count 15->21, only removed assertion was restructured stronger. Master gate 7/7. | first-pass: no | 2026-08-16T15:40Z |
| TASK-017 | CX | rework (3rd) | Round 2's three blocking items ALL genuinely closed and each proven by mutation: LRU cap now has a liveness assertion (deleting eviction fails a test where round 2 passed 21/21); in-flight eviction fixed with probe numbers inverting from 2/2/2-contradictory to 1/1/1-identical; post-expiry recompute explicitly tested per ADR-007 s4; tenant and role now guarded INDEPENDENTLY after the combined test was split. 24/24, all four standing traps still kill. BLOCKING: the ONE new guarantee this round added - in-flight replayed past expiry - has NO test; dropping `!replay.settled ||` passes 24/24 while the guarantee is demonstrably real. Same code-correct-test-blind pattern for the third time, now on the newest construct. Window derivation RESOLVED BY ORCH via ADR-007 s2.4a rather than bounced back, since docs/ is outside builder territory and a builder could not settle an architectural constant. Non-blocking: failClosed sentinel untested (flipping to ALLOW passes); never-settling entries pinned forever (measured); indentation asked a third time. | first-pass: no | 2026-08-16T15:40Z |
| TASK-017 | CX | approved (4th pass) | Blocking item closed with a test that is hard to fake: the victim's dependency promise resolves only AFTER the re-submit with no await across vi.advanceTimersByTime(60_001), so it provably asserts on an UNSETTLED entry rather than exercising the expiresAt>now branch; it composes LRU+expiry (1024 settled entries inserted first) and fails on the recordDecision count, i.e. the second audit event for one toolUseId, which IS the CAN-08 violation not a proxy. ALL TEN mutations bite - eight standing traps plus both sentinels - with tenant and role each caught by their own dedicated test. failClosed nested-failure gap closed unprompted. NO SILENT WEAKENING: diff read for removed/modified assertions, zero of each; the renamed in-flight test kept all four original assertions verbatim and the clock advance strictly strengthens it. Window cites ADR-007 s2.4a and disclaims ADR-001 R3. Master green on the FULL recursive suite, gate 7/7. Carried not forced: never-settling-entry pin (routed to OIK-084, which adds the deadline that bounds it) and the try-block indentation (asked 4x, cosmetic). Four rounds, every rejection the same class - correct code, blind test - never a wrong implementation. | first-pass: no | 2026-08-16T16:20Z |
| TASK-MAINT-2026-08-16 | GB | approved (superseded) | Correct diagnosis (pack-template test scaffolding asserting against a live onboarded config, root cause matches ORCH's own prior findings #11-12) and a verified working fix (3/3 mutations bit as expected in adversarial review). Not merged as-is: the DEVDEPARTMENT pack's own upstream fix for the identical defect landed mid-review via independent resync, with a cleaner mechanism (.devteam/sync_state.json check) plus a second real fix neither GB nor ORCH had found (ancestor-repo commit hazard in git_commit_and_push). One non-redundant GB assertion (marker-stays-in-markers[]) ported forward. Superseded, not reworked - GB's approach and root-cause call were right. | first-pass: yes | 2026-08-16T18:55Z |
| TASK-028 | GB | rework (narrow) | Extraction FAITHFUL and approved in substance: all 88 tests real (diffed vs original zip, 0 skip/todo, no assertions pulled), @anthropic-ai SDK import GONE and Claude query injected+fail-closed (the load-bearing ADR-001 seam), security.ts byte-equivalent, config hand-roll preserves every zod constraint + hardened to drop banned permission modes, no creds, no `any`, territory clean. ONE BLOCKING: providers.grok.test.ts 'aborted mid-stream' is non-deterministic (passes isolated 41/41 x2, reddens pnpm -r test under load, abort races the text_delta) - a flaky red on master corrupts every downstream review's green-check and this is the E4 foundation, so it must be deterministic before merge. Carried forward: Codex/Grok spawn direct subprocesses (broker-bypass surface for OIK-033/034 to gate). | first-pass: no | 2026-08-17T11:45Z |
| TASK-028 | GB | approved (2nd pass) | RE-REVIEW after narrow rework. The sole blocking issue is FIXED: grok 'aborted mid-stream' abort is now gated on the first observed text_delta in the consuming loop (providers.grok.test.ts:194) — the interrupt cannot win a spawn/IO race because it fires only after a delta arrives, not on a fixed timeout. Delegated stress run confirms determinism: 3x full `pnpm -r test` (247 passed / 31 skipped each, all 14 packages green) + 5x grok-file stress; the previously-flaky test passed 8/8 with stable ~5.1s durations, no 'expected false to be true' recurrence. No tests dropped (agent-providers 41 + gateway-telegram 48 ≥ 47). Territory clean (all under packages/agent-providers/** + services/gateway-telegram/test/**), both commits tagged [TASK-028], no PLAN.md edits on branch. Extraction substance was already approved 1st pass. Merged --no-ff (dc57024), branch deleted. Unlocks TASK-029 (OIK-033 harness-factory, CX) — head of E4 serial chain. | first-pass: no | 2026-08-17T13:20Z |
| TASK-038 | CX | rework | Scripts well-built (sha256 manifest, data-fingerprint verify, path-traversal guard, throwaway containers with working trap-cleanup) but ORCH RAN THE DRILL ON CLAWSRV and it FAILED first execution: the readiness check (until pg_isready) races the postgres official-image init server - pg_isready passes against the temp init-only server, then CREATE TABLE hits the socket gap when it tears down. Same race class as the grok flaky test. Fix: gate readiness on a real authenticated SELECT 1, not pg_isready. STRUCTURAL: task can only be demonstrated on clawsrv (Windows worktree cannot run Docker/pg), so ORCH is the drill-verification point (TASK-019 pattern). Caught only because the acceptance bar is demonstrate-not-assert. | first-pass: no | 2026-08-17T13:35Z |
| TASK-029 | GB | approved | Protected-path first-pass approval; adversarial review by a DIFFERENT model than author (GB=grok, reviewer=opus-4-8) per §3. Territory clean: single feat commit 1d5f268 touched only the seven Owned_Paths + its own dossiers/TASK-029.md; the noisy three-dot diff was pure TASK-028 history divergence (branch carried TASK-028 via merge dc57024 vs master's direct commits), and a two-dot tip diff of the non-owned paths (agent-providers, gateway-telegram, dossiers/TASK-028) is ZERO — content-identical to master. c8b9872 preflight present verbatim in dossier (all 7 Owned_Paths inspected). No PLAN.md edits on the builder commit (strict mode; SV control commits only). SPEC-FAITHFUL to ADR-001: L2 permissionMode fixed to `dontAsk` (ADR-001 L43) + explicit allowedTools, L1 PreToolUse hook seam @10s (R3), L3 canUseTool as belt-and-suspenders (never sole), all three INJECTED as ports with no hard-import of OIK-034/035/036 adapters (asserted by test). The CARRIED TASK-028 broker-bypass concern is CLOSED: Codex/Grok direct-subprocess spawns are gated through L1 as a `Bash` tool via gateSubprocess, fail-closed on L1 deny/throw. N9 sole-constructor guard scans packages/**+services/** for SDK imports & stray createHarness, and is SELF-ANCHORED (readFileSync of index.ts asserting it contains the SDK module + createHarness) so it cannot pass vacuously if repoRoot mis-resolves — meets ADR-005 liveness. Banned mode tokens (bypassPermissions/acceptEdits) assembled at runtime via join(), absent as literals from src AND both test files (asserted); banned-modes.mjs clean. Fails closed on L1 and L3 throw (both tested). No creds, no `any`, thorough input validation (asRecord, malformed PreToolUse/PostToolUse guards). Independent full `pnpm -r test` re-run in worktree: 258 passed / 31 skipped, exit 0; N9 "sole harness constructor" + "createHarness — L1/L2/L3 seams" suites confirmed running & passing by name; typecheck/build/lint all exit 0 — matches Test_Evidence. Merged --no-ff (d2f03e7), branch deleted, worktree ref pruned. UNLOCKS E4 fan-out: TASK-030/031/032/034 (all Depends_On TASK-029) now dispatch-ready. | first-pass: yes | 2026-08-17T14:20Z |
| TASK-029 | GB | approved (ORCH adversarial, post-merge) | Loop single-pass already merged it; ORCH ran the MUTATION pass it didn't. FOUNDATION SOUND: N9 guard bites on static/dynamic/require/aliased SDK imports + stray constructor; L1/L2/L3 locked (caller bypassPermissions/widened-allowlist ignored, order-flip fails a test); FAILS CLOSED (missing L1 => MISSING_L1 throw, no path skips L1); gateSubprocess deny/throw/unknown => no spawn; ports injectable, no stub collision, fan-out safe. 258/31 deterministic. CORRECTION: the subprocess bypass is 'mechanism provided, wiring pending' NOT 'closed' - codex/grok still spawn() directly, zero callers of the gate; created TASK-039 to wire it + liveness, made TASK-035 depend on it. Guard blind spot (apps//evals/ unscanned, both empty) folded into TASK-039. | first-pass: n/a (re-review of a loop-merged task) | 2026-08-17T14:20Z |
| TASK-038 | CX | approved (3rd pass) | ORCH re-ran the drill on clawsrv: PASSES end to end (RESTORE_DRILL_VERIFIED / BACKUP_RESTORE_DRILL_TEST_PASSED, exit 0). Fingerprint brittleness fixed at root - backup now runs pg_dump inside the same pinned pgvector/pg16 image as the restore, so both share one client version (was host 16.14 vs container 16.13). Territory clean, no creds, clawsrv clean. Three defects (readiness race, fingerprint brittleness, verify) caught ONLY by running it - demonstrate-not-assert vindicated. Merged [AUTOPILOT]. | first-pass: no | 2026-08-17T19:40Z |
| TASK-030 | GB | approved (ORCH adversarial) | L1 PreToolUse adapter, platform primary enforcement point. FAIL-CLOSED proven by mutation on all three ADR-001 R3 modes (transport/timeout/malformed each flip-to-allow turns a test red). 10s timeout genuinely wired (AbortController, tested with fake timers at 9999/10000ms - real behaviour not a constructed error). Both deny forms incl approval_pending map to hook-deny; unexpected decision default-denies; injected fetch seam, no local re-decision, no bypass. Territory clean, 22/22, master green 268/31. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-17T19:55Z |
| TASK-032 | GB | approved (ORCH adversarial) | L2 validator, CAN-02 config layer. Bare-name rejection bites (invert guard -> 5 tests red incl all CAN-02). Every blind spot rejected: whitespace/case/empty-paren/non-string. dontAsk locked; ADR exception is a code seam not a caller flag. Pure validator, no I/O. Territory clean, 33/33, master 279/31. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-17T20:05Z |
| TASK-031 | CX | approved (ORCH adversarial) | L3 canUseTool adapter. Idempotent-with-L1 (broker-owned toolUseId dedup, forwards original - mangle it, 2 tests red); fail-closed all 3 R3 conditions, real 10s abort; never-sole-enforcement (structurally can't override L1, tested); F5-routed. All 4 mutations bite. Pure delegation, no local re-decision. Nit: claimed 277 tests, actual 276 (off-by-one, not a regression). Territory clean, 30/30, master 276/31. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-17T20:10Z |
| TASK-034 | S5 | approved (ORCH, live-DB) | Run lifecycle on existing schema after ORCH re-carve (S5 correctly blocked the original which had 3 decompose errors). Atomic transitions, illegal ones rejected (mutation-confirmed on live Postgres), resume-after-kill persists session_ref, barrel append-only, no migration/enum change, no raw SQL leak. Nit: unused OPEN_STATUSES const. Merged [AUTOPILOT]. | first-pass: yes (post-recarve) | 2026-08-17T20:15Z |
| TASK-033 | CX | approved (ORCH adversarial) | R4 PostToolUse hook. Shared actionDigest (no reimpl), injected audit sink (mutation-tested), toolUseId correlation, artifact URIs, audit-write failure propagates (no silent drop). Stayed in re-carved territory (no pretooluse/ports touch). ORCH process error: briefly orphaned the reviewed commit by deleting its branch; recovered + merged. Master green. Merged [AUTOPILOT]. | first-pass: yes (post-recarve) | 2026-08-17T20:30Z |
| TASK-039 | CX | approved (ORCH adversarial) | CLOSES the Codex/Grok broker bypass tracked since TASK-028. Deny prevents spawn - proven by SENTINEL FILE (existsSync false), not error text. Absent seam refuses; broker throw fails closed. ADR-005 liveness keys on a blocked spawn and dies when the control is made inert. Guard scanRoots widened to apps//evals/, both verified live. Zero assertions removed from the 41 extracted tests (41->45 additive). Only 2 spawn sites repo-wide, both gated. Master green. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-17T21:35Z |
| TASK-035 | GB | rework (narrow) | G-GOV canary suite. CAN-02 is GENUINE - real composeHarness + real handlePreToolUse, bare-name Tier-3 actually present, and MUTATION-PROVEN: reintroducing the original flaw turned 7 of 10 canary files RED. Subprocess canary proves by ATTEMPTED EXECUTION (tries to spawn a deliberately-nonexistent binary; ENOENT under mutation). CI-blocking confirmed (exit 1). compose.ts wires the 039 gate into both providers. Root edits exactly minimal. Territory + PLAN clean. BLOCKING: CAN-06/CAN-07 Postgres variants FAIL when actually run - green only because they skip. ORCH stood up live pg16, loaded the schema, ran with DATABASE_URL: 2 failed / 8 passed, both on approvals_capability_id_fkey - the DB fixture never seeds the capabilities row. These are the canaries proving N8 atomic consume and N10 digest binding at DB level; G-GOV cannot certify 2 of 8 canaries that never executed green. Fix: seed FK parents (reuse seedInboxTriage) and re-run with DATABASE_URL. | first-pass: no | 2026-08-18T06:20Z |
| TASK-035 | GB | approved (2nd pass) | G-GOV canary suite + composition root. FIXTURE FIX VERIFIED ON A LIVE DB, not taken on claim: seeded FK parents via seedInboxTriage-matching capability; ORCH ran isolated pg16 with schema -> 16/16 passed, 0 skipped (was 2 failed/2 skipped). AND THEY BITE: removing the replay-protection clause from CONSUME_APPROVAL_SQL turned BOTH CAN-06 and CAN-07 red - the N8/N10 canaries are real. CAN-02 mutation-proven (original flaw reintroduced -> 7 of 10 files red); subprocess canary proves by attempted execution. Territory + PLAN clean. ORCH then wired the canaries job into ci.yml WITH a Postgres service so CAN-06/07 execute rather than skip in CI (ADR-001 CI-blocking). Merged [AUTOPILOT]. | first-pass: no | 2026-08-18T06:45Z |
| TASK-036 | CX | rework | Behaviour CORRECT and all 3 core mutations bit: subagent Tier-3 denied via broker; attribution forced to isSubagent:true with child-only sessionRef and UNFORGEABLE by the caller (probed dist directly); banned modes rejected; no path skips L1 (missing broker/fetch/baseUrl all throw). BLOCKING: (1) subagent.ts is exported by NOBODY - not in index.ts, not in the package exports map; only its own test imports it, so no production code can invoke it - ADR-005 configured-but-inert, inside the control meant to close CAN-05. (2) CAN-05 does not cover it - gutting the subagent broker consult left pnpm canaries fully green; the canary tests compose.ts identity plumbing, not subagent.ts, so G-GOV would certify enforcement it never exercises. Granted index.ts + the can-05 canary file to fix both. Non-blocking: obfuscated dynamic import with a FALSE justification comment, retrieved via an `as` cast so a rename would typecheck and fail at runtime. | first-pass: no | 2026-08-18T09:00Z |
| TASK-036 | CX | approved (2nd pass) | Both blockers fixed and verified by the exact mutation that exposed them: subagent.ts now exported from index.ts (real public surface), and CAN-05 imports createSubagentL1 from the public index and drives the real path. DECISIVE: gutting the subagent broker consult now turns CAN-05 RED - the same mutation left canaries green last round. Typed static import replaced the obfuscated dynamic one. Canaries green, harness-factory 60/60, master green. Merged [AUTOPILOT]. | first-pass: no | 2026-08-18T11:15Z |
| TASK-040 | GB | approved (ORCH adversarial) | CRITICAL tier-escalation bypass CLOSED. Replay key now includes toolName + shared actionDigest per ADR-007 §3a (N10, no second digest). DECISIVE: reverting to identity-only turns THREE regression tests RED, named for the exact probes - T1->T3 escalation, payload mutation, and digest provenance. CAN-08 NOT weakened (L1/L3 share toolName/input so the legitimate pair still hits one key). Broker 25->28 tests, canaries green, master green. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-18T13:20Z |
| TASK-041 | CX | approved (ORCH adversarial) | HIGH-1 + HIGH-3 closed. composeHarness now exported via an explicit ./compose subpath with types mapping. approval_pending parks via PARK_REASONS as a SUPERSET of FAIL_CLOSED_REASONS (not folded in) so an approval wait isn't reclassified as a fail-closed error. MUTATION-PROVEN: removing approval_pending from the park set turns the R3c test RED. 66/66, canaries green, master green. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-18T13:40Z |
| TASK-042 | GB | approved (ORCH adversarial) | HIGH-2 closed - the E4 chain finally has a production caller. worker executeTaskRun runs agents through composeHarness via the public ./compose subpath. ADR-005 liveness keys on a broker decision AUDIT EVENT (evidence emitted by doing the job, not config). ORCH-PROVEN, not accepted on GB's own test: rewriting executeTaskRun to bypass composeHarness and drive the raw queryFn turned the liveness assertion RED. Canaries 14->15, master green. Merged [AUTOPILOT]. | first-pass: yes | 2026-08-18T14:00Z |
| TASK-043 | GB | approved | OIK-047 manifest schema + validator, first pass. Territory clean (13 files), preflight logged, no PLAN edits. Every AC verified against named tests: Handover 4.4 gmail fixture byte-for-byte AND ADR-008 path; N5 rejects name the field; tier enum IS @oikonomos/db riskTiers by identity (tested); url_ref N4 rejects literal URLs/userinfo/assembled credential params; ADR-005 liveness (missing-dir + zero-manifests) with TWO self-anchors; CLI exit-0 only on all-valid AND >=1 found. Independent delegated run in review worktree: full suite green, lint 0, canaries 15/2skip, connectors 22/22 - matches Test_Evidence. Mutation INDEPENDENTLY re-proven: literal->string kills 4 tests across 3 files. Merged --no-ff (c53336e), branch deleted. ORCH wired validate script + CI step post-merge, proven both directions (planted bad manifest exits 1 naming file+field). Unlocks TASK-044 (CX registration pipeline). NOTE: gmail.yaml now live - TASK-048 narrows to evals + registration run. | first-pass: yes | 2026-08-18T15:45Z |
| TASK-046 | CX | rework | OIK-051 golden-eval harness. Substance GOOD: composeHarness-only execution, harness-invocation liveness counter MUTATION-VERIFIED by ORCH (deleting the increment kills the fixture test), zero-task suite fails UNOBSERVABLE, independent run matches Test_Evidence (4/4 + full suite/lint/canaries green), territory clean. TWO BLOCKING in the runner core: (1) governance stubs HARDCODED not injected - runSuite bakes in an always-allow handlePreToolUse and a no-op auditSink; same code path is the live `run` CLI and OIK-051 evals feed G-CONN unlocks, so a live eval run executes with enforcement stubbed out (quality bar Governed; ADR-001 spirit); draft-only AC enforced only at suite-YAML validation, not the harness layer. Fix: injectable seams with a fail-closed draft-only default + a test that a Tier-3 call through the composed harness is DENIED. (2) loadSuite reads only definitions[0] - extra task files silently dropped; silent coverage truncation inside the >=90% certification machinery is the ADR-005 s2 failure mode. Fix: load ALL definition files (or fail loudly on >1) + test. Non-blocking: derive draftOnlyTiers from riskTiers, restore deleted dossier brief (dossiers are append-only), reconcile ignored evals.suite path. | first-pass: no | 2026-08-18T15:45Z |
| TASK-046 | CX | approved (2nd pass) | Both round-1 blockers closed and independently mutation-verified: default broker seam is DENY (flip-to-allow kills the harness-seam test, which asserts a Tier-3 tool name denied at the composed harness's canUseTool); loadSuite merges all definition files (first-file-only revert kills the merge test). Baseline matches Test_Evidence (6/6; full suite 346/51skip; lint/canaries green). Dossier brief restored with interface contract. Carried to TASK-048: draftOnlyTiers still literals (work-log claim of DB derivation is inaccurate - flagged); default auditSink silently discards, live runs must inject both seams. Merged --no-ff (cc8690b), branch deleted. CX now free for TASK-044 (unlocked by TASK-043). | first-pass: no | 2026-08-18T16:20Z |
| TASK-044 | CX | approved | OIK-048 registration pipeline, first pass. CX blocked CORRECTLY on shared-DB destructive-test authority; ORCH resolved via throwaway pg16 (TASK-035/038 pattern) - db 29/29 executed zero skips, canaries 17/17 incl CAN-06/07. Both mutations killed independently: validator-guard bypass (store untouched on invalid input) and deregister-WHERE-widened-to-everything (two-connector byte-identical + FK). Unrequested hardening: FOR UPDATE ownership guard blocks cross-connector capability_id theft. One transaction per op, no migrations, territory clean. Merged --no-ff (f509b54). Unlocks TASK-045 (GB); TASK-048 Gmail now fully unblocked (043+044+046 done). | first-pass: yes | 2026-08-18T16:50Z |
| TASK-045 | GB | approved | OIK-049 enumeration, first pass. Territory clean, preflight verbatim. Both named suites confirmed EXECUTED; two mutations killed - ok=true reddens 4 tests including the real-policy deny test (real coupling, not tautology), zero-tools branch removal reddens exactly the ADR-005 liveness assertion. Deny driven through the REAL resolveCapabilityTier. Beyond spec: listTools-throw fail-closed, duplicate/empty tool_name rejection, deterministic sort, injected clock. GB correctly FLAGGED rather than reached outside territory for the policy dep; ORCH wired @oikonomos/policy + package specifier at merge (113aa2c). Merged f273b5e. COMPLETES THE E6 PIPELINE. | first-pass: yes | 2026-08-19T05:25Z |
| TASK-052 | GB | approved | Live MCP mount in composeHarness — the foundational gap; MCP tools now exist at runtime and provably cannot skip L1. PROTECTED, different-model holds (GB=grok / ORCH=opus). Territory clean, 79/79 harness-factory, full suite 399/52skip, lint+canaries green, matches Test_Evidence. BOTH mutations caught on DISJOINT test groups: mcp__* L1 early-return auto-allow reddens 3 (incl. the decisive traversal + N3 fail-closed parity); mount-as-no-op reddens the 4 N9 SDK-options tests. Reviewer note recorded: the decisive L1 test does NOT fail under the mount mutation — it tests L1 independently of mounting, so no single test covers both properties. N4 verified at source (no error path interpolates url/header/command/arg; never reads process.env). Precedence correct — caller options spread first, mcpServers last, composeHarness sole setter. hooks/ and L1/L2/L3 untouched. Merged 3015ad6. | first-pass: yes | 2026-08-20T15:05Z |
| TASK-061 | S5 | rework | db read/CRUD layer. Strong substance: live-DB legs PROVABLY executed (0 skipped with DATABASE_URL vs 28 without, real timings), read-only audit clean (all four list/get functions SELECT-only; auditEvents.ts has no UPDATE/DELETE path — OIK-013 append-only holds), SQL audit clean (all caller input via $N). ONE BLOCKING in its own territory: the pagination test asserts a guarantee it doesn't test — deleting the `task_id DESC` tiebreaker leaves 'cursor never repeats or skips a row' PASSING, because every fixture row has a distinct created_at. Same code-correct/test-blind shape as TASK-017; fix with same-timestamp fixture rows. Non-blocking: broken $N parameterisation also undetected (AC's 'review-visible construction' route is satisfied — ORCH verified — so not blocking, but an assertion would close it); filter.status unvalidated against taskStatuses. NOT S5's fault and ORCH-handled: the red persistence-surface pin is outside its Owned_Paths and it escalated correctly (same call GB made on TASK-045); ORCH verified listPendingApprovals is a pure SELECT and added a source-level N8 guard (exactly one UPDATE in approvals.ts, zero DELETEs) so the control ends up stronger, with the name pin landing as part of 061's merge. | first-pass: no | 2026-08-20T15:05Z |
| TASK-062 | GB | approved | Atomic pending->granted/rejected decisions. PROTECTED, different-model holds. Territory clean; existing approvals/test/** diff EMPTY (13 files byte-identical — nothing weakened to pass). Postgres legs proven to EXECUTE by differential run (5 skipped without DATABASE_URL vs 0 with, real 549-1081ms latencies). THREE guard mutations by ORCH: dropping status='pending' reddens 6 tests incl. concurrency legs (parallel grants 1 -> 16/16, and a REJECTED row became granted); dropping expires_at>now() grants a real expired row. N8 SEPARATION CONFIRMED EMPIRICALLY by an ORCH script on the live DB: post-grant status='granted', consumed_at STILL NULL, and a later verifyAndConsume SUCCEEDS — grant does not consume, approval stays spendable. Recorded non-blocking: the consumed_at IS NULL guard on the grant path is unreachable by construction (a pending row cannot be consumed), so its only sentinel is a SQL string match — correct to keep, nominal not behavioural. Merged 346c811. | first-pass: yes | 2026-08-22T06:40Z |
| TASK-061 | S5 | rework (2nd) | db read/CRUD. FOUND A REAL PRODUCTION BUG unprompted while fixing round 1: pagination cursors built from Date.toISOString() (ms) against microsecond timestamptz silently dropped any page-boundary row with a non-zero microsecond remainder, plus its whole ms bucket, from every later page — common path, unrelated to ties. Fixed via server-side ::text, mirrored into runs.ts; ORCH mutation-verified 3/3 RED on revert. BLOCKING: round-1 finding narrowed not closed — ORCH measured the tiebreak mutation over 17 runs: 14 RED / 3 GREEN = ~82% catch, ~18% escape, i.e. 1 CI run in 6 passes with `task_id DESC` deleted. The in-test comment claims 'close to certainty'. Fix: keep the behavioural test, ADD a deterministic source-level ORDER BY assertion (the pattern TASK-062 uses), and correct the comment to the measured rate. ORCH-owned, not S5's: branch is red on the persistence-surface export pin, scoped to ORCH merge wiring in round 1. | first-pass: no | 2026-08-22T06:40Z |
| TASK-061 | S5 | rework (3rd) | Deterministic source-level ORDER BY pin is exactly right in substance, and the 'close to certainty' overclaim was corrected to ORCH's measured 14/17. BLOCKING — ADR-005 failure mode inside the fix itself: both new pins are nested inside the DATABASE_URL-gated `integration(...)` describe, so they are describe.skip'd when no DB is present. ORCH ran the branch with DATABASE_URL unset: 30 skipped, the pins among them. They readFileSync a source file and need no DB, and CI's `test` job has NO DATABASE_URL (only canaries does) — so the 100%-catch sentinel that justifies keeping the 82% probabilistic one never runs in CI, and the tiebreaker could be deleted with the pipeline green. Fix: hoist both describes to top level; verify they execute with DATABASE_URL unset. | first-pass: no | 2026-08-22T07:35Z |
| TASK-061 | S5 | rework (4th) | Round-3 fix VERIFIED AND CLOSED: pins execute with DATABASE_URL unset (4ms, not skipped) and the tiebreak mutation now reddens 3/3 deterministically, up from 14/17. NEW BLOCKING, found only by an ORCH merge simulation: the round-1 tie fixture is not isolated and reddens master under `pnpm -r test` (1 in 4 recursive runs, never in isolation). Structural: the test filters {tenantId:'basileia', status:'draft'} while cleanup deletes by role_id, and nine other suites INSERT INTO tasks with default tenant/status against the same DATABASE_URL — parallel `pnpm -r` lets a foreign draft row take the head of the result set. Sibling listRuns test is unaffected because it scopes by its own taskId. Test-isolation defect, not a product bug, but blocks merge per the TASK-028 precedent (flaky red on master corrupts downstream green checks). Fix: dedicated tenantId for fixture rows (TaskListFilter already supports it); verify with 3x green `pnpm -r test`. | first-pass: no | 2026-08-22T08:05Z |
| TASK-061 | S5 | approved (4th pass) | db read/CRUD layer, merged after 4 genuine rework rounds. Round-4 tenantId isolation fix INDEPENDENTLY RE-VERIFIED: full merge simulation against current master, 5x consecutive pnpm -r test, all exit 0/498 tests, zero recurrence of the round-3 flake; round-3 deterministic pins confirmed still executing with DATABASE_URL unset. Merged 6e12e62; listPendingApprovals wired into the persistence-surface N8 pin, verified live 51/51 against fresh pg16 plus full suite green both with and without DATABASE_URL. Round history: r1 code-correct/test-blind pagination assertion; fixing it surfaced r2's unprompted find of a real production data-loss bug (ms cursor vs microsecond timestamptz); r3 found r2's fix was DATABASE_URL-gated and inert in CI; r4 found the original r1 fixture itself reddened master under pnpm -r. Three of four rounds findable only by tooling beyond a builder's own run (mutation sampling, CI-env diffing, merge simulation) — not a builder-quality signal. Two pack-process defects surfaced along the way (#17 fabricated control block, #18 no-op resume emitting needs_review) — both logged, neither S5's fault. Unlocks TASK-056 (control-api). | first-pass: no | 2026-08-22T08:40Z |
| TASK-056 | S5 | approved | control-api tasks/runs/approvals/evidence endpoints, first pass, third dispatch attempt (first two killed mid-session by the harness; ORCH preserved real progress as labeled WIP safety-net commits both times rather than discard it). Territory clean. ControlApiDeps is the sole persistence surface - app.ts has zero direct @oikonomos/db calls and zero @oikonomos/approvals import. Both liveness controls proven live: no-raw-sql catches a planted SQL literal; decide-route delegation bypass reddens 8 tests across 3 files incl. the real-DB N8 integration test. Full suite 530/530, lint 0, canaries 17/17, DB legs confirmed executing (not skipped). N4 verified against real pino output — nonce never appears in logs. OpenAPI covers all six routes. Merged ef3914b. Fastify wired via lockfile on master (b8e2b9f) — territory hook blocks lockfile commits on any task branch regardless of identity, same as TASK-043. Unlocks TASK-057 (CX). | first-pass: yes | 2026-08-22T09:45Z |
| TASK-053 | GB | approved | manifest -> MCP config with secret-ref resolution. Territory clean; connectors 50/50, full suite green after clean build, lint 0, canaries 17/17. GB's reported control-api failure did NOT reproduce (stale dist) — right to flag, not paper over. Both mutations caught (unset-returns-empty; dropped N5 ownership guard), one assertion each. N4 PROVEN DIRECTLY by four assertions incl. the strongest: a resolver throwing `leaked ${VALUE}` is wrapped as SECRET_UNSET containing neither the value nor 'leaked'; test sentinel assembled at runtime so the file holds no contiguous secret. Strong design: foreign resolver throws are caught with cause DISCARDED, so a leaky third-party resolver cannot propagate. Residual for TASK-055: resolved url lives in the returned config, so a caller that logs it would leak. Unlocks TASK-054 (credential-gated). | first-pass: yes | 2026-08-23T21:30Z |
| TASK-064 | CX | approved | pending-guarded approval invalidation primitive. PROTECTED, different-model holds. CX implemented fully, blocked only on live-DB (refused an unscoped container — same right instinct as TASK-044); ORCH resolved via throwaway pg16. Both hard constraints held: INVALIDATE_APPROVAL_SQL byte-identical, existing test/** diff EMPTY. Approvals 92/92 0 skipped, full suite 562/0, lint+canaries green, DB legs proven live (22 skipped without DATABASE_URL vs 0 with). DISJOINTNESS PROVEN TWICE — tests plus an ORCH direct-DB probe: each path returns rowCount 0 against the other's status and leaves it unchanged, both positive controls still work. M1 (drop status='pending') reddens 4 tests, 3 behavioural incl. the concurrency leg. Non-consumption defended at 3 layers. Minor recorded: M2's consumed_at clause is behaviourally redundant today so the SQL pin is necessarily its only sentinel; 3 of 5 refusal statuses are memory-only. Unlocks TASK-063. | first-pass: yes | 2026-08-25T06:30Z |
| TASK-080 | GB | rework | PROTECTED, different-model holds (opus-4-8 adversarial vs GB author). Every stated AC independently verified true, incl. two ORCH-run mutations beyond the builder's own (removing BEGIN, and running the invalidate on an independent connection per the AC's literal wording — both red, the second not caught by the source-level pin, only the live behavioural test, confirming that's the real control). Transaction lifecycle correct by construction (ApprovalClient exposes only `query`, so the pool.query()-by-accident bug class is structurally impossible); client.release() proven via an 8-iteration fault loop; all existing SQL byte-identical master-vs-HEAD via git show; ADR-004 digest/render built from one literal object, no stale-reference path. DB legs live (106/106 vs 77/29), gates reproduced exactly. REWORK is one finding beyond the stated ACs: editApproval never compares the replacement's run_id/capability_id/tenant_id against the row it just invalidated, so a successful edit can mint a replacement for a DIFFERENT run/capability/tenant inside the same commit — capability_id drives broker tier/policy matching, so this is a tier-laundering shape the ADR-004 digest re-binding does not close, and TASK-063 (the consumer) cannot enforce it from outside since it's forbidden from reaching into this package and only sees the result post-commit. ~8-line fix + one test, fully in-territory; added as an explicit new AC. Folded into the same round: testHooks is exported production API surface (bounded risk, tag @internal); add coverage for still-pending-but-past-expiry-before-sweep. Advisory only: validation helpers now triplicated across issue.ts/store.ts/editApproval.ts (extraction task worth opening); INSERT_APPROVAL_SQL is a pinned-substring copy of packages/db's shape, not a reuse; no lock/statement timeout (package-wide, pre-existing). | first-pass: no | 2026-08-26T18:20Z |
| TASK-070 | CX | rework | PROTECTED, different-model holds (opus-4-8 adversarial vs CX author). Territory clean, all 5 stated ACs literally met, all suites green, own reproduction confirmed both controls genuinely fire on planted defects in an isolated clone. REWORK on two demonstrated behavioural defects: F1 — publication-tree.mjs's executable-mode-restoration loop runs BEFORE the tree comparison and dies with an opaque exit 2 (no omitted-file report) when the swallowed file is executable, reproduced against infra/backup/backup.sh and infra/ci/hooks/pre-commit; exit 2 is this codebase's established "control could not run" code elsewhere, so a wrapper could misread a true positive as an infra flake — the ADR-005 failure mode, one layer removed. F2 — the RFC 6761 reserved-domain allowlist is exact-match only, so api.example.com (a canonical documentation host RFC 6761 explicitly reserves alongside example.com) is flagged live, AND the shipped test pins that wrong behaviour as intended. Fails safe, not a security gap, but breaks on ordinary fixtures and the test itself needs correcting. Advisory, logged for follow-up not this round: negative-controls' fixture/hostname/env-name matching is narrow enough to be near-inert against this repo's real layout (1 of 11 planted bad values caught, and that one was the F2 false positive — though raw key material is still caught correctly by the pre-existing secret-scan.mjs); new controls absent from infra/ci/run-local.mjs (outside this task's Owned_Paths, not the builder's fault — local CI mirror can show green when GitHub CI would not); assorted minor UX/robustness items. | first-pass: no | 2026-08-26T18:00Z |
| TASK-080 | GB | approved | Round 2. Required finding closed at the strong level: assertIdentityBound runs inside the same transaction against the row-locked invalidate result (no TOCTOU), checks all 3 fields, and the INSERT sources tenant_id/run_id/capability_id SOLELY from that row — caller input for those fields never reaches the write. Reviewer's own mutation (call-site only, keeping the function body so a source-pin couldn't mask it) reddened all 3 fields independently, stronger than the builder's own evidence which named only capability_id. A 7-scenario live probe targeting the real escalation shape (email.create_draft→email.send, T3) confirmed zero writes on every rebind attempt including the original bug trigger. F2/F6 confirmed closed. Round-1 properties re-confirmed untouched, territory clean, gates: build clean, 113/113 with DB across 16 files, all 16 workspaces green recursively, lint clean, canaries 17/17. Merged. 4 non-blocking notes: INSERT-sourcing has no independent regression test (source-pin is satisfied by the guard's own body, live protection holds today per the mutation); omitted tenantId is now a hard refusal for non-default tenants — carried forward as a new AC on TASK-063; capability_id test fixture uses a downgrade not the real escalation shape (verified live by probe instead); round-1 advisories unchanged. Unlocks TASK-063. | first-pass: no (round 2) | 2026-08-26T21:00Z |
| TASK-070 | CX | approved | Round 2. All 3 required fixes independently reproduced (not from builder's tests): F1's exit-2 bug reproduced on pre-fix code in a fresh scratch repo to confirm causation, fix confirmed to generalize beyond the shipped fixture; F2's subdomain fix probed against 9 hostnames specifically for substring-overshoot, none slipped through; F3's new mutation test reddened under 4 independent reverts. Territory clean, banned-modes.mjs/ci.yml re-confirmed unchanged/additive. Gates: pnpm -r test 494/68/0, lint clean, canaries 15/2. Merged. 5 non-blocking follow-ups recorded (latent non-ASCII path bug in the same code the fix touched, a path-resolution inconsistency, a replaced-not-added test losing coverage, an unpinned overshoot boundary) — none gate, none worse than round 1, candidate for a small bundled follow-up task later. | first-pass: no (round 2) | 2026-08-26T20:45Z |
| TASK-063 | S5 | rework | Every stated AC mutation-proven true (deleting tenant forwarding, injecting raw SQL, and removing packages/approvals' BEGIN each reddened a real test, the last one via the actual public-HTTP transaction path, not a mock). Territory clean, gates reproduced (603/603 full recursive suite, canaries 17/17). Rework overrides the reviewer's own "merge + follow-up ticket" recommendation: expiresAt is caller-supplied with zero bound validation — a past timestamp is ACCEPTED and silently bricks the approval (invalidates the original, replacement inserted already-expired, permanently ungrantable) — the exact "invalidated with no replacement" state the task's own hazard section forbids, reached through ordinary use with no forced fault. Added as an explicit new AC, same treatment as TASK-080 round 1. Folded in: document 400 in the OpenAPI schema (currently only 200/409, yet 400 is the only signal for both new refusal paths this task introduces). Advisory only: dual digest encoding in one response body; pre-existing error-mapping pattern; a genuinely repo-wide finding worth flagging — cross-package integration tests resolve to dist/ not src/, so "integration green" is only as trustworthy as the last build. | first-pass: no | 2026-08-26T21:30Z |
| TASK-063 | S5 | approved | Round 2. Required fix genuinely closed, verified by independent live reproduction (own throwaway Postgres, own fixtures, 12 direct DB probes) not by trusting shipped tests: validateEditExpiresAt runs before editApproval; TTL sourced from packages/approvals' real DEFAULT_APPROVAL_TTL_MS. Three reviewer mutations: disabled non-future guard exactly reproduced round 1's original bug; disabled TTL upper bound reddened; moved validation to AFTER editApproval (the specific ordering trap asked about) still returned 400 at HTTP but reddened 4 tests via call-count/status assertions, proving the ordering is mechanically enforced not coincidental. OpenAPI 400 validated at runtime, real shared ErrorResponse schema, both /edit and /decide. Round-1 properties unweakened (zero deleted test lines). Gates: 59/59 with DB, 609/609 recursive, lint clean, canaries 17/17. Merged. One residual, not blocking (fails round-1's own "ordinary use" bar): a ~40-50ms window where a deliberately-chosen expiresAt could still land in the bricked state; recommend a minimum floor before TASK-058 ships. Unlocks TASK-058 (demo centerpiece) and TASK-065. | first-pass: no (round 2) | 2026-08-26T22:00Z |
| TASK-057 | CX | approved | Telegram intake + run status. Territory clean; gateway 54/54, full suite green, lint 0, canaries 17/17 (CX's flagged connectors failure also didn't reproduce). Ports-only: Telegram + control-api clients injected, no SDK, @oikonomos/db not even a declared dep, base URL validated absolute http(s). Allowlist mutation caught by an assertion that checks the control-api client was never CALLED — right shape for authz. Credentials scan clean (only control.example.test, RFC-6761). Recorded non-blocking: the no-DB-import guard works (verified by planting into a file no test imports) but sits behind the stronger fact that the dep isn't installed — belt-and-braces, not load-bearing. Pre-existing, not this task: test/src/ holds tracked shadow copies of salvaged bot source — warrants a cleanup ticket. Unlocks TASK-058 (approval keyboard). | first-pass: yes | 2026-08-23T21:30Z |
| TASK-048 | S5 | approved | OIK-052 Gmail - the platform's FIRST REAL CONNECTOR. Territory clean; work committed by ORCH after an S5 session-limit interruption (TASK-012 recovery precedent, not a violation). Independently verified both directions through the real OIK-051 runner: positive pass_rate 1 / 4 harness invocations / exit 0, negative pass_rate 0 / exit 1 - scoring provably live. No gmail.send scope; email.send T3_external enabled:false confirmed in the registration row. S5 correctly refused to fix two out-of-territory inconsistencies and flagged them (evals.suite path -> ORCH resolved per ADR-008; role_grant cross-product -> recorded as a non-unlock). Carried caveat: substring 'contains' assertions would pass on boilerplate - strengthen before treating evals as quality evidence. Merged 68520b9. docs/connectors/gmail.md written (OIK-050). G-CONN REMAINS CLOSED pending Alister. | first-pass: yes | 2026-08-19T05:25Z |
| TASK-058 | CX | approved | Telegram approval inline keyboard (OIK-086) — the demo centerpiece. Territory clean (4 files: 3 Owned_Paths + own dossier; Owned_Paths had been extended by ORCH 2026-08-27 to include src/index.ts after CX's third correct block on the same decompose-gap family). c8b9872 preflight evidence in dossier, twice. ACs verified against spec + code: stored actionRender used verbatim (ADR-004), never re-composed; Approve/Reject/Edit fully delegated to control-api (decide/edit routes); nonce kept in an in-process Map behind a random UUID handle — callback data carries only `approval:<uuid>:<action>`, tested to not contain the nonce (N4); N8 source-pin guard test (no consume identifiers, no @oikonomos/approvals|db import) present; double-tap resolved at the producer, HTTP 409 mapped to already-resolved. Wire contracts independently cross-checked against control-api's serializeApproval (nonce+actionRender present) and ApprovalWaitSignal (superset of gateway's ReplacementApproval). Independent run at 518a263: build 0, lint 0, canaries 0; pnpm -r test 351 pass / 96 DB-gated skips / 1 fail = evals/harness CAN-03 5s spawnSync timeout under full parallel load, confirmed flake by isolated rerun (1/1) + full package rerun (15/15) — unrelated to this task, matches CX's own noted load-flake class. Non-blocking: approval-handle Map never pruned after decisions (unbounded growth in a long-lived process) — folded into TASK-059/060 wiring note. Merged. Unlocks TASK-059 (evidence delivery). | first-pass: yes | 2026-08-27T10:30Z |
| TASK-058 | CX | (post-merge addendum) | CONCURRENT SESSION COLLISION: this session's own adversarial review of TASK-058 was still running when the entry above merged (~20 min apart, both [ORCH]-tagged; multiple `claude` process trees confirmed active simultaneously on this repo — a second Claude Code session is working this project in parallel). Not reverting — core implementation independently confirmed correct (N8/N4/ADR-004 all sound, N8 genuinely mutation-proven though by a different test than the one whose name claims it). This session's review found two real gaps the merging review missed: both callback-path authorization checks (chat allowlist + cross-chat handle binding) are completely untested — deleting both leaves all 60 tests green, the exact ADR-005 configured-but-inert class, on the actual Tier-3 decision path; and the approvals module is unreachable from outside the package (no re-export, no exports-map entry), so TASK-059/060 cannot import it as shipped. Also found a different pre-existing control-api N8 flake (openapi + decide.route timeout under parallel load) than the CAN-03 flake the merging review recorded — worth its own ticket. Remedy: TASK-082 opened (critical, same territory) to fix both gaps plus two cheap fold-ins; TASK-060 now depends on it. | n/a — see TASK-082 | 2026-08-27T10:45Z |
| TASK-069 | S5 | approved | Closed error registry with payload allowlist (packages/shared/src/errors). Territory clean (5 files, all Owned_Paths + dossier). All 6 ACs independently verified with direct code+test evidence via a dedicated verification subagent, not the builder's summary: unregistered errors drop payload entirely (GENERIC_TAGS); registered errors emit only declared fields, charset `/^[0-9A-Za-z._|:-]{1,64}$/` and 64-char bound enforced; retryable read straight off the definition; explicit N4 test proves a credential-shaped string in an undeclared/out-of-charset field never reaches emitted tags, round-tripped through JSON.stringify. Mutation test (removed the charset check) reddened 3 tests, confirming the assertions are load-bearing not vacuous. Zero package.json diff — no new runtime deps. Full pnpm -r test/lint/canaries independently re-run, all exit 0. Merged. Not on the demo critical path (post-MVP backlog per docs/STUDY-grok-bot-018.md). | first-pass: yes | 2026-08-31T21:50Z |
| TASK-072 | S5 | approved | agent-providers budget hook + registration completeness + namespaced metadata. Territory clean (6 Owned_Paths files exactly). All 5 ACs verified with direct code+test evidence via independent subagent: BudgetSink invoked once/turn before turn_complete surfaces, throwing/rejecting sink fails the turn (7 tests); ValidatingProviderRegistry.register throws IncompleteProviderError naming missing members (8 tests); NamespacedProviderExtras is additive, types.ts untouched, typecheck clean; the 3 metadata tests are legitimate in-source vitest tests (includeSource config), not a gap as first appeared. Provider implementations (providers/*.ts) confirmed untouched beyond wiring. Mutation test (swallowed budget-sink throw) reddened 2 tests as expected. Full pnpm -r test/lint/canaries independently re-run, exit 0. Non-blocking process finding: dossier was uncommitted at handoff, added before merge for a complete audit trail. Merged. | first-pass: yes | 2026-09-01T00:25Z |
| TASK-082 | CX | approved | Telegram approvals authorization test coverage + package export (critical, post-merge fix for TASK-058). Two rounds. Round 1: 9/10 ACs pass with direct evidence — MUTATION-PROVEN authorization checks (AC1-3): reviewer's own mutations of the chat-allowlist check and the cross-chat handle-binding check each independently reddened their test, controlApi.calls verified EMPTY on refusal. AC4 failed — package.json exports `./approvals` correctly but no test resolved the bare package specifier, only a relative import existed; sent to rework with a precise, additive fix specified. Round 2 (commit 00838f3): new test imports `@oikonomos/gateway-telegram/approvals` as a genuine bare specifier via Vite's self-reference exports resolution (no symlink hack); mutation test (removed the exports entry) failed to resolve exactly as expected, reverted clean. All 9 ACs now pass, gateway-telegram 63/63, full pnpm -r test/lint/canaries independently re-run twice, exit 0. Merged. Unlocks part of TASK-060's dependency chain (059/055 still outstanding). Also surfaced and fixed an infra gap this round: git worktrees don't inherit node_modules — pnpm install now run in both active worktrees. | first-pass: no (round 2) | 2026-09-01T00:50Z |
| TASK-074 | S5 | approved | packages/shared named scheduling policies (Deadline/Retry/Polling/IdleWatchdog/Debounce) + canonical-digest fixture pin. Territory clean (5 files, Owned_Paths + dossier). All 5 ACs verified with direct code+test evidence via independent subagent: genuine injected-Clock DI on every timer call site (no hidden Date.now()/setTimeout bypass), all timers .unref()'d, eager RangeError validation, zero new deps, digest pins are literal hex and non-vacuous (mutation test on canonicalJson.ts's .sort() reddened 3 tests live, reverted clean). canonicalJson.ts/actionDigest.ts confirmed byte-identical to master (N10 respected — no platform-wide digest law touched). Full pnpm -r test/lint/canaries independently re-run, exit 0. Non-blocking finding: builder's Test_Evidence claimed a pre-existing harness-factory/corepack failure "confirmed unrelated on a clean stash" that did not reproduce at all in the reviewer's independent run (suite was fully green) — task's own gates still pass so this doesn't block, but the claim itself was inaccurate and shouldn't be treated as precedent. Merged. | first-pass: yes | 2026-09-01T00:55Z |
| TASK-075 | S5 | approved | packages/db intake idempotency ledger (nonce + input-digest binding). Territory clean (4 files, Owned_Paths + dossier). admitIntake is one atomic INSERT...ON CONFLICT(tenant_id,client_nonce) DO NOTHING RETURNING; a live 8-way concurrent Promise.all race against the running local pg16 (not mocked) independently reproduced exactly 1 dispatch/7 duplicates. Same-nonce-different-digest raises typed IntakeDigestMismatchError, tested. No crypto import in the module (digest is caller-supplied via @oikonomos/shared per N10), zero new packages/db deps. Mutation test (weakened ON CONFLICT to DO UPDATE) reddened 4 tests incl. the race test and a source-pin, reverted clean. Migration applied up->down->up live by the reviewer, schema verified, no orphans. Full pnpm -r test/lint/canaries independently re-run, exit 0. Merged. | first-pass: yes | 2026-09-01T01:20Z |
| TASK-059 | CX | approved | Telegram evidence delivery with the approval request (OIK-087). Unusual process journey: 5 dispatch rounds, 4 of them legitimate territory/tooling gaps discovered incrementally (index.ts+approvals.ts+audit/index.ts -> package.json workspace dep -> persistence-surface.test.ts's pinned export list -> pnpm-lock.yaml sync), each verified against source before re-carving rather than rubber-stamped; rounds 3-4 explicitly escalated to and confirmed by the user per this session's self-set 3-gap threshold. The actual code was reviewed for the first time only after round 5. Both safety-critical properties independently mutation-proven: (1) truncation ALWAYS appends a visible marker, even in the degenerate small-limit case — confirmed by reddening the test with a bare-slice mutation; (2) redaction genuinely reuses packages/audit's newly (additively) exported redactPayload, not a second implementation — confirmed by reddening the test with a bypass mutation that leaked a fake API key into the rendered output. Evidence fetch is HTTP-only via control-api's /runs/:id/evidence (OIK-084, no DB import). persistence-surface.test.ts's mutating-name-pattern guard (the actual append-only protection) stayed fully intact — only its literal export list was extended by one name. Full pnpm -r test/lint/canaries independently re-run, exit 0, gateway-telegram 68/68. Merged. Unlocks TASK-077; closes another TASK-060 dependency (055 still Gmail-gated). | first-pass: yes (code); process: 4 legitimate territory rounds first | 2026-09-01T06:20Z |
| TASK-077 | S5 (round 1) / ORCH (final fix) | approved | packages/audit persistent capped outbox + Retry-After backoff + exhaustive-union formatter. Unusual process journey: outbox.ts/format.ts logic was correct and mutation-tested from round 1, but the barrel export to packages/audit/src/index.ts was never wired (same export-list-pin conflict TASK-059 already resolved on this package) — REWORK round 1 with a precise fix specified. Two subsequent resumed S5 sessions each reported "no code changes needed" and made zero commits despite an explicit, literal instruction (a documented resume-state process gap, same class as TASK-061/063) — escalated to the user at the 3rd occurrence rather than redispatching blindly. Per user decision, ORCH wrote the 2-file additive fix directly (index.ts exports + persistence-surface.test.ts pin extension), verified locally, then sent for independent review under deliberately elevated scrutiny given the unusual authorship. Confirmed: outbox.ts/format.ts byte-identical to the original round-1 commit (nothing already-reviewed was touched); Object.keys() on the real built module matched the pinned list 13/13; a genuine cross-package import test from packages/broker confirmed all 8 new symbols resolve correctly at runtime, not just present in source. Full pnpm -r build/test/lint/canaries independently re-run twice, exit 0. Merged. | first-pass: no (3 process-gap rounds, then ORCH-authored fix approved) | 2026-09-01T07:40Z |
| TASK-065 | CX | approved | packages/approvals bind approvals to control-plane generation + user-context epoch (protected path, restart/redirect invalidation). Longest task of the wave: 4 legitimate territory gaps (packages/approvals -> packages/db -> a DATABASE_URL infra gap -> a race-test parameter fix) plus 2 real rework rounds, all resolved without a single quality shortcut — every block was the builder correctly refusing to fake a pass rather than a defect. Round 1 code review: 5/6 ACs pass with strong evidence, MUTATION-PROVEN atomic consume guard (removing the generation clause reddened 18 tests incl. the exact-SQL-text pin, reverted clean); the 1 gap was a genuine cross-package regression — the new Approval.userContextEpoch bigint field broke GET /approvals JSON serialization in services/control-api, proven against master. Round 2: fix confirmed narrowly scoped to serializeApproval's existing Buffer-conversion convention, round-1 work confirmed untouched, and the reviewer independently proved the actual regression is fixed (not just the narrow existing test fixture) by inserting a real bound approval and checking the live HTTP response body. Full pnpm -r test/lint/canaries independently re-run across both rounds, exit 0 (approvals 119/119, control-api 59/59). Migration 002 applied live to the shared Postgres. Merged. | first-pass: no (2 rework rounds after 4 territory rounds) | 2026-09-01T11:15Z |
| TASK-078 | CX | approved | packages/policy tier ceiling as a rank clamp (protected, "org policy only tightens"). Territory clean (3 Owned_Paths files). Clamp direction verified correct across the full 5x5 tier-rank matrix (always returns the stricter tier). Genuinely wired as the single exit point in resolveCapabilityTier — old private resolver deleted, replaced. Two independent mutations reproduced: inverted comparison reddened 4 tests, bypassing the clamp entirely reddened 1; both reverted clean. Exhaustiveness reproduced live (scratch enum member without a rank failed typecheck). Zero I/O imports preserved, existing tests byte-identical. Full pnpm -r test/lint/canaries independently re-run, exit 0. Merged. | first-pass: yes | 2026-09-01T13:35Z |
| TASK-054 | GB | approved | Live-server enumeration + allowedTools derivation from the manifest map (most safety-critical task in the plan given this session's send-tool discovery). Send/reply/forward denial proven BY EXACT NAME: allowedTools.test.ts/enumeration.test.ts assert absence from the allowlist AND independent denial when driven through the REAL production handlePreToolUse from packages/broker (traced to the actual capability.unregistered deny branch, not a duplicated check). Mutation-proven (disabling the enabled:false filter reddened 6 tests incl. the send_message-specific one). Fully-qualified-name-only construction verified. Full pnpm -r test/lint/canaries independently re-run, exit 0. DISCLOSED CAVEAT (non-blocking, flagged for TASK-055): the send/reply/forward proof runs against a local replay of ORCH's authenticated tool list, not a live authenticated call from OIKONOMOS's own headless credentials — the headless run reached the live server but got no bearer token, returning a reduced/unauthenticated surface that never exposed the send-capable tools. GB handled this exactly per the task's own fallback rule (disclosed, didn't fake it). Merged; TASK-055 will hit the same headless-auth wall unless resolved first. | first-pass: yes | 2026-09-01T14:00Z |
| TASK-081 | CX | approved | packages/policy four-way approval resolution (allow-once/deny/always/never) as a standing-mode decision primitive (protected). Territory clean. allow-once/deny pass through unchanged; always/never produce a persistable standing-mode value with zero I/O; non-retroactive epoch rule correct (stale requests fall through to ask). Ceiling composition satisfies the AC's intent via an independently-reimplemented analogous rank-clamp (different type domain than TASK-078, literal reuse wasn't sensible). Two mutations independently reproduced: epoch-guard removal reddened the non-retroactive test; a scratch enum member without a decision-table entry failed typecheck. Zero I/O imports confirmed. Full pnpm -r test/lint/canaries independently re-run, exit 0, policy 30/30 100% coverage. Merged. | first-pass: yes | 2026-09-01T14:00Z |
| TASK-083 | GB | approved (partial) | packages/connectors headless OAuth token acquisition for the Gmail MCP server. Territory clean (5 files, Owned_Paths + dossier). Real Google refresh_token grant shape verified against a local fake HTTP endpoint. Token caching/near-expiry refresh independently verified with an injected clock incl. concurrent-call coalescing. Authorization header attached only when a provider is configured, verified both directions via raw request-header inspection. All failure paths (bad grant, network error, invalid response) throw typed OAuthTokenError, never a silent unauthenticated fallback. N4 credential scan clean, incl. a dedicated self-check test asserting no real Google access-token-shaped string appears in source. Full pnpm -r test/lint/canaries independently re-run, exit 0 (connectors 82/4, corrects the dossier's self-reported 84/2 — non-blocking accuracy note). Merged. One AC deliberately deferred per the task's own design: live authenticated re-enumeration needs a human browser consent to mint a refresh token, not yet done — tracked, not dropped. Unlocks TASK-055's full dependency chain. | first-pass: yes (partial scope by design) | 2026-09-01T14:35Z |
| TASK-055 | GB | approved | End-to-end governed inbox-triage run through services/worker — THE DEMO CENTERPIECE, first fully governed/evidenced/live-adjacent proof of the OIKONOMOS control loop end to end. Maximum-rigor review, all 6 ACs pass with hard traced evidence. packages/** confirmed genuinely untouched. Single composeHarness mount path confirmed, with a dedicated test asserting no bypass import exists. THE CORE DEMO IS REAL ENFORCEMENT: T3 send denial traces through the actual PreToolUse hook -> real handlePreToolUse from packages/broker -> a genuine tier-ceiling deny (email.send registered T3_external, role max T1_draft) -- not a mocked/asserted denial. The denial's audit event is a real write through the actual recordDecision port. ADR-005 liveness MUTATION-PROVEN BY THE REVIEWER PERSONALLY: dropped MCP servers from the composeHarness call, 4 liveness tests went red, reverted clean. Run lifecycle verified via a real live Postgres round-trip (session_ref persisted, read back, cancellation reaches a real terminal state). Live tools/list independently re-run by the reviewer against the real Gmail MCP URL (network-latency-timed) -- honestly scoped to the unauthenticated surface, no overclaiming. Full pnpm -r test/lint/canaries independently re-run, exit 0, worker 25/25. Merged. First dispatch of this task died from a genuine tooling failure (GB session crash, no work lost); redispatch succeeded clean. | first-pass: yes (after 1 tooling-failure redispatch) | 2026-09-01T16:25Z |
| TASK-066 | GB | approved | packages/broker construction-time policy completeness + call-time allowlist re-check (protected). Territory clean (5 files, Owned_Paths + dossier). PolicyRegistry constructor genuinely throws synchronously on incomplete/stale policy coverage (PolicyMissingError/StalePolicyEntryError), not lazy. Call-time recheckAgainstManifest wired into decidePreToolUse, denies a tool absent from the manifest map even if mounted, independent of construction-time state. Mutation-proven: disabling the recheck call reddened 2 tests (the mutation-proof test itself plus a stale-tool-list deny test), confirming real behavioral coverage beyond the source-text pin the test also carries. 28 pre-existing L1 tests confirmed byte-identical. Correctly declined to touch harness-factory per the task's own stated boundary. Full pnpm -r test/lint/canaries independently re-run, exit 0, broker 45/45. Merged. Unlocks TASK-067. | first-pass: yes | 2026-09-01T16:55Z |
| TASK-067 | GB | approved | packages/broker describe-or-deny + model-directed denial guidance (protected). Territory clean (4 files, Owned_Paths + dossier), index.ts wiring correctly and deliberately excluded per the task's own scope. Undescribable tool calls genuinely deny, no allow fallthrough. Oversized target (>10,000 chars) boundary tested both sides. Deny decisions carry the full {code, humanReason, modelGuidance} shape, frozen; all 12 codes carry do-not-retry + alternative guidance. Mutation-proven: flipping the undescribable branch to allow reddened 5 behavioral tests. ADR-004 composition confirmed shape-compatible for future wiring (full reconciliation correctly deferred). 28 pre-existing L1 tests byte-identical. Full pnpm -r test/lint/canaries independently re-run, exit 0, broker 72/72. Merged. Unlocks TASK-073. | first-pass: yes | 2026-09-01T17:20Z |
| TASK-071 | CX | approved | packages/harness-factory tool-decorator enforcement seam (protected). One legitimate territory gap (compose.ts missing from Owned_Paths despite AC4 requiring it, first occurrence, handled directly), then first-pass code approval. withApprovalScope genuinely uses AsyncLocalStorage with unconditional finally-retire (tested including the throw path). withMandatoryCallId throws before execution on missing toolCallId. withToolTimeout uses an injected clock with correct timer-clearing on both branches. MUTATION-PROVEN: reviewer made compose.ts skip decoration for one tool, the "decorates every mounted tool" test went red, reverted clean. Decorator composition order confirmed deliberate (identity before timer, per-invocation scope so timeout races don't leak). N8 hygiene correctly not duplicated. 85 pre-existing tests byte-identical. Full pnpm -r test/lint/canaries independently re-run, exit 0. Merged. | first-pass: yes (code); 1 legitimate territory round first | 2026-09-01T17:25Z |
| TASK-068 | GB | approved | packages/connectors server-set-keyed enumeration cache with stale-while-revalidate. Territory clean (5 src files + test + dossier). Cache key genuinely sorted/deduped server-set; requested-vs-resolved key both recorded, verified against a real simulated mid-flight config-change race. Stale-while-revalidate confirmed to genuinely serve stale on refresh failure AND schedule a retry (not fail-forever), reproduced with the injected clock. getToolsForTurnStart confirmed structurally non-blocking. Partial-failure policy (one-server degrade vs sole-source error) confirmed with real failing-transport scenarios. SAFETY-CRITICAL: isolation from the allowlist/broker-recheck path confirmed structurally, not just by convention -- repo-wide grep found zero cross-imports, and a load-bearing DISCOVERY_CACHE_LAYER constant plus a source-regex test mechanically enforce it. Single mutable cache slot, no unbounded memory growth. Full pnpm -r test/lint/canaries independently re-run, exit 0. Merged. | first-pass: yes | 2026-09-01T17:50Z |
| TASK-079 | CX | approved | packages/agent-providers ephemeral loopback MCP bridge for CLI harnesses. Territory clean (3 src files + test + dossier), reassigned from GB (billing exhaustion) since this package carries no ownership-continuity restriction. Loopback+ephemeral-port binding confirmed literal with a defence-in-depth post-bind address check. Crypto-random capability path, 404/413 behavior all verified via real unmocked HTTP requests. Mount-time tool snapshot confirmed genuinely immutable. MUTATION-PROVEN: removing the broker call reddened 2 tests, reverted clean. Broker deny confirmed as a real MCP error result, never a thrown 500. Annotations confirmed verbatim-from-manifest (proven with a deliberately misleading tool name to disprove heuristic derivation). Teardown-in-finally confirmed via a real throw + post-teardown connection-refused check. No broker import cycle. Full pnpm -r test/lint/canaries independently re-run, exit 0, agent-providers 69/69. Merged. | first-pass: yes | 2026-09-01T18:35Z |
| TASK-073 | GB→CX | approved | packages/broker refusal memory (protected). GB built it then hit Grok budget exhaustion mid-mutation-proof; ORCH preserved the uncommitted work; after a worktree SYNC_MISMATCH fix, CX completed the proof. Independent review: all 6 ACs behavioral; two live mutations — consult-gate bypass reddened 4 tests, eviction-instead-of-saturation reddened 2 (fail-closed real). Digest via @oikonomos/shared (N10). Pre-existing tests byte-identical; full gates exit 0, broker 84/84. Notes: not yet wired into PreToolUse — inert until TASK-087, which owns the liveness assertion. Merged. | first-pass: yes (after budget/sync interruptions) | 2026-09-01T19:50Z |
| TASK-086 | CX | approved | packages/policy EnforcementClass resolver + require-approval precedence (protected, critical — the core mechanism the entire ADR-010 enforced/autonomous split rests on). Territory clean. Six ranks confirmed correctly ordered and exhaustively tested (fail-closed > fixed floor > require-approval rule > refusal-memory > grant ceiling > autonomous). Fixed floor confirmed unconditional (alwaysAllow cannot subtract from it). Rank-1-above-floor safety property tested with ALL other conditions present simultaneously - rank 1 still wins. Two independent mutations: swapping the floor/rule order reddened 6 tests, deleting E1_payment from the floor reddened 2. Correctly composes with (does not duplicate) TASK-073/078 via opaque boolean pass-throughs, architecturally required by policy's zero-I/O constraint. ceiling.ts/RiskTier byte-identical to master. Addendum F's liveness assertion implemented and confirmed non-inert via mutation. Full pnpm -r test/lint/canaries independently re-run, exit 0, policy 54/54 100% coverage. Merged. Unlocks TASK-087. | first-pass: yes | 2026-09-01T20:15Z |
| TASK-087 | CX | approved | packages/broker enforcement gate wiring (protected, critical — the point the ADR-010 pivot went live in production code). Territory clean. resolveEnforcement confirmed genuinely consulted (mutation to hardcoded autonomous reddened 3 tests incl. shipped liveness canary). Enforced path reuses EXISTING ADR-004 approval mechanism, not a parallel one; E2 auth-friction never calls issueApproval, never constructs a credential-bearing payload. MOST IMPORTANT: fail-closed confirmed to be the SAME pre-existing ADR-001 code path, not duplicated — new gate structurally cannot self-trigger fail-closed. TASK-073 refusal memory byte-identical, per-attempt semantics unchanged. Liveness canary independently reproduced via two separate bypass strategies. No canUseTool-only enforcement, banned-modes grep clean. Full pnpm -r test/lint/canaries independently re-run, exit 0, broker 93/93. One non-blocking note: a dead defensive branch (unreachable via real decision path) flagged for future cleanup, not rework. Merged. **OIKONOMOS's autonomy default actually changed in production code as of this merge.** | first-pass: yes | 2026-09-01T23:15Z |
| TASK-084 | S5 (ORCH-authored fix, round 3) | approved | infra/postgres/migrations/004 idempotency fix — ADD CONSTRAINT statements guarded with DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL $$, matching the CREATE TABLE IF NOT EXISTS idempotency already present. 3rd occurrence of the resume-state gap on this task (builder repeatedly reported "no changes needed" against fresh findings); per established precedent (first applied TASK-077) ORCH wrote the minimal fix directly and dispatched extra-scrutiny independent verification given the unusual authorship. All 6 checks PASS: diff scope clean (single file, 15+/4-), correct narrow `duplicate_object` exception class (not WHEN OTHERS), live up→down→up→up re-run confirmed exits 0 with each FK appearing exactly once (no duplication/drift), round-1 checks (FK rejection, backfill guard 9 tests, migrations 001-003 byte-identical) still pass, full gates exit 0 (db 107/107, one transient CAN-03 canary timeout judged environmental — passed clean on immediate retry), no PLAN.md/AUTOPILOT_LOG.md leakage. Out-of-scope finding noted for follow-up (revert.sh down-migration ordering bug, pre-existing, unrelated to this diff). Merged. | first-pass: n/a (ORCH-authored fix after 3rd resume-gap; verification first-pass: yes) | 2026-09-01T23:20Z |
| TASK-085 | S5 | approved | packages/memory + migration 005 — three-scope (agent/project/user) three-tier (profile/log/note) memory store, agent>project>user resolve() conflict order. Territory clean (5 Owned_Paths globs + dossier). All 8 ACs independently verified true: migration correctly extends profile_facts (not a parallel store), CHECK constraint live-tested, profile-tier-only injection reader, 7-day note TTL, cross-role agent-scope isolation (deliberately stricter than Grok Bot's shared filesystem), no implicit writes. MUTATION-PROVEN reproduced independently: inverting conflict order and removing the role_id filter both went RED as required, reverted clean. Migration 005 up→up→down→up live-verified idempotent. One legitimate cross-cutting gap (pnpm-lock.yaml sync for the new pg dependency, correctly outside Owned_Paths) resolved as ORCH merge wiring per TASK-005/006/007/009 precedent, not sent back to the builder. Full pnpm -r test/build/lint/canaries independently re-run post-merge, all green. Merged. | first-pass: yes (code); lockfile sync handled by ORCH as merge wiring, not a rework round | 2026-09-01T23:50Z |
| TASK-088 | CX | approved | packages/broker D3 sealed-secret path guard (N13, protected, adversarial review by different model — CX/Codex authored, Claude reviewed). Territory clean (2 files + dossier, no index.ts). guardSecretPath confirmed structurally unconditional (does not even accept tier/grant/allow-rule params). Path normalization order confirmed correct in code (decode->fold->resolve, then symlink-resolve, then re-normalize, then boundary check). Distinct audit event type confirmed. LIVENESS reproduced independently via stub-guard swap. N4 confirmed: no path field on the audit type at all, serialized payload asserted never to contain the target string. Standalone-module pattern matching TASK-073/TASK-087 precedent, deliberately unwired from index.ts — follow-up wiring task TASK-093 cut. Builder's reported 3 packages/db failures did not reproduce on independent re-run and are moot regardless (zero packages/db files in the diff). Full pnpm -r test/lint/canaries independently re-run, exit 0, broker 102/102. Merged. | first-pass: yes (code); 1 legitimate triage round for a dispatch-side sync issue + a rejected ownership claim, not counted as rework | 2026-09-02T00:10Z |
| TASK-090 | S5 | approved | services/workspace — shared path resolution, D1/D2/D3 durability-tier classification, async handoff mailbox over TASK-084's role_messages. Territory clean (6 Owned_Paths globs + dossier; migration 004 and packages/memory confirmed untouched). Traversal guard verified against dot-segments, percent-encoding, backslash separators, NUL bytes, AND an independently-constructed real filesystem symlink escape. F9 non-boundary test confirmed to assert intentional cross-role read SUCCESS, not failure. send_to_role confirmed structurally reply-less, byte-less, memory-write-less, context-carryover-less. MUTATION-PROVEN reproduced independently: removing the traversal guard reddened 6 tests incl. the liveness canary, neutering the tier classifier reddened 4 incl. its own liveness canary. Integration tests confirmed to run for real against live Postgres, not silently skip. One legitimate lockfile-sync gap (pnpm-lock.yaml, outside Owned_Paths) resolved as ORCH merge wiring per TASK-085 precedent. Full pnpm -r test/lint/canaries independently re-run, exit 0, workspace 29/29. Merged. | first-pass: yes | 2026-09-02T00:40Z |
| TASK-093 | CX | approved | packages/broker — wired secretPathGuard into the real PreToolUse path (protected, critical, adversarial review by a different model — CX/Codex authored, Claude reviewed). Territory clean (2 files + dossier). D3-over-resolver precedence confirmed structurally: guard checked BEFORE capability/role-grant lookup and resolveEnforcementGate; test asserts getCapability never called on a D3 path. Distinct secret_path_attempt audit event confirmed to reach the real audit path unchanged, target-free. MOST IMPORTANT: fail-closed confirmed not duplicated — guardSecretPath is pure/synchronous, cannot throw into the outer ADR-001 catch. TASK-087 resolver and TASK-073 refusal-memory confirmed unchanged (diff purely additive). LIVENESS reproduced independently via a real runtime mutation (guard bypass), 3 tests went RED including the shipped canary, reverted clean. Full pnpm -r test/lint/canaries independently re-run, exit 0, broker 106/106. One non-blocking style note on the shipped liveness test's proof style, not rework. Merged. **Closes the Addendum F D3 sealed-secret enforcement chain (TASK-088->TASK-093).** | first-pass: yes (code); 1 dispatch-side sync fix, not counted as rework | 2026-09-02T00:50Z |
| TASK-089 | S5 | approved | infra/compose — the Office: durable environment (named D1 workspace + D3 secrets volumes), 4 lifecycle verbs (provision/rebuild/recover/restore), durability canary. Territory clean (2 Owned_Paths globs + dossier; docker-compose.local/prod.yml byte-identical; zero packages/db files touched). D3 confirmed structurally absent from office-model's volume list in the actual YAML, mounted only into office-browser. LIVENESS reproduced independently live against real Docker Desktop, not trusted: D1 marker survives a real rebuild with identical digest, D2 marker gone, D3 path confirmed absent from inside the running model container both pre- and post-rebuild (ADR-010 §6's strictest commitment). recover/snapshot/restore lifecycle drill confirmed live; restore confirmed the only data-losing verb, matching its own stdout+README. Zero leftover Docker resources confirmed via pre/post diff. This is a genuine ADR-005-compliant liveness assertion (evidence emitted by the mount doing its job), not a config-presence check. Full pnpm -r test/lint/canaries independently re-run, exit 0. Merged. | first-pass: yes | 2026-09-02T01:15Z |
| TASK-076 | CX | approved | services/worker scheduler — re-keyed to roles.role_id (Addendum F §6.3 reshape), routine firing, lanes, approval-aware idle, watchdog auditing, protected user-lane priority interrupts. Territory clean (2 files + dossier, no schema changes, no other package touched). All 7 ACs verified true directly: per-role serialization with non-poisoning failure via finally-cleanup, routine firing correctly has zero checkpoint/resume machinery and records missed-vs-queued correctly, user-lane dequeue-ordering, busyOnlyAwaitingApproval/lastBusyAt freeze verified with a fake clock, watchdog audits before any kill, priority interrupt checks the active lane FIRST (user lane short-circuits before any abort). MUTATION-PROVEN reproduced independently: removing the serialization guard and the user-lane short-circuit each reddened their respective test, both restored clean. Injected-clock convention confirmed used exclusively (zero raw Date.now()/new Date() in scheduler.ts). Full pnpm -r test/lint/canaries independently re-run, exit 0, worker scheduler 6/6. One informational flag (not a rework item): the wt-codex worktree's services/workspace tests fail on a missing pg/@oikonomos symlink — a stale local node_modules install gap unrelated to this diff (services/workspace isn't touched), noted for a worktree pnpm install refresh, not a code defect. Merged. | first-pass: yes | 2026-09-02T01:20Z |
| TASK-092 | S5 | approved | packages/connectors durable tenant session pool — acquire/release replaces create/destroy (F10, ADR-010 "signed in once, available thereafter"). Territory clean (2 Owned_Paths + dossier; manifest/enumeration/discovery-cache/mcp confirmed byte-identical to master). Pool key confirmed (tenantId, connectorId) with NO role parameter at all — role-sharing across the tenant is structurally guaranteed, not merely tested. release() confirmed a lease-decrement, never a teardown; expiry re-mint verified via fake clock. N4 liveness reproduced independently via a real mutation (injected a fake token field, leak-detection test went red, reverted clean). Full pnpm -r test/lint/canaries independently re-run, exit 0, connectors 109/109. One ORCH post-merge cleanup (not rework): pool.ts's key-separator contained a literal raw NUL byte instead of a \0 escape — functionally identical, passed every test, but made the file register as binary to diff tooling; fixed directly, zero behavior change, re-verified unchanged test count. Merged. | first-pass: yes (code); cosmetic ORCH cleanup post-merge, not a rework round | 2026-09-02T01:35Z |
| TASK-091 | CX | approved | packages/harness-factory — additive `environment` field on ComposeOptions, capstone of the Addendum F wave (protected, adversarial review by a different model — CX/Codex authored, Claude reviewed). Territory clean (4 Owned_Paths files + dossier; all 12 pre-existing test files confirmed byte-identical, only environment.test.ts is new). environment confirmed genuinely optional with no behavior change when omitted. Role identity/workspace root reaching audit identity confirmed by reading bindEnvironment's actual construction (incl. a role-mismatch rejection test), not a comment. sessionPool consumption confirmed to have no destroy path structurally (interface has no destroy method; compose.ts has zero session-teardown code). mcpServers confirmed untouched (zero references in the diff). composeHarness-remains-per-run confirmed via a genuine object-identity test, not a superficial check. Full compose.ts diff read line-by-line: zero hook/decorator/fail-closed-map/permission-mode code touched. Full pnpm -r test/lint/canaries independently re-run, exit 0, harness-factory 89/89. Merged. **Completes the full Addendum F wave (TASK-084 through TASK-093, 10 tasks) — the persistent-office-computer pivot is now live in production code end to end, merged, and independently verified.** | first-pass: yes | 2026-09-02T02:05Z |
| TASK-096 | S5 | approved | packages/agent-providers gemini-3.7-flash pure cost calculator. Territory clean (2 files + dossier). Constants named with source URL + confirmation date in a comment. normalizeTokenCount guard confirmed to handle undefined/null/non-numeric/negative without throwing. Confirmed genuinely pure (no fetch/fs/process.env). Full pnpm -r test/lint/canaries independently re-run, exit 0, agent-providers 78/78. Merged. | first-pass: yes | 2026-09-02T09:00Z |
| TASK-094 | CX | approved | packages/harness-factory Gemini Tier-0 provider adapter — first non-Claude provider ever wired into the enforcement architecture, protected + precedent-setting, reviewed at maximum rigor. Territory clean. Enforcement-by-construction confirmed structurally: the single tool-call loop awaits l1.handle() before any execute() or functionResponse, no bypass path exists. Deny path confirmed via execute-never-called test. LIVENESS reproduced independently via a real bypass mutation (not just reading the shipped test) — 3 tests went RED including the dedicated liveness test, reverted clean. Tier-0 ceiling confirmed as a named constant, denies post-l1, explicitly marked as the Stage-2 lift point. Malformed/timeout handling confirmed to deny on every failure path, never throw. GEMINI_API_KEY confirmed read once, never logged, grepped clean. ComposeOptions.provider confirmed optional with zero pre-existing test modifications. Dynamic-import pattern matches the existing l1/l2/l3/post convention. Full pnpm -r test/lint/canaries independently re-run, exit 0, harness-factory 98/98. Merged. **First non-Claude LLM provider now live, same enforcement guarantee as Claude, proven by construction.** | first-pass: yes | 2026-09-02T09:05Z |
| TASK-060 | ORCH | approved (self, ORCH-executed) | Vertical-slice persistent-office demo, live on real infrastructure. Real control-api HTTP server, real broker decision path, real Postgres approvals/audit, real Telegram round trip to the operator's phone. Live run: autonomous email.list executed without a card (real audit row, verdict=allow); office.local_execute (E3 enforced-floor) parked with a real approval, operator approved via a real phone tap 6.8s later, a follow-up re-attempt with the granted nonce genuinely consumed it (status granted->consumed, not merely simulated); a separate run's D3 secret-path attempt was denied outright with a target-free audit payload, no approval offered. Both halves of the pivot (autonomous + enforced) demonstrated in one session per the task's own AC. Found and fixed 5 real integration issues while building the demo (documented in the runbook); found and deferred one real cross-package naming inconsistency (D3 root constant, TASK-097 cut). Evidence: run/approval IDs and timestamps quoted verbatim in docs/runbooks/persistent-office-demo/README.md. Closes the loop the user asked to "see this thing in action" for. | first-pass: yes | 2026-09-02T09:20Z |
| TASK-095 | S5 | approved | packages/agent-providers Gemini AgentProvider — final task of the ADR-011 wave. Territory clean (6 files + dossier post-territory-gap-fix; claudeCode/codex/grok byte-identical). GeminiQueryFn's type signature confirmed structurally unable to receive any L1/broker port — no enforcement duplication, matching claudeCode.ts's own thin-translator shape. turn_complete.costUsd confirmed computed from real usageMetadata via TASK-096's costForUsage, never a placeholder. GEMINI_API_KEY loading matches existing key hygiene, never logged. metadata.ts confirmed to contain only the necessary GeminiProviderExtras addition. `pnpm -r build` explicitly run as its own gate separate from `pnpm -r test` per the new standard — clean. Full pnpm -r test/lint/canaries independently re-run, exit 0, agent-providers 96/96. Merged. **Completes the ADR-011 multi-provider wave: Gemini is now a fully wired, governed, cost-tracked provider alongside Claude.** | first-pass: yes (after 1 legitimate territory-gap round, not counted as rework) | 2026-09-02T10:05Z |
| TASK-049 | S5 | approved | Google Calendar connector manifest + golden suite (Wave 1). Territory clean (2 files + dossier). Manifest structurally matches the approved Gmail (TASK-048) pattern; T0 list/read enabled, T3 mutations disabled until G-CONN per Addendum F's autonomous-by-default posture. account_ownership: basileia present. Registration confirmed to use genuine ON CONFLICT upsert semantics. Golden suite independently re-run both directions (positive: pass_rate 1, negative: pass_rate 0/exit 1) confirming live scoring, not a stub. pnpm -r build/lint/canaries all exit 0. One pre-existing pnpm -r test DB-contention flake noted, unrelated to this diff. Merged. | first-pass: yes | 2026-09-02T10:35Z |
| TASK-097 | CX | approved | Unify D3 sealed-secret root constant on `/oikonomos/secrets` across packages/broker and services/workspace (protected, adversarial review by a different model). Resolved a genuinely well-founded SPEC_AMBIGUITY the builder raised (original wording asked for both "one literal" and "zero behavior change," mutually exclusive) via an explicit ORCH decision, and a second legitimate territory gap (broker's own test fixtures hardcoded the old literal). Confirmed a pure literal-value rename: broker's guard diff is a 2-line change, guard logic (normalization/traversal/symlink handling) byte-identical apart from the substitution; workspace's own value never moved. Test fixtures updated to the new literal with the same logical cases hand-re-verified to still deny. New shared test asserts both guards agree. Full pnpm -r build independently re-run clean; the one pnpm -r test failure (packages/approvals timeout) independently confirmed pre-existing on master itself, unrelated to this diff. Merged; lockfile synced by ORCH as merge wiring, both worktrees resynced. **Closes the naming inconsistency found live while building TASK-060's demo.** | first-pass: yes (after 2 legitimate territory/ambiguity rounds, not counted as rework) | 2026-09-02T10:35Z |
| TASK-051 | CX | approved | OIK-164 Composio Connect evaluation spike, recommendation only. Territory clean (dossier-only diff, git diff --check clean, no code/dependency changes). All 3 required topics addressed explicitly and well-sourced against Composio's own current docs/pricing (checked live 2026-09-02): N5/R19 data sovereignty (verdict: not approved, hosted OAuth token custody is exactly R19's unresolved exposure), OIK-047 tier-mapping feasibility (verdict: not feasible directly, Connect's dynamic-discovery + up-to-50-action multi-execute meta-tools cannot map to the static per-tool allowlist contract), cost vs R30k/month (verdict: acceptable only behind a dedicated spend cap). Recommendation: do not adopt for G1. Merged; relocated to docs/research/OIK-164-composio-connect-evaluation-2026-09-02.md per the task's own instruction. | first-pass: yes | 2026-09-02T11:15Z |
| TASK-050 | S5 | approved | Google Drive connector manifest + golden suite (Wave 1). Territory clean (2 files). Manifest structurally matches google-calendar.yaml exactly. share_file (T3_external) correctly disabled until G-CONN — the highest-risk capability. get_file_permissions correctly distinguished as a read-only T1_draft lookup from the actual sharing-change action. Registration upsert mechanism confirmed intact. Golden suite independently re-run both directions (positive: pass_rate 1; negative: pass_rate 0/exit 1), confirming live scoring. pnpm -r test 17/17 packages green including services/workspace 29/29 (confirming the pre-review symlink fix held), pnpm -r build 16/16, lint clean, canaries 17/17. Merged. | first-pass: yes | 2026-09-02T11:20Z |
| TASK-101 | S5 | approved | services/control-api's first-ever authentication boundary — fail-closed session auth gate (Bearer token + HMAC-signed httpOnly cookie) + GET /tasks (security-relevant, adversarial review). Territory clean — confirmed index.ts/openapi.ts genuinely absent from the diff (a mid-session redesign to avoid needing them, verified real). Route coverage structurally strong: a single global preHandler gates every route by default, only POST /auth/login and GET /openapi.json opted out explicitly — future routes are automatically protected. Token confirmed never logged/echoed. Session cookie confirmed genuinely HMAC-SHA256 signed with embedded expiry; wrong-token comparison confirmed constant-time. LIVENESS reproduced independently via a real bypass mutation, 10 tests went RED, reverted clean. GET /tasks confirmed to match GET /runs's filtering shape exactly. Full pnpm -r build/test/lint/canaries independently re-run, exit 0, control-api 103/103. Merged. | first-pass: yes (code); 2 legitimate territory/staleness rounds, not counted as rework | 2026-09-02T13:40Z |
| TASK-098 | CX | approved | packages/memory ACL (visible_to) + versioning (superseded_by) extension — a real security boundary (security-relevant, maximum-rigor review per ADR-012 §2.6). Territory clean (9 files + dossier). Migration confirmed correct (visible_to NULL default, superseded_by self-referencing, index migration correctly handles versioned rows sharing a key, migrations 001-005 untouched). writeMemoryFact confirmed to silently discard any visible_to on agent-scope writes before the INSERT — no path can expand agent-scope visibility. Write path confirmed a single CTE, INSERT new + UPDATE only the old row's superseded_by, never an in-place value overwrite. All four read paths confirmed to filter non-superseded + ACL, reproduced with a live 3-version chain. TWO independent mutation-based liveness proofs reproduced (ACL bypass, supersession-filter bypass) — both turned tests RED, both reverted clean. The corrected legacy test confirmed a genuine strengthening, not a weakened assertion. Full pnpm -r build/test/lint/canaries independently re-run, memory 24/24. Merged. **The ACL/versioning substrate the rest of E10 builds on — proven, not nominal.** | first-pass: yes (code); 3 legitimate territory rounds, not counted as rework, 3rd escalated to the user per standing threshold | 2026-09-02T14:15Z |
| TASK-102 | S5 | approved | apps/dashboard scaffold — login + run list/detail (E9.1a). Territory clean (apps/dashboard/** only). Real endpoints throughout: POST /auth/login, RequireAuth redirect-to-login (tested, not just manual), GET /runs cursor pagination, GET /runs/:id + GET /runs/:id/evidence in parallel — no mock/hardcoded data confirmed via grep. Static-hosting question (separate host vs a @fastify/static mount into control-api, out of Owned_Paths either way) explicitly and honestly documented as a deferred gap rather than silently punted. Post-merge, an unrelated pre-existing services/worker Vite server.fs.allow bug surfaced on the mandatory pnpm -r test gate — root-caused and fixed by ORCH directly (packages/shared/vitest.config.ts, not a protected path; not this task's regression, reproduced on a pre-TASK-102 commit). Full pnpm -r test and pnpm -r build (17/17) independently re-confirmed green with the fix in place before closing this record. Merged. | first-pass: yes; one legitimate root-config gap (apps/* workspace registration), fixed directly by ORCH per the pnpm-lock.yaml merge-wiring precedent, not counted as rework | 2026-09-02T16:25Z |
| TASK-099 | CX | approved | services/workspace typed handoff — optional handoffKind/factRef carrying a memory fact REFERENCE, never a value copy. Territory clean (9 files, all in widened Owned_Paths — migration 007, packages/db/roleMessages.*, services/workspace/mailbox.*). Value-copy prevention confirmed at the DB layer itself, not just app discipline: migration 007's role_messages_fact_ref_no_value_check CHECK constraint rejects any fact_ref JSON containing a value key. End-to-end mailbox→memory resolution actually exercised live against real Postgres (write ACL-scoped fact, send typed handoff, read persisted row directly, resolve() the live value), not just typed. Pair-validation (handoffKind requires factRef and vice versa) confirmed pre-write at both the packages/db and services/workspace layers, with tests proving rejection before any DB write. Reviewer ran DB-gated tests live (applied migration 007, ran, reverted via down script, confirmed dev DB left clean). ORCH re-verified post-merge: pnpm -r build 15/15, pnpm -r test green (one stale-dist false alarm fixed by rebuilding packages/db first; two separate single-file shared-Postgres contention flakes under pnpm -r's concurrent load, each confirmed pre-existing/unrelated by isolated 100% pass, matching the TASK-097/TASK-049 precedent). Merged. **Closes E10's typed-handoff leg — TASK-100 (two-role e2e proof) now unblocked.** | first-pass: yes (code); 1 legitimate territory gap (migration 007 + packages/db API), not counted as rework | 2026-09-02T16:35Z |
| TASK-103 | S5 | approved | apps/dashboard approval inbox (E9.1b) — nonce-safe decide flow. Territory clean (4 files, apps/dashboard/** only). actionRender rendered verbatim from GET /approvals, no re-derivation. Approve/Reject genuinely call POST /approvals/:nonce/decide with correct bodies, UI reflects granted/rejected/409-already-decided. Nonce-leak prevention verified adversarially — grepped for localStorage/sessionStorage/history/window.location/useParams, found none; dedicated test explicitly asserts window.location.pathname/search/hash/href never contain the nonce and both storages stay empty, not just eyeballed. 409 path has its own passing test. One minor non-blocking observation: the nonce is interpolated into a thrown Error message on non-409 failures, in-memory only, never logged/persisted — noted, not a rework item. pnpm --filter dashboard test 8/8, build clean; ORCH re-ran full pnpm -r build (15/15) and pnpm -r test (all green, zero flakes this pass), lint clean. Merged. **Closes E9.1b — unlocks TASK-104 (evidence browser + PWA, final E9.1 piece).** | first-pass: yes | 2026-09-02T16:40Z |
| TASK-104 | S5 | approved | apps/dashboard evidence browser + PWA packaging (E9.1c, final dashboard piece). Territory clean (14 files, apps/dashboard/**+dossier only). Decision-trail rendering sourced only from GET /runs/:id/evidence — no direct DB access, no mock data, grepped for counterexamples. sw.js's fetch handler explicitly excludes /runs, /approvals, /auth BEFORE any cache logic (the safer of two possible designs — explicit bypass, not merely unregistered), with a source-order test guarding against the bypass being reordered into dead code later; only navigate/static-asset requests get cached. registerServiceWorker genuinely wired into main.tsx, not dead code. manifest.json structurally valid. Builder flagged a services/workspace/mailbox.test.ts failure in its own pnpm -r test run as pre-existing/unrelated — reviewer independently reproduced it as a stale-packages/db-dist artifact in the worktree (passes clean once packages/db is rebuilt first), matching the recurring TASK-097/TASK-099 pattern, not a real regression. pnpm --filter dashboard test 16/16, build clean; ORCH re-ran full pnpm -r build (15/15) and pnpm -r test (all green, zero flakes), lint clean. Merged. **Closes E9.1 (web dashboard) entirely.** | first-pass: yes | 2026-09-02T17:00Z |
| TASK-100 | CX | approved | Two-role typed-handoff end-to-end ACL proof (research→drafting), security-relevant, integration-level counterpart of TASK-098's unit-level proof — reviewed by a different model than the author per ADR-012 §2.6. Territory clean (2 files, exactly Owned_Paths). All 4 ACs verified against code: AC1 real cross-role reference resolution confirmed live against real Postgres; AC2 confirmed STRONGER than written — asserts null for both the outsider role AND the sender/researcher role itself (correct scope-based ACL semantics, re-asserted post-correction); AC3 (the critical reference-not-copy proof) genuinely exercised — sender corrects the fact after sending the handoff, resolve() called a SECOND time on the stored reference asserts the NEW value; AC4 explicit assertion the sender cannot resolve its own sent handoff, proving no privilege expansion. Persisted role_messages row verified via live read-back (not the sender's in-memory object), confirming TASK-099's DB-layer locator-only CHECK constraint holds end-to-end. Mutation-based liveness proof: bypassed the ACL predicate in packages/memory/src/facts.ts, test went RED exactly at the sender-denial assertion, reverted clean (a second ordering-related mutation attempt was honestly reported as inconclusive rather than glossed over). Tenant isolation via randomUUID()-derived IDs. Two legitimate non-rework blockers along the way: a dead detached dispatch session (redispatched resume-first) and a genuine DATABASE_URL environment-inheritance gap in scripts/dispatch.ps1 itself, root-caused and fixed at the tooling level (419fd94) rather than the builder's fault. Full pnpm -r build (16/16), pnpm -r test (zero flakes), pnpm lint, pnpm canaries (18/18) independently re-run clean. Merged. **Closes E10 (Organizational Memory Exchange) entirely — completes the whole E9.1/E10 wave the user authorized.** | first-pass: yes (code); 2 legitimate non-rework blocker rounds | 2026-09-02T18:39Z |
TASK-105 | CX | approved | first-pass (post spec-fix unblock); non-blocking: MIGRATION_ROUND_TRIP=1 path not in default CI, createThread/getOrCreateThreadForRole duplicate impl | first-pass? yes
TASK-107 | S5 | approved | first-pass (post Owned_Paths-parser unblock); visual bar checked against committed screenshot, real match to reference UI | first-pass? yes
TASK-106 | CX | approved | first-pass (post 2-round scope-correction unblock); rescoped to task-creation only after real MISSING_DEPENDENCY finding, execution split to TASK-111 | first-pass? yes
TASK-108 | S5 | approved | first-pass; AC2 (live bot-reply) conditionally approved pending joint smoke test once TASK-111 lands | first-pass? yes
TASK-109 | S5 | approved | first-pass; non-blocking: onUnauthorized/onDecided props unwired (fails safe) | first-pass? yes
TASK-110 | S5 | approved | first-pass; disciplined territory workaround (local fetch helper, full reload) noted as non-blocking, driven by narrow Owned_Paths not a shortcut | first-pass? yes
TASK-113 | S5 | approved | first-pass; ADR-013 SS2/SS4 implemented exactly, real content returned not just filenames | first-pass? yes
TASK-112 | CX | approved | first-pass, protected path, reviewed by different model (ORCH/Sonnet-5 vs CX/Codex); all C1-C7 checks verified line-by-line against ADR-013; non-blocking: liveness test uses regex manifest parser not real loadManifests (territory-driven) | first-pass? yes
TASK-114 | CX | approved | first-pass, protected path, reviewed by different model (ORCH); verified real Postgres writes directly (23 capability rows, zero builtin grants); pre-existing unrelated ATLAS test flake confirmed reproducible on master | first-pass? yes
TASK-115 | CX | approved | first-pass, protected path, reviewed by different model (ORCH); minimal additive fix, execFileSync no injection surface, real-boundary test mocks; unblocks TASK-111 fully | first-pass? yes
TASK-111 | CX | approved | first-pass; real functionality independently verified against live production Postgres data (real broker decision, real approval, real completed run, real bot reply); required fast-follow TASK-116 (S5) for missing permanent regression tests | first-pass? yes
TASK-116 | S5 | approved | first-pass; test-only, verified core assertion against actual production data byte-for-byte; closes Wave Chat-1 + ADR-013 (16 tasks, 105-116) | first-pass? yes
TASK-120 | S5 | approved | first-pass; highest-risk migration this wave, verified live against shared dev Postgres directly; one full-suite failure traced to ORCH's own prior comment, fixed | first-pass? yes
TASK-117 | CX | approved | first-pass (post 3-round unblock, 2 of 3 rounds ORCH's own gaps); non-default-tier test mutates real capability row and restores it; T0 liveness proof genuine, real allow event + zero pending approvals | first-pass? yes
TASK-118 | S5 | approved | first-pass; grant-write mechanism real and tested, nonce discipline extended correctly; frontend wiring gap honestly documented, spun out as TASK-123 fast-follow | first-pass? yes
TASK-123 | CX | approved | first-pass; minimal diff, real component-tree test coverage; ORCH caught + fixed an unrelated cross-package regression (thread_members FK vs chatRunDriver.test.ts cleanup) during full-suite verification | first-pass? yes
TASK-119 | S5 | approved | first-pass; revoke precision verified, real getRoleGrant read-mechanism proven; activeRoleId wiring gap spun out as TASK-124 | first-pass? yes
TASK-124 | S5 | approved | Territory clean, minimal single-line ChatShell.tsx change deriving activeRoleId from existing state; both AC (initial visibility + bot-switch) genuinely tested through the real component tree; latent GET/POST mock collision fix in ChatPage.test.tsx legitimate. Independently re-verified full pnpm -r test clean (1081/0 failed). | first-pass? yes
TASK-125 | CX | approved | Reported blocked (leaked test row); independent re-verification found zero leaked rows — the test's own finally-cleanup already handles it, false positive not a defect. Territory clean, atomic createGroupThread (BEGIN/COMMIT/ROLLBACK, min-2/no-dup validated), listAllThreadsWithMembers correctly discriminates 1:1 vs group. Full pnpm -r test clean. Unlocks TASK-121. | first-pass? yes
TASK-121 | CX | approved | Two prior rounds were legitimate cross-cutting blockers (listThreads bug fixed on master 8cfe5e5; pg devDependency added), not rework of the same defect. Final diff clean: territory-correct, group-thread routes/schema/discriminated listing/sender attribution all tested, FK-ordered fixture-scoped test cleanup verified working (confirmed via timestamp that the only leftover DB rows predated this run). | first-pass? yes (on the resubmitted diff; 2 legitimate blockers before it, not rework cycles)
TASK-122 | S5 | approved | Territory clean (9 files, all owned, including disciplined structural-type workaround for out-of-territory types.ts). Fan-out approval gate is narrow and well-tested against real Postgres (single-recipient delivers immediately, 2+ recipients produces a real pending approval, mutation-proof verified). Group-thread compose gap honestly documented and safely degraded (ComposeBox disabled, not broken) — matches Grants-1 precedent, spun out as TASK-126. Full pnpm -r test hit the same pre-existing shared-Postgres flake documented across TASK-120/123/122 reviews, confirmed isolated-clean. | first-pass? yes
TASK-127 | S5 | approved | Clean composition of existing OAuth token provider + manifest MCP resolver into a real session minter; lazy-cached token provider, correctly frozen output, additive-only barrel exports. Security-critical no-secret-leakage criterion independently verified via a dedicated diff sweep — no real credential found, all fixtures are assembled-at-runtime fake sentinels. | first-pass? yes
TASK-126 | CX | approved | Recovered from a stale-heartbeat crash (infra stall, not a defect) and completed. Territory clean, all 4 acceptance criteria genuinely proven against real Postgres (senderRoleId null, real pending chat.bot_fanout approval before delivery, nothing delivered to recipients, 1:1 unchanged). fromRoleId:"human" design verified sound (group threads always 2+ members by construction, so the fan-out gate's single-recipient FK path is structurally unreachable here). Full pnpm -r test hit the same documented pre-existing registerCapabilities flake, confirmed isolated-clean twice. Both Grants-1 and Chat-2 Core waves fully done. | first-pass? yes
TASK-128 | CX | approved | The wave's highest-stakes diff (first live external-tool execution path). Grant-derived, per-tool Gmail mounting; a role with zero matching grants mounts nothing extra (structurally identical to today); mutation-proof verified — removing the grant filter would let an ungranted email.send get mounted and called, reddening real assertions. Real HTTP fixture MCP server, real OAuth bearer-token verified in transit, real policy.decision audit event. Required 2 rounds of full-suite verification to get one complete clean pass (first bailed early on a pre-existing DDL-deadlock flake); both known flakes reconfirmed isolated-clean, neither recurred on the complete run. | first-pass? yes
TASK-130 | CX | approved | Clean pg-boss lifecycle (start unwinds on partial-init failure, stop is idempotent+graceful with timeout). Rigorous tests: real job scheduled+observed running via a resolving Promise from the actual pg-boss callback, real zero-orphaned-connection proof via direct pg_stat_activity query. Exact-pinned dependency. Correctly scoped to plumbing only. Unblocks TASK-131. | first-pass? yes
TASK-129 | S5 | approved | Corrected redispatch after an environment/session issue on the first attempt (not a code defect). Honest 300ms server-side-poll SSE design (documented tradeoff, real 6.7x latency win, genuinely satisfies AC1). Client is well-built: hand-rolled fetch+ReadableStream (deliberate over EventSource), correct Last-Event-ID resume, idempotent cleanup, correct 401 handling. All 4 ACs individually tested both sides. Existing GET /threads/:id/messages confirmed unmodified. | first-pass? yes (on the corrected attempt)
TASK-132 | CX | approved | Correctly implements resolved spec ambiguities (role-status env proxy, name/definition.goal/routine:<id> task mapping); reuses existing fireRoutine rather than reimplementing. Clean opt-in pg-boss integration with real durable cron scheduling. Both real acceptance criteria (due-routine fires, hidden-role missed) proven against real Postgres with precise lastFireAt mutation checks. Independent re-run surfaced a new-signature chatRunDriver flake in worker's own package-scoped run, confirmed isolated-clean (10/10) and unrelated to this diff. | first-pass? yes
TASK-131 | CX9 | rework | Excellent security design and coverage otherwise (server-side role identity via process args, grant-gated mounting, typed-handoff path exercised, denial persists zero role_messages). One real, non-flaky finding: this diff's refactor of chatRunDriver.ts (combineConnectorContexts) broke TASK-128's own mutation-proof source-text assertion at chatRunDriver.test.ts:99 — needs the assertion updated to match the current, functionally-equivalent code path. | first-pass? no (rework requested)
TASK-131 | CX9 | approved (round 2) | Rework was minimal and precise (1 line + 1 import, nothing else touched); verified the fixed assertion string still sits at the load-bearing mountedToolNames line. Excellent security design throughout: role identity via server-side process args (never model input), grant-gated MCP mounting, typed-handoff path exercised, denial persists zero role_messages rows. Full suite clean except the documented registerCapabilities flake. | first-pass? no (1 rework round, real finding)
TASK-134 | CX | approved | Cron validation happens synchronously before any DB write (satisfies AC2 precisely); next_fire_at computation reuses cron-parser as a real direct dependency; Routines tab test genuinely exercises the live component tree. 3 legitimate ownership/sync gaps along the way (my own mis-scoping twice, one real DB-schema gap), none builder error. Two "unrelated" issues CX flagged were verified directly: a real stale pnpm workspace link (fixed) and transient full-suite contention noise (confirmed clean on re-run) - neither in this diff. | first-pass? yes (on the diff; 3 legitimate blockers along the way)
TASK-135 | S5 | approved | Exceptional rigor: real call-counter proves zero re-invocation across park->reconcile->resume, real approval grant+consume with the original pre-crash nonce, single-use replay proven safe, mutation-proof companion test. Correctly investigated first and found the property holds by construction (pure DB-layer function), so this was verification work, not new production code - exactly as scoped. Found and honestly documented a real production gap (no chat run ever reaches waiting_approval today) rather than silently ignoring or overreaching to fix it outside Owned_Paths. Opened TASK-136 as immediate fast-follow. | first-pass? yes
TASK-136 | S5 | approved | Exceptionally honest test design: reads the DB from inside the running query loop to prove the park transition is synchronous, correctly and explicitly acknowledges the run still completes within the same execution rather than genuinely pausing, and tests reconciliation via the established construct-persisted-state convention since a real orphaned park doesn't occur within one continuous execution today. parkRun mirrors its siblings exactly. Both "unrelated" environment claims independently checked directly (not delegated) - the control-api one didn't reproduce, registerCapabilities did (matches the documented flake). E11's full routine/durable-resume story is now real and connected end to end. | first-pass? yes
TASK-137 | S5 | approved | Mirrors approved TASK-127 (Gmail) pattern faithfully for Google Calendar. Justified deviation: local thin OAuth wrapper (oauthTokenProvider.ts out of Owned_Paths) calling the shared generic createOAuthTokenProvider directly rather than duplicating refresh-exchange logic. Secret hygiene solid, explicit non-leak test on mint failure. Barrels correctly untouched, reserved for TASK-139. Full recursive suite hit only pre-existing, isolated-clean Postgres-contention flakes unrelated to this diff. | first-pass? yes
TASK-138 | CX9 | approved | Mirrors approved TASK-127 (Gmail) pattern for Google Drive, same justified local-wrapper deviation as TASK-137. Token provider lazily cached across mint calls (dedicated reuse test). Secret hygiene solid, sanitized error never carries resolved value. Review subagent flagged an apparent master-red cron-parser build break; ORCH investigated directly and confirmed it was a stale filtered-build-order artifact, not a real regression - full pnpm -r build/test in correct order genuinely clean. Merged --no-ff. **Connectors-2 (TASK-137/138) both done.** | first-pass? yes
TASK-140 | CX | approved | Live capability kill switch, genuinely built not just drilled: getCapability was already read fresh per-decision, what was missing was the DB-side toggle (setCapabilityEnabled/setAllCapabilitiesEnabled), now real and tested against Postgres, proven to reach a real handlePreToolUse decision same-process/no-restart for both single and platform-wide cases. 3 legitimate blockers, none builder error: a real Owned_Paths gap, a real architectural-layering catch CX made itself (declined to add @oikonomos/db to packages/broker, correctly preserving its DB-agnostic injection design), and a real DEVDEPARTMENT protected-path gap in my own task authoring (docs/** is an absolute builder firewall path — should never have been granted; ORCH wrote the runbook directly). Also caught and fixed a control.py drain replay bug that reverted this task's status mid-flight. Merged --no-ff. **Connectors-2 and OIK-112 both complete — Wave 5 done.** | first-pass? yes (3 legitimate blockers along the way)
TASK-141 | CX | approved | Doubles as OIK-104 Fable review, done directly by ORCH. Genuinely adversarial: spawns the real built workspaceMcpServer.js subprocess with the exact production-computed spawn config, injects a spoofed fromRoleId in tool-call arguments, proves the persisted handoff still carries the true sender identity - the identity-from-process-args property holds through a live subprocess round-trip, not just a unit test. Also proves a real policy.decision audit event and that TASK-100's no-privilege-expansion property survives the live tool-call path. Merged --no-ff. **OIK-103/104 both done.** | first-pass? yes
TASK-142 | S5 | approved | OIK-113/114 Wave-1 foundation: typed OpenSandbox client against the real live /openapi.json (not guessed), secret:// convention established mirroring connectors' envSecretResolver. Adversarial test proves upstream fetch error text is never forwarded verbatim (credential-leak-by-relay guarded, not just fixture literals). R14/R16 compliant. Health check genuinely run live against the real clawsrv server; create/destroy correctly left unverified (no real API key available this session) rather than faked. Lockfile synced by ORCH post-merge. Merged --no-ff. | first-pass? yes
TASK-139 | CX9 | approved | N-ary connector merge (Object.assign/flatMap union, tested 0/1/2/4 contexts) + Calendar/Drive grant-derived mounting via a shared resolveGrantedManifestConnector helper mirroring TASK-128's Gmail pattern exactly. Zero cross-connector leakage (disjoint manifest namespaces confirmed), zero-grant behavior unchanged, existing TASK-116/117/128 tests untouched. Two small non-blocking findings: merged ConnectorContext.manifest keeps only the first connector's identity (currently inert, flagged for a future pass); mutation-proof test's reverse direction inferred not independently asserted. Merged --no-ff. **Connectors-2 (TASK-137/138/139) fully done.** | first-pass? yes
TASK-144 | S5 | approved | First Flutter code in the repo. SSE client is a faithful port of realtime.ts including the subtle bits (exclusive Last-Event-ID cursor set even from data-less frames, heartbeat skip, stop-on-401 without reconnect hammering). Every API path/body verified against the real Fastify routes - no mismatches. Credential hygiene exceeds bar: zero log statements in lib/ or test/, token cleared from widget state on every path, cookie is the only retained credential. 17/17 tests, each AC backed by a dedicated assertion; independently re-run (analyze clean, 17/17). Two non-blocking notes: suspended-completer-on-mid-stream-close (inert), and platform scaffolding deliberately deferred to TASK-149. | first-pass? yes
TASK-146 | CX9 | FROZEN (MAX_REWORK, escalated) | Round 2 left the reported one-line defect untouched (inboxTriage.e2e.test.ts:73 still asserts the old connectorId shape; file absent from the diff). ORCH verified the failure directly in CX9's worktree - deterministic, ~10s, no DATABASE_URL needed. I explicitly tested and disproved my own "gated test invisible to builder" theory before assigning fault. The round-2 dossier nonetheless claims "pnpm -r test PASS" twice; that claim is false. Reads as carried-forward evidence rather than deception (only filtered runs are itemised), but it is an integrity failure regardless - Test_Evidence is a claim and this one did not survive verification. Underlying code work is sound and worth keeping. Recommend reassigning the one-line fix to CX; first such incident from CX9 in ~8 tasks, watch not pattern. | first-pass? no (round 2)
TASK-150 | CX9 | approved | Protected path (harness-factory), Codex author reviewed by ORCH on Opus 5 - different family, adversarial requirement met. AC#1 (empirically-verified SDK cost shape) re-checked by ORCH directly rather than taken on trust, given this unit's prior evidence failure: claim accurate in every particular (v0.3.233, both result variants, total_cost_usd, modelUsage). Correctly captured two SDK subtleties a careless implementation would miss - total_cost_usd is cumulative across turns (read latest, never sum), and modelUsage is preferred over usage because usage excludes subagents. Implementation mirrors budget.ts: passthrough unchanged, report before terminal yield, fail-closed with event withheld, malformed modelUsage degrades to cost-only. ORCH re-ran: 105/105, full build clean. Binding constraint recorded for the follow-up build task: composition must read the latest result, never accumulate. | first-pass? yes
TASK-145 | CX | approved | Push backend: device_tokens registry, injected PushTransportPort, env-gated FCM adapter, triggers in ports.ts. Survived its session being killed mid-write - resumed builder committed the preserved work instead of restarting. Trigger redesign implemented faithfully: notifyAfterChatRun derives the event from persisted run state after awaiting runChatTask, leaving worker/approvals untouched (the point of moving the trigger rather than widening into TASK-146's active files). Per-device try/catch so one bad token can't suppress others or fail the run. N4 swept across the whole diff - no credential in any error path incl. FCM OAuth failures. Every AC has a dedicated test. ORCH re-ran: control-api 140/140, db 139/141, build+lint clean. | first-pass? yes
TASK-146 | CX (reassigned from CX9) | approved | One-line fix made correctly, then CX reported the unfiltered suite RED rather than claiming a pass - the exact opposite of the behaviour that froze this task under the previous unit, and exactly what the handover asked for. ORCH verified the claim both ways: the named registerCapabilities failure reproduces on master with zero TASK-146 changes (full-suite red, isolated green) so it is genuinely the pre-existing flake and outside Owned_Paths; and a full re-run on CX's branch came back 13/13 files, 64 passed, zero failures incl. the previously-red inboxTriage e2e. CX9's underlying production work preserved intact through the handover. Opened TASK-151 to fix the flake that has now taxed four tasks. | first-pass? yes (CX round; task had 2 prior rounds under CX9)
TASK-151 | CX9 | approved | Refuted ORCH's own hypothesis with evidence - I suspected TASK-140's kill-switch drill; the real cause was chatRunDriver.test.ts overwriting manifest descriptions (not enabled flags). A builder that just implemented the supervisor's guess would have fixed the wrong file and left the flake live. Fix is per-run disposable Postgres schema with cloned tables + FKs and scoped search_path - real data isolation, not the suite serialisation the task warned against. Both load-bearing assertions verified untouched; killSwitchDrill.test.ts has zero changes. ORCH re-ran the full unfiltered suite 3x: 64 passed every time. Closes a flake that muddied evidence across TASK-128/136/139/146. | first-pass? yes
TASK-147 | S5 | approved (round 2) | Mobile roster + live chat + create-bot. Round 1 sent back for two ACs claimed but not tested; that rework silently evaporated due to a dispatch-prompt defect (Review_Findings was never in the builder's reading list) - fixed at root, not counted against the builder. Round 2 addressed both properly. Teardown assertion MUTATION-PROVEN by ORCH: removed _subscription?.close() from dispose(), suite went red, source restored clean - the standard round 1 failed. Real SSE no polling, leak-free lifecycle, contract-accurate POST bodies, zero log statements. analyze clean, 36/36. | first-pass? no (1 real rework round + 1 evaporated round that was ORCH's defect)
TASK-148 | CX9 | approved | Mobile approval cards, routines tab, auto-review settings. Every AC has a dedicated named test incl. the no-usage-figure constraint enforced by findsNothing (ORCH swept the diff to confirm no usage figure crept in). Best call in the diff is a refusal: task allowed a rules-creation UI only if routes supported it; CX9 searched app.ts, found no require-approval-rules route, and declined to invent one rather than fabricate a backend contract - ORCH independently confirmed zero matches. Second time this unit tested a stated premise instead of assuming it (cf. TASK-151 refuting ORCH's own hypothesis). analyze clean, 42/42. | first-pass? yes
TASK-149 | S5 | approved | Mobile push client, env-gated FCM. No Firebase credential file committed (name-swept) and zero log statements so no device token can leak. The at-risk AC is genuinely tested: "with the default (dormant) push port, boot behaves exactly as before" - a real NoopPushPort proving the app is unchanged without Firebase, which is the only current reality. Token rotation, foreground decode for both trigger types, port disposal, and null-on-malformed-payload all have named tests. Targets the real POST /devices from TASK-145. analyze clean, 59/59. Completes mobile Wave 1+2. | first-pass? yes
TASK-152 | S5 | approved | APK/Gofile/Telegram release delivery script, ported from a user-supplied Node.js spec to Dart in apps/mobile/tool/. parse_mode exclusion is mutation-proof (test decodes the real sent JSON body, asserts exact key set) not just a happy-path check. Both missing-env-var cases tested independently with named errors and zero-HTTP-calls assertions. Git-shellout exclusion honored exactly - zero Process.run, --note argument stands in its place with its own parse tests. Zero credential literals, sentinel values assembled at runtime, with a dedicated test naming that property. New test: ^1.25.0 dev dep justified in-comment. ORCH re-ran: pub get/analyze clean, dart test 17/17, full flutter test 59/59 (no regression from the new dependency). Runbook written by ORCH (docs/**). Also flagged: the user-supplied spec contained what looks like a real, unrevoked Telegram bot token pasted as an "example" - not used anywhere, user advised to revoke it. | first-pass? yes
TASK-153 | CX9 | approved | SECURITY, critical. agentSdkOptions: {cwd: workspace, env: {}} - truly empty env, not filtered, so no secret this process holds is categorically ever passed to a chat run. Test is genuinely adversarial: injects a real secret into the test's own process env, captures the driver's actual computed values via a fake queryFn, then spawns a REAL bash subprocess with those literal values and proves both scoped cwd and secret absence - reverting the fix reddens this test. Cleanup proven too. ORCH independently re-ran the exact test given the real Windows risk (empty env can break subprocess spawning entirely) - passed for real. Full worker suite 13/13, build/lint clean. Scope explicitly limited to the proven vector; filesystem/network sandboxing (OpenSandbox) and the MCP-connector-leak vector remain separately named, not silently declared solved. | first-pass? yes
TASK-154 | CX | approved | SECURITY, critical - closes the second live incident from tonight. CX disproved ORCH's own hope rather than confirm it: ran a real live probe against the actual resolved system CLI binary (fresh mkdtemp cwd, env:{}, no mocking) and found TASK-153's empty env alone still leaked Gmail/Drive/Mobbin/Calendar/Notion. Found and applied the correct fix: the SDK's own strictMcpConfig flag, proven with a second live probe to fully close discovery while explicitly preserving oikonomos's own real connector mounting. isGovernedInvocation gate correctly scopes this to chat runs (cwd+env both present) without touching other harness-factory callers. ORCH mutation-tested the guard directly - removed it, watched the test redden, restored clean. Full suite/build/lint clean. Both live incidents from tonight now closed. | first-pass? yes
TASK-157 | S5 | approved | Mobile polish: markdown rendering (flutter_markdown_plus, noted the obvious choice is discontinued upstream), system-event styling (reuses a REAL existing MessageRole.system convention already in packages/db/src/messages.ts rather than inventing one), personalized placeholder, title field (investigated real control-api state - title set at creation but never serialized, no PATCH route - correctly scoped read-only per the task's own fallback, no route invented outside territory). Markdown test asserts both raw-syntax absence and structured rendering presence. analyze clean, 63/63. | first-pass? yes
TASK-155 | CX9 | approved | Continue-after-approval - the architecturally deepest task this wave, and it closes a gap that was demonstrably blocking real testing tonight. Reuses existing machinery throughout (resumeInterruptedRun from TASK-133, the same agentSdkOptions object TASK-153 scopes so a resumed run inherits full isolation, the pre-existing chat:thread: convention for deriving thread id). Trigger correctly gated on decision==granted + run genuinely waiting_approval + real session present; failed resume falls through to the existing failTaskRun path, no new fabricated-response risk. Two real tests: a mutation-proof fake-deps dispatch-contract proof (grant dispatches with exact resume payload, reject never dispatches) and a genuine real-Postgres end-to-end integration test. CX9 was honest that its own host couldn't capture clean aggregate test/build output rather than submit unverified - ORCH ran both directly: control-api 142/142 (both new tests confirmed by name), worker hit one transient contention failure then clean 13/13 on rerun, full build/lint clean. | first-pass? yes
TASK-158 | S5 | approved | Mobile routine creation UI. Everything grounded against the real backend (CREATE_ROUTINE_SCHEMA, nextFireAtFromCron's error path, definition.goal) via direct file reads, not spec inference. definition omitted entirely when no goal given - proven by an explicit containsKey assertion, not just call success. Server validation errors surface as real visible messages. Screen-level test proves the literal AC end-to-end: create a routine, see it in the tab immediately, no re-entry needed. Followed the existing CreateBotScreen pattern for consistency. analyze clean, 70/70. | first-pass? yes
TASK-156 | CX | approved | Role instructions/persona - the correctness half of tonight's persona incident. Real nullable roles.instructions column, real PATCH route, buildRoleSystemPrompt constructs a genuine identity from name/title/description/instructions rather than leaving the SDK to default to an ambient persona. Two precise tests: one proves all four fields genuinely appear in the captured systemPrompt, the other proves the custom-instructions section is genuinely ABSENT (not just empty) when NULL while the base identity persists - the degrade-gracefully path proven, not assumed. Empty-string-vs-NULL distinction deliberately documented. Additive to the same agentSdkOptions object TASK-153/155 use - ORCH confirmed both isolation and resume behavior unaffected. worker 13/13, control-api 10/10 (145 tests), db 28/28, build/lint clean. Closes the third and final open item from tonight's incidents. | first-pass? yes

## TASK-159 — Routine run history (CX9)
**Verdict:** approved, first-pass (code) · **Date:** 2026-09-05

Territory diff clean — only Owned_Paths touched. Backend change (`routineId` on `TaskListFilter`/`GET /tasks`) is a tight, correct 4-line addition matching spec exactly. Mobile detail screen reconstructs history via the two real endpoints (`GET /tasks?routineId=`, `GET /runs?taskId=`) as scoped, with an honest real-status display and a genuine empty state.

Builder's own Test_Evidence was NOT trusted at face value and was independently re-run: claimed a pre-existing control-api failure blocking their own branch's exit — this does not reproduce (branch is clean 32/32). Master, re-checked twice, actually carries 2 pre-existing failures, not the 1 the builder named. Neither pre-existing failure is touched by TASK-159's diff (no PATCH-roles/approvals-decide code paths in it) — order/state-dependent flakiness against the shared real-Postgres instance, not a regression. Full recursive `pnpm -r test --no-bail` surfaced 2 further unrelated pre-existing issues (WSL/bash missing for a TASK-153 test; a DB test timeout) — logged as new backlog tasks TASK-161/TASK-162, not blocking this merge.

Approved and merged `--no-ff`. Lesson reinforced: Test_Evidence is a claim, not proof — independent re-verification caught a real (if minor) inaccuracy in a dossier that otherwise represented correct, well-scoped work.

## TASK-160 — Inline cross-bot handoff chips (CX)
**Verdict:** REWORK · **Date:** 2026-09-05

Backend (`GET /roles/:roleId/messages`, merged sent+received handoff feed via the real `packages/db/src/roleMessages.ts` layer) is correct, tenant-scoped, and genuinely wired — independently confirmed (control-api 148/148, real-Postgres route wiring traced through `ports.ts`, not a stub).

CX honestly reported it could not run any Flutter checks at all (dart/flutter not discoverable on its worktree's PATH) rather than fabricate a result — good-faith disclosure, not a violation. ORCH ran the checks CX couldn't: `flutter analyze` clean, but `flutter test` came back 64/74 with all 10 failures in `chat_screen_test.dart`. Bisected against master in a temp worktree to rule out "pre-existing": the same file is 15/15 clean there — this is a genuine regression from this branch's `chat_screen.dart` diff, not a flake.

Root cause identified for the resubmission: `_loadHandoffs()` runs unconditionally in `initState`, adding two new API calls per `ChatScreen` build that the existing widget tests' fake client (apparently call-order-dependent) never anticipated — plausibly explaining otherwise-unrelated-looking failures (wrong HTTP method recorded, wrong field value read back). Findings written into `Review_Findings` for CX: fix at the root (explicit method/URL-matched stubbing, or scope when the new calls fire), not by special-casing the failures away, and prove both the 10 previously-passing tests and the new handoff tests pass together.

Not merged. Branch/worktree left as-is for CX's resumption.

## TASK-160 — Inline cross-bot handoff chips (CX) — RESUBMISSION
**Verdict:** approved, merged · **Date:** 2026-09-05

Root cause fixed correctly, not patched around: the fake test client's FIFO response queue was consuming the new handoff requests before existing transcript/SSE responses. `FakeHttpClient` now matches by exact method+path via `queueJsonFor`, with a safe default for the handoffs endpoint so unrelated tests never need to know it exists. Confirmed by reading the diff — the fix generalizes (path-based matching, not a special case for the one broken scenario) and the handoff/transcript endpoints are provably distinct paths, so no future collision.

Independently re-verified, not trusted: `flutter analyze` clean, `chat_screen_test.dart` 16/16, full `flutter test` 74/74 (all 10 previously-regressed tests now pass), control-api 148/148, full recursive suite clean except the already-known/logged TASK-161 WSL-bash gap (no new failures), build/lint clean. Two harmless local artifacts (pubspec.lock churn, a CRLF-only diff) discarded before merge — neither part of CX's actual committed work.

Merged `--no-ff`. This closes out Wave 7/8's mobile backlog entirely; only TASK-143 (frozen, awaiting a human decision) and the two low-priority TASK-161/162 backlog items remain open.

## TASK-143 — OIK-110/111 budgets, Codex/Grok subprocess path (S5)
**Verdict:** approved, merged, after 2 rounds of mandatory protected-path adversarial review · **Date:** 2026-09-05

This task touches `packages/broker/**`, a CLAUDE.md-protected path requiring adversarial review by a different model than the author. S5 (Claude Sonnet 5) authored it; ORCH is also Sonnet 5 this session, so self-review would have violated the non-negotiable directly. Invoked Codex CLI (gpt-5.6-terra) directly for two genuinely hostile passes rather than self-approve.

Round 1 found 6 real issues (not a rubber stamp): inert production wiring, an off-by-one at the budget ceiling (`>` vs `>=`), a dangling-routine-record fail-open bug, no validation on the platform ceiling value, no bounded timeout on the budget DB reads (violating CLAUDE.md non-negotiable #3), and a TOCTOU race. S5 fixed 5 of 6 across two further sessions, each independently re-confirmed (by Codex again, and by a separate test/code-verification pass) with exact file:line quotes matching both times.

The 6th finding evolved into a genuine scope question: `createGatedSubprocessProviders` (and the entire Codex/Grok subprocess-routing feature it belongs to, ADR-011) has zero production call sites anywhere in this repository — a pre-existing gap that predates TASK-143 and sits outside its Owned_Paths. Chasing this further inside TASK-143 would have meant expanding scope into wiring an unrelated, larger feature into production. Approved instead with the gap documented honestly (same pattern as the already-accepted Claude-SDK chat-path exclusion) and logged as **TASK-164**, tracked separately rather than silently resolved or endlessly re-worked.

Also fixed a genuine `Owned_Paths` path typo mid-review (`src/executeRun.test.ts` never existed) that had been silently blocking a correct territory-firewall refusal since 2026-09-04.

Merged `--no-ff`. This closes out the last active item from the post-incident backlog; only low/medium-priority tracked follow-ons (TASK-161/162/163/164) remain unassigned.

## TASK-165 — Expose bot title/instructions in the API and mobile settings UI (GB)
**Verdict:** approved, merged, first-pass · **Date:** 2026-09-05

Real capability-exposure fix: `serializeRole()` never returned `title`/`instructions` over HTTP despite both being real, working DB columns (instructions persisted via TASK-156's route) — this is why the settings screen showed a hardcoded placeholder and had no way to set a bot's persona at all. GB extended the serializer, honestly grew the existing shape assertions rather than loosening them, and added a real editable multi-line Instructions field to mobile with proper Save/Saving/error states and the correct empty-string-clears contract.

Independently re-verified rather than trusted: control-api 151/151, flutter analyze/test clean (82/82), full recursive suite clean modulo the two already-known pre-existing flakes. Read the actual diff directly — matches the dossier exactly. Merged --no-ff.

## TASK-166 — Chat file/image attachments (GB)
**Verdict:** approved, merged, first-pass on implementation · **Date:** 2026-09-05

A genuinely new capability, well-investigated: GB correctly resolved the real open design question (whether the agent-visible `WORKSPACE_ROOT` path is real or design-only — confirmed design-only by reading the actual module comments and the real chat driver's cwd construction) rather than guessing, and chose a real, working delivery mechanism instead: persist bytes to a local object store, record a structured reference on the message, inject content/metadata into the prompt at send time.

Commit was briefly blocked by a real bug in the task's own `Owned_Paths` authoring (a "TBD at decompose time" free-text prefix broke `territory-precommit`'s parser) — GB correctly refused `--no-verify` and escalated rather than bypass the hook. Fixed the authoring pattern (and the same latent bug in three other queued tasks) before redispatching to commit.

Independent security-focused verification (new upload surface, worth the scrutiny): confirmed no path-traversal vulnerability — storage paths use server-generated UUIDs only, client filenames never reach path construction, both `threadId` and `attachmentIds` are strict-UUID-validated. Confirmed size limits check decoded bytes (no base64-length bypass) and file-count limits are enforced twice. Confirmed the "agent actually reads the content" test is genuine — it captures the real constructed prompt and independently verifies file content, not a reference-only proxy. One accepted, documented limitation: content-type validation trusts the client header with no magic-byte sniffing — acceptable for this feature's current threat model (no execution path from an uploaded file), flagged for revisit if attachment handling ever gains more privilege.

Merged --no-ff. Unlocks TASK-167 (conversational rename), previously sequenced behind it.

## TASK-167 — Conversational bot self-rename (CX)
**Verdict:** approved, merged, first-pass on the actual implementation · **Date:** 2026-09-05

Hit four legitimate territory gaps in a row before landing — none were code defects, all correctly caught before speculative code was written: `chatRunDriver.ts` (a new tool would never be mounted into a governed run without it), `packages/db/src/index.ts` (the barrel export needed for the worker's MCP bridge to consume `updateRoleName`), and two exact-registry-shape tests (`builtinTools.test.ts`, `registerCapabilities.test.ts`) that hardcode the tool/capability inventory and would otherwise silently break. Each was widened deliberately (the last round comprehensively, adding all remaining paired test files at once, after three one-at-a-time rounds made the pattern obvious).

Along the way, uncovered and fixed two real session-wide environment gaps unrelated to this task's code: Docker Desktop and the `oikonomos-postgres-local` container were both dead after the earlier machine restart, and this worktree had zero `node_modules` installed.

The implementation itself is correct and well-scoped: `updateRoleName` validates and persists a real name change, the new `mcp__workspace__rename_self` tool is properly declared/registered at T1_draft, and — the real authorization boundary the task required — it derives its rename target only from the MCP bridge's own worker-bound identity, never a model-supplied `roleId`, with a dedicated test proving a cross-role attempt is rejected. The real end-to-end proof genuinely exercises the mounted tool through the actual dispatch path (matches this codebase's established "real governed chat run" pattern, same shape as TASK-131's precedent) — real Postgres row updated, real audit event recorded.

Independently re-verified rather than trusted: worker 88/88, broker 129/129, db 153/153, full `pnpm -r build`/`pnpm lint` clean. Merged --no-ff.

## TASK-172 | CX | approved | first-pass: no (one rework round)

Real per-user auth (backend): Firebase Google ID token verification replacing the hardcoded single-tenant `"basileia"` literal. `POST /auth/google` verifies a Firebase ID token's signature/issuer/audience/expiry via `jose`'s `createRemoteJWKSet` against Google's real, documented Firebase key endpoint — a deliberately lighter choice than `firebase-admin`, confirmed correct (issuer `https://securetoken.google.com/<projectId>`, audience `<projectId>`, RS256, plus issued-at/auth_time sanity checks). Sessions now carry the verified Firebase UID as a real `tenantId` claim; all 8 previously-hardcoded `"basileia"` call sites in `app.ts` replaced with `request.tenantId`. The existing shared-bearer-token path is preserved unchanged, now explicitly mapped to a named `SERVICE_TENANT_ID` constant rather than left ambiguous.

First pass sent to REWORK for one real, demonstrated gap: the real-Postgres two-user isolation test created roles through the full real HTTP pipeline but never cleaned them up — not a theoretical concern, proven with live evidence (8 leftover rows found already accumulated in the shared dev Postgres from review runs alone, same class of bug TASK-121 was reworked for earlier this session). CX's fix tracks created fixture role IDs and deletes `role_grants` before `roles` in a `finally` block, matching this codebase's established `cleanup()` convention.

Independently re-verified, not trusted: read `auth.ts` in full and confirmed the JWKS/issuer/audience verification is genuinely correct; ran the control-api suite myself (162/162); directly queried the shared dev Postgres after my own run and confirmed 0 leftover fixture rows; `pnpm -r build` and `pnpm lint` clean. Merged --no-ff.

## TASK-173 | S5 | approved | first-pass: yes

Real per-user auth (mobile): replaced the shared-token login screen with real Google Sign-In. `google_sign_in: ^7.2.0` + `firebase_auth: ^6.6.1` (current stable, verified against upstream docs/source rather than assumed from an older major version — v7's `GoogleSignIn.instance` singleton + `authenticate()` + `GoogleSignInException` cancellation shape is genuinely new since older tutorials). `GoogleAuthPort`/`FirebaseGoogleAuthPort` mirrors the existing `PushPort`/`NoopPushPort` fake-injection pattern so widget tests never touch real platform channels. `ApiClient.loginWithGoogle` posts the exact `{idToken}` shape TASK-172's real `/auth/google` route expects (read directly from `app.ts`, not guessed) and reuses the same generic `set-cookie` capture `login` already had. A real, tested `LoginScreen.signOut` clears the session and signs out of both Firebase and the cached Google account.

Two honest, correctly-scoped gaps named in the dossier rather than hidden: no real on-device manual confirmation (no device attached this session — the widget-test evidence is what a headless session can produce, and inventing a device pass would have been fabrication), and no UI entry point for `signOut` (roster/settings screens are outside this task's `Owned_Paths` — reaching into `roster_screen.dart` anyway would have been a territory violation, so a small follow-up, TASK-174, was added instead of guessing at placement).

Independently re-verified, not trusted: read the full `login_screen.dart`/`api_client.dart` diff directly and confirmed the credential exchange, `serverClientId`, and request shape are all genuinely correct against TASK-172's real route; ran `flutter pub get`/`flutter analyze`/`flutter test` myself in the task worktree (101/101, clean) and again in the main checkout after merge (101/101, clean, with an unrelated pre-existing compileSdk/file_picker fix folded in alongside and a regenerated `pubspec.lock`). Test assertions are real (POST body decoded and compared), not just "did not crash." Merged --no-ff.

## TASK-174 | S5 | approved | first-pass: no (one rework round)

Small follow-up to TASK-173: wired a real, visible sign-out button (RosterScreen app bar) to the already-built `LoginScreen.signOut`, clearing the session and returning to a fresh `LoginScreen` with the navigation stack cleared.

First pass sent to REWORK for one real, demonstrated defect found by reading the diff, not by running anything: `RosterScreen` constructs its own fresh `FirebaseGoogleAuthPort` (since `LoginScreen`'s default port is private to its file), and that fresh instance's `_initialized` flag is only ever set inside `signIn()` — so `signOut()`'s `if (_initialized)` guard silently skipped the real `GoogleSignIn.instance.signOut()` call. Firebase-side sign-out worked; the cached Google account never cleared, meaning a real re-sign-in would silently auto-relogin the same account instead of showing the picker — directly violating the task's own acceptance criterion. This is exactly the class of bug the fake-injection test pattern structurally cannot catch (`FakeGoogleAuthPort.signOut()` just increments a counter), so 102/102 green tests said nothing about it — a reminder that passing tests are not proof against every real-world path, only the ones actually exercised.

S5's fix applied exactly as specified: `FirebaseGoogleAuthPort.signOut()` now unconditionally calls `_ensureInitialized()` before `GoogleSignIn.instance.signOut()` (the singleton is safe to initialize from any instance), plus defense-in-depth threading the already-warmed `authPort` instance from `LoginScreen` into the `RosterScreen` it navigates to.

Independently re-verified, not trusted: read the actual fix diff and confirmed both parts are present exactly as specified; ran `flutter analyze`/`flutter test` myself in the worktree (102/102, clean) and again in the main checkout after merge (102/102, clean). Merged --no-ff.

## TASK-175 | S5 | approved | first-pass: yes

Pure mechanical carve of `services/worker/src/chatRunDriver.ts` (515 lines) into four single-purpose modules (`promptAssembly.ts`, `runWorkspace.ts`, `connectorResolution.ts`, `groupFanout.ts`) — the territory prerequisite unblocking five other tasks that all needed to touch the same file. Claimed zero behaviour change, which is exactly the claim a reviewer must verify rather than trust: read every moved function body directly against the pre-carve original (`resolveGrantedWorkspaceConnector`, `combineConnectorContexts`, all three `resolveGranted*Connector` variants, `deliverBotToBotMessage`, `buildRoleSystemPrompt`, `createChatRunWorkspace`/`removeChatRunWorkspace`) — byte-identical apart from the `export` keyword. `CHAT_FANOUT_CAPABILITY_ID` and both `WORKSPACE_*_TOOL` capability-id constants preserved exactly (a changed string here would silently break capability enforcement). Diffed every moved test body named in the task's own Description (TASK-139/128/122 assertions) — byte-identical. `index.ts` needed no changes because `chatRunDriver.ts` re-exports the moved names, correctly preserving the public import surface.

Real, pre-existing Postgres-contention flakiness surfaced during review (`pnpm -r test` failed a different, always-unrelated file each of three runs — `editApproval.test.ts`, `database.test.ts`, `runs.test.ts`, `messages.test.ts` — none touched by this task, each clean in isolation): confirmed as environmental (three concurrent builder sessions hitting one local Postgres), same known class as backlog item TASK-162, not a regression. `pnpm -r build` and `pnpm lint` both unconditionally clean in the worktree and again in the main checkout after merge. Merged --no-ff. Re-pointed TASK-161's Owned_Paths at the carved `runWorkspace.test.ts` per the task's own instruction; checked TASK-163/164/170 and found they needed no change.

## TASK-176 | CX9 | approved | first-pass: yes

Skills persistence layer (`packages/db`): migration 014 (`skills` + `role_skills` tables), typed CRUD/enablement layer mirroring `routines.ts` conventions exactly. Two-role enablement isolation and tenant-scoping tests are real, meaningful DB-integration tests (not "no crash" assertions) with correct fixture cleanup. CX9 deliberately did not apply migration 014 against the shared dev DB to avoid mutating shared state ahead of review — a good instinct, so the migration-reversibility AC anchor wasn't self-tested; independently verified it myself by cloning the real dev DB into a scratch database, applying migration 014 (clean), confirming both tables existed, reversing it (clean), confirming both tables gone, then dropping the scratch DB — leaving the real DB exactly as CX9 left it. Full recursive suite, build, and lint all independently re-run and clean (one transient contention timeout in an unrelated file, confirmed as such). Merged --no-ff.

## TASK-181 | CX | approved | first-pass: no (one rework round)

Six-part bot charter template seeded on bot creation (mobile), extending the TASK-167 conversational-instructions pattern. Real, meaningful tests: all six headings present, explicit negative assertions that the template contains no secrets/URLs/account names.

First pass came back `blocked`, honestly: the new `PATCH /roles/:id` call (seeding the charter) shifted `roster_screen_test.dart`'s fake-response queue out of sequence, breaking one out-of-territory test — CX correctly reported this as `OWNERSHIP_CONFLICT` rather than editing outside its `Owned_Paths` or silently working around it. Independently reproduced the exact failure before acting on the report. Widened `Owned_Paths` to the one affected fixture file and unblocked; the fix applied was exactly the queued response the situation called for. Re-verified independently: `flutter analyze`/`flutter test` clean (104/104) in the worktree and again in the main checkout after merge. Merged --no-ff.

## TASK-180 | CX9 | approved | first-pass: no (one legitimate scope-narrowing round)

Single-owner group routing engine: `route()` (mention/`@everyone`/scored-single-responder with tie-break by most-recent-responder, 6-member cap) and `createTierZeroScorer()` (a real budgeted default scorer via `withBudgetSink`, fails closed on an incomplete provider stream so a partial turn can never evade budget accounting). Wired into `deliverBotToBotMessage` via a strictly optional `routing` field — absent today (the only real caller), so TASK-122's existing approval-gate tests are provably unchanged.

First pass came back blocked on a real, accurately-diagnosed gap: the live production call site has no mention-parsing, classifier, or member-profile/recent-responder context at all. Rather than blindly widen this task into `app.ts`/`ports.ts` under time pressure, re-read the task's own Acceptance_Criteria and confirmed every anchor is genuinely satisfiable within the original four files via injection — the live-wiring gap is real but is separate, undecided architecture (no Tier-0/budgeted-classifier composition seam exists anywhere yet), split off as TASK-189 rather than improvised here.

Independently re-verified: read `groupRouting.ts` and the `groupFanout.ts` diff in full — every AC anchor has a real, meaningful test (including a genuine budget-sink assertion checking the actual provider id and cost, not "no crash"). Ran targeted tests, full build, and lint myself in the worktree and again after merge. Merged --no-ff.

## TASK-161 | GB | approved | first-pass: no (one round, and it caught a real mistake of ORCH's own)

Fixed TASK-153's chat-workspace isolation test, which depended on a `bash.exe`/WSL binary not resolvable on this dev machine. Replaced the shell spawn with a direct `process.execPath -e` child process — same real, external-process proof of cwd/env isolation, no shell dependency. Along the way, *strengthened* the assertion: the old test had a platform-conditional weakening (win32 only checked the reported cwd contained a substring); the fix drops that special case for one unconditional equality check on every platform.

This task also caught a real mistake in my own prior review: after TASK-175's carve, I re-pointed this task's `Owned_Paths` to `runWorkspace.test.ts` based on a doc comment, without checking that file's actual contents — the real failing spawn was still in `chatRunDriver.test.ts`, exactly where TASK-175's carve deliberately left it. GB reported the ownership conflict accurately rather than working around it or guessing. Corrected the re-point once GB's diagnosis was independently confirmed.

Independently re-verified: read the diff directly, ran the target test (16/16) and the full worker suite (87/87) myself in the worktree, plus build/lint, and again in the main checkout after merge. Merged --no-ff.

## Correction — TASK-176 (originally reviewed 2026-09-05, corrected 2026-09-06)

TASK-176's original approval said its two-role enablement isolation test "genuinely exercises the AC anchors, not just 'no crash'" — true of the test's assertions, but the test itself was never actually **run** during that review: migration 014 had not been applied to the real dev DB (I only verified apply/reverse against a disposable scratch clone to avoid mutating shared state), so `skills.test.ts`'s `DATABASE_URL`/migration integration gate silently skipped every DB-backed test, including the one I praised.

Once migration 014 was later applied to the real dev DB (by a subsequent builder session that needed it) and TASK-177 exercised `skills.ts` against real data for the first time, S5 found two real, load-bearing bugs: `UUID_RE` was missing a hex group (rejecting every real UUID — `getSkill`/`updateSkill`/`setEnabledForRole`/`listEnabledForRole` were all completely broken against real data), and `listEnabledForRole`'s join produced an ambiguous-column SQL error. Both confirmed and fixed directly (commit `1daec39`, `packages/db/src/skills.ts` was not owned by any active task) — verified `skills.test.ts` 4/4, full package suite 159/159.

Lesson recorded for future reviews: a `DATABASE_URL`-integration-gated test suite must be checked for **actually running**, not merely present with good assertions — a migration-gate gap is exactly as dangerous as a fixture-cleanup gap, and this session has now hit both classes.

## TASK-190 | S5 | approved (partial scope) | first-pass on the scope actually completed

Cross-tenant IDOR fix, part 1: `GET/PATCH /skills/:id`, `GET /runs/:id`, `GET /runs/:id/evidence`. 404 (never 403 — a 403 leaks existence to a non-owner) on a cross-tenant request; `PATCH` verifies ownership before ever reaching `updateSkill`. Real regression tests, including one proving a cross-tenant PATCH never reaches the mutating call at all.

The `/threads/:id/*` half was investigated (design fully worked out, left as a documented stub) but not shipped this round — a real, mechanically-confirmed tooling problem: TASK-190's own `Owned_Paths` field contained parenthetical rationale prose with commas, and the territory hook's naive comma-split corrupted the very path tokens the task needed to touch. This was an authoring mistake by ORCH (Owned_Paths must be a bare path list; rationale belongs in Description only), not a hook defect — confirmed by reading `hooks/lib.js` directly. Rather than force a fix through a broken parser or guess at a workaround, S5 reported it and reverted the incomplete work. Split into TASK-191 with corrected Owned_Paths.

Independently re-verified: read the diff, ran the new tests, full control-api suite (177/177), and `pnpm -r build`/`lint` — clean, both in the worktree and after merge. Approved and merged the completed scope immediately rather than let a critical security fix wait on the untestable remainder. Live control-api server rebuilt and restarted with the fix.

## TASK-191 | S5 | approved | first-pass: yes

Cross-tenant IDOR fix, part 2 (closes the class opened by TASK-190): `GET/POST /threads/:id/messages`, `POST /threads/:id/attachments`, `GET /threads/:id/stream`. `findTenantOwnedThread()` resolves ownership via the existing `deps.listRoles({tenantId})` source of truth (no new SQL join needed) — a 1:1 thread via `roleId`, a group thread only if **every** `memberRoleIds` entry resolves under the caller's tenant (deliberately not "any member matches"). Both write routes verify ownership before ever reaching `insertMessage`/attachment persistence.

Also fixed `chat.routes.test.ts`/`sse.test.ts`'s pre-existing fixtures to supply the role/thread data the real check now requires — every fix adds missing data to satisfy a real check, never loosens an assertion (confirmed by reading the diffs directly, including that the call-order assertions correctly grew a new `listRoles` step rather than being weakened around it).

Independently re-verified: read the full diff, ran the complete control-api suite myself (183/183, including a real-Postgres group-thread integration test on the actual production path), `pnpm -r build`/`lint` — clean, both in the worktree and after merge. Merged --no-ff. This closes the cross-tenant IDOR class entirely; live server rebuilt and restarted with the complete fix.

## ADR-014 adversarial review — 2026-09-06 (Fable 5.1, research/design session)
ADR-014 | ORCH/Sonnet-5 author | **ACCEPT-WITH-CHANGES** (6 changes) | Renderer finding verified (describe.ts dead, render.ts live); storage gap verified; add OIK-045a split, resolve key via existing OIK_SECRET_* convention, key_version column, WebCrypto AES-GCM with AAD row-binding, factory-not-import-time key check (**TASK-192 AC5 must change**), two audit event types. Full text appended to docs/decisions/ADR-014-dynamic-secret-vault.md. Side finding: TASK-067 describe-or-deny is configured-but-inert (ADR-005) — needs its own task.

## TASK-178 | CX | approved | first-pass: yes

Mobile skills UI: library screen (list/create/edit), per-bot enable toggle, and a `/` slash picker in the chat composer. Verified the two things most likely to be shortcut: the toggle mutates local state only from `setRoleSkillEnabled`'s returned server value (`confirmed`), never optimistically before the request resolves; and the picker sources its list from the real enabled-only endpoint with no client-side re-filtering, matching the spec's "server decides" instruction. Independently re-verified: read both call sites directly, ran flutter analyze/test myself (108/108) in the worktree and again after merge. Merged --no-ff.

## TASK-192 | CX9 | verified, merge HELD | (pending Fable review of ADR-014)

Dynamic secret vault (ADR-014 §2-§5): AES-256-GCM envelope encryption, GCM tag appended to ciphertext (documented in the file's own header), import-time fail-closed key validation, SQL-level role-filtered resolution so a missing ref and a cross-role ref are genuinely indistinguishable to the caller. Real, meaningful tests for every property that matters cryptographically (byte-for-byte round-trip, real pairwise-distinct-nonce assertion over 6 real values, real ciphertext-not-equal-plaintext assertion against the stored row, real module-reimport test for the fail-closed key check via `vi.resetModules()`).

Independently re-verified: read the implementation and tests in full against ADR-014, applied migration 019 to the shared dev DB myself so the migration-gated integration tests actually executed (not left skipped) — 4/4, full packages/db suite 163/163, build/lint clean. Implementation is correct and complete against the ADR as currently drafted. **Not merged**: the user is running ADR-014 past a live Fable session for the different-model architectural review this project's own discipline calls for, before this lands or TASK-184 resumes. This is a deliberate hold, not a finding against CX9's work.

## TASK-179 | S5 | approved | first-pass: yes

Context hygiene backend (Grok Bot parity gap G-03): per-thread context meter, rolling compaction into `thread_summaries` via a Tier-0 provider (same `withBudgetSink`/fail-closed-on-incomplete-stream pattern as TASK-180's group-routing scorer), redaction through the single canonical `redactPayload` before any summary is persisted, and `POST /threads/:id/fresh` epoch semantics so "start fresh" genuinely hides pre-fresh context from the model while keeping it visible in `GET /threads/:id/messages`.

Same real, correctly-handled ownership-gap shape as TASK-180: `ports.ts`/`packages/db/src/index.ts` are outside this task's territory, so production wiring into the live server was left as injectable ports (`ContextCompactionPorts`, a `ThreadContextPort` on `BuildAppOptions`) rather than reached into unowned files — split into TASK-193.

One subtlety worth naming: the compaction threshold check measures **all** messages since the last compaction, not just the kept verbatim tail — S5's own comment explains why (checking the tail alone would make compaction permanently unable to bring an over-threshold prompt back down, since the tail's size never shrinks). Independently re-verified: read `contextCompaction.ts` in full, ran every targeted suite myself (threadContext.test.ts 4/4 against real Postgres, contextCompaction.test.ts 13/13, promptAssembly.test.ts 16/16, threadContext.routes.test.ts 10/10), the full control-api suite (193/193, no regression), and the db/worker package suites — all clean, in the worktree and again after merge. Merged --no-ff.

## TASK-192 | CX9 | approved | second attempt (2 rework rounds, neither a quality issue)

Dynamic secret vault against ADR-014, in full: WebCrypto AES-256-GCM, canonical AAD row-binding (`{key_version, ref, role_id, tenant_id}`, computed identically on encrypt and decrypt), tenant+role SQL filtering (404-never-403, matching TASK-190/191's pattern), and a real `createSecretVault` factory whose construction — not module import — is where a missing/wrong-length key fails closed.

The first pass (reviewed and independently verified earlier) was solid engineering against the ADR as it existed at the time. A live Fable 5.1 adversarial review of ADR-014 landed mid-task with six substantive changes (WebCrypto over Node's `createCipheriv`, the AAD row-binding — "highest-value change in the review" — a `key_version` column, tenant_id in the resolver's WHERE, the factory-not-import-time pattern, two new audit event types) — sent to rework against the corrected design, which CX9 rebuilt correctly. A second, much smaller rework followed: the new tamper test's `UPDATE ... CASE` hit a genuine Postgres parameter-type-inference gotcha (ambiguous branches default to `text`, not `bytea`) — an isolated SQL bug, fixed with two `::bytea` casts.

Independently re-verified at every stage, including applying schema deltas to the shared dev DB myself so the migration-gated integration tests — including the tamper test, which actually proves the AAD binding does something rather than just being present in the code — genuinely ran rather than skipped. Full packages/db suite 164/166 (2 unrelated pre-existing skips), build/lint clean, both in the worktree and after merge. Merged --no-ff.

## TASK-194 | GB | approved | first-pass: yes

Closed a real ADR-005 control-liveness failure surfaced by Fable's ADR-014 review: `packages/broker/src/describe.ts` (TASK-067's fail-closed describe-or-deny gate) was never imported by `handlePreToolUse` — an undescribable T3+ tool call sailed straight to a valid `issueApproval` card instead of being denied. Both approval-issuing call sites (nonce-consume and issue-new) now funnel through a new `resolveApprovalRequired` helper that runs the gate first.

GB caught and fixed a real bug in its own work before submitting: both call sites were missing `return await` inside their try/catch, letting a dependency rejection escape the fail-closed handler — confirmed fixed at both sites by reading the diff directly, not taken on GB's word. GB also hit the identical Owned_Paths comma-parsing bug TASK-190 hit (a parenthetical with a comma inside this task's own Owned_Paths field, corrupting the territory hook's parse) and worked around it cleanly by using the already-reachable `index.test.ts` instead of the unreachable `describe.test.ts` — no rework cycle needed, though the authoring mistake itself (mine) has recurred twice now and is worth remembering as a durable lesson, not a one-off.

Independently re-verified: broker 137/137, evals-harness canary suite 19/19 (including the CAN-01/02/04/06/07/08/09 liveness/atomicity canaries this change sits directly adjacent to), full build/lint/banned-mode-grep clean — both in the worktree and after merge. A reported `services/control-api/skills.routes.test.ts` failure was verified as a pre-existing, unrelated contention flake by running that file alone (11/11, clean). Protected-path different-model review requirement satisfied (GB/Grok author, Sonnet-5 reviewer). Merged --no-ff.

## TASK-184 | CX | approved | first-pass: no (5 blocked rounds, all correctly diagnosed — 3 real territory gaps, an ADR-014 design-pass pause, 2 TASK-194-collision fixes; zero guessed workarounds)

`request_secret` broker tool + secret-intake foundation, protected path. Strict `{label, purpose}`-only input validation (rejecting extra fields, matching migration 017's own length CHECK constraints exactly); a `describeRequestSecret` describer registered in `builtinDescribers` (both directly in `describe.ts` and merged as a fallback in `index.ts`'s `resolveApprovalRequired`); capability-gated mounting via `resolveGrantedWorkspaceConnector`; the tool's own handler creates a `secret_requests` row, audits the request (requestId/label/purpose — never a value, since no value ever reaches this task's code), and parks the run through the same typed `parkTaskRun` RunParkPort path used elsewhere. AC3's "zero rows contain the value" is a structural guarantee of this task's own code, not something an integration test here could meaningfully add to: `fulfillSecretRequest`'s signature can only ever accept an opaque ref string — the plaintext literally never enters any TASK-184-owned code path. Full value-fulfilment (a human providing the actual secret) is TASK-187's job.

Five blocked rounds, all CX-diagnosed correctly and none guessed around: (1) worker mount/dispatch/RunParkPort wiring and a missing `packages/db` export, real, widened; (2) `connectorResolution.ts`'s hardcoded two-capability allowlist, real, widened; (3) no dynamic secret vault existed anywhere in the codebase — correctly recognised as real architecture work, not a quick patch, paused for a proper design pass (ADR-014, later Fable-reviewed and Accepted, built by TASK-192); (4)+(5) two knock-on collisions from ORCH's own TASK-194 merge landing mid-task (`describe.ts`'s new fail-closed gate needing a `request_secret` entry; `registerCapabilities.test.ts`'s hardcoded workspace-capability array; `index.test.ts`'s generic BUILTIN_TOOLS liveness fixture needing valid label/purpose input) — both root-caused by ORCH independently before widening, not CX mistakes. A sixth near-miss (TASK-182 wanting the same `packages/db/src/index.ts` barrel file while TASK-184 was still active) was correctly refused and sequenced via `Depends_On` rather than allowing two builders near one file.

Independently re-verified, not just trusted CX's own counts: broker 139/139, worker 126/126, db 176 passed/2 skipped (the 2 pre-existing and unrelated; `secretRequests.test.ts`'s own 2 tests ran for real against the shared dev DB, confirming migration 017 is genuinely live, not silently skipped). `pnpm -r build`, `pnpm lint`, both banned-mode CI checks (grep + 8/8 liveness) all clean, run directly by ORCH. CX's reported `pnpm -r test` failure (`chat.routes.test.ts`, "sorry, too many clients already") was reproduced by ORCH, then reproduced identically on master with TASK-184's changes entirely absent — confirmed pre-existing shared-Postgres test-infrastructure contention, unrelated to this task, not a rework blocker. Protected-path different-model review requirement satisfied (CX/Codex author, Sonnet-5 reviewer). Merged --no-ff (bb4581c).

## TASK-182 | CX9 | approved | first-pass: no (3 blocked rounds — 2 real territory/sequencing gaps, 1 migration-application gap; zero code-quality findings)

Routine parity semantics: `role_routines` gains `skill_id`/`on_missing_source`/`notify_threshold`/`paused`; a `routine_runs` table with 20-record retention (pruned via `DELETE ... OFFSET 20` on every insert); a 51st-routine 409 via a real `RoutineLimitError`; `routineJob.ts` checks each declared input's grant/availability before creating a run and records a `stopped` fire with zero model call when unavailable (or `skipped_paused` for a paused routine — both checked and recorded *before* `scheduler.fireRoutine` is ever called, a genuine zero-spend guarantee, not just an absence of logs); skill binding reuses TASK-177's existing `/name`-token mechanism entirely inside `routineJob.ts` (no touches to the contested `chatRunDriver.ts`/`promptAssembly.ts`); and real, DB-backed `POST /routines/:id/{pause,resume,test-run}` routes wired through `ports.ts` (ADR-005 liveness satisfied — not stub routes), 404-never-403 tenant-scoped.

Three blocked rounds: (1) pause/resume/test-run needed real `ports.ts` wiring, correctly identified — widened; (2) skill binding was reported as needing `chatRunDriver.ts`/`promptAssembly.ts`, but ORCH found this half of the claim was not a real gap (both files already contested by TASK-184/TASK-193) — `promptAssembly.ts`'s injection is purely a text-scan for a `/name` token, reusable with zero new plumbing entirely within `routineJob.ts`'s existing territory, which CX9 then implemented exactly as prescribed; a genuinely real second half of that same report (`packages/db/src/index.ts` export) hit an active single-owner conflict with TASK-184 and was correctly sequenced via `Depends_On` rather than force-widened; (3) after TASK-184 merged and the file freed up, CX9's code was complete and committed but blocked purely on migration 016 not being applied to the shared dev DB — applied directly by ORCH. A final resubmit's commit was rejected by the territory hook on a `DEVTEAM_UNIT` identity mismatch (dispatch-environment glitch, not a real violation); the staged work was genuinely complete, so ORCH committed it directly with the correct unit identity rather than bypassing the hook.

Independently re-verified, not just trusted CX9's counts: db 178/2skip (2 pre-existing, unrelated), worker routineJob 3/3, control-api routines routes 2/2, full `pnpm -r build`, `pnpm lint`, both banned-mode CI checks all clean — run directly by ORCH. Merged --no-ff (7d93c81). Unlocks TASK-183 (mobile surface), now fully ready to dispatch.

## TASK-183 | CX | approved | first-pass: no (1 SPEC_AMBIGUITY round, resolved by ORCH; also recovered mid-task from ORCH's own dispatch bug)

Mobile surface for TASK-182's routine parity work: pause/resume toggle and a test-run confirm dialog on the routine detail screen, a create-time enabled-skills selector, a context meter (60/80% colour thresholds) in the chat header, and a 'Start fresh' confirm sheet that calls the real fresh endpoint while keeping the visible transcript untouched.

One real blocked round: `POST /routines/:id/test-run`'s warning string is only returned after the side effect already ran, but the AC wants it shown pre-confirmation — resolved by using the known, stable literal string client-side (matching the server's own constant) for the dialog, then surfacing the real post-fire response separately, rather than inventing a new side-effect-free preview endpoint. A second, genuinely separate gap (no `PATCH /routines/:id` for edit-time skill rebinding) was correctly scoped out of this task and split to TASK-195 rather than reached into non-mobile territory.

Also recovered from an ORCH-caused issue mid-task: an earlier CX dispatch had been launched against a stale, already-merged worktree branch (a dispatch.ps1 identity-detection bug — flagged separately), and had begun drafting real, on-task implementation before ORCH killed it and reset the branch; `git checkout -B` doesn't clean the working tree, so those edits survived onto the fresh branch. CX correctly treated the unrecognised uncommitted changes as suspect and left them untouched rather than silently claiming or discarding them, reported the ambiguity, and — once ORCH confirmed the changes were legitimate prior work matching the resolution — built on them to a complete, tested implementation.

Independently re-verified: `flutter analyze` (no issues) and `flutter test` (112/112), both run directly by ORCH in the worktree, not trusted from CX's own report. Merged --no-ff.

## TASK-196 | CX | approved | first-pass: no (3 legitimate rounds — 2 were ORCH's own design mistakes, corrected by CX's pushback; 1 mechanical exhaustive-map widen)

Governed Tier-0 provider composition, split off TASK-189's block: `FreeLlmApiProvider` (a clean, minimal, non-agentic OpenAI-compatible adapter — no tool surface, so it structurally cannot become an agentic path) plus `services/worker/src/tierZeroProvider.ts`'s `createTierZeroProvider`, which runs a fail-closed pre-call budget check (mirroring `subprocessProviders.ts`'s own pattern: `resolveBudgetGate` + real routine/platform spend reads) before every call and records real spend afterward via the already-generic `withBudgetSink`. `ProviderRegistry` (the general interactive-chat registry) explicitly excludes `free-llm-api` via `Exclude<ProviderId, "free-llm-api">` rather than silently omitting it — FreeLLMAPI is Tier-0-only and worker-constructed, never user-selectable, and `config.ts`/`AppConfig` stay untouched.

Notable: ORCH got the design wrong twice before CX's own investigation corrected it — first assuming PreToolUse/broker governance applied to a non-tool text completion (it doesn't; that governs tool calls specifically), then assuming `wrapGateWithBudget` was reusable (it's genuinely typed to `GateSubprocess`/codex-grok only, and its real L1 gate would have silently rejected any other provider — reusing it would have been a lie, not composition). CX pushed back with evidence both times rather than forcing an implementation that wouldn't compile or would misrepresent its own governance, which is exactly the discipline this protocol exists to reward. The third round was a known, mechanical class of gap (an exhaustive `ProviderExtrasMap` needing a new entry — the identical shape as the TASK-095 Gemini addition earlier this project).

CX's claimed "pre-existing TASK-182 @oikonomos/db drift" blocking a full worker build was checked, not trusted — it was a stale local `packages/db/dist` in the worktree, not a real defect; rebuilding cleared it, confirming CX correctly declined to report it as a TASK-196 failure. Independently re-verified: agent-providers 100/100, worker tierZeroProvider 4/4, full `pnpm -r build`/`pnpm lint`/both banned-mode checks clean, all run directly by ORCH. Merged --no-ff. Unblocks TASK-189 (redispatched) and TASK-193 (sequenced behind TASK-189's `ports.ts` claim).

## TASK-189 | CX9 | approved | first-pass: yes (after dependency wait on TASK-196)

Wires group routing into the live production call site: unaddressed messages now first probe `route()` with a scorer that always throws — since `route()`'s own `@everyone`/`@name` branches resolve before ever calling the scorer, this correctly and cheaply detects deterministic addressing with zero wasted inference, falling into a real, budgeted `createTierZeroProvider` (TASK-196) call only for genuinely unaddressed messages, each tracked through its own real task/run row for spend attribution and audit lineage. Multi-recipient cases (`@everyone`/multi-`@name`) still go through the existing approval-gated `requestGroupFanout` completely unchanged (AC4 preserved); a single resolved recipient dispatches directly via `createTask`+`runChatTask` for exactly one real chat run (AC1). Member data threaded from `@oikonomos/db`'s typed accessors, no raw SQL added to `ports.ts`.

Two claimed "unrelated pre-existing" test failures (a `chat.routes.test.ts` full-file run, `packages/db/src/database.test.ts`) were independently checked, not trusted: both pass clean when run in isolation, and the exact same `chat.routes.test.ts` failures reproduce identically against unmodified master — confirmed pre-existing shared-Postgres connection contention, not caused by this task.

Independently re-verified: the new real-Postgres integration test (asserting real FreeLLMAPI scoring, real spend rows, and exactly one chat run) passes in isolation; full `pnpm -r build`, `pnpm lint`, both banned-mode checks all clean, run directly by ORCH. Merged --no-ff. Unblocks TASK-193 (assigned CX9, ready to dispatch).

## TASK-193 | CX9 | approved | first-pass: no (2 legitimate widen rounds — both real production-wiring gaps CX9 correctly diagnosed rather than working around)

Wires context compaction/meter into the live production call site, closing out the wave TASK-179/180 started. `services/control-api/src/index.ts`'s real `start()` now constructs a real `ThreadContextPort` and passes it into `buildApp` — the two context routes were structurally always `501` in production before this, no matter what got built in `ports.ts` alone. `chatRunDriver.ts`'s `compactCompletedChatRun` runs after every real completed chat run and lazily constructs the Tier-0 provider only when `maybeCompact` actually needs to summarize, so an ordinary short turn requires zero FreeLLMAPI environment config — reuses TASK-196's `createTierZeroProvider` directly rather than building a second one.

Two blocked rounds, both real: (1) the process entrypoint gap above, found by reading `index.ts` directly; (2) the AC's own literal wording ("integration test... not a stubbed port") genuinely requires touching the test files that exercise the real wiring, not just the production code — CX9 correctly declined to claim completion via existing fake-port unit tests alone.

Independently re-verified, not trusted: `threadContext.routes.test.ts` 11/11, `chatRunDriver.test.ts` 17/17 (including a new real chat-run compaction integration test), full `pnpm -r build`, `pnpm lint`, both banned-mode checks clean, run directly by ORCH. Merged --no-ff.

## TASK-195 | CX | approved | first-pass: no (1 legitimate widen round — a genuine gap ORCH missed when splitting this task off TASK-183)

Small, self-contained `PATCH /routines/:id` for edit-time skill rebinding, closing the gap left when TASK-183 scoped skill selection to create-time only. `updateRoutineSkill` follows the exact fetch-then-tenant-check-then-update pattern every other routine route this session established (404-never-403); the route's own schema validates `skillId` as a UUID-or-null before it ever reaches the DB, so a malformed id 400s cleanly instead of surfacing the `skill_id`→`skills` foreign key constraint raw.

One blocked round, correctly diagnosed and ORCH's own fault: the DB-layer accessor this task's own description called for lives in `packages/db/src/routines.ts`, omitted from Owned_Paths when the task was split off TASK-183.

Independently re-verified: `routines.routes.test.ts` 4/4, `packages/db/src/routines.test.ts` 10/10, full `pnpm -r build`, `pnpm lint`, both banned-mode checks clean, run directly by ORCH. Merged --no-ff. `validate_plan.py` now reports zero warnings across the entire plan — the Owned_Paths comma bug and every latent isolation warning from this session's wave are fully resolved.

## TASK-169 | CX9 (code) + ORCH (live-proof fix) | approved | closed after a genuine human-credential unblock

CX9's original client code (getEndpoint/ping/runCommand, EXECD_ACCESS_TOKEN injection, SSE parsing) was already reviewed and merged as code-complete in an earlier session — this entry covers closing the task for real once the human supplied the OpenSandbox credentials that had been the sole remaining blocker.

Running the gated live integration test against clawsrv for the first time surfaced a genuine bug the fake-transport unit tests could never have caught: `getEndpoint`'s response from the real server is a scheme-less `host:port/path` string, which `fetch()` rejects as an invalid URL — silently mapped by `execdRequest`'s catch-all into a generic `REQUEST_FAILED` error with no status. Diagnosed directly against the live server with raw `curl` (confirming execd itself responded fine the whole time, isolating the bug to the client's own URL handling) rather than guessing. Fixed by normalizing the scheme from the client's own `baseUrl` before returning the endpoint. Reran: both live tests pass end-to-end — a real sandbox created, execd pinged, `echo hello` executed with exit 0, sandbox destroyed.

Full package suite 23/23 (was 21/23 with 2 gated-skip), full `pnpm -r build`/`pnpm lint` clean. One open, non-blocking item: the execd image version discrepancy (README v1.0.22 vs upstream's 1.0.12) needs `docker images` run on clawsrv itself, which requires host shell access not available from here — flagged for whoever has it. Unblocks TASK-170 (all three Depends_On now satisfied).

## TASK-197 | CX | approved | first-pass: no (1 review-time widen — a real lockfile-sync gap ORCH found and fixed)

The actual OIK-084 broker HTTP surface, long-deferred since the broker's earliest implementation. `POST /v1/broker/pretooluse` on its own dedicated Fastify listener (a genuinely separate port, not merely excluded-by-allowlist on the shared control-api listener — per Fable's ADR-015 review, iptables/Tailscale ACLs filter by port, not by path). Auth is a per-turn HMAC token (`mintBrokerToken`/`verifyBrokerToken`, `timingSafeEqual` comparison mirroring `auth.ts`'s existing house style) bound to `runId`/`roleId`/`tenantId`/`agentRef` — identity is derived FROM the verified token, and any request body claiming a different identity is rejected before the broker handler ever runs, closing exactly the spoofing gap Fable's review identified. Server-side 9s timeout, real sliding-window rate limiting, and a body-size cap all map to a clean deny rather than a crash or hang. The signing key resolves (and fails) at construction time, not module import, correctly reusing this session's own TASK-192 factory-pattern precedent.

One real gap found during review, not by CX: `package.json` declared two new workspace dependencies but `pnpm-lock.yaml` was never synced — confirmed for real with `pnpm install --frozen-lockfile` (which failed with `ERR_PNPM_OUTDATED_LOCKFILE`), not assumed. Fixed by running `pnpm install` and committing the synced lockfile into CX's own branch.

Independently re-verified: brokerHttpRoute/brokerToken suites 10/10 — every AC has its own named test, confirmed by reading the test file directly, not just trusting a pass count. Full `pnpm -r build`, `pnpm lint`, both banned-mode checks clean, run directly by ORCH. Merged --no-ff. This is the first piece of the ADR-015 wave; unblocks TASK-198 (sandboxed hook script).

## TASK-198 | CX9 | approved | first-pass: yes

The sandboxed PreToolUse hook script (OIK-084's client side) — a genuinely separate artifact from the in-process SDK callback, meant to run as a bare CLI subprocess Claude Code's own hook mechanism launches inside the sandbox. Implements all four of ADR-015 change 3's fail-open fixes: `process.exitCode = 2` is the literal first statement in `main()`, before stdin is even read, with `uncaughtException`/`unhandledRejection` handlers installed immediately after, so the process cannot exit non-2 by accident; exit-0 requires HTTP 200 + JSON content-type + an exact `decision === "allow"` match + a matching `toolUseId` echo, with every other outcome (deny/timeout/non-200/malformed/mismatched id) denying. Governance identity (`runId`/`roleId`/`tenantId`/`agentRef`) comes exclusively from explicit `OIK_SANDBOX_*` environment variables, never inferred from the hook's own stdin payload — exactly the ADR-004 discipline this needed. `approval_pending` needed no special-casing since it's already wire-encoded as a `deny`. A nice touch: the banned-permission-mode check constructs its comparison strings to avoid tripping this repo's own banned-mode grep scanner, since the file legitimately needs to reference those literal strings to detect and deny them — confirmed the real scanner still passes clean.

Independently re-verified: `sandboxHook.test.ts` 7/7, one test per AC, confirmed by name. Full `pnpm -r build`, `pnpm lint`, both banned-mode checks clean, run directly by ORCH (one stale-worktree dependency sync needed first — TASK-197's new control-api deps hadn't materialized locally yet, not a TASK-198 defect). CX9's dossier explicitly documents TASK-170's remaining registration responsibilities (managed-settings tier, timeout ordering, no narrow matcher) rather than silently assuming them handled. Merged --no-ff. This completes the ADR-015 wave's two prerequisite pieces — TASK-170 is now fully unblocked.

## TASK-170 | CX9 (+ ORCH) | approved | first-pass: no (9 correctly-diagnosed blocks, full history in the task's own Progress_Notes)

OIK-043 — routes real chat execution through a persistent per-role OpenSandbox office, implementing ADR-015's Resolution point 3 (the sandboxed broker-enforcement design that this task's own 3rd block escalated into existence, reviewed by Fable, delivered as TASK-197/198). This is the largest single task this session: 9 distinct correctly-diagnosed blocks across territory gaps (sandbox-client lifecycle methods, DB barrel/worker manifest, a genuine architecture gap requiring ADR-015, an image-delivery pipeline that didn't exist, a broker-token package-cycle, an unowned test import, a missing mode-selection contract, a `pnpm-lock.yaml` sync, and a wrong Tailscale IP), each verified independently before widening rather than taken on the builder's word — see the task's own Progress_Notes for the complete diagnostic trail. CX9's discipline across every single block was correct: it never bypassed the broker hook, never fabricated image-delivery infrastructure, never invented or borrowed a credential, and always stopped at the exact boundary of its Owned_Paths.

**What shipped:** `executeSandboxChatRun` resolves/creates/resumes a persistent per-role sandbox (never per-turn), polls it Running, re-resolves a fresh execd endpoint after resume (never reuses a stale pre-pause URL), mints a short-lived per-turn broker token (`mintBrokerToken`, relocated from `services/control-api` into `packages/broker` mid-task to avoid a genuine worker/control-api package cycle — verified as a byte-for-byte relocation, no logic changed), and injects it plus the real Anthropic API key into the `/command` call's `envs` only — never spread from `process.env`, never baked into the image (TASK-154's guarantee preserved). A fail-closed SHA-256 integrity check on the sandbox's managed hook settings runs before every dispatch. The office-base Docker image (Node 22, pinned Claude CLI 2.1.263, root-owned mode-0444 managed settings and hook script, no baked account/MCP config) was built and deployed to clawsrv directly by ORCH after the human confirmed clawsrv shell access exists (an earlier session assumption to the contrary was wrong).

**Two genuinely significant findings surfaced along the way, both fixed for real rather than worked around:**
1. The already-written DOCKER-USER firewall remedy (`infra/sandbox/README.md` section 7.1, documented since 2026-08-16) was discovered to have never actually worked, on any host — Docker's own NAT rewrites a published port to the container's internal port before the DOCKER-USER filter chain ever evaluates it, so matching on the published port number there can never fire. Root-caused via a full `nft list ruleset` dump (not by re-reading the broken rule's own syntax), fixed with `-m conntrack --ctorigdstport` (the pre-NAT original port), and independently confirmed via an online port-checker run from an unrelated network (the port genuinely went from open to filtered), then persisted across reboot and re-verified in the saved rules file.
2. CLAUDE.md's platform budget ceiling was corrected this same session from R30,000/month to R350/month (human-directed, confirmed before applying given the roughly 100x change) — applied to the real enforcement constant in `subprocessProviders.ts`, not just the doc, fixing one genuine test regression this exposed cleanly rather than loosening the new ceiling to make the old test pass. The sandboxed CLI is now pinned to `claude-haiku-4-5-20251001` by default (`OIKONOMOS_SANDBOX_MODEL` override), prompted directly by the human's cost question; TASK-201 filed for the equivalent host-path fix (a protected-path change needing its own adversarial review, not rushed into this task).

**The credential question itself had two real course-corrections, both caught before landing:** a proposed `claude setup-token`/`CLAUDE_CODE_OAUTH_TOKEN` shortcut (reusing the human's existing subscription) was retracted after checking Anthropic's actual Consumer Terms directly — it explicitly prohibits automated/non-human access via a subscription credential. A proposed swap to the human's existing Gemini API key was investigated and deferred after finding Gemini CLI's hook mechanism is documented to fail open by default, the opposite of this project's core non-negotiable — swapping providers would need its own from-scratch governance redesign, not a credential substitution, and was correctly left as separate future work rather than decided informally mid-task.

**The hook-removal liveness assertion (this task's hardest AC) was completed by ORCH directly**, since CX9 correctly identified that proving it required mutating or replacing the shared production image — outside its own runtime territory, and the right call to escalate rather than force. Built a second, test-only image (`oikonomos-office-nohook-test`, in its own clawsrv directory, never referenced by any production config) with the identical base but no managed-settings/hook at all. Ran the identical governed `claude -p` Bash probe against a real sandbox created from it: the exact same command that the hook denies actually ran and returned real output (`TASK170_NOHOOK_PROBE_OK`) when the hook was absent. Before accepting CX9's own observed `broker.http_500` reason string at face value, independently reproduced it twice — first with a diagnostic script using a nonexistent role (confirming the reason was an artifact of an audit-log FK failure on fake data, not a real bug), then with a genuine role/task/run created via the real production functions, both in-process and over the actual live HTTP route, getting a clean `capability.disabled` deny with a real audit event both times. The core broker logic is correct; CX9's own live observation (the model explicitly reporting the Bash command didn't run) was accurate regardless of the exact wrapped reason string.

**Independently re-verified, not trusted:** full territory diff clean (every changed file maps to an Owned_Path, including the `brokerToken.ts` relocation which git correctly detects as a rename). Full recursive `pnpm -r test` (delegated to a subagent to keep this review's own context lean, then a targeted isolation rerun done directly): TASK-170's own owned areas are 100% green — `services/worker` 137/137 (includes `chatRunDriver.test.ts` 20/20, TASK-116's full regression suite unchanged), `packages/broker` 142/142 (includes the relocated `brokerToken.ts`), `packages/sandbox-client` 23/23, `packages/db`'s `roleSandboxes.test.ts` 2/2. Two failures elsewhere, both confirmed pre-existing and unrelated: `packages/db/messages.test.ts`'s documented `thread_members_thread_id_fkey` teardown-ordering flake, and `services/control-api/chat.routes.test.ts`'s connection-exhaustion failure — the exact same root cause already independently diagnosed and filed as TASK-199 earlier this session, in a completely different worktree, confirmed unrelated to any of TASK-170's changes. Full `pnpm -r build` and `pnpm lint` clean; both banned-mode checks clean.

**Acceptance criteria:** all nine satisfied with real evidence — sandbox create-once/reuse (fake-client and live proof: create 1,101ms, Running 2,412ms, pause 301ms), pause/resume with fresh-endpoint re-resolution, env isolation (no host secret/canary leaks into `/command` env, live-verified), the hook-removal liveness counterfactual (above), per-turn token minting (never a static secret, code-reviewed), ADR-015 section 4 gap accounting (L2/CAN-05 rebuilt; L3/PostToolUse/park explicitly recorded as open, not silently assumed covered), TASK-116/153/154 regressions unchanged, and `pnpm -r test`/`build`/`lint`/banned-mode all accounted for. The one AC needing action at review time — "TASK-185 dispatched in the same wave" — is being satisfied immediately after this merge (TASK-185's own host-firewall trigger already fired and was closed ahead of schedule by ORCH during this task; TASK-185 itself is being dispatched to CX now that TASK-170 unblocks it).

Merged --no-ff. Unblocks TASK-171/185/186/187/188.
