org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Works around a known Kotlin Build-Tools-API incremental-compilation bug on
# Windows ("Could not close incremental caches" in compileDebugKotlin, hit
# building firebase_core, 2026-09-05) — a file-handle-closing race in the
# compiler's memory-mapped cache storage, unrelated to app code correctness.
kotlin.incremental=false
