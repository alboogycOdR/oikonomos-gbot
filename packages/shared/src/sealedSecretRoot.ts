/**
 * Canonical D3 sealed-secret root shared by every defence-in-depth guard.
 * This namespace is deliberately outside the model-visible workspace.
 */
export const SEALED_SECRET_ROOT = "/oikonomos/secrets";
