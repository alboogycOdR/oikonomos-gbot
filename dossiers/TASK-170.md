# TASK-170 Dossier

## Work Log

- [2026-09-06T14:35:00Z] [CX9] Read AGENTS.md, CODEX briefing, the authoritative coordination protocol, TASK-170's full PLAN block, `docs/research/opensandbox-exec-api-gap-2026-09-05.md` Resolution, and `infra/sandbox/README.md`. No prior dossier exists and `Review_Findings` is empty. Preflight output: `[preflight] TASK-170 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; 9 entries: existing `services/worker/src/chatRunDriver.ts` (344 lines), `services/worker/src/runWorkspace.ts` (13), `services/worker/src/runWorkspace.test.ts` (31), `services/worker/test/executeRun.test.ts` (202), `packages/harness-factory/src/**` (20 files); new `packages/db/src/roleSandboxes.ts`, `packages/db/src/roleSandboxes.test.ts`, `infra/postgres/migrations/018_role_sandboxes.up.sql`, and `.down.sql`.
- [2026-09-06T14:35:00Z] [CX9] Blocked before code: TASK-170 requires idle sandbox pause/resume with endpoint re-resolution, but the real `@oikonomos/sandbox-client` API currently exports only `health`, `createSandbox`, `destroySandbox`, `getEndpoint`, `ping`, and `runCommand`; it has no `pauseSandbox`/`resumeSandbox` methods. `packages/sandbox-client/src/client.ts`, its types, and tests are outside TASK-170 `Owned_Paths`. Implementing direct lifecycle HTTP/auth logic in the worker would duplicate the protected client boundary rather than use the required TASK-169 seam. Need an Owned_Paths widening for the sandbox-client client/types/tests (or a prerequisite task that adds pause/resume) before TASK-170 can satisfy its unit and live acceptance criteria.
- [2026-09-06T12:11:25Z] [CX9] Resume/rework check complete: PLAN.md now widens the sandbox-client lifecycle files and has no REWORK finding. After merging current main and reading the live package boundaries, a second ownership gap blocks implementation: the required typed `roleSandboxes` accessor must be exported from `packages/db/src/index.ts` for `services/worker` to import it (the package exposes only `.`; no deep `@oikonomos/db/*` imports exist), but that barrel is outside Owned_Paths. The worker also has no declared `@oikonomos/sandbox-client` dependency, so `services/worker/package.json` must be changed to make the production import valid; that manifest is also outside Owned_Paths. `pnpm --filter @oikonomos/worker exec tsc --noEmit --pretty false` is currently clean before implementation. Need an Owned_Paths widening to exactly `packages/db/src/index.ts` and `services/worker/package.json`; no out-of-territory workaround attempted.
- [2026-09-06T15:14:00Z] [CX9] Resume/rework check after the two approved widenings: TASK-170 has no REWORK finding. Re-ran `python scripts/preflight_paths.py TASK-170`; it reports 14 owned entries: the four original worker files and `packages/harness-factory/src/**` exist, the two new DB files and migration pair are NEW territory, and the widened sandbox-client client/types/index/tests plus DB barrel and worker manifest are all in territory. New design blocker found before implementation: the real production path is `chatRunDriver -> executeTaskRun -> composeHarness`, where L1/L3 are in-memory callbacks over `createInProcessBrokerPort`, not a network service. An execd `/command` invocation of `claude -p` inside a sandbox cannot call those callbacks. The claimed loopback MCP bridge cannot supply this seam: `packages/agent-providers/src/mcpBridge/mcpBridge.ts` binds only `127.0.0.1` (therefore unreachable from the bridge-network sandbox) and is not mounted by `chatRunDriver`; it also governs MCP tool calls only, not the CLI's PreToolUse hook. `@oikonomos/broker` explicitly says its HTTP adaptation belongs to a separate OIK-084, and no worker-side broker HTTP listener/config generator exists. Consequently, remote execution would bypass the mandated broker-hook liveness assertion, while preserving the local `queryFn` path would mean the harness is not actually running inside the sandbox. Need an architectural decision plus owned implementation territory for a reachable authenticated broker-hook transport and sandbox-consumable hook configuration (or a corrected execution model) before implementation can honestly proceed. Did not run the live workload or the README §7.1 iptables change: TASK-185 owns that shared-host operation, is already dispatched, and depends on this task.
- [2026-09-06T14:26:19Z] [CX9] Resume check complete: the PLAN block has no `REWORK` findings; TASK-197 and TASK-198 are merged and the existing branch was updated with `origin/master`. Current preflight output: `[preflight] TASK-170 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; 15 entries, consisting of the four worker files, four new DB/migration files, five sandbox-client files, the DB barrel, and the worker manifest. The mandatory ADR-015 §3.4 registration remains impossible within that territory: repository inventory finds no Dockerfile/Containerfile or existing managed-settings asset; the only sandbox-related implementation files are the newly merged hook and sandbox-client tests. The hook needs to be installed as a compiled executable in the sandbox image and registered in root-owned Claude managed settings (no narrow matcher; timeout above 10 seconds), but no owned file can define either the image contents or that settings tier. Writing it dynamically through `/command` would not satisfy the image/managed-settings requirement and cannot provide the compiled hook artifact. This is an `OWNERSHIP_CONFLICT`, not an implementation detail to improvise. `pnpm --filter @oikonomos/worker typecheck` passes before implementation. Required next action: widen TASK-170 to the actual sandbox-image build/managed-settings configuration path, or create a dedicated prerequisite that produces the image carrying the TASK-198 hook and its root-owned settings registration.
- [2026-09-06T17:05:00Z] [CX9] Resumed after the sanctioned image/README widen, read the live TASK-170 block, ADR-015 Decision, TASK-197/198 implementations, and re-ran preflight (17 entries; `infra/sandbox/images/office-base/**` is correctly new territory). The required per-turn token handoff remains structurally blocked: ADR-015 §Decision 2 explicitly assigns minting to the worker and TASK-170 directs use of TASK-197's `mintBrokerToken`, but that function is exported only by `@oikonomos/control-api`. `@oikonomos/control-api` already has a production dependency on `@oikonomos/worker`; adding the reverse worker dependency would create a package cycle. Reimplementing the HMAC protocol in the worker would duplicate the security-sensitive canonical signer contrary to the task instruction. No token-mint HTTP endpoint exists, and `services/control-api/**` is outside this task's owned paths. Need a narrow architectural/ownership decision: extract the token pair to a dependency-neutral package, provide a worker-safe minting port, or explicitly authorize a defined signer implementation in the worker. Preflight and `pnpm view @anthropic-ai/claude-code version` completed; npm currently reports `2.1.263`, matching ADR-015's installed-contract pin, but I did not create a partial image/driver change that cannot securely dispatch a governed turn.
- [2026-09-06T14:48:03Z] [CX9] Resumed after ORCH's approved broker-token relocation. Merged current `origin/master` (the PLAN-only relocation/widen) and reran preflight: 24 owned entries. Implemented the first executable foundations: migration 018 + typed role-sandbox persistence accessor, and typed OpenSandbox `getSandbox`/`pauseSandbox`/`resumeSandbox` lifecycle operations needed for resume polling and fresh endpoint resolution. `pnpm --filter @oikonomos/sandbox-client typecheck`, `pnpm --filter @oikonomos/db typecheck`, and `pnpm --filter @oikonomos/worker typecheck` pass; sandbox-client tests pass 23/23 including its real clawsrv create/destroy test. DB tests exercised 178 passing tests plus the new accessor tests, but the package command exits 1 due to two pre-existing `messages.test.ts` cleanup failures: `thread_members_thread_id_fkey` prevents deletion of a thread. This task does not own that cleanup test or schema relationship. Next: move the canonical broker token module into `packages/broker`, then wire the sandbox runner, image, and driver tests.
- [2026-09-06T14:53:19Z] [CX9] Resume/rework check: TASK-170 has no REWORK findings. Re-ran preflight (24 owned entries) and began the sanctioned canonical broker-token relocation into `packages/broker`; broker typecheck/build pass. The deletion required by ORCH's precise relocation instruction exposes an unowned test dependency: `services/control-api/src/brokerHttpRoute.test.ts` imports `./brokerToken.js` directly (line 10) but is not in TASK-170 Owned_Paths. `pnpm --filter @oikonomos/control-api typecheck` now fails only with TS2307 for that deleted module. A compatibility shim would contradict the explicit instruction to delete the old module; changing the test is the correct fix but needs a narrow widen to `services/control-api/src/brokerHttpRoute.test.ts`. No out-of-territory edit attempted. Next: supervisor widens this one test path, then update its import to `@oikonomos/broker`, run route/token tests, commit relocation, and continue sandbox runner/image wiring.
- [2026-09-06T17:05:00Z] [CX9] Resumed after ORCH's narrow test-path widen and re-read the live TASK-170 block (no REWORK), ADR-015, the OpenSandbox resolution, README, and the owned execution surfaces. Updated the stale control-api broker-route test import to the canonical `@oikonomos/broker` export and committed `a0973f1` (`test(control-api): import relocated broker token [TASK-170]`). `pnpm --filter @oikonomos/control-api typecheck`, `pnpm --filter @oikonomos/control-api test -- brokerHttpRoute.test.ts` (6/6), plus broker/worker/control-api typechecks all pass. Sandbox runner/image wiring remains next; no live workload was started, so TASK-185's host-level DOCKER-USER remedy remains a separately owned prerequisite.
- [2026-09-06T15:05:41Z] [CX9] Resume/rework check after syncing `origin/master`: TASK-170 remains `in_progress` and `Review_Findings` contains no REWORK. Re-ran the mandated preflight; all 25 entries are available, with `infra/sandbox/images/office-base/**` the only new glob territory. Confirmed the pinned Claude Code package version with `pnpm view @anthropic-ai/claude-code version` (`2.1.263`). Blocked before adding a sandbox-driver implementation: the authoritative task description requires the existing local temp-dir fallback to be selectable by config while production defaults to sandbox, but neither the task block, ADR-015, nor the OpenSandbox resolution defines a configuration key, allowed values, or selection behavior. Introducing one would be a new execution-security contract. Separately, the required driver-level create/reuse and live integration coverage belongs in `services/worker/src/chatRunDriver.test.ts`, which is not an Owned_Path; the only owned worker test surfaces are `src/runWorkspace.test.ts` and `test/executeRun.test.ts`, neither drives `createChatRunDriver(...).run()` or can prove the task's integration acceptance criteria. Need (1) a specified sandbox/local selection contract and (2) a narrow widen to `services/worker/src/chatRunDriver.test.ts` (or explicit alternative test location) before implementing; no out-of-territory test or configuration workaround attempted.
- [2026-09-06T17:35:00Z] [CX9] Resume/rework check after the mode-contract and `chatRunDriver.test.ts` widen: no REWORK finding. Synced `origin/master` and re-ran `python scripts/preflight_paths.py TASK-170` (26 owned entries). Confirmed the installed pinned CLI is `claude 2.1.263` and its help exposes the required `-p`, `--permission-mode dontAsk`, `--system-prompt`, and `--output-format text` flags. The sandbox runner must import `@oikonomos/sandbox-client`; `services/worker/package.json` is owned and was updated transiently, but `pnpm --filter @oikonomos/worker typecheck` proves the existing installed workspace links lack that package and TypeScript cannot resolve it. `pnpm install --lockfile-only` is the normal required next step, but it necessarily changes root `pnpm-lock.yaml`, which is outside TASK-170 Owned_Paths. No lockfile or node_modules workaround was applied; the scoped source edits were reverted cleanly. Need a narrow `pnpm-lock.yaml` ownership widen (or a supervisor-provided lockfile sync) before implementation can compile/test.
