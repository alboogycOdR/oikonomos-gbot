# TASK-049 — OIK-053 Wave 1: Google Calendar connector

## Brief
Calendar manifest + golden suite ≥90% through the OIK-051 runner; same shape as TASK-048 (see that dossier for the worked pattern). Read/list T0; event mutations tiered explicitly, externally-visible ones T3_external `enabled: false` until G-CONN. Backlog — ORCH assigns after the pipeline (043/044/046) merges.

## Work Log

- [2026-09-02T09:30:00Z] [S5] Session start (resuming an already-claimed/branched task; `.devteam/CHECKPOINT.md` was stale — referenced TASK-095, already merged per `git log` — deleted per protocol). Read AGENTS.md, briefing, PLAN.md fresh; on `task/TASK-049-s5` already (no re-branch needed), dossier was a brief-only stub (no prior work).
  Preflight (`python scripts/preflight_paths.py TASK-049`), verbatim:
  ```
  [preflight] TASK-049 Owned_Paths inspected in E:/DELL-PROJECTS/wt-s5-GROKBOT-CLONE
  [preflight] 2 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.
    NEW    packages/connectors/manifests/google-calendar.yaml  -> does not exist; parent packages/connectors/manifests/ exists
    GLOB   evals/golden/suites/google-calendar/**  -> matches nothing yet (new territory)
  ```
  Dependencies (TASK-043/044/046) already confirmed `done` per ORCH's dispatch note.
- Read TASK-048's dossier in full (the worked Gmail pattern named in this task's brief) and mirrored its approach. Unlike TASK-048, no pre-existing manifest fixture for calendar — authored fresh. Checked `docs/architecture/OIKONOMOS_Master_Work_Breakdown_v1.0.md` (OIK-053 row), `WBS_Addendum_F` (ADR-010 autonomous-by-default note re: calendar writes), and the current PLAN.md progress note confirming: despite Addendum F listing calendar writes as autonomous-by-default (no approval card), this task's `enabled: false`-until-G-CONN posture is unchanged — it's a G-CONN gate, not a tier gate. Read `packages/connectors/src/manifest/schema.ts` for the exact Zod contract (tool/role_grant/evals shape) rather than guessing.
- Authored `packages/connectors/manifests/google-calendar.yaml`: `connector_id: google-calendar`, `mcp_server` remote transport with `secret://mcp/google-calendar/url`, `oauth_scopes: [calendar.readonly, calendar.events]`. Tools: `list_events`/`get_event` at `T0_observe` (enabled); `create_event`/`update_event`/`delete_event` at `T3_external` with `enabled: false` (externally-visible — invites/notifies/cancels attendees) per AC1. Single `role_grants` entry (`calendar-assistant`, `max_tier: T0_observe`) matching the enabled-capability ceiling. `evals.suite` points at the real runner convention path `evals/golden/suites/google-calendar` (no Handover-verbatim-text constraint here unlike Gmail, so no discrepancy to carry forward this time).
- Authored `evals/golden/suites/google-calendar/suite.yaml`: 4 tasks, all `T0_observe` (draft/list/read only, matching the manifest's enabled-capability set — no write-capability tasks since mutations are disabled): `list-today`, `list-upcoming-week`, `read-event-details`, `find-free-slot`.
- **AC1 — manifest validates.** Rebuilt `@oikonomos/db`, `@oikonomos/harness-factory`, `@oikonomos/connectors` (`tsc`, all clean). Ran the real CLI: `node packages/connectors/dist/manifest/cli.js --dir packages/connectors/manifests` → `ok: 2 manifest(s)` / `exit=0` (both gmail.yaml and the new google-calendar.yaml). `pnpm --filter @oikonomos/connectors test`: 11 files, 109/113 pass (4 skipped, pre-existing), 0 failures.
- **AC2 — registration run against a local DB, idempotent.** Same throwaway-container pattern as TASK-048: spun up `oikonomos-task049-pg` (`pgvector/pgvector:pg16`, `127.0.0.1:55511`, isolated from the two pre-existing host containers `curry-orders-db-1`/`oikonomos-postgres-local`, never touched). Applied all 5 migrations in `infra/postgres/migrations/*.up.sql` via `docker exec -i ... psql` (no local `psql` client available in this environment, unlike TASK-048's note — used `docker exec` instead, all statements succeeded). Registration script kept entirely outside the repo (`%TEMP%\task049-registration-run\run.mjs`, deleted after; symlinked its `node_modules` to `packages/db/node_modules` for `pg` resolution rather than writing anything inside the repo). One correction versus my first draft of the script: the real table name is `capabilities` (not `connector_capabilities` — I'd misremembered from the dossier prose) and the role-grant join needed explicit `rg.`/`cc.` qualification; fixed both before trusting the output. Also had to pre-seed a `roles` row for `calendar-assistant` (FK `role_grants.role_id -> roles.role_id`) — role provisioning is out of `registerConnector`'s scope by design (confirmed via `packages/db/src/capabilities.ts`), so seeding it directly via `docker exec psql` is the correct proof-of-registration path, not a workaround.
  Ran `registerConnector` twice in sequence; row-snapshot JSON identical both times (`idempotent: true`). Snapshot confirms: `calendar.create_event`/`calendar.delete_event`/`calendar.update_event` land `enabled: false` at `T3_external`; `calendar.list`/`calendar.read` land `enabled: true` at `T0_observe`; `calendar-assistant`'s role grant cross-products onto all 5 capabilities at `max_tier: T0_observe` (TASK-044's flattening design, same as TASK-048 observed for Gmail — the per-tool `enabled: false` is what actually gates tier resolution, not the grant row, per Handover §4.2).
  Cleanup: temp script + `node_modules` symlink deleted, `docker rm -f oikonomos-task049-pg` confirmed removed (`docker ps -a` clean of it), pre-existing containers untouched throughout.
- **AC3 — golden eval suite, ≥90% via the OIK-051 runner.** Ran the real runner CLI (`evals/golden/src/cli.ts`, unmodified, outside my territory) with a fake `queryFn` + manifest-mirror module, both outside the repo (`%TEMP%\task049-golden-run\{manifest,query}.mjs`, deleted after):
  ```
  $ pnpm --filter @oikonomos/evals-golden run run google-calendar <tmp>/manifest.mjs <tmp>/query.mjs
  {"connector_id":"google-calendar","tasks":[
    {"id":"list-today","pass":true,...,"output":"schedule summary prepared"},
    {"id":"list-upcoming-week","pass":true,...,"output":"schedule summary prepared"},
    {"id":"read-event-details","pass":true,...,"output":"event summary prepared"},
    {"id":"find-free-slot","pass":true,...,"output":"schedule summary prepared"}
  ],"pass_rate":1,"min_pass_rate":0.9,"passed":true,"harness_invocations":4}
  exit=0
  ```
  4/4 pass, `pass_rate: 1` ≥ `0.9`, `harness_invocations: 4` (composeHarness seam genuinely exercised once per task — ADR-005 liveness satisfied). This structured report is the AC3 evidence, reproduced here per the TASK-048 precedent (a report file dropped into `evals/golden/suites/google-calendar/` would be parsed by `loadSuite` as suite content and corrupt it). Also ran the package's own fixture suite: `pnpm --filter @oikonomos/evals-golden test` → 1 file, 6/6 pass.
- **AC4 — full checks.** `pnpm -r test` (all workspace projects, full recursive suite per the CLAUDE.md amendment, not just my own package): **exit 0**, every package green (`packages/connectors` 109/113, `evals/golden` 6/6, all others unaffected and passing — full per-package tallies captured in the session log). `pnpm lint`: **exit 0**. `pnpm canaries`: **exit 0**, 11 files / 17 passed.
- Cleanup verified: `git status --short` shows only the two new territory paths (`packages/connectors/manifests/google-calendar.yaml`, `evals/golden/suites/google-calendar/`) — no stray `dist/`, no leftover temp scripts, no lingering docker container (`docker ps -a` clean of `oikonomos-task049-pg`).
- All 4 acceptance criteria satisfied and evidenced above. Handing off `needs_review`.
