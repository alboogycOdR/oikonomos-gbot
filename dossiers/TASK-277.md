# TASK-277 Dossier

## Work Log

- [2026-09-16T21:00:00Z] [CX9] Rework/resume preflight against the shared main checkout passed: `packages/broker/src/capabilityRegistry.ts` (FILE, 173 lines), `packages/broker/src/index.ts` (FILE, 800 lines), and `packages/broker/src/capabilityRegistry.test.ts` (FILE, 198 lines). Implementing corrected ADR-019 Invariant A: unconditional enabled-state construction drift rejection plus a registry-backed L1 declared-disabled deny before allowlist/mount evaluation.
- [2026-09-16T20:02:17Z] [CX9] Preflight completed: `packages/broker/src/capabilityRegistry.ts` is an existing file and `packages/broker/src/*.test.ts` matches the existing protected-package tests. Read ADR-019, CLAUDE.md, current registry and all live construction callers. Blocked before edits: ADR-019 §4 Invariant A requires construction to reject enabled-state drift unconditionally in either direction, while TASK-277's Description/Acceptance_Criteria permits a declared-disabled/persisted-enabled row when a matching audited enable event exists. No audit-read port or event schema exists in the registry/database composition, and the necessary production caller/database changes are outside Owned_Paths. ADR-019 also mandates L1 deny reason `capability.declared_disabled` and disabled built-in declarations, both outside Owned_Paths (`packages/broker/src/index.ts`, `builtinTools.ts`). Supervisor decision required: (1) retain ADR's unconditional rejection and widen territory to the required L1/declaration files, or (2) amend ADR and grant owned paths for a defined durable audit-reader/event contract plus its production wiring.

- [2026-09-16T20:39:58Z] [CX9] Implemented corrected Invariant A. `CapabilityRegistry.build` now unconditionally rejects either enabled-state drift direction with exported `CapabilityEnabledDriftError`; registry ports suppress declared-disabled capabilities without a persistence read and expose the declaration state to L1; `decidePreToolUse` emits the distinct `capability.declared_disabled` deny before manifest/mount checking. Added construction, mount/port, direct granted-role L1, audit-evidence, and liveness coverage. `pnpm --filter @oikonomos/broker test` passed (15 files, 173 tests); package typecheck and build passed. Required `pnpm -r test` was also run: unrelated packages/db integration failures prevented a green recursive run (shared test fixture/parallel-state failures in spend, capabilities, and inbox-triage; no broker failures). No shared-infrastructure repair attempted because it is outside TASK-277 territory.
