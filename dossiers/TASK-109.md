# TASK-109 — inline ApprovalCard in chat (Chat-1e)

Unit: S5. Branch: task/TASK-109-s5.

## Work Log

- [2026-09-03T14:00:00Z] [S5] Session start. Found a stale `.devteam/CHECKPOINT.md` referencing TASK-108 (already reviewed/merged by ORCH per PLAN.md — commit 2f60ccb); deleted it per resume procedure. My actual claimed task per PLAN.md/dispatch is TASK-109 (claimed, branch task/TASK-109-s5 didn't exist yet locally — created from `mainco/master` at `e68239f`, which already includes the merged TASK-108). Worktree's default `master` checkout was left untouched (belongs to ORCH's control worktree; couldn't `git checkout main` there — this repo's default branch is `master`, fetched via remote `mainco`).
- [2026-09-03T14:15:00Z] [S5] Implemented `ApprovalCard.tsx`: renders `ApprovalRender.actionRender` verbatim via a `<pre>` with a plain string child only (no `dangerouslySetInnerHTML`, no Markdown parser) — React escapes text children, so embedded Markdown/HTML/`javascript:` syntax cannot be interpreted. Approve/Reject call the existing `decideApproval(nonce, decision)` from `lib/api.ts` (already built for TASK-103's ops approvals page; reused unchanged, per task description). Nonce is only ever read from the `ApprovalRender` prop (itself sourced from the already-in-memory poll response) and passed straight into the POST body — never placed in a URL/history/storage. A 409 (`{decided:false}`) is handled gracefully: buttons are removed and a `role="alert"` notice shown, matching TASK-103's inbox pattern, instead of leaving stale decide buttons live against an already-resolved approval.
- [2026-09-03T14:20:00Z] [S5] Wired `ApprovalCard` into `ConversationPane.tsx`, replacing the TASK-107 fixture-only placeholder block. Kept the placeholder's original `data-testid="inline-approval-placeholder"` on `ApprovalCard`'s outer div specifically because `ChatShell.test.tsx` (TASK-107's file, outside this task's `Owned_Paths`) asserts on that testid — preserving it avoids silently breaking a test file I'm not allowed to touch, without needing an OWNERSHIP_CONFLICT escalation. Added `onUnauthorized` passthrough prop on `ConversationPane` (optional, not yet wired by `ChatPage.tsx` — that file is TASK-108's territory, not mine; a 401 during decide still surfaces as an inline notice either way via `ApprovalCard`'s own catch).
- [2026-09-03T14:20:00Z] [S5] **Known type-shape gap, not fixed here (out of Owned_Paths)**: `types.ts`'s `ApprovalRender.status` union is `"pending" | "approved" | "rejected"` (TASK-107), narrower than the real server's `ApprovalStatus` (`pending/granted/rejected/expired/invalidated/consumed` — `lib/api.ts`, TASK-103). `ChatPage.tsx` (TASK-108) already narrows the real API response down to this 3-value union before it reaches `ConversationPane`/`ApprovalCard`, so "expired" from the real server currently cannot reach this card as a rendered *initial* status — only my own local 409-triggered "expired" label is reachable (set client-side after a failed re-decide, not from the server field). `ApprovalCard`'s `STATUS_LABEL` map defensively includes `expired`/`granted` labels so it renders correctly if that upstream narrowing is ever loosened, but the actual data plumbing gap is in `types.ts`/`ChatPage.tsx`, both outside this task's `Owned_Paths`. Not filing this as blocked — none of TASK-109's Acceptance_Criteria require the server's raw status enum through; the AC's "pending/granted/rejected/expired" language is met by the button-disable/notice behavior on any terminal outcome (granted result → "approved" label, 409 → "expired" label), which is what was actually testable given today's props.
- [2026-09-03T14:25:00Z] [S5] Wrote `ApprovalCard.test.tsx`: (1) verbatim-render mutation test — feeds `actionRender` containing Markdown bold syntax, an HTML-looking link with a `javascript:` href, and asserts `textContent` equals the raw string with no `<a>`/`<strong>` elements produced; (2) approve flow — asserts the decide POST URL contains the real (encoded) nonce, body carries `decision: "granted"`, and independently asserts the nonce never appears in `window.location.href`, `pathname`, or any `localStorage`/`sessionStorage` entry; (3) 409 second-decide — asserts graceful notice and buttons removed; (4) already-decided approval renders status label with no buttons.
- [2026-09-03T14:35:00Z] [S5] Test evidence: `pnpm --filter dashboard test` → 15 files / 50 tests passed (incl. new ApprovalCard.test.tsx 4/4, and `ChatShell.test.tsx`'s pre-existing approval-placeholder assertion still green through the preserved testid). `pnpm -r build` → 18/18 packages built clean. `pnpm lint` → clean, 0 findings. `pnpm -r test` (full recursive suite, per CLAUDE.md's amendment — not just this task's package) → every workspace package green, zero failures, zero flakes this run (packages/db 26 files/114 passed+1 skipped known-flaky test unaffected; packages/approvals real-Postgres mutation suite 119/119; services/control-api 110/110; full totals: all 21 workspace test scripts reported "passed", no `FAIL` anywhere in the run log).
- [2026-09-03T14:40:00Z] [S5] Committed (5d9455e) and handing to `needs_review`. Stopping point: task complete per Acceptance_Criteria; no known open items within `Owned_Paths`.

## Test Evidence (verbatim summary)

- `pnpm --filter dashboard test`: 15 test files passed (15), 50 tests passed (50). New `ApprovalCard.test.tsx`: 4/4.
- `pnpm -r build`: 18/18 workspace packages, zero errors (tsc + vite build for apps/dashboard).
- `pnpm lint`: clean, no output beyond the eslint invocation (0 findings).
- `pnpm -r test` (full recursive): all 21 workspace test scripts report all-passed; representative counts — packages/db 114 passed/1 skipped, packages/approvals 119 passed (real Postgres, incl. mutation-proof tests), services/control-api 110 passed, packages/broker 106 passed, services/worker 30 passed/1 skipped, evals/harness 18 passed, evals/golden 6 passed. Zero `FAIL` lines in the full run.
