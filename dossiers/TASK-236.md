# TASK-236 — Workspace-1 — dashboard workspace controller: single selection owner, per-thread pending/draft state, message merge, members roster, /workspace/:threadId route

**Unit:** S5 · **Priority:** high · **Depends_On:** —

## Brief
Fix the four confirmed dashboard defects and the page-boundary gap with one controlled state module. Today `ChatPage.tsx:116` and `ChatShell.tsx:54-64` each own `activeBotId`; the parent's `handleSelectBot` (`ChatPage.tsx:177-191`) never calls its own setter, so the SSE effect (`ChatPage.tsx:219-253`) and the routines lookup stay on the first thread while the child renders another. `isBotResponding`/`pendingSinceRef` are one value for all threads (`ChatPage.tsx:119,125,179-181`). `ComposeBox` holds one draft, never remounts on switch, and clears before the void `handleSend` resolves (`ComposeBox.tsx:18-25`, `ChatPage.tsx:297`), so a failed POST loses the text. `ChatPage.tsx:292` passes `members={[]}` and the Members tab is the right panel's default, so it is permanently empty; the sidebar's create-bot dialog works but the thread list is never refreshed after creation.

## Spec pointers
specs/OIKONOMOS_WORKSPACE_WAVE_v1.0.md §2.1–§2.7; specs/OIKONOMOS_WORKSPACE_WAVE_v1.0.md §1

Read `specs/OIKONOMOS_WORKSPACE_WAVE_v1.0.md` §1 first for the product shape and §10 for what must not be claimed. The independent review that produced this wave (verdict, disposition matrix, execution proposal) is at `E:\DELL-PROJECTS\GROKBOT-RESEARCH-DOCS\ORCH_REVIEW\` — read the rows cited in Spec_References; do not treat the advisory documents themselves as spec.

## Intended approach
Fix the four confirmed dashboard defects and the page-boundary gap with one controlled state module. Today `ChatPage.tsx:116` and `ChatShell.tsx:54-64` each own `activeBotId`; the parent's `handleSelectBot` (`ChatPage.tsx:177-191`) never calls its own setter, so the SSE effect (`ChatPage.tsx:219-253`) and the routines lookup stay on the first thread while the child renders another. `isBotResponding`/`pendingSinceRef` are one value for all threads (`ChatPage.tsx:119,125,179-181`). `ComposeBox` holds one draft, never remounts on switch, and clears before the void `handleSend` resolves (`ComposeBox.tsx:18-25`, `ChatPage.tsx:297`), so a failed POST loses the text. `ChatPage.tsx:292` passes `members={[]}` and the Members tab is the right panel's default, so it is permanently empty; the sidebar's create-bot dialog works but the thread list is never refreshed after creation.

  Approach: introduce `apps/dashboard/src/lib/workspaceState.ts` (a reducer/hook keyed by threadId holding `messages`, `pending`, `draft`, `lastSeenMessageId`) owned by `ChatPage`; make `ChatShell`/`ComposeBox` fully controlled (`activeThreadId`, `draft`, `onDraftChange` props; no internal `useState` copies). Merge messages by id and never let a slower history fetch overwrite streamed messages. Add `/workspace/:threadId` in `App.tsx`; `/` redirects to the most recent thread. Fetch the roster (`listRoles()` already returns it — `ChatPage.tsx:153` discards it) and refresh threads after create. Keep tab close, run cancel, routine pause and mark-read as distinct actions (§1). Do NOT touch `lib/api.ts`, `AuthContext.tsx` or `LoginPage.tsx` — those belong to TASK-239/241, which follow this task with the same owner.

## Owned_Paths
apps/dashboard/src/pages/ChatPage.tsx, apps/dashboard/src/pages/ChatPage.test.tsx, apps/dashboard/src/components/chat/**, apps/dashboard/src/lib/realtime.ts, apps/dashboard/src/lib/realtime.test.ts, apps/dashboard/src/lib/workspaceState.ts, apps/dashboard/src/lib/workspaceState.test.ts, apps/dashboard/src/App.tsx, apps/dashboard/src/App.test.tsx

## Work Log

- [2026-09-12T01:30:00Z] [S5] Implementation complete. Files changed (all within Owned_Paths):
  - apps/dashboard/src/lib/workspaceState.ts (NEW) + workspaceState.test.ts (NEW): thread-keyed reducer (messages/draft/pending/pendingSince/lastSeenMessageId), merge-by-id never drops a streamed message on a later history load, pending clears only on a newer bot reply, draft only clears on message-sent (never on a failed send).
  - apps/dashboard/src/components/chat/ComposeBox.tsx + .test.tsx: fully controlled (value/onChange props), no internal useState — the old eager `setValue("")` on submit (which lost text on a failed POST) is gone.
  - apps/dashboard/src/components/chat/ChatShell.tsx + .test.tsx: fully controlled activeBotId (no internal useState — the exact TASK-236 defect), draft/onDraftChange threaded through, onThreadCreated/onUnauthorized threaded to BotSidebar.
  - apps/dashboard/src/components/chat/BotSidebar.tsx: onThreadCreated/onUnauthorized props forwarded to CreateBotDialog/GroupThreadDialog's onCreated/onUnauthorized.
  - apps/dashboard/src/components/chat/CreateBotDialog.tsx, GroupThreadDialog.tsx (+ their tests): dropped `window.location.reload()` — success now only calls `onCreated`, letting ChatPage refresh+navigate without a full reload (AC 2.5).
  - apps/dashboard/src/components/chat/preview/main.tsx: updated for ChatShell's new controlled props (typecheck-only fix, no behavior change).
  - apps/dashboard/src/pages/ChatPage.tsx (rewrite) + ChatPage.test.tsx (rewrite/extended): single owner of activeThreadId derived from `/workspace/:threadId` route (useParams/useNavigate) rather than local state; `/` redirects to most-recently-updated thread or renders existing empty state; unknown/foreign threadId renders a dedicated not-found state without ever calling the messages/stream endpoints for it (never fetches a thread outside the owned `GET /threads` list); workspaceState wired for messages/pending/draft; Members panel computed from `listRoles()` roster resolved against the active thread's roleId (single bot) or memberRoleIds (group); routines fetch unchanged.
  - apps/dashboard/src/App.tsx: added `/workspace/:threadId` route (RequireAuth-wrapped, same as `/`).
  All four originally-cited defects and the page-boundary gap are fixed; §2.1–§2.7 acceptance criteria addressed with dedicated tests (multi-thread A→B→C→A switch, per-thread pending, per-thread draft incl. failed-send-keeps-draft, message merge never dropping a streamed message, Members roster for both single-bot and group threads, redirect/not-found/empty states for the new route).

  Test evidence:
  - `pnpm --filter @oikonomos/dashboard test` (via `pnpm exec vitest run --config ./vitest.config.ts --root .`): 19 files / 107 tests, all green.
  - `pnpm --filter @oikonomos/dashboard typecheck`: clean.
  - `pnpm --filter @oikonomos/dashboard build` (tsc + vite build): clean, `dist/` produced.
  - Full recursive `pnpm -r test` per CLAUDE.md's DEVDEPARTMENT amendment (never scope to own package only): first pass surfaced failures in packages/approvals, audit, connectors, harness-factory, services/worker, services/gateway-telegram, evals/harness — all traced to workspace packages (`@oikonomos/db`, `@oikonomos/shared`, `@oikonomos/approvals`, `@oikonomos/workspace`) never having been built in this fresh worktree (no `dist/`), a pre-existing environment/build-order issue with zero relation to this task's Owned_Paths. After running `pnpm -r --filter "./packages/**" build`, `pnpm --filter @oikonomos/gateway-telegram build`, `pnpm --filter @oikonomos/workspace build`, `pnpm --filter @oikonomos/worker build` (build-only, no source edits — none of these are in TASK-236's Owned_Paths), every one of those suites went green on the next run.
  - **Remaining, NOT fixed (out of Owned_Paths, flagging for ORCH):** `services/worker`'s suite (`src/main.test.ts`, `src/jobs/workerJobQueue.test.ts`, `src/chatRunDriver.ts`'s control-liveness test) still fails intermittently even run standalone — all either "Hook timed out"/"Test timed out" against a real Postgres/pg-boss connection, or one `budget.platform_exceeded`/phase-ordering assertion. Reran `chatRunDriver.test.ts` alone right after a failing full-suite pass and it went 44/44 green, so this reads as live-infra contention (a real dev Postgres + pg-boss shared across this worktree and whatever else is hitting it concurrently — CX9's worktree runs against the same shared dev DB per AGENTS.md/CLAUDE.md's shared-infra note) rather than a code defect, and definitely not something TASK-236's dashboard-only diff could cause (zero files touched outside `apps/dashboard/**`). `services/worker` is not in this task's `Owned_Paths`, so it is not mine to fix — flagging via this note and the devteam-control `progress_note` for ORCH to triage (rerun in isolation / check for a genuine regression) rather than silently absorbing it into this task's scope. apps/dashboard's own 19 files / 107 tests are the authoritative evidence for TASK-236 and are 100% green, reproduced on three separate full runs.

- [2026-09-12T00:00:00Z] [S5] Session start. Read AGENTS.md, briefing, PLAN.md TASK-236 (Status: claimed, Review_Findings: — fresh, no rework). No prior dossier work. Worktree branch task/TASK-236-s5 was missing and had to be created from a stale base (HEAD was behind master by several commits, PLAN.md showed Status: pending here vs claimed on master — territory-firewall correctly blocked the first write until I rebased onto master). Explored full component tree: ChatPage.tsx, ChatShell.tsx, ComposeBox.tsx, BotSidebar.tsx, RightPanel.tsx, CreateBotDialog.tsx, GroupThreadDialog.tsx, types.ts, api.ts (read-only), realtime.ts, App.tsx, AuthContext.tsx/RequireAuth.tsx (read-only), and all associated tests. Confirmed the four defects described still exist as written. Plan: (1) workspaceState.ts reducer+hook, thread-keyed messages/draft/pending, merge-by-id — DONE, needs test file. (2) ComposeBox -> fully controlled (value/onChange props, no internal useState). (3) ChatShell -> fully controlled activeBotId (drop internal useState), thread onCreated/onUnauthorized down to BotSidebar->dialogs. (4) CreateBotDialog/GroupThreadDialog -> drop window.location.reload(), rely solely on onCreated so parent can refresh without a full reload (AC 2.5). (5) ChatPage.tsx -> derive activeThreadId from useParams()/react-router, redirect `/` to most-recent thread or empty state, 404 state for an unowned/unknown threadId, wire workspaceState, compute Members roster from listRoles() + active thread's roleId/memberRoleIds. (6) App.tsx -> add `/workspace/:threadId` route. (7) Rewrite/extend all touched tests, including multi-thread ChatPage.test.tsx fixture per AC 2.7. Created apps/dashboard/src/lib/workspaceState.ts.
