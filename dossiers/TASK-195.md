# TASK-195 — Work Log

- [2026-09-06T13:55:00Z] [CX] Preflight completed before modification: `services/control-api/src/app.ts`, `ports.ts`, `openapi.ts`, and `routines.routes.test.ts` all exist. Read the complete routine API and DB routines layer. `@oikonomos/db` exposes `createRoutine`, `getRoutine`, `listRoutines`, `recordRoutineFire`, and `setRoutinePaused`, but no existing general or skill-specific update accessor. `createDatabaseBackedDeps` can only preserve the required 404-never-403 tenant scope by first fetching with `dbGetRoutine` and then calling a typed DB update helper; control-api is expressly prohibited from embedding SQL. The needed helper and its `@oikonomos/db` barrel export are `packages/db/src/routines.ts` and `packages/db/src/index.ts`, both outside TASK-195 `Owned_Paths`. Stopped without code edits under `OWNERSHIP_CONFLICT`; please widen ownership to both paths (and `packages/db/src/routines.test.ts` if DB-layer integration coverage is expected).

- [2026-09-06T14:35:00Z] [CX] Supervisor widened TASK-195 to the DB routine helper, DB integration test, and barrel as requested. Added typed `updateRoutineSkill`, wired it through the database-backed tenant fetch (cross-tenant and missing both return null before the mutation), and added `PATCH /routines/:id` with required UUID-or-null `skillId` schema, OpenAPI documentation, and route coverage for rebinding, clearing, and 404 handling. The DB integration test creates a real skill before binding it, proving the live FK-compatible persistence path. Verification: `pnpm --filter @oikonomos/db test` — 36 files passed, 179 tests passed, 2 skipped; `pnpm --filter @oikonomos/control-api test -- routines.routes.test.ts` — 4/4 passed; `pnpm --filter @oikonomos/db build` and `pnpm --filter @oikonomos/control-api build` — both exit 0; `pnpm lint` — exit 0. Full control-api test run reached 198 passing tests but has two unrelated pre-existing group-thread integration failures: Postgres reports `too many clients already`, followed by its dependent FreeLLMAPI scenario receiving 400 rather than 201. No TASK-195 files participate in either failure.
