# TASK-280 — `packages/templates` bot template manifest schema, role-to-manifest projection, credential scan, digest

## Work Log

- [2026-09-17T02:55:00Z] [S5] Resumed a dispatcher-claimed session (control.mode=strict). Found a stale `.devteam/CHECKPOINT.md` referencing TASK-278 (a prior task from before ORCH redirected this session onto TASK-280) — deleted it per the resume procedure once confirmed PLAN.md's own record (TASK-280, `claimed`, branch `task/TASK-280-s5`) was current and authoritative. Branch `task/TASK-280-s5` already existed with zero commits (a pure ancestor of `mainco/master`), so treated this as a fresh start, not a resume-in-progress.
- [2026-09-17T02:55:00Z] [S5] Read ADR-018 (bot templates) in full, `specs/OIKONOMOS_TEMPLATES_v1.0.md` §§0–10, and this task's own PLAN.md entry, which explicitly warned its own title/description might have the wrong credential-scan class count ("not the 5 this task's own earlier, incorrect filing said -- verify the real count directly from the ADR"). Verified directly: ADR-018 §2 (Resolution section) and spec §3.2/§10 both say **five** detector classes explicitly, matching what the task title already said. The self-doubt in the filing appears to have been unwarranted (or referred to something already resolved by the time this task was filed) — implemented 5 classes per the ADR/spec text, not blindly per the task's own prose warning, per the briefing's own instruction to trust the source document.
- [2026-09-17T02:55:00Z] [S5] Confirmed via `git log`/direct file reads that `packages/templates` genuinely does not exist yet (TASK-276 built only `bot_templates`/`packages/db/src/templates.ts`, explicitly deferring `role_template_installs` and all domain logic — its own migration file and dossier say so directly).
- [2026-09-17T02:55:00Z] [S5] Real, in-territory workaround found for a genuine gap: ADR-018 §2 / spec §3.2 require credential-scan class 1 to reuse `matchesSecretPattern` from `@oikonomos/audit` "reused, never copied" — but that function is **not** part of `packages/audit`'s public surface (only `redactPayload` is re-exported from `packages/audit/src/index.ts`; confirmed by reading the file and grepping the whole repo for any existing cross-package caller — none exists). `packages/audit` is outside this task's `Owned_Paths`, so adding an export there is not this task's edit. Rather than copy the eight regex families (which the ADR explicitly forbids: "a fourth copy would drift") or block on OWNERSHIP_CONFLICT for a one-line export, used `redactPayload`'s own documented public contract as the reuse path: probing it with a single-field payload (`redactPayload({value: text})`) and checking whether the value changed gives the identical true/false signal `matchesSecretPattern` would, without touching `packages/audit` or duplicating a single regex. Documented this reasoning directly in `credentialPolicy.ts`'s `matchesAuditPatternFamily` doc comment for the reviewer.
- [2026-09-17T02:55:00Z] [S5] Built `packages/templates` (new zero-I/O package, mirrors `packages/policy`'s convention — the only cross-package import besides `@oikonomos/audit`/`@oikonomos/shared` is `@oikonomos/db`'s plain `riskTiers` tuple, reused rather than re-declared, same pattern `packages/connectors/src/manifest/schema.ts` already uses for the same reason):
  - `src/manifest.ts` — strict zod schema exactly per spec §2.1 (identity/skills/routines/integrations/memories/provenance), `template_version` pinned to the literal `1`, routines' `paused` pinned to the literal `true`.
  - `src/projection.ts` — pure `projectRoleToManifest(bundle, exportedAt)`. `RoleBundle`'s input types have no fields for grants, secrets, approvals, attachments, threads, spend, audit, or source-tenant ids (spec §1.3, enforced by type as the spec requires) — `exportedAt` is passed in rather than read from `new Date()` internally so the function stays deterministic. Validates that a routine's referenced skill exists in the same bundle, throwing `UnknownRoutineSkillError` otherwise (spec §2.3). Validates its own output against `templateManifestSchema` before returning.
  - `src/credentialPolicy.ts` — `scanManifestForCredentials`: all five detector classes (audit pattern family via the `redactPayload` bridge above; JWT shape; credential-bearing URL; high-entropy opaque blob with a structural-identifier allowlist; `secret://` refs), applied recursively to every string in the manifest AND to the bounded free-prose surface (`identity.description`/`instructions`, skill `body`/`description`/`when_to_use`, routine `definition`, memory `value`) joined in canonical field order with **no separator** — documented in-code why a printable separator would defeat the exact "split across two adjacent fields" case the ADR names (it would break the very character contiguity the regex/entropy checks require). Returns `{refused, fieldPaths, classes}` shaped to match the `template.export_refused {field_paths, classes}` audit event schema directly.
  - `src/digest.ts` — `templateDigest`: `sha256(canonicalJson(manifest minus provenance))`, reusing `@oikonomos/shared`'s single canonical-JSON implementation (N10) rather than hand-rolling serialization.
  - `src/index.ts` — re-exports.
  - `infra/postgres/migrations/028_role_template_installs.{up,down}.sql` — field-for-field per spec §4, the table TASK-276 explicitly deferred.
  - `src/migration.test.ts` — real Postgres-backed test for the new table (column shape, both FKs — to `roles` and to `bot_templates(template_id, version)` — and the PK uniqueness constraint), plus an opt-in `MIGRATION_ROUND_TRIP=1`-gated down/up reversibility test. Uses its own short-lived `pg.Pool` directly (not `@oikonomos/db`'s `withPool`) because `packages/db` is outside this task's `Owned_Paths` — mirrors `packages/db/src/threads.test.ts`'s own precedent for exactly this situation (a `.test.ts` file constructing its own `Pool` is explicitly exempt from the "accessor modules never construct their own Pool" liveness rule; only real accessor modules are bound by it).
- [2026-09-17T03:05:00Z] [S5] First test run surfaced 4 real bugs in `credentialPolicy.ts`, all from the same root cause: `isJwtShape`/`isCredentialUrl` required the ENTIRE field string to be nothing but the token/URL, so embedding either inside ordinary prose ("token: eyJ...", "See https://...") silently failed to match. Fixed both to extract candidate substrings from within the larger text first, then validate each candidate — matching the real-world shape of a manifest field (prose containing a credential, not a bare credential). Also found and fixed a test-fixture bug of my own: a "non-adjacent split" negative test accidentally produced a false positive because the no-separator join happened to concatenate the end of one field directly into the start of unrelated filler text, forming a coincidental audit-pattern match — fixed by giving the filler text leading/trailing spaces (breaking token contiguity at the boundary, as real prose punctuation would) rather than changing the scan algorithm, since the algorithm's behavior (concatenate-with-no-separator) is what correctly catches the genuine adjacent-split case one test above it.
- [2026-09-17T03:10:00Z] [S5] Test evidence:
  - `pnpm --filter @oikonomos/templates typecheck` / `build`: clean.
  - `pnpm -r typecheck` (all 19 packages/services): clean.
  - `pnpm -r build` (all 19 packages/services): clean.
  - `scripts/test-isolated.ps1 -Init -Filter @oikonomos/templates`: 5 files, 28/29 passed + 1 correctly skipped (the round-trip test, gated off by default).
  - `MIGRATION_ROUND_TRIP=1 scripts/test-isolated.ps1 -Filter @oikonomos/templates`: 5 files, 29/29 passed (round-trip test included: down cleanly drops `role_template_installs`, up recreates it).
  - Full recursive suite (`scripts/test-isolated.ps1`, no filter) run **twice** to separate a real regression from a flake, per this project's own review standard (CLAUDE.md DEVDEPARTMENT amendments: "always run the FULL recursive suite, never only the task's own package"). `packages/templates` green both times. Both runs: only failures are in `services/worker` (`chatRunDriver.test.ts` 7 tests, `workerJobQueue.test.ts` 1 test, `main.test.ts` 1 test on run 1) and, on run 2 only, `services/control-api`'s `chat.routes.test.ts` (1 test) — none reference `templates`, `role_template_installs`, or `bot_templates` anywhere in their failure output, and the specific failing tests differ between the two runs (not the same test failing identically twice), consistent with concurrency-triggered flakiness rather than a real regression. This matches both TASK-276's own dossier finding (a different pre-existing `services/worker/src/main.test.ts` flake under full-suite load) and this session's own `orchestrator_notes` about a real, disclosed incident of `chatRunDriver.test.ts` hitting "worker-lock collisions" under concurrent execution.
- [2026-09-17T03:12:00Z] [S5] Territory note for ORCH: `pnpm install` modified the root `pnpm-lock.yaml` (unavoidable when adding a new workspace package) — the territory pre-commit hook correctly rejected staging it (outside `Owned_Paths`), consistent with prior task commits in this repo's own history that did include lockfile syncs (e.g. TASK-185, TASK-170, TASK-197 commit messages), but the mechanical hook is stricter than that older precedent and this session followed the hook rather than the precedent. Restored `pnpm-lock.yaml` to its committed state before finishing (`git checkout -- pnpm-lock.yaml`) so the worktree is clean; ORCH will need its own `pnpm install` after merge to resync the lockfile for the new `@oikonomos/templates` package.
- Handing off to `needs_review`.

## Status: needs_review

All four Acceptance_Criteria met:
- [x] Manifest schema, projection function, and credential scan all exist as pure, directly-testable functions (no I/O in `manifest.ts`/`projection.ts`/`credentialPolicy.ts`/`digest.ts` themselves; `migration.test.ts` is the one file with real I/O, and it is a test file, not part of the package's exported surface).
- [x] Credential scan catches a real, non-toy example of every one of the five pattern-family classes ADR-018 §2 actually specifies (verified the real count directly from the ADR, not from this task's own earlier, self-doubting prose) — a fragment-assembled negative test per class, plus the adjacent-split and non-adjacent-split cases.
- [x] Digest is stable (same manifest content produces the same digest twice, unaffected by `provenance`) and changes when manifest content changes.
- [x] `role_template_installs` migration exists, reversible (opt-in round-trip test passing), matching ADR-018's own schema for it field-for-field.
- [x] Real Postgres-backed test for the new migration (shape, both FKs, PK uniqueness); pure unit tests for everything else (manifest schema, projection, credential scan, digest all DB-free).

**Artifacts:** `packages/templates/package.json`, `packages/templates/tsconfig.json`, `packages/templates/vitest.config.ts`, `packages/templates/src/manifest.ts`, `packages/templates/src/projection.ts`, `packages/templates/src/credentialPolicy.ts`, `packages/templates/src/digest.ts`, `packages/templates/src/index.ts`, `packages/templates/src/migration.test.ts`, `infra/postgres/migrations/028_role_template_installs.up.sql`, `infra/postgres/migrations/028_role_template_installs.down.sql`.
