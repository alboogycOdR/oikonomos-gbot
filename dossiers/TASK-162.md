# TASK-162 Dossier

## Work Log

- [2026-09-06T15:18:00Z] [CX] Preflight: `services/control-api/src/chat.routes.test.ts` (1561 lines) and `packages/db/src/runs.test.ts` (463 lines) exist. PATCH-instructions, approval-resume, and `runs.test.ts -t listOpenRuns` pass independently; `runs.test.ts` full passes 13/13.
- [2026-09-06T15:18:00Z] [CX] Root-cause evidence: `chat.routes.test.ts` fails in isolation with `sorry, too many clients already`; the group fixture alone reproduces it twice. Its normal `GET /threads` path returns that error, proving this is not an assertion/order defect. Immediately after failure, a read-only pg query reported 6 `pg_stat_activity` connections with Postgres `max_connections=100`, pointing to the shared pooler/client layer. Test-only one-client-pool and lifecycle synchronization attempts did not resolve it; they remain uncommitted.
- [2026-09-25T15:52:00Z] [CX9] Resumed on ORCH-repaired `task/TASK-162-cx9` (base `ef8fb69d`). Preflight: `services/control-api/src/chat.routes.test.ts` (1913 lines), `packages/db/src/runs.test.ts` (726 lines), `packages/db/src/database.ts` (297 lines), `packages/db/src/database.test.ts` (257 lines), and `services/control-api/test/edit.route.integration.test.ts` (438 lines) all exist. Starting the required current-master isolated re-baseline before changing code.
- [2026-09-25T15:55:00Z] [CX9] Re-baseline evidence: `scripts/test-isolated.ps1 -Init` recreated and seeded `oikonomos_test`. Three foreground `-Filter @oikonomos/control-api` runs completed with exit 0, including `src/chat.routes.test.ts` (47 tests) and `test/edit.route.integration.test.ts` (14 tests); three foreground `-Filter @oikonomos/db` runs each reported `Test Files 46 passed (46)` and `Tests 301 passed | 2 skipped (303)`, including `src/runs.test.ts` (19 tests) and `src/database.test.ts` (10 tests). The first full foreground recursive isolated run completed cleanly. A read-only query using the harness-derived `oikonomos_test` URL reached `172.18.0.2:5432`, reporting `max_connections=300`; sampled activity was 26 unnamed idle, 5 unnamed/background, 5 idle worker-run-execution, and 4 unnamed active connections. This confirms the old max=100 failure was a different Postgres target, not the current isolated harness server.
- [2026-09-25T16:02:00Z] [CX9] Completed two further foreground `scripts/test-isolated.ps1` full recursive runs (three total); no test failures or connection-exhaustion symptom recurred. No source/test change is warranted: current master’s shared-pool, isolation, environment-scrub work plus the harness’s correctly targeted 300-connection local server resolves the historical reproduction. Stopping point: evidence-only handoff ready for review.
