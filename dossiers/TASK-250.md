# TASK-250 — ADR-014 dynamic secret vault has no production composition site — wire createSecretVault/resolveSecretValue into control-api fulfilment and the connector minters, or record why not

**Unit:** TBD · **Priority:** medium · **Depends_On:** TASK-242

## Brief
Found by TASK-240's config check on 2026-09-12: `OIK_SECRET_VAULT_KEY` is unset in every scope and nothing breaks, because a grep for createSecretVault/resolveSecretValue under services/ returns zero non-test hits. TASK-192 built the vault (`packages/db/src/secretVault.ts`, migration 019) and TASK-187 built the fulfilment route, but the path that decrypts a fulfilled value for a connector minter (ADR-014 §1: the only legitimate callers are host-side connector session minters and the future OIK-045a injector) has no production caller. Same shape as TASK-224's skills finding: a real subsystem with zero production call sites. Investigate first: does `POST /secret-requests/:id/fulfil` write a `secret_values` row in production or only flip `secret_requests.status`? Does any minter resolve a vault-minted `secret://<ref>`? Then wire the missing half with a control-liveness test keyed on the `secret_vault.read` audit event, or record in ADR-014 why it stays unwired. control-api index.ts/ports.ts are owned by TASK-238/242 first; sequence after.

## Spec pointers
docs/decisions/ADR-014-dynamic-secret-vault.md §1, §4, §5; specs/OIKONOMOS_GROKBOT_PARITY_DISPOSITION_v1.0.md §3 G-05; TASK-184/187/192 Progress_Notes

## Owned_Paths
— (investigation-first; likely services/control-api/src/index.ts and ports.ts for composition and packages/connectors/src/mcp/*.ts for the minter-side resolver call — confirm before widening)

## Work Log

- [2026-09-14T19:10:11Z] [CX9] Investigation complete; no implementation was made because TASK-250 has no concrete Owned_Paths. Preflight output (verbatim): `[preflight] TASK-250 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `[preflight] 1 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `  GLOB   — (investigation-first; likely services/control-api/src/index.ts and ports.ts for composition and packages/connectors/src/mcp/*.ts for the minter-side resolver call — confirm before widening)  -> matches nothing yet (new territory)`; `[preflight] Paste this output into your first Progress_Note as the c8b9872 filesystem check.`
- [2026-09-14T19:10:11Z] [CX9] Production-caller finding (grep: `rg -n -S "createSecretVault|resolveSecretValue|fulfillSecretRequest|registerSecretRequestRoutes|create.*SessionMinter|audit.*secret_vault|SECRET_VAULT" services packages --glob '!**/*.test.ts'`): `packages/db/src/secretVault.ts` exports the factory, store/resolve methods, and `secret_vault.write`/`.read` constants, but there are zero non-test service callers. `services/control-api/src/index.ts:start()` calls `buildApp` without `secretRequests`; `services/control-api/src/app.ts:2103-2105` and `2129-2131` consequently return 501. `SecretRequestsPort` is only an injected interface (`app.ts:183-194`); its own comment (`app.ts:129-146`) explicitly defers its real lifecycle wiring. `packages/db/src/secretRequests.ts:88-99` only changes `secret_requests.status`, `secret_ref`, and `fulfilled_at`; it cannot write `secret_values` because it accepts only an opaque ref.
- [2026-09-14T19:10:11Z] [CX9] Connector finding: `services/worker/src/chatRunDriver.ts:211-247` composes Gmail, Calendar, and Drive minters, but each receives `resolveGmailMcpUrl` or `envSecretResolver`; `packages/connectors/src/mcp/envSecretResolver.ts` resolves solely `secret://` -> `OIK_SECRET_*`. No minter accepts the vault's role-and-tenant-bound resolver, so none can consume a fulfilled dynamic ref. `rg -n -S "insertAuditEvent|SECRET_VAULT_(READ|WRITE)_EVENT|secret_vault\\." packages/db/src services packages --glob '!**/*.test.ts'` found the event constants only at their declaration/export sites, not emitted anywhere. `scripts/check-config.mjs:34-37` keeps `OIK_SECRET_VAULT_KEY` optional explicitly because production composition is absent.
- [2026-09-14T19:10:11Z] [CX9] Blocked pending an explicit Owned_Paths widening. Minimum confirmed territory: `services/control-api/src/index.ts`, `services/control-api/src/ports.ts`, their focused tests, `services/worker/src/chatRunDriver.ts` plus its focused test, and `scripts/check-config.mjs` plus test coverage. The eventual implementation may need a dedicated host-side dynamic-ref resolver adapter in an owned package and a liveness test that asserts both `secret_vault.write` and `secret_vault.read`; that path must be named before editing. `packages/db/src/secretVault.ts` currently lacks audit emission hooks, so achieving ADR-014 §5 audit events also requires either widening that file (and test) or an approved equivalent composition-level audit design. No code tests were run: this is investigation-only and every possible implementation path is outside current territory.
- [2026-09-14T19:30:56Z] [CX9] Rework/resume assessment after the supervisor widened TASK-250: the owned control-api/worker/db paths are sufficient to make `POST /secret-requests/:id/fulfil` store a vault row and emit `secret_vault.write`, but they cannot meet the remaining required consumption path without further scope or an ADR note. Live evidence: `packages/connectors/manifests/{gmail,google-calendar,google-drive}.yaml` each contain only fixed operator refs; `services/worker/src/chatRunDriver.ts:211-247` creates shared, role-agnostic minter pools; and all three `packages/connectors/src/mcp/*SessionMinter.ts` implementations resolve only their fixed manifest/OAuth refs. The dynamic `secret://<uuid>` returned by fulfilment is never available to any minter, and the shared pool cannot safely cache a resolver bound to one role/run. The stated alternative in TASK-250 AC2 is a dated ADR-014 explanation, but `docs/decisions/ADR-014-dynamic-secret-vault.md` is outside `Owned_Paths` and protected from builder edits. No production code was changed or tests run: proceeding would either silently fail the minter-read AC or violate the territory firewall. Required supervisor decision: either widen to the connector-minter/resolver composition plus tests and specify the dynamic-ref transport, or have ORCH add the ADR-014 dated deferral note and revise the acceptance bar.
