# TASK-131 Dossier

## Work Log

- [2026-09-04T13:39:13Z] [CX9] Fresh-task preflight completed: `[preflight] TASK-131 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `GLOB packages/broker/src/** -> 20 file(s)`; `FILE services/worker/src/chatRunDriver.ts -> exists, 277 line(s), 13919 bytes`; `FILE services/worker/src/chatRunDriver.test.ts -> exists, 498 line(s), 23738 bytes`. Read ADR-012, broker declarations, existing chat-run MCP mounting, and the real `services/workspace/src/mailbox.ts` implementation. Blocked before code changes: `services/worker/package.json` does not declare `@oikonomos/workspace`; the package is not linked in root or worker `node_modules`. A production MCP wrapper must import the specified real `sendToRole` function, which requires adding that workspace dependency (and resulting lockfile update). Neither `services/worker/package.json` nor `pnpm-lock.yaml` is in TASK-131 Owned_Paths, so changing either would violate the ownership firewall. Next: supervisor must either extend TASK-131 Owned_Paths to include those two dependency files or add the worker-to-workspace dependency in a separate integration task, then re-dispatch TASK-131.
- [2026-09-04T16:18:00Z] [CX9] Resumed after TASK-130 merged. Synced `task/TASK-131-cx9` with integration master; current preflight confirms expanded territory: `packages/broker/src/**` (20 files), `services/worker/src/chatRunDriver.ts` (277 lines), `services/worker/src/chatRunDriver.test.ts` (498 lines), `services/worker/package.json` (41 lines), and `pnpm-lock.yaml` (5,093 lines). Read ADR-012 §2 items 4/6, mailbox implementation, broker declaration/registry, and TASK-128's real-run MCP fixture. Implementing an internal stdio MCP wrapper around the existing `sendToRole` function, with its broker declaration and grant-derived per-run mount.
- [2026-09-04T16:24:00Z] [CX9] Blocked before code changes by a newly verified ownership dependency: a local MCP declaration must use `adapter: mcp:workspace` and `mcpServerName: workspace` to satisfy `CapabilityRegistry`. The production registration step at `services/worker/src/registerCapabilities.ts` currently treats every `BUILTIN_TOOLS` entry as `adapter: sdk:builtin`; it must be extended to register the internal MCP declaration with its declared adapter. Otherwise `CapabilityRegistry.build` fails closed with `CapabilityOwnershipError` on every real chat run, so a partial change to the owned files would not meet AC1. `services/worker/src/registerCapabilities.ts` is not in TASK-131 Owned_Paths. Needed resolution: expand TASK-131 territory to include that file (and re-dispatch), or land an integration task that makes built-in registration honor each declared tool's adapter. No TASK-131 code was written.
- [2026-09-04T16:25:00Z] [CX9] Implemented `mcp__workspace__send_to_role` as a T1 `mcp:workspace` declaration and a worker-local stdio JSON-RPC MCP server. The driver mounts it only for a persisted `workspace.send_to_role` grant, passes role/tenant identity as process arguments (never tool input), and routes `toRoleId` through normal broker destination extraction. Capability registration now persists SDK and internal-MCP declarations with their distinct adapters. Focused real-Postgres TASK-131 chat-run tests pass: a granted typed handoff persists `role_messages`; no grant produces `policy.decision` deny and no row. `pnpm --filter @oikonomos/worker typecheck` passes. The pre-existing broader chat-driver test has shared-DB interference (its old TASK-116 role was deleted mid-run); TASK-131's filtered tests are clean.
- [2026-09-04T16:27:00Z] [CX9] Stopped and reverted the implementation before commit: the territory hook confirmed `services/worker/src/workspaceMcpServer.ts` is outside TASK-131 Owned_Paths. That file is required for the stdio MCP wrapper; without it, the remaining owned-file changes would be invalid. No TASK-131 code remains staged or committed. Required scope expansion: `services/worker/src/workspaceMcpServer.ts` (and optionally its dedicated test path, or authorize coverage in the already-owned chatRunDriver test only).
