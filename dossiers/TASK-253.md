# TASK-253 — Connector minters and dynamically-vaulted secrets

## Work Log

- [2026-09-14T21:00:00Z] [CX9] Resumed task review (no prior TASK-253 dossier; PLAN Review_Findings is empty). Preflight output (verbatim): `[preflight] TASK-253 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `[preflight] 1 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `  NEW    — design-first: this changes chatRunDriver.ts's minter composition from shared-pool to scoped-per-request; needs a design decision before any file is touched; see Description  -> does not exist; parent ./ exists`; `[preflight] Paste this output into your first Progress_Note as the c8b9872 filesystem check.`
- [2026-09-14T21:00:00Z] [CX9] Design decision: do not add a dynamic-secret minter path yet. The live worker constructs and retains one connector pool per Gmail, Calendar, and Drive in `services/worker/src/chatRunDriver.ts:211-247`; that pool is independent of tenant, role, and run. Each shipped connector manifest uses only its fixed `secret://mcp/<connector>/url` ref (`packages/connectors/manifests/gmail.yaml:3`, `google-calendar.yaml:3`, `google-drive.yaml:3`; Steel similarly uses its fixed local endpoint ref). The Google minters also resolve fixed OAuth operator refs. No manifest declares a capability, URL, or OAuth field backed by the UUID-shaped vault ref written by fulfilment, so there is no named real consumer.
- [2026-09-14T21:00:00Z] [CX9] Caching/revocation conclusion: a pool keyed only by connector is unsafe because a resolver that can decrypt a `secret://<uuid>` is bound by the vault to one tenant and role. Per-(tenant, role) caching is also rejected for a future consumer unless the cache is explicitly invalidated on revocation/rotation and never retains a decrypted secret or session credential past its issuance/expiry; the current `secret_values` model has no revocation/version notification path to prove either condition. The safe starting design when a real manifest consumer exists is a fresh resolver/minter/session scope per run (tenant, role, run), resolving the ref at mint time and retaining only a non-secret opaque ref outside the mint operation. That implementation must prove `secret_vault.read` is audited and that plaintext reaches only the connector session boundary, never a model prompt, tool result, log, or audit payload.
- [2026-09-14T21:00:00Z] [CX9] Required trigger for follow-up implementation: an approved connector manifest must explicitly declare which role-bound dynamic secret it consumes and whether it supplies a URL, OAuth credential, or connector-specific header/body injection. Then widen a successor task to the worker composition, relevant connector minter/resolver, manifest/schema, DB vault read/audit surface, and liveness tests. Until then, changing `chatRunDriver.ts` would be speculative infrastructure and would violate this task's design-first Owned_Paths boundary. Full isolated recursive test run is next.
- [2026-09-14T21:24:28Z] [CX9] Test checkpoint: invoked the required `powershell -ExecutionPolicy Bypass -File scripts/test-isolated.ps1` three times while the command runner detached their child `pnpm` processes after its 30-second output window. They are still active against the shared `oikonomos_test` database (PIDs 11428, 2304, 17980, 10736, and their workers), so their results are invalid as concurrent isolated-database evidence. A separately invoked `pnpm --filter @oikonomos/worker test` was likewise contaminated and timed out only at `src/registerCapabilities.test.ts > PostgreSQL idempotency` (5s); this is not attributable to this design-only task. No processes were terminated because they may include other builders' suite runs. Wait for all shared-database test processes to finish, then run exactly one serial `scripts/test-isolated.ps1` invocation and record its final exit/result before handoff.
