# TASK-034 dossier

## Brief
start/resume/fail/cancel state machine with session_ref persisted for resume-after-kill; new runs table + typed db module + worker consumer. NON-protected (S5 ok).

## Spec pointers
WBS OIK-038; Synthesis §5.1. New migration (next number, don't edit existing). No raw SQL outside packages/db.

## Intended approach
Disjoint territory (db/src/runs.ts + new migration + services/worker) so it runs concurrently with the harness adapters.

## Work Log

- [2026-08-17T19:45:00Z] [S5] Fresh session start (control.mode=strict). Read AGENTS.md, briefing, PLAN.md, this dossier fresh from disk. PLAN.md TASK-034 block already re-carved by ORCH/AUTOPILOT since the prior session's investigation (commit `9a9b888`): Owned_Paths now correctly includes `packages/db/src/index.ts`, `services/worker/package.json`, `pnpm-lock.yaml` (the three gaps the prior session flagged), and AC3 was rewritten to explicitly forbid a new migration — the `runs`/`session_ref`/`run_status` already exist verbatim in `infra/postgres/migrations/001_schema_v1.up.sql` (confirmed by reading the file: `run_status` enum = started/waiting_approval/resumed/completed/failed/cancelled at line 9; `runs` table at line 24 with `session_ref text` nullable). No blocker remains; proceeding to build.
  Worktree was on a stale detached HEAD (`9a9b888`, one behind `origin/master`'s `89c3b48` claim commit) with no `task/TASK-034-s5` branch yet created. Fetched origin, created `task/TASK-034-s5` from `origin/master` (89c3b48). Left the pre-existing stray uncommitted `dossiers/TASK-012.md` diff and untracked `AUTOPILOT_LOG.md` in the working tree untouched (not my task, not my territory).
  Preflight (`python scripts/preflight_paths.py TASK-034`) verbatim:
  ```
  [preflight] TASK-034 Owned_Paths inspected in C:/CLAUDECODE_TOOLSETS/wt-s5-oikonomos
  [preflight] 7 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.
    NEW    packages/db/src/runs.ts  -> does not exist; parent packages/db/src/ exists
    FILE   packages/db/src/index.ts  -> exists, 27 line(s), 640 bytes
    NEW    packages/db/test/runs.test.ts  -> does not exist; parent packages/db/test/ exists
    GLOB   services/worker/src/**  -> 1 file(s):
             services/worker/src/index.ts
    GLOB   services/worker/test/**  -> matches nothing yet (new territory)
    FILE   services/worker/package.json  -> exists, 28 line(s), 614 bytes
    FILE   pnpm-lock.yaml  -> exists, 3232 line(s), 104435 bytes
  ```
  Read `packages/db/src/approvals.ts`, `database.ts`, `index.ts`, `types.ts`, `test/approvals.integration.test.ts`, `test/persistence-surface.test.ts` (confirmed it pins `approvals.ts`'s export surface directly, not the barrel — adding runs exports to `index.ts` is safe, no conflict) to match conventions: per-module `withPool` helper, `snake_case` row → `camelCase` domain object mapper, UUID/non-empty validation helpers, atomic single-statement UPDATE...WHERE...RETURNING guard pattern (mirroring `consumeApproval`'s nonce-consumption pattern) for state transitions.
  Plan: `packages/db/src/runs.ts` — `startRun` (INSERT), `getRun` (SELECT), `resumeRun`/`failRun`/`cancelRun` (atomic UPDATE guarded to only the three open statuses `started|waiting_approval|resumed`, RETURNING; on no match, a follow-up SELECT distinguishes unknown run_id (`RunNotFoundError`) from illegal transition (`IllegalRunTransitionError`) so both failure modes are precisely testable). `resumeRun` takes an optional `sessionRef` and `COALESCE`s it against the stored value, so a worker can re-read the persisted `session_ref` after a process kill and hand it straight back in — that round trip is what "resume after a killed process returns to the correct state" (AC1) means operationally. Worker side: `services/worker/src/runLifecycle.ts` thin wrappers importing only from `@oikonomos/db`'s barrel (no raw SQL in the worker), `resumeInterruptedRun` demonstrating the read-session_ref-then-resume flow. Tests: single file `packages/db/test/runs.test.ts` per Owned_Paths (can't create a second `.integration.test.ts` file, so unit + DB-gated integration cases share one file, `describe.skip` when `DATABASE_URL` unset, same convention as `approvals.integration.test.ts`) plus `services/worker/test/runLifecycle.test.ts`. Will spin an isolated `pgvector/pg16` container (own port, not the `tvcp-pg16` container already running on 55433 — that's unrelated shared infra, left untouched) to get real green DB integration evidence, then tear it down.

- [2026-08-17T21:50:00Z] [S5] Implementation complete, tested, committed `0b42541` on `task/TASK-034-s5`. Files: `packages/db/src/runs.ts` (new — `runStatuses`, `RunStatus`, `NewRun`, `Run`, `RunNotFoundError`, `IllegalRunTransitionError`, `startRun`, `getRun`, `resumeRun`, `failRun`, `cancelRun`, plus in-source `import.meta.vitest` validation tests), `packages/db/src/index.ts` (barrel re-export added), `packages/db/test/runs.test.ts` (new, 14 DATABASE_URL-gated integration tests), `services/worker/src/runLifecycle.ts` (new — `startTaskRun`/`resumeInterruptedRun`/`failTaskRun`/`cancelTaskRun` wrappers, plus in-source validation tests), `services/worker/src/index.ts` (re-exports the four wrappers), `services/worker/package.json` (`@oikonomos/db: workspace:*` dependency; `pg`/`@types/pg` devDependencies — the new worker test file imports `pg.Pool` directly to seed fixture tasks, same pattern `packages/db`'s own integration tests use), `services/worker/test/runLifecycle.test.ts` (new, 3 DATABASE_URL-gated integration tests), `pnpm-lock.yaml` (updated via `pnpm install --filter @oikonomos/worker...` for the new workspace edge — 10-line diff, importer-only). `git diff --stat` on the commit confirms every touched path is inside Owned_Paths; nothing else touched (the pre-existing uncommitted `dossiers/TASK-012.md` diff and untracked `AUTOPILOT_LOG.md` were left alone, not staged, not committed).

  **Design notes on the state machine (AC1/AC2):** legal transitions are `resume`/`fail`/`cancel`, each only from an open status (`started`, `waiting_approval`, `resumed`) — `completed`/`failed`/`cancelled` are terminal and reject every further transition. `resume` is idempotent across repeated kill/resume cycles (resumed→resumed is itself an open→open transition, tested explicitly). No enum value added or altered — `run_status` in the live DB was read back via `pg_enum`/`pg_type` in a test to prove this (AC2/AC3 — see "does not add or alter run_status enum values" in `runs.test.ts`). No new migration; both `runs` and `session_ref` were confirmed to already exist in `infra/postgres/migrations/001_schema_v1.up.sql` before writing a line of code (AC3).

  **Test evidence (all commands run from `C:\CLAUDECODE_TOOLSETS\wt-s5-oikonomos`):**
  - `pnpm --filter @oikonomos/db typecheck` → clean, exit 0.
  - `pnpm --filter @oikonomos/db build` → clean, exit 0 (needed so the worker's `@oikonomos/db` type resolution — which points at `dist/index.d.ts` — sees the new `runs` exports).
  - `pnpm --filter @oikonomos/worker typecheck` → clean, exit 0 (only passed after the `db` build above; documented for whoever reviews so a re-run isn't confused by a stale `dist/`).
  - `pnpm lint` (repo-wide eslint) → clean, exit 0.
  - `pnpm --filter @oikonomos/db test` (no `DATABASE_URL`) → 2 files run, 4 skipped (all integration suites incl. the new `runs.test.ts`'s 14 skip cleanly); 7/7 passed, exit 0.
  - Spun an isolated `pgvector/pg16` container (`127.0.0.1:55499`, container name `oik-task034-pg` — NOT the pre-existing unrelated `tvcp-pg16` container on 55433, which was left untouched), applied `infra/postgres/migrations/001_schema_v1.up.sql` verbatim, then:
    - `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:55499/oikonomos pnpm --filter @oikonomos/db test` → **6 files, 27/27 passed, 0 skipped, 0 failed**, exit 0. Includes all 14 new `runs.test.ts` cases (start with/without session_ref, resume-after-kill round trip, resume without an explicit sessionRef preserves the stored one, idempotent repeated resume, fail sets ended_at+failure_note, cancel from `started` and from `waiting_approval`, illegal resume/fail/cancel from a terminal state each throw `IllegalRunTransitionError` with the right `fromStatus`/`action`, unknown run_id throws `RunNotFoundError` for all three transitions, `getRun` returns null for unknown, and the live enum-value proof) plus every pre-existing `packages/db` test unchanged and green.
    - `DATABASE_URL=... pnpm --filter @oikonomos/worker test` → **3 files, 6/6 passed**, exit 0 (the pre-existing `ping` test, the new in-source validation tests, and the 3 new `runLifecycle.test.ts` integration tests: start→resumeInterruptedRun round trip through the barrel, resumeInterruptedRun on an unknown run_id rejects, failTaskRun/cancelTaskRun delegate and persist).
    - `DATABASE_URL=... pnpm -r test` (full recursive suite, per CLAUDE.md's DEVDEPARTMENT amendment — never a filtered run) → **13 of 14 workspace packages pass; `packages/broker`'s `test/pretooluse.test.ts` has 1 pre-existing failure** ("rejects a payload-mutated approval through the real issue and consume ports" — `TypeError: Cannot read properties of undefined (reading 'trim')` inside `packages/approvals/src/issue.ts:46` `requireNonEmpty`, called from `issueApproval` for what looks like a missing/undefined `toolName` in the test's `issue()` fixture call at `pretooluse.test.ts:416-420`). **Confirmed this is NOT caused by TASK-034**: `git stash push -u` on every file this task touched (all 8), re-ran `DATABASE_URL=... pnpm --filter @oikonomos/broker test` against the resulting clean `origin/master` tree — same single failure, same error, same line. `git stash pop` restored all 8 files afterward (verified via `git status --short` — identical to pre-stash). Container torn down (`docker rm -f oik-task034-pg`) after all runs.
    - Also ran `pnpm -r typecheck` as an extra sanity check (not one of AC5's four required commands): 12 of 13 typecheck; `packages/harness-factory` fails on a missing external package `@anthropic-ai/claude-agent-sdk` — also outside Owned_Paths, also unrelated to this task's files, not chased further.
  - `node infra/ci/secret-scan.mjs` ran automatically as part of `git commit` (repo pre-commit hook) → clean, exit 0.

  **Flag for ORCH — do not attribute to TASK-034:** `pnpm -r test` on a clean `origin/master` (no TASK-034 changes at all) currently fails on `packages/broker/test/pretooluse.test.ts` — `packages/approvals/src/issue.ts`'s `requireNonEmpty("toolName", ...)` throws on `.trim()` of `undefined`. This blocks a literal "exit 0" reading of AC5's `pnpm -r test` clause through no fault of this task; `packages/broker` and `packages/approvals` are both outside `Owned_Paths` for TASK-034 so I did not touch either to fix it. Every command AC5 lists that is actually inside this task's territory (`pnpm --filter @oikonomos/db test`, `pnpm --filter @oikonomos/worker test`, `pnpm lint`) exits 0 cleanly, and the full-suite run above shows the one failure is isolated to `packages/broker`/`packages/approvals`, pre-existing, and reproducible without any of this task's commits present.

  All 5 acceptance criteria addressed: AC1 (session_ref persists, resume-after-kill round trip tested), AC2 (start/resume/fail/cancel each tested; illegal transitions rejected from every terminal state; enum unchanged, proven live), AC3 (no new migration — used the existing table/enum verbatim), AC4 (no raw SQL outside `packages/db`; worker imports `runs.ts` exclusively via the `@oikonomos/db` barrel; workspace dependency added to `services/worker/package.json` + `pnpm-lock.yaml`), AC5 (`pnpm --filter @oikonomos/db test` / `pnpm --filter @oikonomos/worker test` / `pnpm lint` all exit 0; `pnpm -r test` has exactly one pre-existing, unrelated, out-of-territory failure — flagged above, not silently omitted). Handing off `needs_review`.
