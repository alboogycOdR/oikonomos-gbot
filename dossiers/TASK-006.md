# TASK-006 — OIK-014/016 packages/db typed layer + seed (BACKLOG, TBD)

**Brief:** Typed query layer over the TASK-002 schema with pooling limits, plus the idempotent capabilities/role_grants seed for inbox-triage. Assign at next dispatch once TASK-001 and TASK-002 are merged.

**Spec pointers:** WBS OIK-014/016. Synthesis §5.1 (schema). Handover §4.4 (Gmail manifest shape — seed tiers must match: email.list T0_observe, email.create_draft T1_draft, email.send T3_external disabled; inbox-triage max_tier T1_draft, rate 40/h).

**Intended approach:** pg (node-postgres) with a typed function per access path, pool limits in config; no raw SQL escapes the package. Seed as an idempotent upsert script. Integration tests against the compose database.

## Work Log

- [2026-08-14T21:18:41Z] [CX] Preflight completed before code changes: `packages/db/src/**` contains `packages/db/src/index.ts`; `packages/db/test/**` and `packages/db/seeds/**` are new territory. Blocked before implementation: this worktree is detached at `36064ba` and `refs/heads/task/TASK-006-cx` does not exist, despite dispatch identifying it as already claimed/resumed. Additionally, the specified node-postgres implementation is unavailable: neither `pg` nor `@types/pg` is installed or declared in any workspace manifest/lockfile, while `packages/db/package.json` is outside the task's Owned_Paths. Supervisor must create/attach `task/TASK-006-cx` and grant the narrowly scoped `packages/db/package.json` + `pnpm-lock.yaml` changes needed to add `pg` and `@types/pg`, or provide an approved dependency alternative.
- [2026-08-14T22:05:00Z] [CX] Resumed after the supervisor's branch/dependency unblock: created `task/TASK-006-cx` from the dispatched detached HEAD, added only `pg` and `@types/pg`, and implemented the typed capability/role-grant repository, bounded pool configuration, and executable inbox-triage seed. The seed applies Gmail tiers (`email.list` T0, `email.create_draft` T1, `email.send` T3 disabled) and `inbox-triage` grants (T1, 40/h, `domains: ["*"]`) through idempotent upserts.
- [2026-08-14T22:05:00Z] [CX] Test evidence: started local compose Postgres with local-only credentials; applied `001_schema_v1.up.sql`; `DATABASE_URL=… pnpm --filter @oikonomos/db test` passed 2/2 integration tests (pool posture and two consecutive seed runs with identical typed reads). `pnpm --filter @oikonomos/db typecheck`, `build`, and compiled seed CLI double-run passed. Workspace `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm test` (DB 2/2 integration), `pnpm install --frozen-lockfile`, and `git diff --check` all passed.
