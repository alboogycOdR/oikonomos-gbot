# TASK-121 — Group thread control-api endpoints

## Work Log

- [2026-09-04T05:47:41Z] [CX] Preflight completed: `services/control-api/src/app.ts` and `services/control-api/src/ports.ts` exist; the owned test glob resolves to `auth.test.ts` and `chat.routes.test.ts`. Blocked before implementation: `@oikonomos/db` exports only 1:1 `createThread`/`getOrCreateThreadForRole` APIs requiring a non-empty `roleId`; it provides `addThreadMember`/`listThreadMembers` but no public accessor to create a `threads` row with `role_id = NULL` or list nullable/group thread rows. TASK-121 may only change control-api files, while the OIK-084 port boundary forbids route/port SQL. A DB accessor expansion is required before `POST /threads/group` can create the real group thread required by the acceptance criteria.
- [2026-09-04T06:10:00Z] [CX] Resumed after TASK-125 merged its public group-thread accessors. Preflight output: `[preflight] TASK-121 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex-GROKBOT-CLONE`; `FILE services/control-api/src/app.ts -> exists, 784 line(s), 29760 bytes`; `FILE services/control-api/src/ports.ts -> exists, 137 line(s), 6040 bytes`; `GLOB services/control-api/src/**/*.test.ts -> 2 file(s): services/control-api/src/auth.test.ts, services/control-api/src/chat.routes.test.ts`. Implemented the group-create route, the discriminated thread summaries, and per-message sender attribution through the control-api port. Focused route test passed 22/22, including real Postgres proof of persisted memberships, mixed 1:1/group summaries, and a sender-attributed group message. `pnpm lint`, `pnpm -r build`, and control-api typecheck/build passed. `pnpm -r test` is currently blocked by a pre-existing/shared-DB isolation conflict: `packages/db/src/threads.test.ts` calls legacy `listThreads()`, which intentionally rejects every persisted group thread; its failure was `Thread d0b15654-7acf-40f3-8691-878ce9619d7e has no role_id`. The control-api package cannot import `pg` or perform raw cleanup, and its Owned_Paths exclude the DB test/accessor territory. A supervisor-owned DB reset or an isolation/cleanup change in packages/db is required before the global suite can be green.
- [2026-09-04T06:30:40Z] [CX] Resumed for the requested integration-test cleanup rework. The required FK-ordered cleanup needs a direct PostgreSQL client (messages, thread_members, threads, role_grants, roles). `services/control-api/package.json` has no `pg` dependency; adding `import { Pool } from "pg"` to the owned test produces `Cannot find package 'pg' imported from .../chat.routes.test.ts` before tests collect. `Database` deliberately keeps its pool private and its public API has no delete accessors, while control-api's OIK-084 boundary forbids raw SQL in app/ports. I reverted the un-runnable test-only cleanup patch and made no code changes. A scoped devDependency addition to `services/control-api/package.json`, or DB-owned fixture cleanup accessors, is required; both are outside TASK-121 Owned_Paths.
- [2026-09-04T06:36:55Z] [CX] Resumed after the ownership expansion. Added test-only `pg` and `@types/pg` dependencies, then added fixture-scoped cleanup to the TASK-121 real-Postgres integration test: it tracks its created roles and threads and deletes messages, thread memberships, threads, grants, and roles in FK order from `finally`. Verified `pnpm --filter @oikonomos/control-api test` (125/125), typecheck/build, `pnpm lint`, `pnpm -r build`, and `pnpm -r test` (all recursive packages green; DB 126 passed/2 skipped).
