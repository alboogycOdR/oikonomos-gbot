# TASK-125 — Group-thread DB accessors

## Work Log

- [2026-09-04T05:52:22Z] [CX] Started on `task/TASK-125-cx`. Preflight against the dispatcher main checkout reported: `FILE packages/db/src/threads.ts -> exists, 193 line(s), 7638 bytes`; `FILE packages/db/src/threads.test.ts -> exists, 223 line(s), 12337 bytes`. Blocked before implementation: the required public accessors cannot be consumed by TASK-121 under the current territory. `packages/db/package.json` exports only `.` to `dist/index.js`/`dist/index.d.ts`; control-api imports database APIs solely from `@oikonomos/db`; and `packages/db/src/index.ts` is the existing barrel that exports the current thread APIs. TASK-125 does not own `packages/db/src/index.ts`, so exporting `createGroupThread`, `listAllThreadsWithMembers`, and their types would violate Owned_Paths. Creating them only in `threads.ts` would leave the package entrypoint—and therefore TASK-121—unable to import them. Requested resolution: add `packages/db/src/index.ts` to TASK-125 Owned_Paths, then resume.

- [2026-09-04T06:00:00Z] [CX] Resumed after the dispatcher added `packages/db/src/index.ts` to TASK-125 territory. Implemented additive `createGroupThread` (validates at least two unique non-empty role IDs before opening a pool; wraps parent insert plus member inserts in one transaction) and `listAllThreadsWithMembers` (discriminated `Thread | GroupThread` output), then exported both APIs/types through the existing threads barrel block only. Added real-Postgres coverage for persisted null-role thread, memberships, rollback on FK failure, and mixed 1:1/group listing. `pnpm --filter @oikonomos/db typecheck` passed. Focused integration run initially exposed and fixed an ambiguous join-column query; after that, the new group accessor test passed, but the unchanged earlier 1:1 `listThreads` test failed because a group row leaked by that first failed run remains in the shared dev database. Its fail-loud behavior is explicitly required by TASK-125. Per shared-infrastructure rules I did not issue a manual `DELETE FROM`; supervisor cleanup/retry is required. Also corrected test cleanup ordering so future successful runs remove memberships before their parent test rows.
