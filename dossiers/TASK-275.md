# TASK-275 — Bounded-room anti-runaway caps for group threads

## Work Log

- [2026-09-16T23:22:36Z] [CX9] Resumed `task/TASK-275-cx9`; no prior dossier and no `Review_Findings` rework verdict. Preflight output, read verbatim: `[preflight] TASK-275 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `[preflight] 3 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `FILE services/worker/src/groupFanout.ts -> exists, 107 line(s), 4770 bytes`; `FILE services/worker/src/groupRouting.ts -> exists, 159 line(s), 6441 bytes`; `NEW packages/db/src/threads.ts (if a per-thread counter column is needed) -> does not exist; parent packages/db/src/ exists`. The referenced `specs/OIKONOMOS_GROKBOT_PARITY_REMAINING_WORK_2026-09-16.md` is absent from this worktree. Read the live group path: `services/control-api/src/app.ts` accepts the human group post and is the only call site that submits group executions; `services/control-api/src/ports.ts` loads all history and calls `route()`; `services/worker/src/main.ts` maps a group execution to a role's 1:1 thread before `groupFanout.ts` persists a bot message. Therefore the owned files cannot enforce any of the three required group-thread controls server-side: they neither receive the group thread ID/history at bot-response persistence nor decide whether a group post may submit a run. Implementing a counter or quiet detector in the owned files would be inert and fail the stated live/server-side criteria. Required territory expansion: `services/control-api/src/app.ts` and `services/control-api/src/ports.ts` (plus their existing tests, or explicit authorization to add tests under the worker-owned files with an injectable group-thread context). Blocked rather than adding speculative thresholds or a non-live control.
- [2026-09-17T00:05:00Z] [CX9] Re-read the expanded task territory, then merged the current integration base and inspected the durable production path. `app.ts` now owns live group POST/routing and `ports.ts` loads the full group history, but `services/worker/src/main.ts` (not owned) unconditionally calls `getOrCreateThreadForRole({ roleId: task.roleId })` and then `driver.run({ task, threadId: thread.id, ... })`, ignoring `task.execution.threadId` for every `kind: "chat"` group execution. Thus all generated bot messages are persisted only in per-role 1:1 threads; the group transcript has user rows only. A server-side cap in the newly owned API files would always observe zero group bot turns, while quiet-room comparison and rolling bot history would likewise never run over a bot response. The task also has no current bot-to-group re-routing/round loop to stop after a quiet turn. Required resolution: authorize/assign `services/worker/src/main.ts` plus its test coverage and define the intended bounded group-round orchestration, or provide a different live response-persistence seam. The named parity spec is absent from the repository; `docs/STUDY-grok-bot-018.md` supplies reference values (3 rounds, 10 member turns, 2 messages/turn, 24 history) but TASK-275 explicitly reserves final thresholds for ORCH review, so none were speculatively selected.
- [2026-09-17T00:24:00Z] [CX9] Resumed after ORCH's 2026-09-16T22:35Z firm scope decision and merged `origin/master` into `task/TASK-275-cx9`. Preflight output, read verbatim: `[preflight] TASK-275 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `[preflight] 6 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `FILE services/control-api/src/app.ts -> exists, 2715 line(s), 116256 bytes`; `FILE services/control-api/src/ports.ts -> exists, 1023 line(s), 46488 bytes`; `FILE services/control-api/src/app.test.ts -> exists, 116 line(s), 5909 bytes`; `FILE services/worker/src/groupFanout.ts -> exists, 107 line(s), 4770 bytes`; `FILE services/worker/src/groupRouting.ts -> exists, 159 line(s), 6441 bytes`; `FILE packages/db/src/threads.ts -> exists, 274 line(s), 10995 bytes`; `[preflight] Paste this output into your first Progress_Note as the c8b9872 filesystem check.` The new control-api path is present: `app.ts:1840` invokes `routeGroupMessage`, and `ports.ts:675` loads group-thread messages before routing. However `git ls-tree -r --name-only HEAD` and `origin/master` both show no `specs/OIKONOMOS_GROKBOT_PARITY_REMAINING_WORK_2026-09-16.md` (only `OIKONOMOS_GROKBOT_PARITY_DISPOSITION_v1.0.md` exists), despite PLAN.md calling it committed. More importantly, the corrected task Description says exact cap/window/quiet thresholds are ORCH's call and must not be hardcoded speculatively. No implementation can satisfy the configured-cap or live quiet-room acceptance criteria without these policy values. Blocked pending the missing spec and explicit values for consecutive bot cap, rolling history window, and quiet-repeat condition/count.
