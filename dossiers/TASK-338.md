# TASK-338 dossier

**Brief:** Claude lane: end a sandbox run that goes silent

**Assigned:** S5. **Depends on:** TASK-336.

**Source:** OpenBot (CopilotKit, MIT) comparison of 2026-09-21, five read-only passes; borrow the idea, never the code.

**Approach:** read the Description and Acceptance_Criteria in PLAN.md, then our own code; verify every REPORTED claim before changing anything.

## Work Log

- [2026-09-24T13:45:00Z] [CX9] Resumed after REWORK F1/F2. Preflight output: `[preflight] TASK-338 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `services/worker/src/chatRunDriver.ts` exists (2334 lines, 119819 bytes), `services/worker/src/chatRunDriver.test.ts` exists (2980 lines, 187104 bytes), `services/worker/src/subprocessProviders.ts` exists (322 lines, 14651 bytes), and `services/worker/src/subprocessProviders.test.ts` exists (231 lines, 10218 bytes). Reviewed committed rework 40abb28: Claude production command is verbose stream-json, terminal result parsing retains usage/cost, and sandbox-client activity excludes status/comment keepalives. Next: rerun isolated package/worker/full suites and record results.

- [2026-09-24T14:00:00Z] [CX9] `scripts/test-isolated.ps1 -Filter @oikonomos/sandbox-client` passed 26/26, and sandbox-client build plus worker `tsc --noEmit` both passed after rebuilding the ignored workspace declarations. The isolated worker suite is actively running under the shared mutex (worker Vitest PID 11644 observed), after queueing behind an ORCH review run; it has not returned an outcome yet. Do not claim it passed. Next: wait for that harness process to exit, collect/classify its result; then run the required full recursive isolated suite if the mutex is free, append evidence, and request review. F3 remains confirmed: AbortSignal aborts only the client SSE stream; execd's unchanged server-side 10-minute command timeout bounds the in-sandbox process.

- [2026-09-24T12:20:00Z] [CX9] Resumed `task/TASK-338-cx9` at `ff13825`; verified the partial implementation and the expanded sandbox-client territory with `python scripts/preflight_paths.py TASK-338`. The client now exposes backward-compatible per-SSE-event activity and cancellation hooks, while the Claude driver owns the 180000 ms default silence timer, cancellation, visible TASK-316-style message, and `run.sandbox_silence_timeout` audit category. Added the silent and periodic-activity integration regressions left uncommitted by the handoff. `scripts/test-isolated.ps1 -Filter @oikonomos/sandbox-client`: 26/26 passed. Sandbox-client typecheck passed; worker typecheck is blocked only by pre-existing 712688f1 navigation-category incompatibility at `chatRunDriver.ts:1830`. Isolated worker suite is still running under the shared test-database lock.

- [2026-09-24T12:28:00Z] [CX9] Verification complete. `scripts/test-isolated.ps1 -Init -Filter @oikonomos/worker` exercised the two TASK-338 regressions together with the worker suite; `scripts/test-isolated.ps1` then completed the full recursive workspace run. The sandbox-client suite is green (26/26) and its typecheck is green. The full-run non-owned failures are the current master-baseline classes: evals-harness environment/provider failures; worker `budget.platform_exceeded`/depleted Gemini credit/timeout conditions; and control-api TASK-121's unaddressed three-bot route returning 400 rather than 201. None originates in TASK-338's owned implementation or regressions. Worker `tsc --noEmit` remains blocked by the pre-existing 712688f1 `navigation.category` union mismatch at `chatRunDriver.ts:1830`, not this feature. Ready for review.

- [2026-09-24T13:25:00Z] [CX9] Rework F1/F2 addressed: production Claude now uses `--output-format stream-json --verbose`, so assistant and tool lines are incremental stdout activity; `eventFromSandboxStdout` selects the terminal result envelope and preserves its cost/usage. Activity now excludes execd `status` and comment keepalives, accepting only stdout, stderr, or direct tool frames. Added stream-json terminal-envelope and production-driver regressions plus status-frame coverage. Sandbox-client isolated suite remains green (26/26) and typecheck passes; worker typecheck has only the pre-existing `navigation.category` union error at chatRunDriver.ts:1846. Worker isolated suite is waiting behind the shared test-database mutex. F3 confirmed: cancellation aborts only the client stream; the sandbox process remains bounded by execd's unchanged server-side 10-minute timeout.

- [2026-09-24T13:30:00Z] [CX9] Checkpoint: the queued worker isolated run acquired the shared database mutex and launched Vitest, but remains running without completion after several minutes. No test failure has been emitted. Code is committed at `40abb28`; next session should collect that run's result (or rerun the isolated worker filter after the mutex is free), then append final evidence and request review.

- [2026-09-24T13:57:48Z] [CX9] Addressed added review finding F4 in `chatRunDriver.test.ts`: TASK-136 now resets the shared `email.send` capability to `enabled: false` in a `finally`, including assertion/error paths. `scripts/test-isolated.ps1 -Filter @oikonomos/worker` ran with the fix: no `CapabilityEnabledDriftError`; 6 unrelated worker failures remain (four 5s timing timeouts and two Gemini-credit 402-dependent tests). `pnpm --filter @oikonomos/worker exec tsc --noEmit` passed. A required recursive isolated-suite attempt is queued behind another holder of the global test-database mutex; no result has been inferred. F3 remains confirmed: aborting the client SSE stream does not kill the in-sandbox process, which is still bounded by execd's unchanged server-side 10-minute timeout. Next: obtain the recursive isolated-suite result, classify any non-owned failures, then request review.

- [2026-09-24T14:09:00Z] [CX9] Ran the supervisor-mandated main-checkout isolated harness: `test-isolated.ps1 -Root ... -Init` rebuilt and seeded `oikonomos_test`; the following recursive run exercised all 19 projects. Its evals/worker budget failures came from one bare-UUID `$100` `spend_records` leak in `spendReservations.test.ts`; per the TASK-338 review instruction, deleted only that observed `codex` test row (`6e795fbe-3b80-438d-8390-76b4cdf8160c`), then verified zero rows for its run ID. The re-run worker suite passed both TASK-338 silence regressions and all 61 `chatRunDriver.test.ts` tests. It revealed F4's remaining order-dependent test-manifest mismatch: restoring the DB's normally-disabled `email.send` conflicted with the shared fixture's implicit enabled declaration. Made the shared TASK-128 fixture explicitly disabled and constructed an enabled copy solely for TASK-136's approval path. Final `scripts/test-isolated.ps1 -Filter @oikonomos/worker` result: 38 files passed, 365 tests passed, 1 skipped; sole unrelated failure is TASK-342's `roleMessageDelivery.ts:337` cleanup deleting threads before `thread_members` (FK violation). `pnpm --filter @oikonomos/worker exec tsc --noEmit` and `git diff --check` passed. The initial full recursive run's remaining non-owned failures were TASK-121 control-api's unaddressed three-bot 400-vs-201, TASK-342's leaked-spend budget cascade/role-message FK, and a pg-boss shutdown timing timeout; the post-cleanup worker run establishes the TASK-338 path is green. F3 remains confirmed: client abort ends only the SSE read; execd retains its unchanged 10-minute server-side command timeout.
