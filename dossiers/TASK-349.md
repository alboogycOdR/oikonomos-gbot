# TASK-349 dossier

**Brief:** Web: avatar colour and shape picker saved and rendered.

Web CreateBotDialog gets the same 12x8 picker, sends avatarColor/avatarShape, and Avatar.tsx renders the saved colour and shape (sidebar, conversation header, group dialog). Null keeps the derived avatar. Use TASK-347's token names so mobile and web match.

**Assigned:** CX9. **Depends on:** TASK-347.

**Spec pointers:** specs/OIKONOMOS_GROKBOT_PARITY_DISPOSITION_v1.0.md; memory: grok-bot-mobile-reference (owner screenshots 2026-09-04/05); TASK-347 palette

**Approach:** Read the Description and Acceptance_Criteria in PLAN.md, then the current code in Owned_Paths, before changing anything. Work on the pre-cut branch from master. Run tests in the FOREGROUND and wait for them to finish before emitting the control block. Review requires `pnpm build` and `pnpm typecheck` to exit 0 (Flutter tasks: `flutter test` plus `flutter analyze lib`). A failure already on master is still named and classified, never waved off as "baseline".

## Work Log

- [2026-09-25T00:00:00Z] [CX9] Resumed the pre-cut branch, merged the completed TASK-347 dependency, and implemented the 12-color/8-shape web picker plus persisted-avatar rendering across sidebar, conversation header, and group selection. Preflight output (run before merge, against the branch's then-stale PLAN snapshot): `[preflight] TASK-349 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `FILE apps/dashboard/src/components/chat/CreateBotDialog.tsx -> exists, 221 lines, 8271 bytes`; `FILE apps/dashboard/src/components/chat/CreateBotDialog.test.tsx -> exists, 147 lines, 5978 bytes`; `FILE apps/dashboard/src/components/chat/Avatar.tsx -> exists, 46 lines, 1309 bytes`; `FILE apps/dashboard/src/components/chat/Avatar.test.tsx -> exists, 21 lines, 824 bytes`; `FILE apps/dashboard/src/components/chat/types.ts -> exists, 67 lines, 2241 bytes`; `FILE apps/dashboard/src/lib/api.ts -> exists, 687 lines, 23316 bytes`; `FILE apps/dashboard/src/lib/api.test.ts -> exists, 67 lines, 3524 bytes`. `pnpm typecheck` exited 0; next: run dashboard tests/lint/build and full isolated suite.
- [2026-09-25T00:00:00Z] [CX9] Verification: `scripts/test-isolated.ps1 -Filter @oikonomos/dashboard` passed 25 files / 166 tests; `pnpm typecheck` and `pnpm build` exited 0; the dashboard package has no `lint` script. Full suite was freshly initialized with `scripts/test-isolated.ps1 -Init`, then run in the foreground with `scripts/test-isolated.ps1`; dashboard passed, but the recursive run exited 1 outside this task's territory. The observed control-api group-routing test initially returned 400 instead of 201 and passed when rerun; worker's three `chatRunDriver.test.ts` real-Claude tests failed only because the provider reported `You've hit your session limit`, including the governed end-to-end, second-turn continuity, and mounted Read cases. No avatar test failed.

- [2026-09-24T22:23:56Z] [CX9] Preflight completed: all seven declared Owned_Paths exist (CreateBotDialog.tsx/.test.tsx, Avatar.tsx/.test.tsx, types.ts, api.ts/.test.ts). Read TASK-347/348’s merged API and mobile implementation: tokens are colors `red, orange, amber, yellow, lime, green, teal, cyan, blue, indigo, violet, pink` and shapes `circle, square, rounded, hexagon, diamond, star, triangle, teardrop`. Blocked before code: persisted avatar fields must be mapped from `Thread` into `BotSummary` in `apps/dashboard/src/pages/ChatPage.tsx` and passed from `BotSummary` into `Avatar` at the sidebar, conversation header, and group-dialog call sites (`BotSidebar.tsx`, `ConversationPane.tsx`, `GroupThreadDialog.tsx`); those required rendering files are outside TASK-349 Owned_Paths. Please extend territory or arrange an integration task. No TASK-349 source changes made.
