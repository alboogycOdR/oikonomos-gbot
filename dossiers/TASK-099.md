# TASK-099 Dossier

## Work Log

- [2026-09-02T14:34:00Z] [CX] Resumed `task/TASK-099-cx`. Read `AGENTS.md`, `briefings/CODEX_BRIEFING.md`, the TASK-099 PLAN block, OIK-102, and ADR-012 §2.4–2.5. Preflight output: `services/workspace/src/mailbox.ts` exists (274 lines); `services/workspace/src/mailbox.test.ts` is NEW; `services/workspace/src/index.ts` exists (25 lines). Blocked before edits: TASK-099 needs typed `handoffKind`/`factRef` persisted in `role_messages`, but `@oikonomos/db` currently exposes only `workspaceRefs` and its INSERT targets only `(tenant_id, from_role_id, to_role_id, body, workspace_refs)`. Required changes are outside owned paths: `packages/db/src/roleMessages.ts` plus the repository's database schema/migration location. Encoding the fact reference as a workspace reference would violate the existing schema contract and the acceptance criterion that the persisted row carry a fact reference.
- [2026-09-02T14:18:00Z] [CX] Implemented typed handoffs on `task/TASK-099-cx`: exported closed `handoffKinds`, typed `HandoffFactReference`, pair/shape/tenant validation before persistence, additive nullable `role_messages.handoff_kind`/`fact_ref` migration, and a DB check that rejects copied `value` payloads. Added unit coverage for the pair-before-write invariant and a DATABASE_URL-gated mailbox→persisted-row→TASK-098 `resolve()` integration proof. Verified `pnpm --filter @oikonomos/db build`, `pnpm --filter @oikonomos/workspace build`, `pnpm --filter @oikonomos/db test` with DATABASE_URL cleared (51 passed, 57 intentional integration skips), workspace test with DATABASE_URL cleared (30 passed, 3 intentional integration skips), `pnpm lint`, and `pnpm canaries` (17/17) pass. With the inherited live DATABASE_URL, integration tests fail because shared Postgres has not applied even migration 004 (missing FKs) nor new migration 007 (`role_messages.handoff_kind` absent); CX did not alter shared infrastructure. Global `pnpm -r build`/`test` also stop in unrelated `apps/dashboard` because its declared React/Vite test dependencies are absent from its node_modules.
