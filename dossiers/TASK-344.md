# TASK-344 dossier

## Work Log

- [2026-09-24T17:15:00Z] [CX9] Started on `task/TASK-344-cx9`. Preflight: `[preflight] TASK-344 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `services/control-api/src/chat.routes.test.ts` is an existing FILE (1913 lines, 98289 bytes). No dossier or PLAN Review_Findings existed. Ran the required isolated control-api suite after `-Init` and again without it; both retain the reported group-route failure. The test already injects a deterministic FreeLLMAPI transport, so the next step is to expose and correct the fixture/request-level 400 cause without touching route production code.
- [2026-09-24T17:14:00Z] [CX9] Fixed the fixture drift in `services/control-api/src/chat.routes.test.ts`: TASK-284 changed this deterministic, providerless fixture to assert Gemini, although `scripts/test-isolated.ps1` deliberately scrubs `OIK_DEFAULT_ROLE_PROVIDER` and Gemini credentials. The role runtime therefore correctly falls back to Claude. Restored the test's Claude expectations at the selected-run poll and direct-run query while retaining real FreeLLMAPI scoring, three recorded prompts/spend rows, and selected-recipient assertions (the liveness proof: removing Tier-0 routing leaves those assertions unsatisfied). `pnpm --filter @oikonomos/control-api typecheck` and `git diff --check` pass. Isolated `-Init -Filter @oikonomos/control-api` followed by two consecutive `-Filter @oikonomos/control-api` runs passed; the second complete report includes `src/chat.routes.test.ts (47 tests)` green and the TASK-121 FreeLLMAPI route green in 1242ms.
