# TASK-038 dossier

## Brief
Backup + restore for Postgres + evidence volume, with a restore drill into a clean environment (the drill is the acceptance bar). NON-protected. Wave-1 parallel filler so both units are fed while E4's head is serial.

## Spec pointers
WBS OIK-015; Handover §8. Notes go in infra/backup/README.md (docs/** is builder-blocked). Deps only OIK-011 (done) = eligible now.

## Intended approach
Scripted backup + a demonstrated clean-env restore. No credentials anywhere.

## Work Log

- [2026-08-17T13:10:00Z] [CX] Started TASK-038 on `task/TASK-038-cx`. Preflight output: `[preflight] TASK-038 Owned_Paths inspected in C:/CLAUDECODE_TOOLSETS/wt-codex-oikonomos`; `GLOB infra/backup/** -> matches nothing yet (new territory)`. Read WBS OIK-015 and Handover §8. Existing compose defines the Postgres volume; no evidence-volume implementation exists yet, so backup tooling will require its Docker volume name explicitly.
- [2026-08-17T13:28:00Z] [CX] Implemented `backup.sh`, `restore-drill.sh`, and a fully disposable `test-drill.sh`, with deployment and drill instructions in `infra/backup/README.md`. Backup produces a custom pg dump, evidence tarball, and checksums; the drill verifies both archive checksums, a restored DB content fingerprint, and restored evidence entry count before deleting only its own `oikonomos-restore-drill-*` resources. Static shell syntax passed in Alpine, `git diff --check` passed, and `node infra/ci/secret-scan.mjs` reported clean. Full Docker drill is presently blocked on this worker: Windows has Docker, but its WSL Ubuntu distribution lacks Docker integration and Windows has no `pg_dump`/`openssl`; command attempted: `wsl -d Ubuntu -- bash -lc 'cd /mnt/c/CLAUDECODE_TOOLSETS/wt-codex-oikonomos && sh infra/backup/test-drill.sh'` (fails before executing the scripts: `docker could not be found in this WSL 2 distro`). Next: review and run `sh infra/backup/test-drill.sh` on a Linux/Docker host with PostgreSQL client tools before merge/deploy.
- [2026-08-17T14:59:04Z] [CX] Applied ORCH review finding: both Postgres readiness loops now wait for authenticated `psql -h 127.0.0.1 ... -c 'SELECT 1'`. The TCP requirement cannot be satisfied by the official image's Unix-socket-only init server, avoiding the init-server shutdown race before seed or restore. Verified `git diff --check`, POSIX shell syntax in Alpine, and `node infra/ci/secret-scan.mjs` (clean). The full disposable drill remains for ORCH to run on clawsrv because this Windows worker lacks a usable Linux Docker/`pg_dump` environment; no clean-environment restore result is claimed here.
- [2026-08-17T15:10:00Z] [CX] Resume validation complete after the readiness-race fix (`18bf136`): `git diff --check` passed; all three shell scripts passed `sh -n` inside `alpine:3.20`; `node infra/ci/secret-scan.mjs` was clean. `git diff --name-only master...HEAD` confirms only `infra/backup/**` and this dossier changed. The remaining required evidence is an ORCH-run `sh infra/backup/test-drill.sh` on clawsrv/Linux with Docker and host `pg_dump`; this Windows worker cannot perform that live drill.
- [2026-08-17T19:33:00Z] [CX] Rework round 2 complete. Replaced the host/client split fingerprint with a pinned `pgvector/pgvector:pg16` client on both source and restored data; `POSTGRES_DUMP_DOCKER_NETWORK` defaults to `host` and the isolated drill supplies its disposable Docker network. The drill no longer needs host `pg_dump` or OpenSSL. Corrected Git-Bash bind-mount/path conversion by using `--mount` for the archive directory and shell-wrapped container paths. Demonstrated locally: `sh infra/backup/test-drill.sh` exited 0 and printed `RESTORE_DRILL_VERIFIED` plus `BACKUP_RESTORE_DRILL_TEST_PASSED`. Follow-up static validation: `git diff --check`; `sh -n` for all three scripts in Alpine; and `node infra/ci/secret-scan.mjs` all passed.
