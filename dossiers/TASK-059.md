# TASK-059 - Evidence delivery with the approval (OIK-087)

## Brief
Attach what the operator needs to decide: the draft body and recipient, plus the artifact URIs PostToolUse recorded. Fetched from control-api, never the DB or filesystem.

## Spec pointers
- OIK-087 - "Screenshots/diffs delivered with the approval request".
- Directive 5 Evidenced - truncation must be **visible**; a silently truncated draft means approving something you did not fully see, which defeats the approval.
- N4 - reuse `packages/audit`'s redaction; never write a second implementation.

## Work Log

- [2026-08-31T22:36:54Z] [CX] Preflight completed before any implementation: `services/gateway-telegram/src/evidence/**` is a new, empty glob; `services/gateway-telegram/test/evidence.test.ts` is new (parent test directory exists). BLOCKED — OWNERSHIP_CONFLICT: OIK-087 cannot be wired to an approval request within the assigned paths. `ControlApiClient` in `services/gateway-telegram/src/index.ts` has no `getRunEvidence(runId)` method and its HTTP adapter has no `/runs/:id/evidence` request; `registerTelegramApprovals` in `services/gateway-telegram/src/approvals/index.ts` is the sole publisher of `sendApprovalMessage` and must invoke the evidence renderer. Both files are outside `Owned_Paths`. Further, `packages/audit` deliberately does not export its redaction helpers, so rendering evidence safely requires a supported public redaction export (and likely its package export surface) or an explicit approved alternative; duplicating redaction would violate N4. No source files were changed and no tests were run because an isolated evidence module would be unreachable and fail the acceptance criterion.
- [2026-09-01T00:58:00Z] [CX] Resumed after the re-carve and read the live control-api evidence contract plus the Telegram approval publisher. BLOCKED — OWNERSHIP_CONFLICT: the required supported import of `redactPayload` from `@oikonomos/audit` needs an additive `"@oikonomos/audit": "workspace:*"` dependency in `services/gateway-telegram/package.json`, but that manifest is not in TASK-059's Owned_Paths. Directly importing audit source would evade the public package boundary and violate N4's required reuse. Please add this manifest to TASK-059's territory, then implementation can add the public audit export, evidence renderer, `GET /runs/:id/evidence` client method, and publisher wiring.
- [2026-08-31T23:25:00Z] [CX] Resumed after the second re-carve. Read the live `GET /runs/:id/evidence` control-api contract, gateway approval publisher, audit redaction implementation, and audit persistence-surface test. BLOCKED — OWNERSHIP_CONFLICT: N4 requires exporting `redactPayload` publicly from `packages/audit/src/index.ts`, but `packages/audit/test/persistence-surface.test.ts` pins the exact runtime export list to `["AuditWriteError", "recordAuditEvent", "recordDecision", "toDecisionAuditEvent"]`. The required export necessarily makes that existing test fail. Updating its expected list preserves the pin (rather than weakening it) but the test is outside TASK-059's Owned_Paths; changing it would violate strict-mode territory rules. Please add `packages/audit/test/persistence-surface.test.ts` to TASK-059's territory, then implementation can proceed and prove the audit reuse safely.
- [2026-09-01T01:36:00Z] [CX] Implemented the widened TASK-059 paths: public `@oikonomos/audit` redaction export (with the persistence-surface export pin updated), the gateway's `getRunEvidence(runId)` control-api client method, redacted evidence/artifact rendering with explicit Telegram truncation marker, publisher wiring for original and edited approvals, and focused evidence tests. BLOCKED — OWNERSHIP_CONFLICT: adding the required `@oikonomos/audit: workspace:*` runtime dependency to `services/gateway-telegram/package.json` requires the corresponding `pnpm-lock.yaml` importer update, but the lockfile is not an Owned_Path. Targeted `pnpm --filter @oikonomos/gateway-telegram test -- evidence.test.ts` and `pnpm --filter @oikonomos/gateway-telegram typecheck` both fail only because the uninstalled workspace dependency cannot resolve. Please add `pnpm-lock.yaml` to TASK-059 territory (or have the supervisor update it); then run `pnpm install --lockfile-only`, re-run all gates, and complete handoff.
- [2026-09-01T03:45:09Z] [CX] Completed after lockfile territory was added. Committed the regenerated workspace importer entry, added a control-api endpoint/URL-encoding assertion, and preserved legacy no-runId approval rendering while every live approval with a runId fetches evidence. Verified: gateway 68/68 tests and typecheck; `pnpm -r test` exited 0; `pnpm lint` exited 0; `pnpm canaries` exited 0 (17 tests, 2 skipped).
