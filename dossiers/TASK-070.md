# TASK-070 dossier

## Work Log

- [2026-08-26T16:52:00Z] [CX] Stopped before implementation: dispatcher reports claimed branch `task/TASK-070-cx`, but this worktree is detached at `18a521e` and `git branch -a --list '*TASK-070*'` returns no branch/ref. The worktree's local PLAN is also stale (`scripts/preflight_paths.py TASK-070` reports task not found), while the authoritative main checkout at `C:\CLAUDECODE_TOOLSETS\oikonomos\PLAN.md` records TASK-070 as claimed. Need supervisor sync/branch repair before work can safely continue.
- [2026-08-26T17:03:00Z] [CX] Resume sync is repaired: worktree is on `task/TASK-070-cx`; authoritative PLAN preflight reports all three allocated paths as new territory. Implementation is blocked by ownership: required CI wiring can only be made in `.github/workflows/ci.yml`, but TASK-070 owns only `infra/ci/checks/publication-tree.sh`, `infra/ci/checks/negative-controls.sh`, and `infra/ci/workflows/**`. The existing workflow has explicit jobs and no discovery/dispatch of `infra/ci/workflows/**`; therefore owned-path-only additions cannot satisfy the required-CI-step acceptance criterion. No CI or control implementation was written, avoiding an inert, unwired control.
- [2026-08-26T16:00:00Z] [CX] Implemented the corrected live allocation from the dispatcher: `publication-tree.mjs` plus adversarial self-test, `negative-controls.mjs` plus fixture-liveness self-test, and additive CI jobs with each self-test immediately before its control. Local self-tests pass and the negative control is clean. The publication proof intentionally fails on current HEAD because tracked `board/index.html` is matched by existing `.gitignore:24` (`board/`), yielding `omitted from clean add: board/index.html`. Making current HEAD pass requires an `.gitignore` correction, which is outside TASK-070 Owned_Paths; the proof must not be weakened. Broad checks: `pnpm canaries` passed (11 files/15 tests); `pnpm -r test` failed at existing `@oikonomos/connectors` unresolved `@oikonomos/policy` import because the suite was run before a build; `pnpm lint` failed on pre-existing untracked `apps/__sole-constructor-fixture__/stray.ts`, which is outside territory. Both blockers are preserved unchanged.
- [2026-08-26T16:02:00Z] [CX] Resumed after ORCH merged the `.gitignore` repair. The clean-head publication proof initially exposed a Windows-only archive extraction issue: executable mode bits are not retained by `tar`, producing a different tree ID despite identical paths/content. The control now restores executable modes from `HEAD` into the scratch index before the required exact `write-tree` comparison; its clean fixture includes a tracked executable to prove that portability path. `node infra/ci/test-publication-tree.mjs` passed (2/2, including deliberately ignored tracked file failing); `node infra/ci/publication-tree.mjs` passed clean on HEAD; `node infra/ci/test-negative-controls.mjs` passed (3/3, deliberately planted OPENAI_API_KEY and live hostname exit 1); `node infra/ci/negative-controls.mjs`, `node infra/ci/test-banned-modes.mjs`, and `node infra/ci/banned-modes.mjs` all passed. After `pnpm install --frozen-lockfile`, `pnpm -r test` passed (16/17 workspace projects), `pnpm lint` passed, and `pnpm canaries` passed (11 files, 15 passed / 2 skipped). CI diff is additive: existing banned-modes implementation is unchanged; new jobs run each sibling self-test immediately before its control.
- [2026-08-26T19:23:07Z] [CX] Completed ORCH rework findings. `publication-tree.mjs` now obtains the initial scratch tree before executable-mode restoration and skips paths omitted by clean add, so an ignored executable reaches the normal mismatch report instead of throwing exit 2. Its adversarial self-test now makes the ignored tracked file itself executable, invokes the CLI, and asserts exit 1 plus `omitted from clean add: ignored-source.sh`. `negative-controls.mjs` now accepts RFC 6761 reserved example-domain subdomains (`api.example.com`, `.net`, `.org`) and its self-test uses a genuinely non-reserved `.live` host for the liveness case. Targeted controls all passed; `pnpm -r test`, `pnpm lint`, and `pnpm canaries` all exited 0.
