# TASK-021 dossier

## Work Log

- [2026-08-15T16:27:00Z] [CX] Resumed as a newly claimed strict-control task: no prior dossier or local task branch existed, so created `task/TASK-021-cx` from the claimed integration head. Preflight output: `[preflight] TASK-021 Owned_Paths inspected in C:/CLAUDECODE_TOOLSETS/wt-codex-oikonomos` / `[preflight] 1 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.` / `GLOB infra/ci/** -> 12 file(s): infra/ci/README.md, infra/ci/banned-modes-allowlist.txt, infra/ci/banned-modes.mjs, infra/ci/hooks/pre-commit, infra/ci/lib/allowlist.mjs, infra/ci/lib/walk.mjs, infra/ci/protected-path-review.mjs, infra/ci/run-local.mjs, infra/ci/secret-scan.mjs, infra/ci/test-banned-modes.mjs, infra/ci/test-protected-path-review.mjs, infra/ci/test-secret-scan.mjs.`
- [2026-08-15T16:31:00Z] [CX] Implemented the ADR-005 controls-live gate and induced-failure self-test. The current unscanned ATLAS database correctly reports `last scan: never`; this is expected to fail the new gate until the index is scanned, rather than being masked by its database mtime.
- [2026-08-15T16:33:00Z] [CX] Verified successful liveness evidence after `python scripts/atlas.py scan --full`: `atlas.py status` reported 244 indexed files and a recorded scan timestamp; all six controls-live checks passed. `node infra/ci/test-controls-live.mjs` passed 1/1; `pnpm lint`, `pnpm typecheck`, and `pnpm build` passed. The protected-path gate rejected this branch without its marker (exit 1) and passed with the marker.
- [2026-08-15T16:34:00Z] [CX] BLOCKED on an existing integration failure outside TASK-021 territory: `node infra/ci/run-local.mjs --approval-marker fable-reviewed` has exactly one failing job, `pnpm test`. `packages/db/test/persistence-surface.test.ts` asserts that `consumeApproval` is absent, while the same baseline `master` already exports it from `packages/db/src/approvals.ts`; confirmed with `git show master:` for both files. The controls-live job itself passed all six checks in that run. I cannot repair `packages/db/**` under `infra/ci/**` ownership.
- [2026-08-15T17:31:00Z] [CX] Resumed from `fb5d65b` and merged current `master` (including TASK-022) into the retained branch. Strengthened the controls-live self-test: each of the six induced inert states now proves both its individual failure and the aggregate non-zero process exit decision. `node infra/ci/test-controls-live.mjs` passes. After the CI-equivalent `pnpm build`, `pnpm test` passes (all workspace tests green; database and approval integration tests skip cleanly without DATABASE_URL), and controls-live reports all non-ATLAS checks passing, including fresh dist outputs. The sole remaining failure is correct: ATLAS reports its last recorded scan is 19 commits behind HEAD. Refreshing that generated index writes `.devteam/atlas.db`, which is outside TASK-021's strict owned territory, so I did not mask the failure or write it. Supervisor must run `python scripts/atlas.py scan --full` after the final branch commit, then `node infra/ci/run-local.mjs --approval-marker fable-reviewed` should be green.
- [2026-08-15T19:25:00Z] [CX] Applied the supervisor's amended AC: ATLAS staleness is now measured only through the merge-base with the local integration branch (`master`, falling back to `main`), not through builder-only HEAD commits. Focused controls-live and all existing infra/ci self-tests pass. The full local CI sequence passes typecheck, build, workspace tests, lint, banned-mode, secret-scan, and protected-path jobs; its only failure is the correctly fail-closed ATLAS check, which reports the integration merge-base is 17 commits newer than the last recorded scan. I did not write generated `.devteam/atlas.db` outside owned territory.
- [2026-08-15T20:11:00Z] [CX] Applied the 19:35Z descoping instruction: removed ATLAS liveness and its induced-failure case, leaving the five maintained checks (hook evidence, drained control queue, active builder model pins, collected policy coverage, and fresh workspace dist). Documented the deferral to TASK-024: the gitignored per-worktree ATLAS index is refreshed only in the main checkout and has no builder-worktree maintainer. `node infra/ci/run-local.mjs --approval-marker fable-reviewed` is fully green; the same protected diff without its marker exits 1 as required.
- [2026-08-15T21:15:00Z] [CX] Reworked all four blocking review findings in commit f268fe1. Coverage parsing now strips ANSI sequences and is proven against real FORCE_COLOR Vitest output; the installed territory hook is exercised through `git hook run` with an isolated alternate index staging an out-of-territory AGENTS.md deletion and must emit its real rejection; a missing `.devteam/control/` now fails as unobservable and is regression-tested alongside empty/undrained states; dist staleness now has a real older-output test and apps/ is included in workspace discovery. `node infra/ci/test-controls-live.mjs` passes 5/5; typecheck, build, test, lint and existing infra self-tests pass. `run-local` fails exactly one job: controls-live reports `.devteam/control/` is missing in this worktree. Creating it would be outside TASK-021's `infra/ci/**` territory, so this is an honest fail-closed blocker for the supervisor to provision.
- [2026-08-15T21:29:30Z] [CX] Resolved the supervisor's per-worktree diagnosis without weakening fail-closed behavior. `collectControlQueueEvidence()` now resolves the primary checkout from `git rev-parse --git-common-dir` and observes only that checkout's `.devteam/control/`; an absent primary queue or unresolved common directory remains a named failure. Added a regression test proving that resolution and updated the liveness README. Verification: `node infra/ci/test-controls-live.mjs` 6/6 pass; `node infra/ci/controls-live.mjs` all five checks pass; `node infra/ci/run-local.mjs --approval-marker fable-reviewed` all 12 jobs pass; `node infra/ci/protected-path-review.mjs --base $(git merge-base HEAD master)` exits 1 without a marker as required.
