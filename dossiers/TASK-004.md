# Dossier — TASK-004 · ATLAS A3 — hash-pinned cards + docs/ATLAS.md

**Brief:** Build `scripts/atlas_cards.py`: Layer 1 per-file summary cards generated by one headless `claude-sonnet-4-6` call per changed file, hash-pinned to the source (R1), atomically written to the `cards` table TASK-002 created. Also author `docs/ATLAS.md`. Core query already renders FRESH/STALE from your `source_hash` values — you build generation, pinning, and `--stale`; you never edit atlas.py/atlas_core.py.

**Spec:** specs/DEVDEPARTMENT_ATLAS_SPEC.md — read in full; load-bearing: R1 (cards are claims), R4 (graceful degradation), §1 (Cards bullet + cards schema row), §3 (contract incl. `--only`, `--model`), §6 row A3 (fake-model-transcript tests), §7 A3 exit criteria, §8 Q1 (add `--max N`).

**Intended approach:**
- `cards --generate [--only <glob>] [--model M=claude-sonnet-4-6] [--max N]`: select files whose hash differs from their card's source_hash (or cardless), one headless CLI call per file (`claude -p` with structured-output prompt), parse to purpose/invariants/gotchas/entry_points/tokens_estimate, single INSERT-OR-REPLACE transaction per card (atomic — a killed run leaves no partial card).
- Never called from `scan`; model unreachable → clear error, DB untouched, everything else still works (R4).
- `cards --stale` lists source_hash mismatches (§3).
- Tests: fake model transcript fixture (stub the subprocess), zero live calls; include the §7 doctored-hash test proving every query path shows STALE-with-warning — end-to-end through `atlas.py query`, not just your module.
- docs/ATLAS.md: architecture, R1–R4 verbatim, CLI contract, card lifecycle, degradation. TASK-006 will append an Integration section later.

**Territory note:** scripts/** and docs/** are builder-protected; your Owned_Paths are a deliberate per-task grant (firewall exception added at dispatch). Concurrent with TASK-003 (GB) — stay strictly inside your three files.

## Work Log
