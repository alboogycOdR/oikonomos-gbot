# TASK-284 Work Log

- [2026-09-17T06:50:00Z] [CX9] Preflight: `services/control-api/src/chat.routes.test.ts` (FILE, 1898 lines), `services/control-api/src/ports.ts` (FILE, 1104 lines), and `services/worker/src/groupRouting.ts` (FILE, 159 lines) all exist. Root cause established with isolated real-Postgres runs: this is a test-only defect, not a group-routing production regression. The Tier-0 transport seam is called and records three `free-llm-api` spends; selected bot runs use the live default `gemini` provider, but the test expected stale `claude` rows. Its 2-second asynchronous queue polling window also raced the control API's deliberately out-of-process producer. Removed the unused in-process driver seam, asserted the real Gemini queue state, and increased bounded waits/timeouts. Hardened the adjacent TASK-155 queue-audit assertion in the same owned test file against the same producer race.
- [2026-09-17T06:50:00Z] [CX9] Test evidence: `pnpm --filter @oikonomos/control-api typecheck` passed. `powershell -ExecutionPolicy Bypass -File scripts/test-isolated.ps1 -Filter @oikonomos/control-api` passed twice after the fix (24 files, 303 tests each); TASK-121 FreeLLMAPI/group-routing case passed in both.
- [2026-09-17T06:54:00Z] [CX9] Final verification: TASK-121 passed in three consecutive isolated control-api suite runs; latest full control-api result was 24/24 files and 303/303 tests, and typecheck passed. The repository-wide isolated runner also passed control-api (including TASK-284), but exited non-zero on unrelated shared-environment failures outside Owned_Paths: gateway-telegram cannot resolve its generated `@oikonomos/gateway-telegram/approvals` export, and worker/evals fail because the isolated DB has a stale registered `workspace.create_bot` capability lacking a declaration. No out-of-territory changes made.
