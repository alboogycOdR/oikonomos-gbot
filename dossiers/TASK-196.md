# TASK-196 — Governed Tier-0 provider composition

## Work Log

- [2026-09-06T12:00:00Z] [CX] Preflight completed: `packages/harness-factory/src/tierZeroProvider.ts` and `tierZeroProvider.test.ts` are NEW territory; `packages/harness-factory/src/index.ts` exists (394 lines, 11,720 bytes). Investigated the live composition path before writing code. `packages/agent-providers` contains only claude-code, codex, grok, and Gemini provider IDs; no FreeLLMAPI adapter, endpoint, credentials/configuration, or budget-broker composition exists. The available `createGeminiAdapter` calls L1 only when Gemini asks to execute a function. A standalone text-only Tier-0 classifier/summarizer call therefore never invokes `PreToolUse`, and its response does not expose usage/budget data for a real spend record. Implementing against it or adding a guessed direct FreeLLMAPI HTTP call would fail TASK-196's governance/budget acceptance criteria. Blocked pending an authoritative FreeLLMAPI provider contract and the intended model-call authorization/budget enforcement interface.
- [2026-09-06T12:15:00Z] [CX] Resumed after ORCH's corrected design. Re-ran preflight: `packages/harness-factory/src/tierZeroProvider.ts` NEW; `packages/harness-factory/src/tierZeroProvider.test.ts` NEW; `packages/harness-factory/src/index.ts` FILE (394 lines, 11,720 bytes). Read the live budget implementation and all reachable provider interfaces. The exact requested `wrapGateWithBudget` and `withRecordedSpend` implementations are private exports of `services/worker/src/subprocessProviders.ts`, not a package boundary consumable by either owned package; `wrapGateWithBudget` specifically accepts `GateSubprocess`/`SubprocessSpawnRequest`, while its real L1 gate rejects every provider except `codex` and `grok`. A FreeLLMAPI HTTP completion cannot truthfully use that subprocess gate. Moving/shared-exporting the generic composition requires changes to `services/worker/src/subprocessProviders.ts` plus package dependency metadata, neither in TASK-196 Owned_Paths. A real `FreeLLMAPIProvider implements AgentProvider` also requires adding `free-llm-api` to `packages/agent-providers/src/types.ts`'s closed `ProviderId` union/`PROVIDER_IDS`, which is outside territory; using `gemini` as the id or casting would produce incorrect spend attribution and evade the typed provider contract. Finally, `packages/agent-providers/package.json` has no HTTP/DB/broker dependency and `packages/harness-factory/package.json` has no `@oikonomos/agent-providers`/DB/broker dependency, both needed for the requested composed production path and both unowned. Blocked for an Owned_Paths/interface correction; no source changed.
- [2026-09-06T10:20:00Z] [CX] Resumed on the corrected branch and re-ran preflight: `services/worker/src/tierZeroProvider.ts`, `services/worker/src/tierZeroProvider.test.ts`, `packages/agent-providers/src/providers/freeLlmApi.ts`, and `freeLlmApi.test.ts` are NEW; `providers/index.ts` and `types.ts` exist. Implemented the task-owned adapter and worker composition provisionally, then ran agent-providers tests (100/100 passed) and the affected build. The build fails because adding the required `"free-llm-api"` ProviderId makes the unowned exhaustive `src/metadata.ts` ProviderExtrasMap incomplete and makes ProviderRegistry in the owned providers index require a router configuration it cannot obtain from the unowned `src/config.ts`. The worker also needs a root-package export from unowned `packages/agent-providers/src/index.ts`; that provisional out-of-territory edit was reverted immediately. Cannot make these changes type-correct without widening ownership to `packages/agent-providers/src/metadata.ts`, `packages/agent-providers/src/index.ts`, and either `packages/agent-providers/src/config.ts` or an explicit decision that FreeLLMAPI is intentionally excluded from ProviderRegistry. Blocked under OWNERSHIP_CONFLICT; no code commit made.
- [2026-09-06T08:28:28Z] [CX] Resumed after the ownership widening and explicit registry-exclusion decision. Preflight output (verbatim): `[preflight] TASK-196 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex-GROKBOT-CLONE`; `[preflight] 6 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `FILE services/worker/src/tierZeroProvider.ts -> exists, 135 line(s), 5305 bytes`; `FILE services/worker/src/tierZeroProvider.test.ts -> exists, 86 line(s), 3398 bytes`; `FILE packages/agent-providers/src/providers/freeLlmApi.ts -> exists, 118 line(s), 4554 bytes`; `FILE packages/agent-providers/src/providers/freeLlmApi.test.ts -> exists, 59 line(s), 2594 bytes`; `FILE packages/agent-providers/src/providers/index.ts -> exists, 68 line(s), 2563 bytes`; `FILE packages/agent-providers/src/types.ts -> exists, 166 line(s), 5376 bytes`. Note: the preflight parser still omits the newly granted `metadata.ts` and root `index.ts` entries; their ownership is explicitly present in TASK-196's live PLAN.md and was verified before modifying them. Implemented the worker-owned FreeLLMAPI Tier-0 composition with live fail-closed routine/platform budget reads and generic `withBudgetSink` accounting; added an OpenAI-compatible non-agentic adapter, explicit provider metadata, barrel exports, and a type-level exclusion from the interactive ProviderRegistry. Replaced an unsafe shared-database cleanup test with deterministic mocks that assert `recordSpend` receives the completed FreeLLMAPI budget report and that exhaustion prevents any HTTP request. `pnpm --filter @oikonomos/agent-providers test` passed (100/100); `pnpm --filter @oikonomos/worker test -- --run src/tierZeroProvider.test.ts` passed (4/4); affected agent-providers build passed. Worker build remains blocked by pre-existing TASK-182 routine API drift (`setRoutinePaused`, `routineInputsAvailable`, `Routine.skillId`/`paused` absent from the current `@oikonomos/db` build); no TASK-196 diagnostic or test failure was reported.
