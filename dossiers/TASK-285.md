# TASK-285 Dossier

## Work Log

- [2026-09-17T07:42:46Z] [CX9] Resume preflight completed. `task/TASK-285-cx9` exists and is checked out with no branch-only commits or working-tree changes. `python scripts/preflight_paths.py TASK-285` reports all six owned source/test files exist: `packages/broker/src/capabilityRegistry.ts` (178 lines), `packages/broker/src/index.ts` (807 lines), `packages/broker/src/builtinTools.ts` (77 lines), `packages/policy/src/enforcement.ts` (90 lines), `packages/policy/src/enforcement.test.ts` (128 lines), and `packages/broker/src/enforcementGate.test.ts` (190 lines). No prior dossier exists and PLAN.md has no REWORK findings. Blocked before implementation: acceptance criterion 1 mandates recording the security-policy classification decision in an ADR amendment/new ADR, but `docs/**` is outside `Owned_Paths` and this strict-mode dispatch expressly prohibits all writes to `docs/`. A docs ownership expansion (for the chosen ADR path) is required before a compliant implementation can begin; otherwise the task cannot meet its acceptance bar.
- [2026-09-17T08:18:58Z] [CX9] Re-dispatch after ADR-019's amendment: read the amendment, current TASK-285 block, and all five revised owned files. Preflight output: `builtinTools.ts` (77 lines), `builtinTools.test.ts` (162), `enforcement.ts` (90), `enforcement.test.ts` (128), `workspaceTools.test.ts` (153), all existing. Blocked on a direct source/territory contradiction: `DeclaredTool` in `packages/broker/src/capabilityRegistry.ts` has no `enforcementEnabled` or `enforcedActionClasses` fields, and `CapabilityRegistry.brokerPorts()` returns a `RegisteredCapability` containing only `{ toolName, capabilityId, defaultTier }`. Therefore adding the specified fields only in the owned `builtinTools.ts` is rejected by TypeScript and, even if bypassed, cannot reach the already-correct routing in `index.ts`. Confirmed the same absence on current `mainco/master` after `git fetch mainco`; no concurrent merge resolves it. The required minimal implementation must also modify `packages/broker/src/capabilityRegistry.ts` (and likely its tests), but that path is explicitly not owned by TASK-285. No code changes or tests run; requesting an Owned_Paths correction before implementation.
- [2026-09-17T09:00:00Z] [CX9] Resumed after ORCH widened ownership. Preflight: all seven owned files exist (`builtinTools.ts` 77 lines; `builtinTools.test.ts` 162; `capabilityRegistry.ts` 178; `capabilityRegistry.test.ts` 240; `enforcement.ts` 90; `enforcement.test.ts` 128; `workspaceTools.test.ts` 153). Implemented E6, declared/enabled `retire_bot`, and propagated both enforcement fields through `CapabilityRegistry.brokerPorts`; added policy, declaration, propagation, and all-grant-tier non-autonomous tests. `scripts/test-isolated.ps1 -Filter @oikonomos/broker` passes 15 files/178 tests; `scripts/test-isolated.ps1 -Filter @oikonomos/policy` passes 7 files/54 tests; broker/policy/worker typechecks pass after rebuilding package declarations. BLOCKED: real production `services/worker/src/chatRunDriver.ts`'s exported `destinationFor` has no `mcp__workspace__retire_bot` (or create_bot) extractor. The actual broker composition invokes that function before L1, so an authentic manager `retire_bot` call fails closed with `No governed destination...` and can never issue or consume its real approval. Adding a fake lambda in `workspaceTools.test.ts` would violate the AC's real-production-composition requirement. Need Owned_Paths expanded to include `services/worker/src/chatRunDriver.ts` before completing the real approval+execution integration test.
- [2026-09-17T12:30:00Z] [CX9] Resumed after ORCH landed the production `destinationFor` extractors. Added a real-Postgres integration scenario in `services/worker/src/workspaceTools.test.ts` that composes the exact production broker dependencies (CapabilityRegistry ports, exported production destinationFor, PolicyRegistry manifest map, `issueApproval`/`verifyAndConsume`, and persisted audit recording), then calls the real workspace MCP handlers only after broker allow. Rebuilt policy/broker/worker outputs; worker typecheck is clean. The isolated worker suite reaches the new scenario but it cannot meet AC3 as written: `create_bot` is a legacy `T3_external` declaration with no `enforcementEnabled`; `packages/broker/src/index.ts` therefore routes it to `resolveApprovalRequired` (the explicit legacy T3+ behavior), returning `approval_pending`, not the required no-approval allow. `retire_bot` is not reached because the required initial create assertion correctly fails. The ADR/task description authorizes `enforcementEnabled` only for `retire_bot`; making create_bot no-approval would require a new explicit policy decision (e.g. migrate it to enforcement with an empty class set, which changes T3 semantics) and should not be improvised. Working tree intentionally retains the failing integration assertion as executable evidence. BLOCKED: SPEC_AMBIGUITY — reconcile AC3's “create_bot ... T3_external, no approval needed” with the current documented legacy T3 approval rule and state whether create_bot is to be separately migrated/declared enforcementEnabled.
- [2026-09-17T10:17:43Z] [CX9] Resumed against the corrected AC (no REWORK findings): updated the real production-composition integration test so `create_bot` first returns `approval_pending` at its established T3 tier, its real persisted approval is granted and consumed, and only then is the real handler invoked. The same test then proves `retire_bot` creates a distinct real E6-backed pending approval and can execute only after that nonce is granted and consumed; pre-existing broker all-grant-tier coverage confirms it cannot default-autonomously allow. Preflight confirmed all seven owned paths exist. Verification: `scripts/test-isolated.ps1 -Filter @oikonomos/broker` 15 files/178 tests passed; `scripts/test-isolated.ps1 -Filter @oikonomos/policy` 7 files/54 tests passed; broker/policy/worker typechecks passed; isolated worker run reports `workspaceTools.test.ts` 7/7 passed, with one unrelated pre-existing `src/jobs/routineJob.test.ts` 5-second timeout in the full worker package run.
