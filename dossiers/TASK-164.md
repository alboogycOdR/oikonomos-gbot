# TASK-164 — Codex/Grok production-routing investigation

## Work Log

- [2026-09-15T03:00:00Z] [CX9] Preflight evidence: `[preflight] TASK-164 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `FILE services/worker/src/chatRunDriver.ts -> exists, 1815 line(s), 91987 bytes`; `FILE packages/db/src/routines.ts -> exists, 411 line(s), 16715 bytes`.
- [2026-09-15T03:00:00Z] [CX9] Investigated before implementation. The only production construction of a role-selected execution lane is `chatRunDriver.ts`: it resolves the persisted role choice (`getRole` / `resolveRoleRuntime`, lines 333-366) and intentionally rejects every value except `claude` and `gemini` (lines 367-376). Claude executes through `executeTaskRun` (lines 428-454); Gemini has its own adapter (`executeGeminiChatRun`, lines 420-427). `executeTaskRun` can accept `subprocessProviders` (`executeRun.ts:76,93-109`), but no production caller supplies it. A repository search found `createGatedSubprocessProviders` only at its definition, export, and tests; there is no production invocation.
- [2026-09-15T03:00:00Z] [CX9] Product-need finding: no current product decision names Codex or Grok as a role/routine provider. ADR-011's accepted multi-provider decision names Gemini as the additional/default provider, keeps Claude as reference/fallback, and requires a proven adapter before a provider can execute tools (ADR-011 §§2-3). `packages/db/src/roles.ts:319-325` deliberately preserves arbitrary provider strings, but that is a persistence/default-resolution mechanism, not authorization to add an execution lane. `packages/db/src/routines.ts` keeps `definition` advisory and does not define provider selection (lines 5-12); the shipped routine-parity work likewise did not establish a per-routine provider choice.
- [2026-09-15T03:00:00Z] [CX9] Decision: defer without code. Wiring the subprocess factory now would require inventing an unsupported role/routine selection contract and an untested CLI configuration/secrets/adapter lifecycle. It would also violate the current driver’s explicit fail-closed provider allowlist. TASK-143’s mandatory-budget factory remains ready for a future, named use case, but there is no legitimate production call site today.
- [2026-09-15T01:53:22Z] [CX9] Verification completed. `pnpm -r build` exited 0. `pnpm lint` exited 0 with three pre-existing unused-eslint-disable warnings in `packages/db/src/routines.ts`, `services/control-api/src/app.ts`, and `services/worker/src/routineTool.ts`. Direct `pnpm -r test` did not pass because its pre-existing shared database lacks migration 024's `role_routines.timezone`; it failed 10 DB cases on that missing column. The project-clean-schema command `powershell -ExecutionPolicy Bypass -File scripts/test-isolated.ps1 -Init -Filter @oikonomos/worker` recreated `oikonomos_test`, applied migrations 001 through 025 including 024, and passed all worker files, including `subprocessProviders.test.ts` (13/13) and `test/executeRun.test.ts` (9/9). No TASK-164 code change was needed or made.

## Follow-up trigger

Create a new implementation task only after a product decision explicitly names a Codex or Grok role/routine use case and specifies: provider selection scope (role versus routine), CLI credential/configuration ownership, session/resume semantics, tool-enforcement parity/liveness tests, and an end-to-end production execution test. That task should own the driver, its tests, and any necessary provider contract/configuration paths; its call site must supply `budget: { db, runId, routineId }` to `createGatedSubprocessProviders` and must not use `unsafeAllowUnbudgeted`.

## Verification

Pending: full repository test, build, and lint commands.
