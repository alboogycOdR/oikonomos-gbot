# TASK-197 — Broker HTTP surface for sandboxed enforcement

## Work Log

- [2026-09-06T14:20:00Z] [CX] Resumed the dispatcher-claimed task on `task/TASK-197-cx`; no prior dossier existed and `Review_Findings` is empty. Synced the branch to current integration state and read ADR-015 §Decision §§1–2, Fable changes 2/5, ADR-001 R3, ADR-004, and the live broker/control-api composition. Preflight completed verbatim: `[preflight] TASK-197 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex-GROKBOT-CLONE`; `[preflight] 6 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.`; `NEW    services/control-api/src/brokerHttpRoute.ts  -> does not exist; parent services/control-api/src/ exists`; `NEW    services/control-api/src/brokerHttpRoute.test.ts  -> does not exist; parent services/control-api/src/ exists`; `NEW    services/control-api/src/brokerToken.ts  -> does not exist; parent services/control-api/src/ exists`; `NEW    services/control-api/src/brokerToken.test.ts  -> does not exist; parent services/control-api/src/ exists`; `FILE   services/control-api/src/index.ts  -> exists, 36 line(s), 1745 bytes`; `FILE   services/control-api/package.json  -> exists, 40 line(s), 871 bytes`. Implementing the task-owned isolated listener, signed per-turn binding, body/rate bounds, and defensive timeout next.
- [2026-09-06T15:36:00Z] [CX] Implemented the distinct broker Fastify application and `BROKER_PORT` startup listener; it uses real `handlePreToolUse`, directly imported broker contracts, a token-derived immutable identity binding, explicit body/rate bounds, an 9-second defensive timeout mapped to `BrokerFailure` taxonomy, and a fail-closed production DB composition. Added route and token tests covering a real TCP round-trip through the broker handler, mismatch/expiry/auth isolation, limits, and hung calls. Passing: `pnpm --filter @oikonomos/control-api exec vitest run src/brokerToken.test.ts src/brokerHttpRoute.test.ts --config ../../packages/shared/vitest.config.ts --root .` (9/9); `pnpm --filter @oikonomos/control-api typecheck`; `pnpm --filter @oikonomos/control-api build`; `pnpm lint`; `node infra/ci/test-banned-modes.mjs`; `node infra/ci/banned-modes.mjs`. The full control-api suite exercised 206/208 tests including its existing live-Postgres coverage, but two pre-existing group-thread integration tests failed after the suite exhausted PostgreSQL clients (`sorry, too many clients already`, followed by an expected 201/received 400 fixture failure). The focused TASK-197 suite is green. `pnpm -r build` progressed through every dependency and worker before the terminal's 30-second command-output ceiling cut it while compiling control-api; its affected package build is green independently.
