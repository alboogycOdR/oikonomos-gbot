# TASK-241 — Workspace-1 — dashboard Google sign-in via Firebase web SDK against POST /auth/google (shared web/mobile identity)

**Unit:** S5 · **Priority:** medium · **Depends_On:** TASK-239

## Brief
The server side exists (`POST /auth/google` verifies a Firebase ID token and mints a UID-tenant session, `app.ts:947-961`, TASK-172); the dashboard has zero Firebase code and logs in with the shared `CONTROL_API_TOKEN`, landing in the `basileia` service tenant — so web and Android see different data. Add the Firebase web SDK (pin exact version), a `lib/firebase.ts` initialised from `VITE_FIREBASE_*` env (public web config only — no server secrets), a "Sign in with Google" button on `LoginPage` that obtains the ID token and calls a new `loginWithGoogle(idToken)` in `api.ts`. Keep the token login available behind an "operator" toggle. `AuthContext` from TASK-239 already bootstraps from `/auth/me`. This task alone may touch `pnpm-lock.yaml` and `apps/dashboard/package.json` in this wave. Acceptance case A20 becomes runnable when this merges.

## Spec pointers
specs/OIKONOMOS_WORKSPACE_WAVE_v1.0.md §3.3; TASK-172 and TASK-173 Progress_Notes; docs/runbooks/firebase-android-setup.md

Read `specs/OIKONOMOS_WORKSPACE_WAVE_v1.0.md` §1 first for the product shape and §10 for what must not be claimed. The independent review that produced this wave (verdict, disposition matrix, execution proposal) is at `E:\DELL-PROJECTS\GROKBOT-RESEARCH-DOCS\ORCH_REVIEW\` — read the rows cited in Spec_References; do not treat the advisory documents themselves as spec.

## Intended approach
The server side exists (`POST /auth/google` verifies a Firebase ID token and mints a UID-tenant session, `app.ts:947-961`, TASK-172); the dashboard has zero Firebase code and logs in with the shared `CONTROL_API_TOKEN`, landing in the `basileia` service tenant — so web and Android see different data. Add the Firebase web SDK (pin exact version), a `lib/firebase.ts` initialised from `VITE_FIREBASE_*` env (public web config only — no server secrets), a "Sign in with Google" button on `LoginPage` that obtains the ID token and calls a new `loginWithGoogle(idToken)` in `api.ts`. Keep the token login available behind an "operator" toggle. `AuthContext` from TASK-239 already bootstraps from `/auth/me`. This task alone may touch `pnpm-lock.yaml` and `apps/dashboard/package.json` in this wave. Acceptance case A20 becomes runnable when this merges.

## Owned_Paths
apps/dashboard/src/lib/AuthContext.tsx, apps/dashboard/src/pages/LoginPage.tsx, apps/dashboard/src/pages/LoginPage.test.tsx, apps/dashboard/src/lib/firebase.ts, apps/dashboard/src/lib/firebase.test.ts, apps/dashboard/src/lib/api.ts, apps/dashboard/package.json, pnpm-lock.yaml, apps/dashboard/.env.example

## Work Log

- [2026-09-12T12:20:00Z] [S5] Session start. Note on setup: worktree's local `PLAN.md`/branch were initially stale (created before the dispatcher's claim commit `3e9a0f6 chore(plan): claim TASK-241 [SV origin=S5]` had propagated here) — `git fetch mainco` + `git reset --hard mainco/master` on `task/TASK-241-s5` fixed it; PLAN.md now correctly shows `Status: claimed`. Preflight (`python scripts/preflight_paths.py TASK-241`) c8b9872 evidence:
  ```
  [preflight] TASK-241 Owned_Paths inspected in E:/DELL-PROJECTS/wt-s5-GROKBOT-CLONE
  [preflight] 9 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.
    FILE   apps/dashboard/src/lib/AuthContext.tsx  -> exists, 97 line(s), 3856 bytes
    FILE   apps/dashboard/src/pages/LoginPage.tsx  -> exists, 82 line(s), 3032 bytes
    FILE   apps/dashboard/src/pages/LoginPage.test.tsx  -> exists, 129 line(s), 5014 bytes
    NEW    apps/dashboard/src/lib/firebase.ts  -> does not exist; parent apps/dashboard/src/lib/ exists
    NEW    apps/dashboard/src/lib/firebase.test.ts  -> does not exist; parent apps/dashboard/src/lib/ exists
    FILE   apps/dashboard/src/lib/api.ts  -> exists, 436 line(s), 14680 bytes
    FILE   apps/dashboard/package.json  -> exists, 37 line(s), 966 bytes
    FILE   pnpm-lock.yaml  -> exists, 5138 line(s), 171009 bytes
    NEW    apps/dashboard/.env.example  -> does not exist; parent apps/dashboard/ exists
  ```
  Read server-side `POST /auth/google` (`services/control-api/src/app.ts:967-978`): body `{idToken}`, on success sets session cookie + `{authenticated:true}` (200), on failure `401 {error:"invalid or expired Firebase ID token"}`. Read TASK-239's `AuthContext.tsx`/`LoginPage.tsx`/`api.ts` (bootstrap via `/auth/me`, in-memory-only auth state) and TASK-172/173's dossier notes (Firebase project `basileia-oikonomos-gmail`, project number `461377597606`; web OAuth client id noted there is Android's `serverClientId`, not directly needed for the web SDK's own popup flow). `docs/runbooks/firebase-android-setup.md` documents only the Android/Flutter side — no web SDK env vars exist there yet; that runbook is `docs/**`, outside my territory, so I cannot add the `VITE_FIREBASE_*` note AC asks it to carry. Flagging for ORCH below rather than reaching out of territory. Plan: `firebase` npm package (pinned exact version, confirmed current stable via `npm view firebase version` = 12.19.0) → `lib/firebase.ts` (lazy-init app/auth from `VITE_FIREBASE_*` env, `signInWithGooglePopup()`), `api.ts` gains `loginWithGoogle(idToken)`, `AuthContext` gains a `loginWithGoogle` action, `LoginPage` defaults to a "Sign in with Google" button with an "operator" toggle revealing the existing token form.

- [2026-09-12T12:45:00Z] [S5] Implementation complete, committed `d679820 [TASK-241]`. Details and two deliberate deviations from the literal Description wording, both forced by needing to keep files outside this task's `Owned_Paths` (`apps/dashboard/src/App.test.tsx` owned by TASK-236, `apps/dashboard/src/pages/ChatPage.test.tsx` owned by TASK-236/239) green without touching them:

  1. **Button label is "Continue with Google", not "Sign in with Google"** (the Description's literal quoted phrase). `App.test.tsx`'s existing `screen.getByRole("button", { name: /sign in/i })` is a broad regex that, once a second button whose accessible name contains "sign in" exists on the page, resolves to *two* matches and throws `getMultipleElementsFoundError` — a real ambiguity, not a flake. Renaming only the new Google button's own label removes the collision with zero edits to `App.test.tsx`. No AC or spec line pins the exact string "Sign in with Google" (§3.3 only says "authenticates through the Firebase web SDK"), so this is a safe, minimal accommodation, not a scope cut.
  2. **The operator-token form's toggle starts open (shown), not closed**, i.e. it does not literally hide the token login "behind" the toggle by default as the Description's phrasing suggests. `App.test.tsx` (`redirects an unauthenticated visit to /runs to the login screen`, `logs in against POST /auth/login...`) and `ChatPage.test.tsx` (`bootstrap-unauthenticated: no session cookie shows the login screen, never the workspace`) all call `screen.getByLabelText(/access token/i)` synchronously, with no click, immediately after an unauthenticated bootstrap resolves — they assume the token field is unconditionally present. Defaulting the toggle closed breaks all three, in two files outside this task's `Owned_Paths`. The toggle itself is real and works (`Hide operator token login` / `Sign in with an operator token instead`, covered by `LoginPage.test.tsx`'s first case) — it just doesn't start collapsed. Recommend a fast, low-risk follow-up task (Owned_Paths: `App.test.tsx`, `ChatPage.test.tsx` only) to update those three assertions to open the toggle first, after which this toggle's default can flip to closed — named here rather than silently worked around or silently left broken.

  Also flagging, not fixed (both genuinely outside `Owned_Paths`, both already noted in the preflight entry above): (a) `docs/runbooks/firebase-android-setup.md` (protected, `docs/**`) does not yet name the `VITE_FIREBASE_*` variables the AC asks the runbook to carry — `apps/dashboard/.env.example` (in territory) documents them with comments instead; ORCH/a docs-owning task should extend the runbook to cover the web SDK. (b) Real Firebase Console web-app registration (getting actual `VITE_FIREBASE_*` values for `basileia-oikonomos-gmail`) was not performed this session — no Firebase CLI credentials available here; the code correctly fails closed with a specific `FirebaseConfigError` when those env vars are absent, tested in `firebase.test.ts`, rather than silently no-op'ing.

  **Test evidence:**
  - `pnpm --filter @oikonomos/dashboard typecheck` — exit 0.
  - `pnpm --filter @oikonomos/dashboard build` — exit 0 (`tsc && vite build`, dist emitted).
  - `pnpm --filter @oikonomos/dashboard test` — **21/21 test files, 124/124 tests passed** (includes new `firebase.test.ts` 5/5, rewritten `LoginPage.test.tsx` 8/8 covering both the Google path with a fake ID-token provider and the operator-token path).
  - `pnpm --filter '!@oikonomos/control-api' -r test` (full recursive suite minus one package, per CLAUDE.md's amendment to always run the full suite, never just the task's own package) — **exit 0, all 18 filtered workspaces green** (services/worker 245/245, evals/harness 19/19, etc., confirmed in the run's tail output).
  - `pnpm -r typecheck` **without** the `control-api` exclusion surfaces a **pre-existing, unrelated failure**: `services/control-api/src/ports.ts`/`ports.test.ts` reference `createRunGate`/`QueuedRun`/`RunGate` (from `@oikonomos/worker`) and `listWorkspaceSummary`/`WorkspaceSummary` (from `@oikonomos/db`) that don't exist on `master` yet. Confirmed genuinely pre-existing and unrelated to this task, not a regression I introduced: reproduced identically after `git stash`-ing every one of this task's own changes and re-running `pnpm --filter @oikonomos/control-api typecheck` on a clean tree (same 8 errors, same lines), then restored my stash (`git stash apply <sha>` + `git stash drop <sha>`, by exact hash per the shared-stash-stack protocol — verified `git stash list` afterward showed only the unrelated `TASK-177` entry). `services/control-api` is TASK-242's (CX9) active territory (`ports.ts` is in TASK-242's `Owned_Paths`, "claimed" per PLAN.md) — this is very likely CX9's in-flight `TASK-242` work mid-implementation on shared `master`, not something for this task to touch or fix.
  - `git diff main...task/TASK-241-s5 --stat` — only files under this task's `Owned_Paths` (`apps/dashboard/package.json`, `apps/dashboard/src/lib/AuthContext.tsx`, `apps/dashboard/src/lib/api.ts`, `apps/dashboard/src/lib/firebase.ts`, `apps/dashboard/src/lib/firebase.test.ts`, `apps/dashboard/src/pages/LoginPage.tsx`, `apps/dashboard/src/pages/LoginPage.test.tsx`, `apps/dashboard/.env.example`, `pnpm-lock.yaml`) plus this dossier — no `App.test.tsx`/`ChatPage.test.tsx`/`vite-env.d.ts` touched, confirmed by preflight + this diff.

  Acceptance criteria: AC1/AC2 (same tenant for web+Android via a real UID) and AC3 (real Android companion pairing) cannot be end-to-end verified this session — no live Firebase web-app credentials or a paired Android device/build available here; the server-side contract (TASK-172) and this client's exchange of it are implemented and unit-tested for real, but the cross-device claim itself is honestly unverified, not fabricated. AC4 (LoginPage.test.tsx covers both paths) — met. AC5 (dashboard test/typecheck/build green; lockfile change limited to the new dependency) — met for the dashboard package itself and for the rest of the monorepo (the one exception, `control-api`, is pre-existing and unrelated, evidenced above); lockfile diff is additive dependency resolution for `firebase` only.
