# TASK-323 dossier

**Brief:** Expire unanswered human-takeover and secret requests (never while a human holds control)

**Source:** OpenBot (CopilotKit, MIT) comparison of 2026-09-21. Borrow the idea, never the code.

**Approach:** see the task Description and Acceptance_Criteria in PLAN.md. Read our own code first and verify every UNVERIFIED or REPORTED claim.

## Work Log

- [2026-09-21T12:18:00Z] [CX9] Preflight completed before edits:
  ```text
  [preflight] TASK-323 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE
  [preflight] 4 entr(y/ies). FILE/DIR/GLOB = exists, NEW = you are creating it.
    FILE   services/worker/src/takeover.ts  -> exists, 127 line(s), 6219 bytes
    FILE   services/worker/src/takeover.test.ts  -> exists, 147 line(s), 7235 bytes
    FILE   packages/db/src/secretRequests.ts  -> exists, 164 line(s), 7326 bytes
    FILE   packages/db/src/secretRequests.test.ts  -> exists, 53 line(s), 3135 bytes
  [preflight] Paste this output into your first Progress_Note as the c8b9872 filesystem check.
  ```

- [2026-09-21T12:18:00Z] [CX9] BLOCKED — no expiry sweep exists. `services/worker/src/takeover.ts:62-84` only derives pending state from the audit trail; it has no timer/caller. `packages/db/src/secretRequests.ts:5, 69-164` supports only `pending`, `fulfilled`, and `declined`, with no expiry accessor. `infra/postgres/migrations/017_secret_requests.up.sql:9,13-14` constrains `secret_requests.status` to those same three values, so an `expired` transition cannot be persisted without a new migration outside `Owned_Paths`. An unattended expiry also requires a production caller: `services/worker/src/main.ts:77-162` is the only worker boot composition and existing interval lifecycle, while `services/worker/src/jobs/workerJobQueue.ts` is the explicit queue-registration territory excluded by AC6. Calling a new sweep from `getTakeoverState` or `completeTakeover` would not expire an ignored request and would violate the requested 10-minute guarantee. Required territory expansion: a new `infra/postgres/migrations/033_secret_request_expiry.{up,down}.sql` (or equivalent migration) plus `services/worker/src/main.ts` for a safe interval/scheduler lifecycle; if the intended design is pg-boss, additionally `services/worker/src/jobs/workerJobQueue.ts` (which AC6 expressly says must cause an ownership-conflict stop). Once supplied, implement one atomic sweep that marks stale pending secret requests expired; fails still-unclaimed takeover/secret-request runs with the TASK-316 visible-message path; records a reason-only audit event; and excludes any takeover with a later `run.human_takeover_completed` event.

- [2026-09-21T21:17:49Z] [CX9] Resumed after ORCH widened TASK-323 to include migration 033 and `main.ts`; fetched and merged current integration base, then re-ran preflight. It now confirms the six original files plus NEW `infra/postgres/migrations/033_secret_request_expiry.{up,down}.sql` and existing `services/worker/src/main.{ts,test.ts}`. No expiry sweep exists: `takeover.ts` only derives takeover state; `secretRequests.ts` supports only pending/fulfilled/declined; `main.ts` owns the existing unattended interval lifecycle. A new blocking ownership gap remains: `services/worker/src/main.ts` can import database APIs only from `@oikonomos/db`, whose package export is solely `packages/db/src/index.ts`; the new `expirePendingSecretRequests` function must be exported there to be callable from the required worker caller. `packages/db/src/index.ts` is not in Owned_Paths. Importing `packages/db/src/secretRequests.ts` directly is impossible under `@oikonomos/db`'s package `exports` map and bypassing the package boundary with a new raw `pg.Pool` in the worker would duplicate database access policy. Required expansion: `packages/db/src/index.ts` only. No code changes made beyond this dossier heartbeat.

- [2026-09-21T21:32:00Z] [CX9] Re-verified the resumed branch and live source before implementation. `services/worker/src/takeover.ts:62-84` derives whether a takeover remains pending entirely from audit events, and `packages/db/src/secretRequests.ts:5-164` has no expiry operation. `services/worker/src/main.ts:8,76-186` composes and owns the existing worker lifecycle but imports DB APIs exclusively from `@oikonomos/db`; `packages/db/package.json` exports only the root entry point, and `packages/db/src/index.ts:252-266` is the sole barrel for secret-request APIs. Thus a correctly encapsulated `expirePendingSecretRequests` implementation in the owned `secretRequests.ts` cannot be called by the required owned `main.ts` caller without adding its export to the unowned barrel. Direct subpath imports are rejected by the package exports map; creating a raw worker pool would bypass the DB access boundary. `CLAUDE.md:12` confirms expiry must fail rather than resume a CAPTCHA/MFA run. No REWORK finding is present. Blocked pending the minimal ownership expansion of `packages/db/src/index.ts`; no tests can meaningfully run before the sweep is exportable and callable.

- [2026-09-22T00:25:00Z] [CX9] Implemented the owned expiry path. There was no pre-existing expiry sweep: takeover state was audit-derived only (`takeover.ts:62-84`) and secret requests had only pending/fulfilled/declined transitions (`secretRequests.ts`). Migration 033 admits `expired`; `expirePendingSecretRequests` atomically selects only stale pending requests whose run is still `waiting_approval`; `listExpiredTakeovers` excludes a completed human hand-back. `main.ts` owns one unref'd, non-overlapping, immediate-and-interval sweep (default 10 minutes, configurable `OIK_HUMAN_REQUEST_EXPIRY_MS` / `OIK_HUMAN_REQUEST_SWEEP_INTERVAL_MS`). It fails—never resumes—the parked run, writes `run.human_request_expired` with kind/reason only, and posts a visible system message. `pnpm --filter @oikonomos/db build`, worker typecheck, and `git diff --check` pass. Isolated DB suite passes 290 tests/2 skipped after the migration; an initial run exposed and then verified the fixed SQL ambiguity. Worker isolated suite is still draining a pre-existing long run/lock, so its final classification remains pending.

- [2026-09-21T22:43:55Z] [CX9] Resumed-test verification: the real-worker liveness test initially had an invalid synthetic `threadId`, so expiry correctly failed/audited but its visible-message insert met the messages FK and the assertion failed. The test fixture now creates the actual inbox-triage thread before creating its task execution (`15052b2`); production code is unchanged. `pnpm --filter @oikonomos/db build`, `pnpm --filter @oikonomos/worker typecheck`, ESLint on all touched TypeScript, and both diff checks pass. `scripts/test-isolated.ps1 -Init -Filter @oikonomos/worker` reached the task test and confirmed it passes (real worker sweep: failed run, expiry audit, persisted visible message). The package remains red only on pre-existing environment/baseline defects: depleted Gemini prepaid credits (HTTP 402), provider-dependent 5 s timeouts, `email.send` capability enabled-state drift, role-message cleanup FK, and the unrelated worker re-drive test. Next: run the required full isolated recursive suite and classify it against this same baseline.

- [2026-09-21T22:48:00Z] [CX9] Full isolated recursive verification completed: `powershell -ExecutionPolicy Bypass -File scripts/test-isolated.ps1 -Init`, then the same command without `-Init`. Migration 033 applied cleanly. The full run is red in 4 packages, all outside TASK-323: DB cleanup deadlock/thread-members FK; evals harness and worker stale `task-119.grants-suite.capability-a` registry row (with cascading non-task failures); worker's unrelated interrupted-run re-drive assertion; and control-api's FreeLLMAPI route returning 400 rather than 201. Crucially, `services/worker/src/main.test.ts` reports the TASK-323 real-worker expiry liveness test passing in 2.473s: it starts the real sweep, fails the parked run, emits `run.human_request_expired`, and persists the visible message. The corresponding DB test passed in the full run as 3 `secretRequests` tests. No task-caused regression appeared.

- [2026-09-22T03:14:00Z] [CX9] REWORK addressed. The real-worker ADR-005 test had polled `runs.status === "failed"`, but production intentionally transitions the run before awaiting its audit/message writes. It now polls the required `run.human_request_expired` audit evidence (kind `secret_request`) before asserting the failed run and visible message, eliminating the observation race without changing production ordering. `pnpm --filter @oikonomos/worker typecheck`, ESLint for `main.test.ts`, and `git diff --check` pass. Fresh `scripts/test-isolated.ps1 -Init -Filter @oikonomos/worker` passed the expiry test; fresh full `scripts/test-isolated.ps1` reports it passing in 2.408s. The full recursive run remains red only in prior environmental/baseline classes: DB cleanup FK/deadlock, evals stale capability registry, worker provider budget exhaustion/email capability drift/queue timing plus unrelated re-drive, and control-api FreeLLMAPI 400-vs-201. No TASK-323 regression was observed.
