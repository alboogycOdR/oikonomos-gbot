# TASK-186 — G-06 — Browser lane v1: Steel Browser inside the role sandbox, bot-private profile, governed `browser.*` capability family (protected paths)

## Brief

Nothing in the product can click a website today. Build the image `office-browser` (Playwright/Chromium + Steel Browser, Xvfb screen, execd-compatible entrypoint) and a manifest `steel-browser.json` declaring the `browser.*` tools (navigate/read/click/type/screenshot) with default tiers per ADR-010's tier map (read-only T0/T1; writes T2; anything on a payment/login page escalates to human takeover). Profile directory is per role (`bot_private` — deliberately stricter than Grok Bot's user-shared profile) and lives under the D3 sealed root so the secretPathGuard (TASK-088/093) denies any `/command` or file-tool access to it. Steel's stealth/anti-detection features are OFF; a CAPTCHA/2FA/login-wall detection emits `human_takeover_required` (consumed by TASK-188) and never attempts a solve. The Steel session URL is what TASK-171's live view embeds. Application-layer egress allowlist in Steel session config stays as defence in depth beneath TASK-185's network layer (R15).

## Spec pointers

specs/OIKONOMOS_GROKBOT_PARITY_DISPOSITION_v1.0.md §3 G-06 (AC anchors: profile cookies unreadable from a /command run — observed denial; CAPTCHA raises human_takeover_required, no solve attempt); ADR-006 Addendum B (Steel inside OpenSandbox; OIK-076/077 narrowing); ADR-010 Amendment + Addendum F N13 (browser credentials inaccessible by construction); CLAUDE.md NN#6 (stealth off, no circumvention); report §12.5(d), §12.7; ADR-013 (manifest declares tiers). PROTECTED PATHS packages/connectors/manifests/**, packages/harness-factory/** — author CX, reviewer ORCH opus-4-8.

## Territory

packages/connectors/manifests/steel-browser.json, infra/sandbox/images/office-browser/**, packages/harness-factory/src/browserLane.ts, packages/harness-factory/src/browserLane.test.ts, packages/connectors/src/steelSession.ts, packages/connectors/src/steelSession.test.ts

Depends_On: TASK-170, TASK-185

## Intended approach

Read the Spec pointers first, then the existing files named in Territory (run the preflight and paste it into the first Progress_Note). Match surrounding conventions exactly — packages/db follows routines.ts; control-api routes follow the chat routes + openapi.ts; mobile follows the TASK-157/168 visual bar. Every acceptance criterion maps to a spec sentence; test the criterion, not the summary. Anything outside Territory is a block, not an edit.

## Work Log

- [2026-09-07T02:35:00Z] [CX9] Resumed on `task/TASK-186-cx9`; no `REWORK` finding. Preflight output: `packages/connectors/manifests/steel-browser.json` NEW (parent exists); `infra/sandbox/images/office-browser/**` GLOB matches nothing (new territory); `packages/harness-factory/src/browserLane.ts` NEW (parent exists); `packages/harness-factory/src/browserLane.test.ts` NEW (parent exists); `packages/connectors/src/steelSession.ts` NEW (parent exists); `packages/connectors/src/steelSession.test.ts` NEW (parent exists). Read TASK-186 spec pointers and live surrounding code; investigating the existing sandbox lifecycle and sealed-root integration before implementation.
- [2026-09-07T00:12:24Z] [CX9] Blocked before source changes: `packages/connectors/src/manifest/scan.ts:10` deliberately accepts only `.yaml`/`.yml`, so the explicitly granted/requested `packages/connectors/manifests/steel-browser.json` is invisible to `scanManifests`/`loadManifests` and cannot register (AC1). Production browser execution also requires changes outside territory in `services/worker/src/chatRunDriver.ts`: it hardcodes the `office-base` image (line 93/379), OpenSandbox entrypoint (392), and command allowlist `Bash Read` (459), so none of the owned new modules/image can be selected, supplied a per-role sealed profile, surfaced to the live viewer, or cause the CAPTCHA takeover event. The requested observed sealed-path denial/audit is enforced by the existing `packages/broker/src/secretPathGuard.ts`, also outside territory; adding a parallel local guard would not prove production enforcement. Required widened paths: `packages/connectors/src/manifest/scan.ts` (or change the manifest target to `.yaml`), plus the production browser integration owner must explicitly grant the relevant worker/registry/broker paths. No uncallable implementation was added.
- [2026-09-07T00:12:24Z] [CX9] Commit attempt `git commit -m "docs(task): record TASK-186 scope blockers [TASK-186]"` was rejected by the required `territory-precommit` hook before a commit was created: `DEVTEAM_UNIT='CX' but this worktree belongs to CX9`. The dossier remains staged and uncommitted; `--no-verify` is explicitly prohibited. Supervisor must align the dispatched unit identity/registry before this task can produce any commit.
- [2026-09-07T02:42:00Z] [CX9] BLOCKED — the required `packages/connectors/manifests/steel-browser.json` cannot validate or register: live `packages/connectors/src/manifest/scan.ts` accepts only `.yaml`/`.yml`, so it ignores the mandated `.json` file. Registration and the C5 tier-drift check are also outside this task's territory (`packages/connectors/src/manifest/scan.ts`, `packages/connectors/src/manifest/load.test.ts`, `packages/connectors/src/registration/**`, and the broker registry). Separately, live sandbox creation is hard-coded in out-of-territory `services/worker/src/chatRunDriver.ts` to `oikonomos-office-base` with entrypoint `/opt/oikonomos/egress-entrypoint.mjs`; no owned path can select `office-browser`, start Steel, expose its session viewer URL, or create/mount the sealed `/oikonomos/secrets/...` profile. ADR-006 R14 also requires a reviewed immutable Steel image/version pin, but TASK-186 specifies none and the repository has no installed Steel/Playwright dependency. A self-contained image or helper under the current grant would be inert and would not satisfy any acceptance criterion. Requested resolution: widen territory to the named manifest/registration/worker lifecycle surfaces and specify the approved pinned Steel image/version (or revise the required manifest extension and integration design).
- [2026-09-07T02:44:00Z] [CX9] Dossier-only commit was correctly rejected by `territory-precommit`: `DEVTEAM_UNIT='CX'` but this worktree belongs to `CX9`. Did not bypass the hook or alter environment identity. The dossier remains staged and must be committed after the dispatcher/control identity is corrected.
- [2026-09-07T03:05:00Z] [CX9] Re-ran preflight after ORCH's scope correction: `steel-browser.yaml`, office-browser image glob, browserLane and steelSession modules are new; worker driver and its tests exist. Verified live: `docker manifest inspect ghcr.io/steel-dev/steel-browser:latest` reports immutable linux/amd64 digest `sha256:54750fc5deb3102b5f8cc844abfdc7ec46298addfdc5dd5fb40a8382b02a5819`; `git ls-remote https://github.com/steel-dev/steel-mcp-server.git` reports signed release tag `v2.0.1` peeled at `4c0c583a3704ea3605fe3c72d7d0e9a9b1b94994`. The worker can now select a different image, but no owned/in-scope implementation surface can make a browser capability callable: `connectorResolution.ts` only resolves Gmail/Calendar/Drive/Workspace and is outside territory; `chatRunDriver.ts` receives only their composed contexts, so `browser.*` tools never reach `mountedToolNames`, an MCP server config, or the CLI's allowed tools. No existing `human_takeover_required` event contract/persistence exists either; emitting one requires its owning event/park integration surface, outside territory. Building the manifest/image/helpers would therefore be inert and fail AC1 and AC3. BLOCKED pending an explicit territory widening for the generic browser connector-resolution/mount surface and the takeover-event contract/parking surface (or a revised integration design naming those owners).
