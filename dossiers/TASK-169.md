# TASK-169 — OpenSandbox Wave 2 execd client

## Work Log

- [2026-09-05T21:20:00Z] [CX9] Preflight completed before code changes: `[preflight] TASK-169 Owned_Paths inspected in E:/DELL-PROJECTS/wt-codex9-GROKBOT-CLONE`; `GLOB packages/sandbox-client/src/** -> 5 file(s): client.ts, errors.ts, index.ts, secretResolver.ts, types.ts`. Read the live coordination-plan task block and the updated Resolution section from the primary checkout (the worktree copy of that document is stale because the primary checkout has the document modification uncommitted). Blocked before implementation: Owned_Paths permits only `packages/sandbox-client/src/**`, but required unit and gated live-test changes must be made in existing `packages/sandbox-client/test/sandboxClient.test.ts` and `packages/sandbox-client/test/sandboxClient.integration.test.ts`. The requested `getEndpoint`, `ping`, and `runCommand` APIs do not yet exist, so existing tests cannot cover their request/SSE behavior. Please widen Owned_Paths to include `packages/sandbox-client/test/**`; no source or test files changed.
- [2026-09-05T21:27:00Z] [CX9] Implemented the execd client slice: lifecycle endpoint resolution defaults to `use_server_proxy=true`; `createSandbox` resolves the `secret://opensandbox/execd_access_token` value and injects it as `EXECD_ACCESS_TOKEN`; every execd ping/command request resolves and sends that token as `X-EXECD-ACCESS-TOKEN`; and `/command` consumes fragmented SSE JSON into stdout, stderr, and exit code. Added fake-transport request-shape/SSE/credential tests plus a gated live test that creates an Alpine sandbox, resolves port 44772, pings it, proves an otherwise authenticated no-execd-token request is 401, and runs `echo hello`. Verified locally: package typecheck; package tests 21 passed / 2 live skipped; `pnpm -r test`, `pnpm -r build`, and `pnpm lint` completed successfully before the terminal host later failed. Live proof remains blocked because `SANDBOX_INTEGRATION_URL`, `OIK_SECRET_OPENSANDBOX_API_KEY`, and `OIK_SECRET_OPENSANDBOX_EXECD_ACCESS_TOKEN` are all absent. The terminal host subsequently returned Windows process error `0xC0000142`, preventing the required final git status/commit verification in this session.
- [2026-09-05T21:53:00Z] [CX9] Resumed verification from the existing task branch. Re-read the architect-reviewed resolution and the current upstream execd contract, then ran `pnpm --filter @oikonomos/sandbox-client typecheck` (pass), `pnpm --filter @oikonomos/sandbox-client test` (21 passed, 2 live tests skipped), `pnpm -r test`, `pnpm -r build`, and `pnpm lint` (all exit 0). Confirmed without printing values that `SANDBOX_INTEGRATION_URL`, `OIK_SECRET_OPENSANDBOX_API_KEY`, and `OIK_SECRET_OPENSANDBOX_EXECD_ACCESS_TOKEN` are all unavailable, so the required live create/endpoint/ping/auth-rejection/echo proof cannot run. `use_server_proxy=true` is asserted in the fake-transport test, but cannot be empirically confirmed until those settings are supplied. Code is committed for supervisor review; resume by exporting the three settings and running the gated package integration test.
