# TASK-060 - Vertical-slice demo wiring + runbook (ORCH-executed)

## Brief
ORCH-executed (`docs/**` is outside builder territory). Once both lanes land, run the full loop on real infrastructure and write the runbook.

## Closes only when the loop has actually been run
Demonstrate-not-assert, per the TASK-038 precedent - three real defects there surfaced *only* by running the drill. Target: task in via Telegram, governed run, Tier-3 parks, approve on phone, action completes, evidence retrievable, plus a denied Tier-3 attempt in the same audit trail.

## Work Log
