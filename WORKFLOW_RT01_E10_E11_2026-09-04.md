# Workflow — RT-01 + E11 (Routines/Scheduling/Budgets) + E10 (OME/Handoffs)

**Date:** 2026-09-04 · **Kickoff:** started early, 15:40 SAST · **Builders:** CX (Codex), CX9 (Codex, second login), S5 (Claude Sonnet 5)

**Readiness:** both Codex accounts confirmed logged in and CX9 activated (verified directly, not just taken on the browser's word — `codex login status` on both, plus a byte-diff confirming the two `auth.json` files genuinely differ). All three builders dispatchable.

CX9 is treated as a full peer to CX for task assignment, including protected-path work (`packages/broker`/`policy`/`approvals`/`harness-factory`) — it's the same Codex model family, just a second account, so it satisfies the "different model than the ORCH/S5 reviewer" requirement exactly like CX does.

## Before sequencing: what's already built

Investigated the codebase before planning (same discipline as Connectors-1, which found the Gmail OAuth/MCP plumbing already built and just unwired). Same pattern here — **substantially more exists than the WBS labels suggest:**

| Already built (schema/DB layer, tested, but not wired to a live tool/endpoint) | File |
|---|---|
| `role_routines` table — schedule, lane, `definition jsonb`, `last_fire_at`/`next_fire_at`/`last_fire_status` | `infra/postgres/migrations/004_roles_routines_rules.up.sql` |
| `routines.ts` — `createRoutine`, `getRoutine`, `listRoutines`, `recordRoutineFire` | `packages/db/src/routines.ts` |
| In-process scheduler policy — lanes, watchdog/priority interruption, `RoutineFire`/`RoutineFirePorts` contracts | `services/worker/src/scheduler/scheduler.ts` |
| `role_messages` table — typed handoffs, `from_role_id`/`to_role_id`, `workspace_refs jsonb` | same migration |
| `roleMessages.ts` — `sendRoleMessage`, `listRoleMessages`, `markRoleMessageRead`, `HandoffKind` (`research.complete`, `draft.ready_for_review`), `HandoffFactReference` | `packages/db/src/roleMessages.ts` |
| `mailbox.ts` — real `send_to_role` handoff built on the above, "no privilege expansion" already designed in (F8/R13), fully tested | `services/workspace/src/mailbox.ts` |
| Routines tab in the live chat UI (read-only) | `apps/dashboard/src/components/chat/RightPanel.tsx` |

**What's confirmed NOT wired (the real gaps, same shape as Gmail before Connectors-1):**
- Nothing calls `createRoutine`/`listRoutines` from `services/worker` or `apps/dashboard` — `ChatPage.tsx` passes `routines={[]}` hardcoded.
- No cron/timer mechanism actually fires a routine — `scheduler.ts` defines the ports, nothing implements them.
- `mailbox.ts`'s `send_to_role` is not registered as an invokable tool anywhere in `packages/broker`/`chatRunDriver` — a bot cannot actually call it today.
- **`pg-boss` is not a dependency anywhere in the repo.** OIK-105 is genuinely greenfield — everything above either doesn't need it yet or has ports waiting for it.
- **`org_facts` does not exist.** OIK-098–101 (the fact-publishing/ACL side of OME, as opposed to the handoff-messaging side) is genuinely greenfield.

This means: **E11's routine story and E10's handoff story are more "wire it up" than "build it from scratch."** Budget the wave accordingly — don't re-estimate from the WBS's raw S/M/L labels without adjusting for this.

**Second, larger discovery, made while decomposing Wave 1 (15:40):** `docs/decisions/ADR-012-ome-extends-memory-not-parallel-store.md` (2026-09-02, outranks the WBS per CLAUDE.md's document precedence) already redirected the entire E10 fact-storage design away from a standalone `org_facts` table and into extending `packages/memory` — and that redirected work **is already built and tested**, not just planned:
- OIK-098/099/101 (schema, publish, versioning) → `profile_facts` already has `visible_to text[]` (ACL) and `superseded_by` (versioning), fully wired in `packages/memory/src/facts.ts` (write-time supersession chain, read-time `VISIBLE_TO_CALLER` SQL filter).
- OIK-100 (ACL read enforcement + required negative test) → `packages/memory/src/facts.test.ts`'s `"ACL and versioned fact history (TASK-098)"` suite already has the exact negative test ADR-012 calls for ("populated ACLs deny other roles"), plus agent-scope-owner-only-even-with-a-supplied-ACL.
- OIK-102's typed-handoff *shape* (fact references, not payload copies) → `packages/db/src/roleMessages.ts`'s `HandoffFactReference`/`handoffKinds`/`factRef` and `services/workspace/src/mailbox.ts`'s `sendToRole` (validates `handoffKind`+`factRef` supplied together) — both real, both explicitly commented `TASK-098`/`TASK-099`.
- OIK-102's pg-boss requirement → **explicitly NOT authorized by ADR-012** ("This ADR does not authorize a pg-boss migration"). `role_messages` stays the transport.

**Net effect: OIK-098/099/100/101 are DONE, not scheduled work.** What's left of E10 is much narrower than the WBS suggests: `sendToRole` is not registered as an invokable tool anywhere (confirmed — zero references in `packages/broker`/`chatRunDriver`), so no bot can actually call it today. That's the real remaining gap, plus the end-to-end demo and adversarial review ADR-012 §2 item 6 still calls for.

## Protected-path routing (binds who can author what)

I (ORCH) am Claude Sonnet 5 — the same model as S5. CLAUDE.md requires adversarial review by a *different* model on `packages/broker/**`, `packages/policy/**`, `packages/approvals/**`, `packages/harness-factory/**`. If S5 authors those, I cannot be the sole reviewer. ADR-012 §2 item 6 separately requires the same different-model rigor on the ACL/handoff model specifically, by policy rather than the letter of the protected-paths list. **Any task touching those goes to CX or CX9, never S5**, for this entire program:
- Registering `sendToRole` as an invokable broker tool → `packages/broker` → **CX/CX9 only**.
- OIK-103 (two-role handoff end-to-end, proves "no privilege expansion") → touches the same ACL/handoff surface → **CX/CX9 only**.
- Everything else below (DB layer, `services/worker` scheduler wiring, `services/control-api` endpoints, `apps/dashboard` UI) is unprotected — any of the three.

## Dependency graph

```
RT-01 (independent)

OIK-105 (pg-boss) ─┬─→ OIK-108 (routine spec, mostly wiring) ─→ OIK-109 (cron+NL scheduling)
                    ├─→ OIK-106 (durable resume: mid-run) ─→ OIK-107 (durable resume: mid-approval)
                    └─→ OIK-110 (per-routine budgets) ─→ OIK-111 (spend ceiling)

E10 remaining (OIK-098/099/100/101 DONE per ADR-012 — see above):
Register sendToRole as a broker tool (independent, ready now) ─→ OIK-103 (two-role handoff e2e) ─→ OIK-104 (Fable review)

OIK-112 (kill-switch drill) — no code dependency, S-sized, slot in opportunistically
OIK-113 (browser routine recording) — XL, needs its own decomposition pass before scheduling; not in this wave
OIK-114 — depends on OIK-113, not in this wave
```

## Wave plan (three parallel tracks: CX / CX9 / S5)

Three builders, and E10's rediscovered scope being much smaller, compresses this to 4 waves and covers the corrected RT-01+E10+E11 scope (everything except the explicitly-deferred OIK-113/114).

**Wave 1 — started early (15:40, both Codex logins confirmed), no shared files:**
| Track | Task | Why first |
|---|---|---|
| S5 | RT-01 — real-time push (SSE/WebSocket for messages + approvals) | Independent, user-visible, zero overlap with everything else this wave |
| CX | OIK-105 — pg-boss integration + job definitions | Single highest-leverage unlock — gates 3+ downstream E11 items |
| CX9 | Register `sendToRole` as a real, invokable broker tool | Independent, ready now (ADR-012's data layer is already built) — **protected-path, CX9 only** |

**Wave 2 — once OIK-105 / the broker-tool task land:**
| Track | Task | Notes |
|---|---|---|
| CX (frees from OIK-105) | OIK-108 — routine spec format | Mostly wiring `routines.ts`/`scheduler.ts`'s existing ports to pg-boss, not greenfield — re-estimate down from WBS's "M" |
| CX9 (frees from broker-tool task) | OIK-103 — two-role handoff end-to-end | Depends on the broker-tool registration; **protected-path** — proves "no privilege expansion" using ADR-012's already-built ACL/versioning/typed-handoff layer |
| S5 (frees from RT-01) | OIK-106 — durable resume: kill worker mid-run | Depends on OIK-105 only; unprotected (`services/worker`), fine for S5 |

**Wave 3:**
| Track | Task | Notes |
|---|---|---|
| CX | OIK-109 — routine scheduling (cron + NL description) | Depends on OIK-108; this is where "schedule a bot" becomes real and demoable |
| CX9 | OIK-110 — per-routine token/cost budgets via FreeLLMAPI routing | Depends on OIK-105 only |
| S5 | OIK-107 — durable resume: mid-approval-wait | Depends on OIK-106 |

**Wave 4:**
| Track | Task | Notes |
|---|---|---|
| CX9 | OIK-111 — platform-wide spend ceiling enforcement | Depends on OIK-110 |
| S5 | OIK-112 — kill-switch drill (documented + rehearsed) | S-sized, no real code dependency — good filler slot |
| ORCH (not a builder task) | OIK-104 — Fable adversarial review of OIK-103's ACL/handoff model | I review directly (Claude vs. CX9's Codex, satisfies "different model") — same pattern used all session; can run as soon as Wave 2's OIK-103 lands, doesn't need to wait for Wave 4 |

**Not in this program (flagged, not scheduled):**
- OIK-098/099/100/101 — DONE per ADR-012, not scheduled (see discovery above).
- OIK-113/114 (browser routine recording) — XL, explicitly flagged in the WBS as needing decomposition before it can be scheduled; a future planning pass, not this one.

## What I'm doing now

Both Codex logins confirmed (direct `codex login status` check on each, plus a byte-diff confirming the two `auth.json` files are genuinely distinct). CX9 activated. Decomposing each Wave-1 item into real PLAN.md task blocks (Owned_Paths, Acceptance_Criteria, Spec_References) the same way Grants-1/Chat-2 Core/Connectors-1 were, validating territorial disjointness across all three simultaneous tracks, then dispatching CX → OIK-105, CX9 → the broker-tool task, S5 → RT-01.
