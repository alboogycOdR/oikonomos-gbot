# Local development only. Replace with an unguessable value outside version control.
POSTGRES_DB=oikonomos
POSTGRES_USER=oikonomos
POSTGRES_PASSWORD=CHANGE_ME_LOCAL_ONLY

# Production bind address: set to clawsrv's Tailscale IPv4 address.
TAILSCALE_POSTGRES_HOST=100.64.0.1
